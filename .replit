# --- CLEAN AUTOSCALE CONFIG (2025) ---

run = "npm run build && npm run start"

entrypoint = "package.json"
modules = ["nodejs-20"]

[env]
HOST = "0.0.0.0"
PORT = "5000"
NODE_ENV = "production"

[deployment]
deploymentTarget = "autoscale"