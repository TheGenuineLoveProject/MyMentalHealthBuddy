# MyMentalHealthBuddy — Replit autoscale config (FINAL)

run = "npm start"

[deployment]
deploymentTarget = "autoscale"
run = ["npm", "start"]
build = ["npm", "run", "build"]

# Autoscale port mapping — backend listens on 5000
[[ports]]
localPort = 5000
externalPort = 80