run = "npm run dev"
entrypoint = "server/index.mjs"

modules = ["nodejs-20", "postgresql-15"]

[deployment]
deploymentTarget = "autoscale"
build = ["npm", "install"]
run = ["npm", "start"]

[ports]
localPort = 5000
externalPort = 80

[env]
NODE_ENV = "production"