entrypoint = "server/app.mjs"

run = "npm run dev"

modules = ["nodejs-20"]

[deployment]
deploymentTarget = "cloudrun"
build = ["sh", "-c", "npm install"]
run = ["sh", "-c", "npm start"]

[[ports]]
localPort = 5000
externalPort = 80
