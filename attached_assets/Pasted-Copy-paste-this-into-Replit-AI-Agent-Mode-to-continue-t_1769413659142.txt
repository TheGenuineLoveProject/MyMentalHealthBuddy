Copy-paste this into Replit AI (Agent Mode) to continue to the next wave with an in-depth “no duplicate work” scan, then implement the NEXT 50 processes (and only the ones missing), apply changes, validate, and checkpoint.

NAME: GLP_BATCH_7__DEEP_SCAN_NO_DUPLICATES__NEXT50_PROCESSES_APPLY
MODE: APPLY CHANGES
PROJECT: TheGenuineLoveProject.com
OWNER: Aaliyah Draws Art LLC (Maria Landa)

MISSION:
Continue A→Z 360° platform evolution by:
1) Running an in-depth scan that prevents duplicate work permanently.
2) Completing any INCOMPLETE integrations (up to 50) first.
3) Implementing the NEXT 50 missing “top platform processes” (high impact, low risk first).
4) Updating an Integration Registry so each process is done exactly once.
5) Validating build + tests + route-collision checks.
6) Creating a checkpoint commit.

NON-NEGOTIABLE RULES:
- Replit-safe: single port, no background daemons, no cron watchers.
- No destructive deletes. Prefer “deprecate + forward” wrappers.
- Human-triggered only.
- Zero plagiarism / original writing for content.
- Legal safe language: educational/supportive only; no diagnosis/treatment/cure/guarantees.

========================
PHASE 0 — CHECKPOINT NOW
========================
1) Ensure git is initialized. If not: init and commit current state.
2) Commit message:
   "checkpoint: before Batch 7 deep scan + next50"

=============================================
PHASE 1 — DEEP SCAN (ANTI-DUPLICATE GUARANTEE)
=============================================
GOAL: Create an authoritative, machine-readable record of what exists, what is incomplete, what is missing, and what conflicts.

A) Ensure these files exist (create if missing):
- client/src/content/meta/integrationRegistry.ts
- docs/batch-7/deep-scan.json
- docs/batch-7/deep-scan.md

B) Scan and write into deep-scan.json:
- Repo inventory counts (pages/components/endpoints/scripts)
- Canonical “single source of truth” files:
  routes, route meta registry, tokens, schema
- Endpoint table: METHOD + PATH + file + router + handler name
- Duplicate detection:
  - exact duplicate pages/components
  - near-duplicate blocks (repeat markup patterns)
  - endpoint collisions
- Incomplete detection:
  - TODO/FIXME markers
  - placeholder exports
  - empty handlers / “return null”
  - “mock” / “stub” / “TEMP”
  - routes without pages, pages without routeMeta
- Security posture scan:
  - missing CSP/CSRF/secure cookies/rate limits
  - missing env validation
  - missing auth guards on admin routes
- Observability scan:
  - missing requestId logger
  - missing error normalization
  - missing health/readiness/version endpoints
- SEO/content scan:
  - missing canonical tags / og tags
  - missing sitemap generation
  - routeKey vs path mismatches

C) Update integrationRegistry.ts:
- For every discovered capability/process:
  status = done | in_progress | planned
  canonicalFiles = [...]
  routeKeysTouched = [...]
  lastUpdated = ISO timestamp
- Create an “INCOMPLETE” list in registry (status=in_progress) for anything partially implemented.

STOP RULE:
Do not implement anything until:
- deep-scan.json and deep-scan.md are written
- integrationRegistry.ts updated

====================================================
PHASE 2 — COMPLETE INCOMPLETE INTEGRATIONS (UP TO 50)
====================================================
GOAL: finish partial work first (highest leverage, lowest churn).

1) From the registry + scan results, collect up to 50 items where status=in_progress.
2) Prioritize in this order:
   a) build correctness
   b) route correctness / collisions
   c) auth/admin protection
   d) data integrity
   e) observability
   f) UI regressions

3) Implement in patches of MAX 10 items per patch:
- Patch naming: docs/batch-7/patch-01.md, patch-02.md, ...
- Each patch must list:
  files changed
  why safe
  how validated
- After each patch: run build/typecheck/tests.

========================================================
PHASE 3 — IMPLEMENT NEXT 50 “TOP PLATFORM PROCESSES” (NEW)
========================================================
GOAL: Add the next 50 missing processes found in the platform matrix, without duplicates.

A) Ensure matrix exists (create/update if missing):
- docs/platform/top-platform-processes-matrix.md

Matrix must include process keys in buckets:
SECURITY/PRIVACY, OBSERVABILITY/RELIABILITY, PERFORMANCE/UX,
SEO/CONTENT OPS, GROWTH/CRM, DEVEX/CI, DATA/ANALYTICS, ADMIN OPS

B) Auto-select NEXT 50 processes:
Selection rules:
- Must NOT already be status=done in integrationRegistry.ts
- Must NOT duplicate another process key
- Must be implementable safely in Replit
- Prefer “baseline hardening” first

C) REQUIRED “NEXT 50” baseline set (implement if missing):
SECURITY/PRIVACY (12)
1) Env validator on boot (required vars + safe defaults)
2) Secure cookies defaults (httpOnly, sameSite, secure in prod)
3) CSRF protection for state-changing routes
4) CSP baseline + helmet config (safe defaults)
5) Rate limit for auth + sensitive endpoints
6) Input validation standard (Zod schema map + shared validator helper)
7) Central auth middleware (requireAuth)
8) RBAC guard for /admin routes (role check)
9) Audit log table for admin actions
10) Secret scanning guard (fail build if obvious secrets in repo)
11) Security headers test/validator script
12) Minimal privacy logging policy (no raw journal content)

OBSERVABILITY/RELIABILITY (10)
13) RequestId middleware + structured logger
14) Error normalization (one error format API-wide)
15) /healthz endpoint (liveness)
16) /readyz endpoint (readiness)
17) /version endpoint (commit hash/build time)
18) Server timing instrumentation (basic)
19) Frontend ErrorBoundary + user-friendly fallback
20) Server-side unhandledRejection/uncaughtException safe handler (log + graceful)
21) Minimal metrics endpoint (counts only, no PII)
22) Log redaction utility

PERFORMANCE/UX (8)
23) Route-level code splitting for heavy pages
24) Button minimum size enforcement (>=44px) via shared Button component
25) Skeleton/loading pattern standard component
26) Image defaults: lazy + width/height + decoding async
27) Reduce motion preference support
28) Bundle analyzer script (optional dev-only)
29) Cache headers for static assets (production)
30) Remove render-blocking patterns (audit + fix top offenders)

SEO/CONTENT OPS (8)
31) Canonical tags default + per-route overrides (routeMeta-driven)
32) OpenGraph + Twitter card defaults (routeMeta-driven)
33) Automatic sitemap.xml generator from route registry
34) robots.txt generator (safe default)
35) InternalLink component resolves routeKey → canonicalPath (no raw paths stored)
36) Breadcrumb generator from route meta
37) JSON-LD Article/FAQ helper (for blog/docs pages)
38) 404 page with route search (routeKey search)

GROWTH/CRM (6)
39) Newsletter capture component (stores email only with consent)
40) UTM capture + attribution storage (session-scoped + optional DB)
41) Lead magnet delivery placeholder (manual export; no auto email)
42) Admin Social Studio: “Repurpose to Blog” handoff stub
43) Referral/share link generator (no tracking by default)
44) Consent-based CTA library (soft language)

DEVEX/CI (4)
45) CI workflow: install → typecheck → lint → build → route validator
46) Route collision validator runs in CI
47) “No duplicate routeKey” validator runs in CI
48) Pre-commit optional hook docs (do not require)

DATA/ANALYTICS (2)
49) Minimal event tracking abstraction (view/start/complete/save)
50) Admin analytics page reads aggregate counts (no PII)

D) For each implemented process:
- Add/Update entry in integrationRegistry.ts (status=done)
- Add canonicalFiles + notes + lastUpdated
- Add validation steps to docs/batch-7/patch-XX.md

================================
PHASE 4 — WELLNESS MICROCOPY CORE
================================
GOAL: lock consistent microcopy across 322 pages without repetition.

1) Create:
- client/src/content/microcopy/wellnessMicrocopy.ts
Export categories:
- CTAs (soft)
- Error states
- Empty states
- Success states
- Consent/pacing
- Disclaimer snippets (non-clinical)
- Gentle boundary language
- “Try again” variants (rotation)

2) Create helper:
- client/src/lib/microcopy/getMicrocopy.ts
Supports:
- getMicrocopy(category, key, {variant/rotationSeed})
So pages can reuse tone without being repetitive.

3) Replace the TOP 25 highest-traffic pages/components to use microcopy helper
(Do not refactor all 322 in one pass—start with 25.)

==================================
PHASE 5 — VALIDATION + PROOF CHECK
==================================
You MUST run and pass:
- npm run build
- npm run typecheck (or tsc)
- npm test (if exists)
- route collision validator
- duplicate routeKey validator
- smoke run app start

If any step fails:
- fix immediately
- do not proceed to next patch until green

========================
PHASE 6 — FINAL OUTPUT
========================
Return:
1) Summary of completed INCOMPLETE items (count + list)
2) Summary of NEXT 50 processes implemented (count + list)
3) Files changed (grouped by patch)
4) Remaining optional queue for Batch 8 (top 20)
5) Updated registry stats: done/in_progress/planned
6) Commit hash for checkpoint at end:
   "checkpoint: after Batch 7 deep scan + next50"

FINAL STEP:
Create a final commit:
"checkpoint: after Batch 7 deep scan + next50"
Then STOP and wait for: "RUN BATCH 8"

If Replit AI asks whether to “dry-run,” choose APPLY (because this prompt already forces scanning first, patching in small safe chunks, and validating after every patch).