MASTER COMMAND — PLATFORM PERFECTION GOVERNANCE (v1.1)
Repo: “The Genuine Love Project”

Intent (professional translation of “100B power / 360° A→Z”):
Execute ONE disciplined, production-grade governance cycle that covers the entire platform A→Z:
Quality (tests + audits + determinism) → UX coherence (tokens + contrast + hierarchy) → Navigation integrity → 404 excellence → Docs governance.
No scope creep. Minimal diffs. Evidence-driven changes only.

ABSOLUTE RULE: IF A CHANGE IS NOT REQUIRED BY THIS PROMPT OR BY FAILING TESTS/AUDITS, DO NOT DO IT.

══════════════════════════════════════════════════════════════════════
HARD CONSTRAINTS (NON-NEGOTIABLE)
1) DO NOT rename/refactor existing CSS variable names. Keep `--glp-*` exactly.
2) NO raw hex colors anywhere except: `client/src/styles/brand-tokens.css`.
3) Fonts allowed ONLY: Inter + Playfair Display (+ system fallbacks). No other fonts.
4) All internal navigation links MUST match canonical routes in `client/src/App.jsx` (wouter Switch/Route). No drift.
5) Prefer minimal diffs; no breaking refactors; no broad rewrites. Tokens + Tailwind semantic classes only.
6) DO NOT loosen/bypass tests/audits. Fix root causes.
7) DO NOT invent new routes unless an intended page already exists and is referenced.
8) DO NOT add dependencies unless absolutely necessary to fix failing tests/audits (must justify).

Brand/Safety: supportive “healing-luxury” tone OK; do NOT make medical claims or promises.

SOURCE OF TRUTH (READ FIRST)
- Canonical router: `client/src/App.jsx`
- Tokens: `client/src/styles/brand-tokens.css`
- Brand CSS: `client/src/styles/brand.css` (if present)
- Tailwind config: `tailwind.config.js`
- Global CSS entry: `client/src/index.css` (and `client/src/main.css` if used)
- Nav components: Header/Navbar/Footer/Sidebar + any Link/CTA/cards
- Audits: `tools/nav-link-audit.mjs`, `tools/visual-doctor.mjs`
- Reports: `docs/NAV_LINK_AUDIT.md`, `docs/VISUAL_DOCTOR_REPORT.md`
- Tests: `tests/auth.test.mjs` and `server/tests/*` if present

EXECUTION MODEL: 3 AGENTS, STRICT ORDER
Agent 1 — QA Guardian → Agent 2 — UX Finisher → Agent 3 — Content Curator
Do not start Agent 2 until Agent 1 is green.

══════════════════════════════════════════════════════════════════════
AGENT 1 — QA GUARDIAN (Determinism + Ports + Auth/Admin)
Goal: eliminate EADDRINUSE / ECONNREFUSED and make auth tests deterministic.

A) BASELINE EVIDENCE (must run + record outputs)
Run and capture snippets:
- `node -v`
- `npm -v`
- `npm install`

B) PORT SANITY (must run; your logs show :5000 collisions)
Run and capture:
- `lsof -i :5000 || true`
- `kill -9 $(lsof -t -i :5000) 2>/dev/null || true`
- `lsof -i :3000 || true`
- `kill -9 $(lsof -t -i :3000) 2>/dev/null || true`

C) DETERMISTIC TEST HARNESS (fix root cause)
Read:
- `package.json` scripts
- `tests/auth.test.mjs` (BASE_URL, endpoints, expectations)
Implement the MINIMUM change so `test:auth` never requires a manually running server:
Preferred:
1) Make `npm run test:auth` start server as a child process
2) Wait for `/api/health` to return 200 (poll with timeout)
3) Run vitest
4) Tear down server process reliably (even on failure)

If scripts-based approach is too risky, use vitest hooks in the auth test file setup:
- `beforeAll`: spawn server, wait for health
- `afterAll`: kill server
Keep diff minimal.

D) SECURITY + STATUS CONSISTENCY (only when needed)
Enforce consistent safe responses:
- Missing required fields → 400 (generic safe message)
- Invalid credentials OR rate-limited login → 401 "Invalid credentials" (uniform; no rate limit disclosure)
- Unauthenticated access → 401
- Authenticated non-admin on admin routes → 403
Do not leak stack traces, DB errors, or internal details in responses.

E) VERIFY UNTIL GREEN
Run until passing:
- `npm run test:auth || npx vitest run tests/auth.test.mjs`

STOP CONDITION (Agent 1):
- Auth tests GREEN
- No ECONNREFUSED / EADDRINUSE during test run

══════════════════════════════════════════════════════════════════════
AGENT 2 — UX FINISHER (Premium UI + Mode + Nav + NotFound)
Goal: consistent, higher-contrast, premium “healing-luxury” UI across the platform using existing tokens (no hex).

A) VISUAL HEALING-LUXURY CONSISTENCY (token-driven, minimal diffs)
Fix the “washed-out / wireframe” feel with minimal, systematic upgrades:
1) Contrast & hierarchy
   - Raise body text contrast (token-based)
   - Ensure headings read as headings (Playfair, spacing, weight)
   - Reduce overuse of thin dividers; replace with spacing + surfaces
2) Buttons system
   - Primary: teal bg + ivory text; clear hover/active; gold focus ring
   - Secondary/Ghost: teal text; subtle surface hover; gold focus ring
   - Disabled: muted surface/text; cursor-not-allowed
   - Consistent padding, radius, icon spacing, transitions
3) Links
   - teal text
   - underline on hover only
   - gold focus ring visible on keyboard focus
4) Cards/Surfaces
   - semantic surface tokens only
   - consistent radius + soft shadow (reduced in low-stim)
   - avoid “flat blend-in” cards by improving surface separation via tokens
5) Icons
   - standardize 24px base
   - outline feel, token-driven colors only

B) MODE SYSTEM (Default / Low-Stim / Reading)
Requirements:
1) HTML dataset on <html>
   - Default: dataset.mode unset or ""
   - Low-Stim: "low-stim"
   - Reading: "reading"
2) Mode Toggle UI in TWO places:
   - Header top-right
   - Settings page section “Display / Mode” with short descriptions
3) Persistence:
   - localStorage key: `glp-mode`
   - apply BEFORE first paint (bootstrap in earliest client init, e.g., `client/src/main.jsx`)
4) Accessibility:
   - keyboard reachable
   - aria-pressed / aria-current
   - focus-visible ring uses gold token (no hex)
5) Mode styling:
   - Default: luxury (subtle gradients via vars OK)
   - Low-Stim: reduce saturation/shadows/gradients/motion
   - Reading: maximize legibility and reduce decorative noise

C) NAVIGATION LOCKDOWN (ZERO DRIFT)
- Treat `client/src/App.jsx` as canonical route list
- Find all internal links (header/footer/sidebar/CTAs/cards)
- Every internal link path must match App.jsx EXACTLY
- Fix or remove anything pointing to missing routes; do not invent routes

D) NOTFOUND (404) PREMIUM
Upgrade NotFound used by fallback route:
- Playfair headline + calm supportive copy
- Primary: “Go Home”
- Secondary: “Go Back”
- Quick Links (4–6 max) using ONLY routes from App.jsx
- Responsive layout; clean spacing; no cramped typography
Re-run nav audit after quick links.

E) VERIFY AGENT 2
Run and confirm:
- `npm run audit`
Confirm reports show:
- `docs/NAV_LINK_AUDIT.md` → 0 broken links
- `docs/VISUAL_DOCTOR_REPORT.md` → 0 violations
Confirm constraints:
- No hex outside `client/src/styles/brand-tokens.css`
- Only Inter + Playfair Display fonts

STOP CONDITION (Agent 2):
- Audit passes + reports clean
- Mode toggle present in Header + Settings + persists + no flash-of-wrong-mode

══════════════════════════════════════════════════════════════════════
AGENT 3 — CONTENT CURATOR (Docs + Canva + Governance)
Goal: create a governance kit that keeps the platform coherent long-term.

Create/update:
1) `docs/COLOR_SYSTEM.md`
   - enumerate existing `--glp-*` tokens (no renames)
   - semantic mapping
   - mode deltas (Default vs Low-Stim vs Reading)
   - do/don’t rules (no hex outside brand-tokens.css)
2) `docs/TYPOGRAPHY_GUIDE.md`
   - Playfair (headings) + Inter (body/UI)
   - hierarchy scale, spacing, line-height guidance
3) `docs/CANVA_TEMPLATE_SPECS.md`
   - 1080×1080 and 1080×1920
   - safe margins, grid, spacing unit
   - type scale + usage patterns (quote cards, prompt cards, carousel)
   - color rules (teal primary, gold accent, ivory surfaces)
   - export settings + naming conventions
4) `README.md`
   - how to run audits: `npm run audit`, `npm run nav:audit`, `npm run visual:doctor`
   - how to switch modes: Header + Settings, localStorage `glp-mode`, html data-mode

OPTIONAL (only if safe; no CI break):
- Add `npm run governance:loop` (no deps) that:
  1) best-effort port cleanup
  2) runs `test:auth`
  3) runs `audit`
Document it in README.

══════════════════════════════════════════════════════════════════════
FINAL VERIFICATION (MUST RUN)
- `npm run test:auth`
- `npm run audit`

FINAL REPORT FORMAT (MANDATORY)
1) Checklist (Agent 1–3) with pass/fail.
2) Exact commands run + results (include key excerpts).
3) Files changed (each with 1-line summary).
4) NotFound Quick Links (label + exact path).
5) Rollback steps:
   - `git status`
   - `git diff`
   - `git checkout -- <file>` for each changed file
   - optional: `git reset --hard HEAD` (warn: destructive)
6) OPTIONAL improvements (clearly labeled OPTIONAL; not implemented unless asked).
Proceed now. Do not stop early. Verify, then report.