You are operating in my Replit repo “The Genuine Love Project”.

MISSION ORDER (NON-NEGOTIABLE):
1) Make auth tests GREEN: register + login + me + admin gate.
2) Fix deployment blockers (502s) and CI blockers (lint parse errors).
3) Only after 1–2 are solid: do nav + UI polish and run audits.

HARD RULES:
- Do not introduce duplicate routes or duplicate exports.
- Do not loosen tests; fix code to match expectations.
- Keep security defaults: safe error messages, rate limiting, server-side RBAC.
- Keep existing CSS vars `--glp-*` unchanged. No raw hex outside `client/src/styles/brand-tokens.css`.
- Fonts: Inter + Playfair Display only.

WHAT YOU MUST DO (EXECUTE, DON’T ADVISE):
A) AUTH TEST FAILURES (PRIMARY)
- Locate the canonical login endpoint used by tests (tests/auth.test.mjs).
- Fix two failures:
  1) “login with valid credentials” must return 200 and include token (and safe user)
  2) “login with missing fields” must return 400 (not 401)
- Ensure register stores password hash in the same field login checks.
- Ensure email normalization is consistent (trim + lowercase on both register and login).
- Ensure `/me` returns 401 only for missing/invalid token (not for validation errors).
- Ensure rate limit middleware does NOT change status codes expected by tests.

B) DEPLOYMENT 502 / INTERNAL SERVER ERROR (SECONDARY)
- Identify why deployed endpoints return 502/500:
  - check server entrypoint, router wiring, missing imports, failing middleware, env vars.
- Fix so `GET /api/health` and `GET /api/auth/github` behave correctly.
- Ensure GitHub OAuth callback URL matches deployed callback route:
  https://thegenuineloveproject.replit.app/api/auth/github/callback
- Verify GitHub OAuth env vars are read and errors are handled gracefully.

C) LINT BLOCKER
- Fix `shared/disclaimer.mjs` parse error: “return outside of function”.
- Remove or use unused variables in `server/routes/auth.mjs` (or adjust lint config minimally if appropriate).
- Do not silence lint with blanket disables unless absolutely necessary.

D) RBAC / ADMIN GATE
- Confirm admin role enforcement for admin routes is server-side:
  - unauthenticated -> 401
  - authenticated non-admin -> 403
- Ensure tests cover this and pass.

E) VERIFICATION (MUST RUN)
Run these commands in Replit Shell and confirm results in your final message:
1) npm run test:auth
2) npm run lint
3) npm run audit (or nav:audit + visual:doctor + test:all)

DELIVERABLE FORMAT (RETURN EXACTLY):
1) FAST TRIAGE: top 10 issues found + root cause for each.
2) PATCH SET: git-apply-ready unified diffs (file-by-file).
3) TEST RESULTS: paste command outputs showing green.
4) RUNBOOK: how to test login/admin + OAuth + env vars list.
5) ACCEPTANCE CHECKLIST: pass/fail.
6) HARDENING REPORT: what security controls are implemented.

Start now. Fix auth tests first. Do not proceed to UI polish until auth tests are green.