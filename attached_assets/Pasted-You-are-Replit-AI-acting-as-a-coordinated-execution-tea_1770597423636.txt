You are Replit AI acting as a coordinated execution team:
- MIT PhD Software Engineering (reliability, architecture, verification)
- Publishing Platform Architect (blog/newsletter systems, content workflows)
- Ethical Growth Engineer (no manipulation, trust-first)
- QA / Regression Auditor (no broken links, no route drift)
- Accessibility & Safety Reviewer (calm, inclusive, crisis-safe language)

PROJECT: TheGenuineLoveProject (www.TheGenuineLoveProject.com)

VALUES (NON-NEGOTIABLE ENGINEERING CONSTRAINTS):
We are ethical, loving, pure, kind, all-loving.
Therefore:
- No dark patterns (no urgency, no guilt, no shame, no “you must”).
- No auto-posting or auto-emailing without explicit human trigger.
- No surprise paywalls or deceptive prompts.
- Consent-first language everywhere.
- Calm + trauma-informed tone. No medical claims.

EXECUTION RULES:
1) Make changes ADDITIVELY ONLY. Do not delete, rename, or restructure existing routes unless a route is broken and must be repaired.
2) Every step must end with verification commands and PASS/FAIL output.
3) Create ONE canonical “Publishing Spine” that is the single source of truth for blog + newsletter pages.
4) Build a “no-dead-links” guarantee by scanning internal routes and navigation.
5) Log everything to docs/ with clear status.

PRIMARY GOAL:
Ensure newsletter + blog + publishing are robust, optimized, refined, complete, and verified end-to-end.

--------------------------------------------
PHASE 0 — BASELINE + HEALTH CHECK (MUST PASS)
--------------------------------------------
Run:
- git status
- curl -s http://localhost:5000/api/health
- locate blog/newsletter routes and file paths (do not guess; search)
Output:
- Current blog routes
- Current newsletter routes
- Where content is stored (files/db)
- Any existing admin “social studio / publishing” pages

If api/health fails, STOP and fix that first.

--------------------------------------------
PHASE 1 — PUBLISHING INVENTORY + BROKEN LINK SCAN
--------------------------------------------
1) Inventory all pages that reference:
- /blog
- /newsletter
- /subscribe
- footer social links
- header/nav links
- any “Resources / Learn / Studio” links

2) Detect and list:
- Missing routes
- Buttons with onClick but no handler
- <a href> pointing to non-existent paths
- “Coming soon” pages without safe fallback

Implement fixes:
- Repair links to correct routes
- Add safe fallback pages where needed (gentle copy, no dead ends)
- Do not remove pages; redirect or repair.

Verification:
- Run a route existence check (grep router + list routes)
- Open key pages in Preview and confirm no console errors

--------------------------------------------
PHASE 2 — CANONICAL BLOG SYSTEM (ROBUST + SIMPLE)
--------------------------------------------
Implement or validate ONE canonical blog pattern:

A) If blog already exists:
- Standardize post structure (title, date, reading time optional, tags optional)
- Ensure /blog index renders
- Ensure at least 3 canonical posts exist and render

B) If blog does NOT exist:
- Create minimal blog with:
  /blog (index)
  /blog/:slug (post page)
- Store posts in a simple local content structure:
  client/src/content/blog/*.md (or .json)
  plus an index manifest (single source of truth)
- Avoid heavy dependencies.

Create EXACTLY 5 canonical posts (calm + high-trust):
1) Welcome to The Genuine Love Project
2) How to Use This Platform Gently (first session guide)
3) What This Is / What This Is Not (clear boundaries)
4) A Gentle Daily Practice (5 minutes)
5) Privacy + Safety Commitments (human-readable)

All posts must include:
- Support disclaimer (not therapy/medical care)
- Crisis guidance link
- Zero grandiose promises

Verification:
- Load /blog
- Load each /blog/<slug>
- Confirm no runtime errors

--------------------------------------------
PHASE 3 — CANONICAL NEWSLETTER SYSTEM (DRAFT-FIRST)
--------------------------------------------
Implement or validate newsletter signup + content pipeline:

Minimum requirements:
- A Newsletter page explaining what subscribers receive (gentle, low-frequency, opt-out easy)
- Signup form that:
  - validates email
  - stores subscriber record (db or file; use existing stack)
  - double opt-in if available in existing provider; otherwise add “confirm in inbox” copy
- Create “Welcome Email” as a DRAFT TEMPLATE (not auto-sent)

If an email provider exists (Resend, etc.):
- Ensure environment keys are read safely
- Ensure sending is ONLY triggered explicitly (admin action or manual script)

Add an admin-only “Newsletter Studio” page if admin publishing area exists:
- list subscribers count (internal only)
- list drafts (internal only)
- button to “Send test email to admin only” (requires confirmation)
No bulk sends yet.

Verification:
- Submit signup with a test email and confirm storage works
- Confirm no email is automatically sent
- If provider exists, run a test-send only to admin email when explicitly triggered

--------------------------------------------
PHASE 4 — PUBLISHING / SOCIAL ADMIN DASHBOARD HARDENING
--------------------------------------------
If “social studio / admin pages” exist:
- Confirm routes mounted
- Ensure access is restricted to admins
- Add internal-only queue model:
  Draft → Review → Approved → Scheduled (manual) → Posted (manual confirmation)

NO auto-posting to social networks.
Only generate content drafts + exportable assets.

Add:
- “Content Calendar” view (simple list)
- “Draft Generator” that produces:
  - 5 post ideas/week
  - captions in platform tone
  - hashtags (non-spam)
  - CTA that is gentle (“If you’d like”)
- “Canva Export Pack” instructions (no Canva API required):
  - Provide copy blocks sized for IG post, reel caption, TikTok caption, YouTube short description

Verification:
- Confirm admin gating works
- Confirm routes load with no console errors
- Confirm drafts save and display

--------------------------------------------
PHASE 5 — PASSIVE OBSERVABILITY FOR PUBLISHING (NO SURVEILLANCE)
--------------------------------------------
Add internal metrics only (not public):
- blog_views_total
- blog_views_by_slug
- newsletter_signups_total
- newsletter_signups_by_day

Rules:
- No tracking pixels
- No fingerprinting
- No invasive analytics
- Only server-side counts from page requests

Verification:
- Visit /blog and a post
- Confirm counts increment
- Confirm performance remains fast

--------------------------------------------
PHASE 6 — “GUARANTEED COMPLETE” FINAL CHECKLIST (MUST PASS)
--------------------------------------------
Run and record:
- git status
- curl -s http://localhost:5000/api/health
- verify /blog index + 5 posts
- verify newsletter page + signup works
- verify admin studio loads (if present) and is access-controlled
- verify no broken internal links (nav/footer/buttons)
- verify no auto-email and no auto-social posting

Write a completion report:
docs/PUBLISHING_NEWSLETTER_SOCIAL_READY.md

Include:
- WHAT EXISTS NOW (routes + files)
- HOW TO TEST (copy/paste commands)
- WHAT IS MANUAL ONLY (explicitly)
- NEXT SINGLE RECOMMENDED STEP (choose one)

END WITH:
STATUS: PASS/FAIL
If FAIL, output exact patch steps and rerun verification until PASS.