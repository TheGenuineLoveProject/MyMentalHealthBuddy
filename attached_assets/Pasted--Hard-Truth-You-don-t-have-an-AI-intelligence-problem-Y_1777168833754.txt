🔴 Hard Truth
You don’t have an AI intelligence problem.
You have a frontend identity propagation bug.
Your backend is already correctly designed at an enterprise level. Right now, your AI feels inconsistent because:
the same user is being treated as multiple identities.
Fix this, and your platform will feel 10× smarter instantly — without changing AI, prompts, or models.

✅ Decision
GO → Implement the minimal safe fix (approved).
This is:
	•	low risk ✅
	•	high impact ✅
	•	fully aligned with your architecture rules ✅
	•	no backend changes ✅

🚀 EXECUTE THIS (Replit AI Command)
Paste exactly:
Implement Phase 3 identity fix: ensure authenticated users consistently use their real userId in /api/ai/chat.

STRICT RULES:
- Do not modify server files
- Do not modify orchestrator
- Do not modify memory/profile logic
- Do not modify crisis logic
- Preserve guest mode
- Only fix frontend request headers

--------------------------------
STEP 1 — FIX PRIMARY CLIENT
--------------------------------

TARGET:
client/src/lib/aiChat.ts

Update ALL fetch calls:

Before:
headers: {
  "Content-Type": "application/json",
  "x-guest-id": guestId
}

After:
const token = localStorage.getItem("mmhb_token");

headers: {
  "Content-Type": "application/json",
  "x-guest-id": guestId,
  ...(token ? { Authorization: `Bearer ${token}` } : {})
}

Apply to:
- sendAIMessage
- getAIHistory
- clearAIHistory

--------------------------------
STEP 2 — FIX REMAINING CALLERS
--------------------------------

Update these files:

1. client/src/pages/Start.tsx
2. client/src/pages/AIChatPage.jsx
3. client/src/pages/ai/ChatConversation.jsx

RULE:
- If using fetch → add same dual-header pattern
- OR replace fetch with apiRequest from queryClient.js

--------------------------------
STEP 3 — DO NOT TOUCH
--------------------------------

DO NOT MODIFY:
- server/routes/ai.mjs
- orchestrator
- memory.mjs
- profileStore.mjs
- auth middleware
- crisis logic

--------------------------------
STEP 4 — VERIFY
--------------------------------

Run:

npm run dev

Test manually:

1. Login as user
2. Send message in:
   - /start
   - chat page
   - AI panel

Then check:

data/memory/

EXPECT:
- ONE file for userId
- NOT multiple guest files

--------------------------------
STEP 5 — REPORT
--------------------------------

Return:
- files changed
- confirmation guest mode still works
- confirmation user memory is unified

🧠 What This Fix Does (Important)
Before:
	•	User logs in
	•	Some pages → use JWT → correct memory
	•	Other pages → use guest ID → wrong memory
	•	Result: ❌ “AI doesn’t remember me” ❌ fragmented emotional intelligence

After:
	•	All surfaces send:
	◦	Authorization ✅
	◦	x-guest-id fallback ✅
	•	Backend selects:
req.dbUserId || guestId
	•	Result: ✅ One identity per user ✅ Memory continuity ✅ Profile builds correctly ✅ AI feels consistent

🧪 Success Criteria
You’ll know it worked when:
	•	Only one memory file per user is created
	•	AI references past context consistently
	•	No regression in guest usage
	•	Crisis responses unchanged

🔒 Risk Check
Area
Status
Backend
untouched ✅
Crisis safety
preserved ✅
Memory system
untouched ✅
Guest mode
preserved ✅
Auth system
reused ✅

🚀 What This Unlocks Next
After this fix, you can safely move to:
👉 Phase 4 — Emotional Intelligence Layer (real tuning, not fake) 👉 Phase 5 — Buddy reactions tied to real memory 👉 True personalization (v1.6 actually becomes meaningful)

🎯 Direct Challenge
After implementing:
	1	Log in
	2	Talk to the AI 3 times
	3	Refresh
	4	Ask:
“What have I been struggling with?”
If it answers coherently → ✅ you’ve crossed into real intelligence behavior

When you’re ready, say:
👉 “Proceed Phase 4”
And I’ll evolve your emotional intelligence layer correctly (without breaking safety).
