# ⛩️ REPLIT AI - CONTINUE360°.PROMPT

agent_mode: true
purpose: >
  Execute and evolve TheGenuineLoveProject.com layout system
  across all pages, devices, and user interactions — 360° from A to Z
  to the 100B^100B quantum level.

tasks:
  - ✅ Generate full UI/UX layout (HTML/CSS) across 7 primary pages:
      • landing.html
      • onboarding.html
      • homepage.html
      • crm.html
      • content.html
      • qa.html
      • login.html

  - ✅ Auto-inject brand:
      • Colors: #8fbf9f, #f4c7c3, #2f5d5d, #faf9f7, #3a3a3a, #eac33b
      • Logo: /assets/logo.svg
      • Fonts: Elegant serif for headings, clean sans-serif for body
      • Icons: Calming, gold-accented SVGs

  - ✅ Structure layout with semantic tags:
      • header, hero, section.content, testimonial, footer
      • nav, CTA, modal, CRM zone, Q&A post, quote-block

  - ✅ Label components for developer handoff:
      • ID/class naming for Figma/Webflow/Framer
      • Comments in HTML for dev context
      • CSS classes use BEM naming or Tailwind-compatible structure

  - ✅ Ensure mobile-first responsive design:
      • Auto-generate media queries or Tailwind breakpoints
      • Tag every section as mobile/tablet/desktop

  - ✅ Inject accessibility & SEO standards:
      • ARIA labels
      • WCAG color contrast checks
      • meta title/desc
      • Open Graph + JSON-LD schema
      • Sitemap logic (flowchart.svg)

  - ✅ Evolve with each request ("Continue 360°"):
      • Every task triggers automated regeneration if upstream file changes
      • Missing modules (blog, testimonials, support) added upon request
      • Structural bugs flagged and fixed in each cycle

  - ✅ Output:
      • /project.zip (downloadable bundle)
      • /Continue360.prompt (upgraded)
      • /component-map.figma.json (Figma integration)
      • /flowchart.svg (site navigation map)
      • /README.md (dev instructions + style guide)

rules:
  - Never overwrite working content unless updated upstream
  - Never inject placeholder images
  - Use light gradients + whitespace to enforce calm UX
  - Prioritize healing, clarity, balance in all UI visuals

triggers:
  - "Add module: /[page].html" → generates new file + style
  - "Upgrade: [page]" → rebuilds layout with latest standard
  - "Continue 360°" → re-initiates all AI audit + rebuilds system
  - "Export All" → bundle into .zip + prompt + flowchart + Figma