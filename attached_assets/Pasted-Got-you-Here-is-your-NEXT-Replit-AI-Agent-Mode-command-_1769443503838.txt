Got you. Here is your NEXT Replit AI (Agent Mode) command — Batch 13 (APPLY) implementing NEXT50 processes (#301–#350) with an even stronger deep scan to avoid duplicates, and it finishes incomplete items first (up to 50) before starting #301–#350.

Paste this into Replit AI exactly:

NAME: GLP_BATCH_13__DEEP_SCAN_V6__INCOMPLETE_FIRST__NEXT50_301_350__APPLY
MODE: APPLY CHANGES (NO DRY-RUN)
PROJECT: TheGenuineLoveProject.com
OWNER: Aaliyah Draws Art LLC (Maria Landa)

MISSION (Batch 13):
1) Run Deep Scan v6 (strongest “no duplicate work” + “proof of done”).
2) Complete any remaining incomplete integrations FIRST (up to 50).
3) Implement NEXT 50 platform processes (#301–#350), auto-skip anything already DONE in registry.
4) Validate + checkpoint after every patch.

NON-NEGOTIABLE RULES:
- APPLY changes.
- Non-destructive: no deletes without explicit instruction; deprecate/redirect instead.
- Single source of truth: routeKey only; internal links store routeKeys; UI resolves canonicalPath.
- Replit-safe: single port, no background daemons/watchers, no multi-port.
- No clinical claims; supportive/educational only; avoid diagnosis/treatment/cure/guarantees.
- Zero plagiarism; write original; no copying.
- Brand lock: do not modify locked logo/brand assets; no recolor/rename.
- Privacy: never log secrets; never store raw journaling text in server logs.
- Security: safest defaults; sanitize inputs; rate limit sensitive endpoints.

================================================
PHASE 0 — CHECKPOINT BEFORE BATCH 13
================================================
Commit:
"checkpoint: before Batch 13 deep-scan v6 + next50 (301-350)"

=========================================================
PHASE 1 — DEEP SCAN v6 (NO DUPLICATE WORK — MAXIMUM)
=========================================================
Write to:
- docs/batch-13/deep-scan.json
- docs/batch-13/deep-scan.md
- docs/batch-13/duplicate-risk-ledger.md

Deep Scan v6 MUST include:

A) Registry + Canonical Ownership Enforcement
- Locate the integration registry (source of truth).
- For EVERY major domain (routes, pages, APIs, DB, design system, microcopy, admin):
  define canonical owner files.
- Anything duplicate must be tagged as:
  (canonical | duplicate | legacy | orphan)
No deletion—only tagging and routing decisions.

B) Proof-of-Done v2 (Stricter)
Create/Upgrade:
- scripts/proofOfWorkV2.ts (or .mjs)
It must verify DONE claims via:
- build passes
- typecheck passes
- routeKey -> canonicalPath resolves
- key pages render (headless smoke via playwright if present; else server fetch checks)
- endpoint contracts respond (200/4xx expected, never 500)
- DB table exists if referenced
Any DONE without proof becomes in_progress + queued.

C) No-Duplicate-Work v6 (Hard Fail + Similarity)
Create/Upgrade:
- scripts/noDuplicateWorkV6.ts (or .mjs)

It MUST hard fail on:
- duplicate routeKey
- duplicate canonicalPath
- duplicate endpoint signature (METHOD+PATH)
- duplicate DB table name
- duplicate schema exports
- duplicate UI component name collisions (same export name)
- duplicate microcopy keys

It MUST soft-flag (report only) on:
- near-duplicate pages (similarity > 0.80)
- near-duplicate content blocks (similarity > 0.85)
- repeated microcopy on same page > 2 times

D) “50-at-a-time” Incomplete Queue Builder
Create:
- docs/batch-13/incomplete-queue.md
Sort priority:
1) build/typecheck blockers
2) auth/billing blockers
3) broken nav / broken routeKey resolution
4) broken admin pages
5) UI overcrowding / tiny buttons / poor hierarchy

E) Design System Drift Scan
Create:
- docs/batch-13/design-system-drift.md
Report where spacing/typography/button tokens are not used.
Enforce:
- button min height 44px
- consistent H1/H2 scale
- consistent container widths
- card-based content, no walls of text

F) Safety & Legal Copy Scan v2
Create:
- docs/batch-13/safety-copy-scan.md
Find + replace risky language:
"treat, cure, diagnose, guaranteed, therapy, medical advice"
Replace with:
"may help, some people find, educational, supportive, consider, at your pace"
Ensure:
- disclaimers present on all relevant pages
- crisis resources appear only where risk indicates

After scan run:
- node scripts/noDuplicateWorkV6.*
- node scripts/proofOfWorkV2.*
Fix failures FIRST before implementing new processes.

=============================================================
PHASE 2 — COMPLETE INCOMPLETE INTEGRATIONS FIRST (UP TO 50)
=============================================================
If incomplete items exist:
- Fix them first, up to 50, in patches of 10.
Patch constraints:
- Max 10 items per patch
- Max 15 files per patch
Each patch ends with:
  npm run build
  npm run typecheck
  node scripts/noDuplicateWorkV6.*
  node scripts/proofOfWorkV2.*
  npm test (if present)
Document each patch:
- docs/batch-13/patch-XX.md
Update registry status:
- in_progress -> done ONLY when proof passes

Commit after each patch:
"Batch 13 Fix Patch X: incomplete integrations (items ...)"

Only when incomplete queue is empty (or first 50 resolved), continue.

=============================================================
PHASE 3 — NEXT 50 TOP PLATFORM PROCESSES (#301–#350) — APPLY
=============================================================
Implement ONLY missing processes (auto-skip if registry says done).
Update:
- docs/platform/top-platform-processes-matrix.md
Mark #301–#350 planned -> in_progress -> done.

Required Process Set #301–#350 (implement if missing):

A) PRODUCT + ONBOARDING (10)
301) First-run onboarding flow (3 steps, calm)
302) Personal goals selection (values-based, non-clinical)
303) “My Plan” dashboard (next best step)
304) Gentle reminders system (opt-in UI, no auto emails unless enabled)
305) Tool recommendations engine (rule-based, transparent)
306) “Progress snapshots” (weekly summary, non-shaming)
307) Export progress (PDF/JSON) with privacy guardrails
308) Account deletion request flow (safe confirmation)
309) Data download (portable export)
310) Help center hub (FAQ + contact)

B) CONTENT OPS + PUBLISHING (10)
311) Blog editor improvements (preview + autosave local)
312) Content moderation queue (admin approvals)
313) Tag taxonomy governance (admin)
314) Internal linking assistant (routeKey suggestions)
315) Plagiarism risk guard (heuristics; warn only)
316) Canonical content templates (Beginner/Intermediate/Advanced)
317) “Microcopy library” enforcement helper
318) Content versioning metadata (revision notes)
319) “Evidence notes” internal section (citations stored internally)
320) Sitemap auto-update on publish

C) AI + SAFETY EXPERIENCE (10)
321) AI prompt guardrails per tool (risk-based)
322) AI refusal + safe redirection patterns (crisis-aware)
323) Session-level “tone controls” (gentle/direct)
324) “Softer version” always available on AI responses
325) AI journaling privacy: client-side encryption option (if feasible) OR strong minimization
326) AI rate limit + cost guardrails (admin configurable)
327) AI audit log (metadata only; no user text)
328) AI evaluation harness (prompt tests, snapshot)
329) AI fallback responses when provider fails
330) “Transparency: how AI is used” page (trust)

D) BILLING + MONETIZATION (10)
331) Pricing page refinement (clear tiers)
332) Feature gates (server + client)
333) Stripe webhook hardening (idempotency, retries)
334) Subscription status sync job (human-triggered)
335) Grace period handling (soft messaging)
336) Upgrade/downgrade flow UX (simple)
337) Invoice history page
338) Refund request info page (policy + contact)
339) Lead magnet funnel wiring (free -> email -> offer)
340) Affiliate/referral tracking stub (privacy-safe)

E) PERFORMANCE + QUALITY (10)
341) Route-level code splitting audit (Vite)
342) Image optimization rules + lazy loading
343) Lighthouse baseline script + report
344) Bundle analyzer script + threshold warnings
345) Error boundary coverage across key routes
346) i18n readiness scan (no hardcoded strings in core UI)
347) Accessibility automated checks (if tooling present)
348) E2E smoke suite expansion (top 10 routes)
349) CI pipeline “required checks” doc + enforcement
350) Release notes generator (based on commits)

IMPLEMENTATION METHOD:
- 5 patches total (10 processes each).
After EACH patch run:
- npm run build
- npm run typecheck
- node scripts/noDuplicateWorkV6.*
- node scripts/proofOfWorkV2.*
- npm test (if present)

Commit after each patch:
"Batch 13 Patch X: processes ###-###"

================================================
PHASE 4 — FINAL VALIDATION + CHECKPOINT
================================================
Must pass:
- npm run build
- npm run typecheck
- node scripts/noDuplicateWorkV6.*
- node scripts/proofOfWorkV2.*
- Smoke: start server + verify 10 key pages load (home, tools hub, 2 tools, pricing, login, admin, blog, onboarding, my-plan)

Final commit:
"checkpoint: after Batch 13 next50 (301-350) + deep-scan v6"

FINAL OUTPUT TO USER:
1) Deep scan summary + docs paths
2) Incomplete integrations completed list
3) Processes #301–#350 completed list
4) Remaining Top 20 priorities for Batch 14
STOP and wait for: "RUN BATCH 14"

If you also want immediate UI fixes (tiny buttons + overcrowding like your screenshots), paste this second:

NAME: GLP_UI_FIXPACK__BATCH13__APPLY
Task:
Find the pages matching my latest screenshots (CTA block, hero, button row).
Apply:
- buttons min-height 44px, px-6 py-3, rounded-lg
- increase vertical spacing between sections
- reduce text density: convert paragraphs to cards
- enforce 1 hero promise + 1 primary CTA
- align H1/H2 scale for consistent hierarchy
Run build + typecheck after.

When Batch 13 finishes, tell me “RUN BATCH 14” and I’ll generate NEXT50 (#351–#400) with the same APPLY + deep-scan structure.