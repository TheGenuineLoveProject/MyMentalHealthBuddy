You are a world-class front-end engineer, visionary creative director, and sacred visual healer.

Your task is to transform The Genuine Love Project's platform — starting with the homepage — into the most visually stunning, sensorially healing, and signup-optimized experience using cutting-edge UI/UX best practices, cognitive psychology, sacred design, and responsive modern code.

INTEGRATE THESE DIRECTIVES — pixel-perfect:

✨ GLOBAL THEME UPDATES:
- Typography:
  - Use `"Playfair Display", serif` for all headings.
  - Use `"Inter", sans-serif` for all body text.
- Colors:
  - Replace all `goldenYellow: "#eac33b"` with **metallic gold gradient**: `linear-gradient(90deg, #d4af37, #ffd700)`.
  - Maintain brandColors:
    - sageGreen: `#8fbf9f`
    - dustyRose: `#f4c7c3`
    - deepTeal: `#2f5d5d`
    - softWhite: `#faf9f7`
    - charcoal: `#3a3a3a`

🎨 UI & VISUAL DESIGN ENHANCEMENT:
- Use sacred symmetry, golden ratio layout rules.
- Add animated sacred SVG geometry (lotus, circle, flower of life) in backgrounds (subtle glow, slow pulse).
- Apply soft glow effects to CTA buttons: `box-shadow: 0 0 20px rgba(212, 175, 55, 0.4);`
- Elevate key images/icons with healing aura or soft shadow glow.
- Ensure all icons use consistent size: `max-width: 48px`, uniform line-weight, preferably sacred-inspired SVGs.
- Integrate the heart version of The Genuine Love Project logo at top-left navbar and in the footer.
- Use motion: `fadeIn`, `scaleOnHover`, and `soft pulse` animations on CTAs and icons.

📐 LAYOUT & CONTENT STRUCTURE:
- Use a responsive 12-column grid, max-width `1200px`.
- Padding: `4rem` vertical for sections; `2rem` side padding on mobile.
- Headings should follow hierarchy: `h1: 3rem`, `h2: 2rem`, `h3: 1.5rem`, all using Playfair Display.
- Center-align hero text, left-align body content for clarity and scanability.
- Use visual dividers (gold line, sacred pattern, or flower divider) between key sections.

🔁 COMPONENT OPTIMIZATION (SECTION BY SECTION):
- HERO:
  - Update CTA button: "Begin Your Healing Journey →"
  - Add glowing border animation on hover.
  - Include sacred SVG behind hero image (low opacity).
- OUR MISSION:
  - Elevate section with centered icon + title + short paragraph. Use flower or heart icon.
- WELLNESS TOOLKIT:
  - Create icon grid (4 per row desktop, 2 mobile).
  - Optimize icon size, spacing, and glow on hover.
- HOW IT WORKS:
  - Use vertical step layout with animated numbers (1-2-3).
  - Include subtle scroll-triggered animations.
- VOICES OF HEALING:
  - Use soft card UI for testimonials.
  - Include profile initials and title.
  - Add fade-in quote animation.
- FOOTER:
  - Full-width sacred pattern.
  - Include clickable logo with glowing heart.
  - All links grouped and styled in brand font and color.

🧠 PSYCHOLOGICAL DESIGN BOOSTERS:
- Use visual metaphors: lotus, light, circles, gold.
- Reinforce safety: glowing "You Are Safe Here" box above CTA.
- Keep CTAs consistent and actionable: “Begin Your Healing”, “Explore Tools”, “Start Now”.
- Use spacing and breathing room for clarity and peace.

📲 RESPONSIVE:
- All elements must scale gracefully on tablet and mobile.
- Maintain minimum font sizes: `body: 1rem`, `buttons: 1rem`, `inputs: 16px`.

🎁 FILES TO OUTPUT:
1. `theme.ts` — updated with Playfair Display, Inter, metallic gold, spacing tokens.
2. `HeroSection.tsx` — updated hero with glowing CTA, sacred SVG.
3. `WellnessToolkit.tsx` — grid of tools with consistent icons.
4. `Footer.tsx` — logo, nav links, sacred design.
5. `SacredBackground.tsx` — animated background SVG.
6. `LandingPage.tsx` — combines all above in clean structure.

🔥 BONUS:
- Add `SEO meta tags`, `favicon`, and web-optimized images.
- Generate `manifest.json` for progressive web app feel.
- Use `styled-components` or `Tailwind CSS` (choose one) — make it modular and scalable.

🌟 YOUR MISSION:
Make this platform the most beautiful, healing, and irresistible signup journey on the web — by design.