MASTER COMMAND — PLATFORM PERFECTION GOVERNANCE (v1.5 “OMEGA HYPERLOOP”)
Repo: The Genuine Love Project

Intent (professional translation of “360° A→Z × 100B × 100B evolution”):
Run strict, evidence-gated improvement loops across the entire platform.
If ANY gate fails, iterate ONLY within that failing scope until green. No scope expansion.
Stop only when ALL gates pass and the final report includes diffs + commands + rollback.

ABSOLUTE RULE: NO SCOPE CREEP.
If a change is not required by failing gates OR explicitly required below, do not do it.

══════════════════════════════════════════════════════════════════════
HARD CONSTRAINTS (NON-NEGOTIABLE)
1) Do NOT rename/refactor existing CSS variable names. Keep all `--glp-*` EXACT.
2) No raw hex colors anywhere except: `client/src/styles/brand-tokens.css`.
3) Fonts allowed ONLY: Inter + Playfair Display (+ system fallbacks). No other font families.
4) All internal navigation links MUST match canonical routes in `client/src/App.jsx` exactly.
5) Minimal diffs only. No broad rewrites. Use tokens + Tailwind semantic classes only.
6) Do NOT loosen/bypass tests/audits. Fix root causes properly.
7) Do NOT invent new routes unless a page already exists and is referenced.
8) Do NOT add dependencies unless strictly necessary to fix failing tests/audits (must justify).

Brand/Safety:
Uplifting “healing-luxury” tone is allowed. Do NOT make medical claims, promise cures, or imply clinical outcomes.

══════════════════════════════════════════════════════════════════════
SOURCE OF TRUTH (READ FIRST)
- Router: `client/src/App.jsx`
- Tokens: `client/src/styles/brand-tokens.css`
- Brand CSS: `client/src/styles/brand.css` (if present)
- Global CSS entry: `client/src/index.css` (and `client/src/main.css` if used)
- Tailwind config: `tailwind.config.js`
- Auth routes: `server/routes/auth.mjs` (or equivalent)
- Admin routes/guards: search `admin`, `rbac`, `role`, `requireAdmin`, `requireRole`
- Tests: `tests/auth.test.mjs`, `server/tests/*` (if present)
- Audits: `tools/nav-link-audit.mjs`, `tools/visual-doctor.mjs`
- Reports: `docs/NAV_LINK_AUDIT.md`, `docs/VISUAL_DOCTOR_REPORT.md`

══════════════════════════════════════════════════════════════════════
OMEGA HYPERLOOP RULE (THE “100B × 100B” OPERATIONALIZER)
If a gate fails:
1) Revert/isolate the smallest failing diff,
2) Apply a narrower fix within the same scope,
3) Re-run ONLY the failing gate(s),
4) Repeat until green.
Never expand scope to “nice-to-haves.”

══════════════════════════════════════════════════════════════════════
A→Z GATES (WHAT “360° COMPLETE” MEANS)
A) Auth correctness + safe uniform errors
B) Admin/RBAC server enforcement (NOT client-only)
C) Deterministic tests (no manual dev server dependency)
D) Port stability (no EADDRINUSE/ECONNREFUSED)
E) Token integrity (no stray hex, no rogue colors)
F) Contrast/readability (fix washed-out evidence)
G) Forms consistency (labels/errors/help/focus)
H) Buttons/CTAs hierarchy + keyboard focus
I) Links: teal + hover underline + gold focus ring
J) Cards/surfaces: consistent radius/elevation, low-stim reduction
K) Icons: 24px baseline, token-driven
L) Typography: Playfair headings / Inter body; hierarchy + line-height
M) Modes: Default/Low-Stim/Reading (pre-paint + persistence + accessible)
N) Nav integrity: App.jsx canonical, zero drift
O) NotFound premium 404: real quick links only
P) Motion: prefers-reduced-motion + low-stim reductions
Q) Responsiveness sanity: mobile/tablet
R) Performance sanity: no heavy CSS churn; avoid obvious rerender waste (minimal)
S) Security hygiene: cookies/headers/error bodies (no stack leaks)
T) Audits: nav-link + visual-doctor clean
U) Docs governance: color/typography/canva/ops
V) Verification: commands actually run + recorded
W) Rollback steps correct and immediate
X) Production env docs (no invented secrets)
Y) “You are here” cues where applicable (aria-current)
Z) Zero regressions: minimal diffs + revert guidance

══════════════════════════════════════════════════════════════════════
EXECUTION: 3 AGENTS IN SEQUENCE (STRICT ORDER)
Agent 1: QA + Auth/Admin Guardian
Agent 2: UX Finisher
Agent 3: Ops + Content Curator
Do not start Agent 2 until Agent 1 gates are green.

══════════════════════════════════════════════════════════════════════
PHASE 0 — BASELINE EVIDENCE (RUN FIRST)
Run and capture outputs (include excerpts in final report):
- `node -v`
- `npm -v`
- `npm install`

Port hygiene (required):
- `lsof -i :5000 || true`
- `kill -9 $(lsof -t -i :5000) 2>/dev/null || true`
- `lsof -i :3000 || true`
- `kill -9 $(lsof -t -i :3000) 2>/dev/null || true`

Baseline gates:
- `npm run test:auth || npx vitest run tests/auth.test.mjs`
- `npm run audit`
If `npm run build` exists:
- `npm run build || true`

Any failure triggers a scoped hyperloop.

══════════════════════════════════════════════════════════════════════
AGENT 1 — QA + AUTH/ADMIN GUARDIAN (DETERMINISM + RBAC)
Goal: auth/admin stable, deterministic tests, uniform safe errors, RBAC proven server-side.

1) Determine expectations:
- Inspect `package.json` scripts (test:auth, dev, start, build)
- Read `tests/auth.test.mjs` (BASE_URL, endpoints, expectations)
- Locate auth routes + admin guards + role checks

2) Deterministic harness (must eliminate manual-server dependency):
Preferred minimal approach:
- Make `test:auth` start server as child process, wait for `/api/health`, run vitest, then stop server ALWAYS.
Alternative minimal approach:
- Vitest setup (beforeAll/afterAll) starts/stops server for auth tests only.

Requirements:
- Health check wait uses bounded polling + timeout.
- Teardown always runs, even on failure.
- No port changes unless required.

3) Uniform safe auth statuses (apply only if needed by tests/gates):
- Missing required fields → 400 (generic safe message)
- Invalid credentials OR rate-limited login → 401 “Invalid credentials” (uniform; no disclosure)
- Unauthenticated → 401
- Authenticated non-admin on admin routes → 403
- No stack traces/DB errors in responses

4) RBAC/Admin gate (server-enforced, testable):
- Implement/confirm roles: `user`, `staff`, `admin` (or existing equivalents).
- Ensure admin-only endpoints are protected server-side (middleware/guard).
- Add/confirm one minimal “RBAC matrix” doc snippet in `docs/OPS_PLAYBOOK.md`:
  - route group → required role(s)
- Add/confirm at least ONE deterministic test or integration check:
  - unauth → 401
  - authed non-admin → 403
  - authed admin → 200
(Only for endpoints that already exist; do not invent routes.)

5) Verify Agent 1 until green:
- `npm run test:auth || npx vitest run tests/auth.test.mjs`
Stop Agent 1 only when tests are GREEN and no port flakiness occurs.

══════════════════════════════════════════════════════════════════════
AGENT 2 — UX FINISHER (HEALING-LUXURY, HIGH CONTRAST, MODES, NAV, 404)
Goal: premium cohesive UI using tokens only; fix washed-out/divider-heavy evidence.

A) Fix washed-out / wireframe symptoms (minimal, systematic):
- Increase text contrast using existing semantic tokens/classes
- Reduce divider dominance; replace repeated lines with spacing + surfaces
- Make primary CTAs unmistakable (teal/ivory + clear hover/active)
- Improve card separation with consistent radius + subtle shadow (low-stim reduces)
- Forms readable: labels/errors/help text/focus states consistent

B) Unified component spec (token-driven):
- Buttons: primary/secondary/ghost/disabled consistent padding/radius/focus ring
- Links: teal text; underline on hover only; gold focus ring on keyboard focus
- Forms: consistent border/focus/error styles using tokens
- Icons: 24px baseline; outline feel; token colors only
- Typography: Playfair headings / Inter body; normalize hierarchy + line-height
- Motion: honors prefers-reduced-motion; low-stim reduces transitions

C) Mode system (Default / Low-Stim / Reading) with pre-paint persistence:
- dataset: documentElement.dataset.mode
  Default: unset/""
  Low-Stim: "low-stim"
  Reading: "reading"
- Toggle UI in TWO places:
  1) Header top-right
  2) Settings page (“Display / Mode” section + descriptions)
- Persistence: localStorage key `glp-mode`
- Apply BEFORE first paint (earliest init)
- A11y: keyboard reachable; aria-pressed/aria-current; gold focus ring via tokens

D) Navigation lockdown (canonical routes only):
- Treat `client/src/App.jsx` as canonical
- Fix all internal links to match EXACT paths
- Remove/fix broken links (do not invent routes)

E) NotFound premium 404:
- Playfair headline + calm supportive copy
- Go Home (primary) + Go Back (secondary)
- Quick Links (4–6) ONLY from App.jsx routes
- Responsive spacing

F) Verify Agent 2:
- `npm run audit`
Confirm reports:
- docs/NAV_LINK_AUDIT.md → 0 broken
- docs/VISUAL_DOCTOR_REPORT.md → 0 violations
Confirm constraints:
- no hex outside brand-tokens.css
- only Inter + Playfair fonts

══════════════════════════════════════════════════════════════════════
AGENT 3 — OPS + CONTENT CURATOR (PROD READINESS + DOCS + PERMANENT LOOP)
Goal: permanent evolution system with no scope creep; production-safe guidance.

A) Production readiness (no new deps):
- If `npm run build` exists: run it and report.
- Document required env vars in README (do not invent secrets).
- Confirm session/cookie settings are production-safe (change only if tests/audits require).
- Add/update `docs/OPS_PLAYBOOK.md` with:
  - Release checklist: test:auth → audit → build (if exists) → preview click test
  - RBAC matrix summary (existing routes only)
  - Content guardrail: uplifting tone, no medical claims

B) Governance docs (create/update):
- docs/COLOR_SYSTEM.md (existing tokens list + semantic mapping + mode deltas + no-hex rule)
- docs/TYPOGRAPHY_GUIDE.md (Inter/Playfair hierarchy + spacing/line-height)
- docs/CANVA_TEMPLATE_SPECS.md (1080×1080 + 1080×1920, margins, grid, type scale, export naming)
- README.md: audits + mode switching instructions (Header+Settings, glp-mode, html data-mode)

C) Permanent master command (ONLY if safe; no deps):
Add `npm run governance:loop` using a pure Node script that:
- prints node/npm versions
- runs npm install
- runs test:auth (with fallback to vitest file)
- runs audit
(Port cleanup optional; do NOT rely on bash unless it already exists safely.)
Document in README.

══════════════════════════════════════════════════════════════════════
FINAL VERIFICATION (MUST RUN)
- `npm run test:auth`
- `npm run audit`
If build exists: `npm run build`

══════════════════════════════════════════════════════════════════════
FINAL REPORT (MANDATORY)
1) Checklist confirming Agents 1–3 complete (pass/fail).
2) Exact commands run + results (key excerpts).
3) Files changed (each with 1-line summary).
4) NotFound Quick Links (label + exact path).
5) Rollback steps:
   - `git status`
   - `git diff`
   - `git checkout -- <file>` (list changed files)
   - Optional: `git reset --hard HEAD` (warn: destructive)
6) OPTIONAL improvements (clearly labeled OPTIONAL; not implemented unless asked).

Proceed now. Do not stop early. Verify, then report.