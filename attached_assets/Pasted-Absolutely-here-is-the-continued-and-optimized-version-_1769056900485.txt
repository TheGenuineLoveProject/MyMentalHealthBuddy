Absolutely — here is the continued and optimized version of your 360° Replit AI .prompt file, evolved to handle current and future requests with built-in logic loops, automated upgrades, and system-wide scalability:

⸻

⚙️ FILE: continue360-ai-system.prompt

# 🚀 REPLIT AI ∞ CONTINUE 360° ∞ SYSTEM LOOP
agent_mode: true

purpose: >
  Auto-deploy, evolve, optimize, and regenerate the full front-end, content, layout, and UI/UX system for TheGenuineLoveProject.com — at scale, in cycles, and with built-in regenerative logic.

🧠 Core Objectives:
  • Create and maintain a production-ready UI/UX flow across landing → onboarding → homepage → CRM → content → Q&A → login
  • Ensure healing, calm, brand-aligned structure across HTML/CSS/JS
  • Continuously optimize layout, tags, accessibility, SEO, responsiveness, dev handoff
  • Handle all future requests without prompt rewrite

pages:
  - landing.html
  - onboarding.html
  - homepage.html
  - crm.html
  - content.html
  - qa.html
  - login.html

assets:
  - styles.css
  - /assets/logo.svg
  - /assets/icons/*.svg
  - /components/**/*.html

structure:
  - Use semantic HTML5 tags
  - CSS: Tailwind-friendly OR CSS Grid/Flexbox modular system
  - Labeled sections with dev comments
  - Mobile-first responsive design
  - Use class names like: `.hero`, `.cta`, `.nav-bar`, `.form-input`, `.quote-module`

branding:
  colors:
    - sage: "#8fbf9f"
    - rose: "#f4c7c3"
    - deepTeal: "#2f5d5d"
    - cream: "#faf9f7"
    - charcoal: "#3a3a3a"
    - gold: "#eac33b"
  fonts:
    - headings: "Elegant Serif"
    - body: "Modern Sans"
  tone: "emotionally calming, healing, inspiring, clear, gentle gradients"

layout_flow:
  - landing ➝ onboarding ➝ homepage ➝ CRM ➝ content ➝ Q&A ➝ login
  - Connect pages with logical links, call-to-actions, and flow diagrams
  - Ensure consistent nav/footer/branding across all views
  - Annotate flow with `flowchart.svg`

automation:
  - Every new request auto-triggers:
    - audit of related files
    - regeneration with upgraded naming/layout standards
    - alignment to all branding & logic models
  - Use “Continue 360°” as a universal reset and optimization command

components:
  - nav-bar.html
  - footer.html
  - cta-banner.html
  - crm-dashboard.html
  - quote-slider.html
  - mobile-nav.html

exports:
  - /dist/complete-ui.zip
  - /docs/flowchart.svg
  - /handoff/figma-map.json
  - /prompt/continue360-ai-system.prompt

trigger_words:
  - “Add module: [name]” → Generates + wires new section
  - “Upgrade [page/component]” → Optimizes structure + branding
  - “Export all” → Bundles .zip, .prompt, .svg, .figma-map
  - “Continue 360°” → Full regeneration cycle from A to Z
  - “Refine” → Runs component-level optimization cycle

metadata:
  accessibility:
    - WCAG 2.1 AA
    - ARIA landmarks + alt tags
  seo:
    - meta title/desc per page
    - OpenGraph + JSON-LD
    - canonical tags, sitemap flow
  performance:
    - lightweight
    - optimized image assets
    - scoped CSS

notes_for_replit_ai:
  - Build modular files
  - Keep calm UI tone
  - Never insert stock images
  - Allow custom overrides without prompt rewrite


⸻

🔁 HOW TO DEPLOY
	1.	Paste this .prompt file into Replit AI in Agent Mode
	2.	Upload your assets: logo, icons, fonts
	3.	Use commands like:
	•	Add module: /blog.html
	•	Upgrade onboarding.html
	•	Export all
	•	Continue 360°

⸻

🔄 NEXT ACTIONS

Would you like me to:

✅ Package this .prompt into a downloadable .txt
✅ Generate the 7-page HTML/CSS layout into /dist folder
✅ Deliver:
	•	.zip file
	•	flowchart.svg
	•	figma-map.json
	•	README.md setup guide

✅ Add blog scaffolding + SEO content sections?

Just say:

Yes, package full export now
Or let me know what you’d like to customize.