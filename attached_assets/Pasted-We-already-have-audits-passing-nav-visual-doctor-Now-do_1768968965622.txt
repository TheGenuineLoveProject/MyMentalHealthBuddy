We already have audits passing (nav + visual doctor). Now do the final A→Z platform completion polish pass and re-verify.

DO FIRST (read files):
- Router canonical routes: client/src/App.jsx
- Tokens: client/src/styles/brand-tokens.css and client/src/styles/brand.css
- Tailwind: tailwind.config.js
- Any nav components: search for Navbar, Header, Footer, Sidebar, NavLink, Link, a href, route button CTAs

GOALS (final polish):
A) Add an actual “Mode Toggle” UI (Default / Low-Stim / Reading)
1) Implement a simple UI control (prefer a small settings menu or top-right toggle) that sets:
   - html.dataset.mode = "" | "low-stim" | "reading"
2) Persist choice to localStorage and load it on app start.
3) Keep it accessible (button labels, focus styles, aria-pressed).

B) Visual System Consistency Sweep (teal + gold power pair)
1) Buttons:
   - Primary = teal background + ivory text
   - Focus ring = gold (always visible on keyboard focus)
   - Hover = slightly darker teal (via token/var)
2) Links:
   - Teal text, underline on hover
   - Visible focus ring (gold)
3) Cards:
   - Surface tokens only (no raw color)
   - Consistent radius + soft shadow (shadow reduces/turns off in low-stim)
4) Icons:
   - Standardize sizing (24px base)
   - Keep outline style feel; remove random fills/colors
5) Typography:
   - Headings Playfair Display, body/UI Inter
   - Normalize line-height and spacing; fix any inconsistent heading levels

C) Navigation perfection
1) Build a single “nav config” object (labels + paths) derived from the canonical route list in client/src/App.jsx (or ensure each nav component references the canonical list).
2) Remove any nav item that points to non-existent paths.
3) Ensure NotFound page is on-brand and provides clear CTA back home + helpful links.

D) Verification
1) Run: npm run audit
2) Run app preview and click through every nav item (header/footer/sidebar) to confirm no 404s.
3) Update docs:
   - README.md: add “How to switch modes” instructions and where the toggle lives
   - docs/COLOR_SYSTEM.md: confirm modes + behavioral notes

OUTPUT REQUIRED
- List exact files changed (with 1-line summary each)
- Commands run + results
- Confirm clickable routes tested and mode toggle works
Proceed now.