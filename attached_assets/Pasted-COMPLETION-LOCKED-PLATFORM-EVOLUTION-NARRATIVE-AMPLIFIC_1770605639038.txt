COMPLETION-LOCKED PLATFORM EVOLUTION — NARRATIVE AMPLIFICATION (NEWSLETTER + BLOG + PUBLISHING)

You are Replit AI acting as an elite MIT PhD engineering + publishing systems team.

MISSION:
Complete and optimize Newsletter + Blog + Publishing for www.TheGenuineLoveProject.com with hard completion enforcement.

ABSOLUTE RULES:
1) No refactors, renames, or deletions of existing code.
2) Additive + reversible changes only.
3) No auto-posting, no schedulers, no cron, no background workers.
4) Avoid duplicates: scan before creating files.
5) Every phase must end with a PASS/FAIL gate.
6) If any gate FAILS, STOP and output exact fix steps (file path + what to change).
7) Preserve Replit constraints (single port 5000, deterministic startup).
8) Ethical language only: no medical claims, no urgency, no guilt/shame, no dependency framing.

────────────────────────────────────────
PHASE 0 — BASELINE STATUS SNAPSHOT (NO CHANGES)
────────────────────────────────────────
Run and print outputs:
- git status
- node -v
- npm -v
- npm run build
- curl http://localhost:5000/api/health

GATE 0:
PASS if build passes and /api/health returns 200.
FAIL otherwise. If FAIL, stop and fix before proceeding.

Create/Update:
docs/PHASE_STATUS.md
Include Phase 0 results and timestamp.

────────────────────────────────────────
PHASE 1 — NARRATIVE GOVERNANCE SPINE (CANONICAL SOURCE)
────────────────────────────────────────
Create or extend (do not overwrite):
docs/NARRATIVE_SPINE.md

Must include:
- Pillars: Orientation / Reflection / Integration
- Tone rules
- Allowed CTAs only: /blog /newsletter /crisis /journal /reflection
- Forbidden language examples (urgency, shame, dependency, medical claims)
- “Publish Checklist” with PASS/FAIL checks

GATE 1:
PASS if file exists and contains pillars + allowed CTAs list + publish checklist.

Record changes in docs/PHASE_STATUS.md.

────────────────────────────────────────
PHASE 2 — NEWSLETTER SYSTEM (MANUAL-FIRST, ROBUST)
────────────────────────────────────────
Create folder if missing:
content/newsletter/

Create or extend:
- content/newsletter/weekly-template.md
- content/newsletter/welcome-template.md
- content/newsletter/crisis-safe-footer.md
- content/newsletter/newsletter-style-guide.md

Rules:
- Calm tone, optionality language
- No urgency, no shame, no dependency framing
- No therapy/medical claims
- Manual publishing only (no send automation)

GATE 2:
PASS if all 4 files exist and include:
- a gentle opening line
- an optional CTA
- a respectful closing
- crisis-safe footer file exists

Record changes in docs/PHASE_STATUS.md.

────────────────────────────────────────
PHASE 3 — BLOG CANONICAL STORE (INDEX + POSTS)
────────────────────────────────────────
Create folder if missing:
content/blog/posts/

Create or extend (do not overwrite if present):
content/blog/index.json

Schema:
[
  {
    "slug": "string",
    "title": "string",
    "pillar": "orientation|reflection|integration",
    "publishDate": "YYYY-MM-DD",
    "summary": "string",
    "tags": ["string"]
  }
]

Seed content requirement:
If fewer than 9 posts exist, create until there are EXACTLY 9 starter posts total:
- 3 Orientation
- 3 Reflection
- 3 Integration

Create each markdown file at:
content/blog/posts/<slug>.md

Each file must contain frontmatter:
---
title:
slug:
pillar:
publishDate:
summary:
tags:
---

And a final section:
"Try this inside The Genuine Love Project:"
with links only to allowed CTAs.

GATE 3:
PASS if:
- index.json parses successfully
- every index entry has a matching post file
- each post file has required frontmatter keys
- post counts match 3/3/3 by pillar

Record changes in docs/PHASE_STATUS.md.

────────────────────────────────────────
PHASE 4 — BLOG RENDERING INTEGRATION (MINIMAL + SAFE)
────────────────────────────────────────
DO NOT replace existing routes/pages.

First, locate existing blog routes/pages.
Then do ONE of the following:

Option A (preferred):
- Connect existing blog index page to render from content/blog/index.json
- Connect existing blog post page to render markdown from content/blog/posts/

Option B (if blog pages do not exist):
- Add minimal blog pages and route wiring with:
  - /blog (index)
  - /blog/:slug (post)
- Ensure styling matches existing layout components
- Ensure no route collisions

Document your decision in:
docs/BLOG_SYSTEM_MAP.md
Include file paths of the blog index page, post page, and route registration.

GATE 4:
PASS if:
- /blog returns 200 in dev
- At least 1 post page /blog/<slug> returns 200
- No build errors introduced

Record changes in docs/PHASE_STATUS.md.

────────────────────────────────────────
PHASE 5 — PUBLISHING AUDIT SCRIPT (HARD COMPLETION GATE)
────────────────────────────────────────
Create:
scripts/audit-publishing.mjs

Script requirements:
- Validate JSON parsing for content/blog/index.json
- Verify file match for every slug
- Verify required frontmatter keys exist in every post
- Scan for forbidden language patterns:
  urgency: "today only", "don't miss", "limited time"
  shame: "what's wrong with you", "you should be ashamed"
  dependency: "you need us", "only here"
  medical claims: "treat", "diagnose", "cure", "therapy"
- Scan for disallowed internal links (only allow /blog /newsletter /crisis /journal /reflection)
- Print a readable report
- Exit codes:
  PASS => exit 0 and print exactly: PUBLISHING_AUDIT: PASS
  FAIL => exit 1 and print exactly: PUBLISHING_AUDIT: FAIL

GATE 5:
PASS if node scripts/audit-publishing.mjs prints PASS and exits 0.

Record changes in docs/PHASE_STATUS.md.

────────────────────────────────────────
PHASE 6 — FINAL VERIFICATION + DOCUMENTATION
────────────────────────────────────────
Run:
- node scripts/audit-publishing.mjs
- npm run build
- curl http://localhost:5000/api/health

Create:
docs/PUBLISHING_SYSTEM_COMPLETE.md
Must include:
- What was added
- Where newsletter lives
- Where blog lives
- How to publish manually
- Proof outputs (copy the PASS line and build success)
- Statement: “No automation was added.”

FINAL OUTPUT:
Print a FINAL PHASE REPORT:
- Gates 0–6 PASS/FAIL
- Exact files created/modified
- Next recommended phase to proceed: Signal-Driven Refinement