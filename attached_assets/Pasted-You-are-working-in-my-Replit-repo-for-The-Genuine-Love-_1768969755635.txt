You are working in my Replit repo for “The Genuine Love Project”.
Audits already pass (nav-link audit + visual doctor). Now do final product polish: implement the 3-mode toggle (Default / Low-Stim / Reading), unify UI styling, and re-verify.

NON-NEGOTIABLE RULES
- Do NOT rename/refactor existing CSS variable names. Keep current `--glp-*` names exactly.
- No raw hex colors outside `client/src/styles/brand-tokens.css`.
- Fonts allowed only: Inter + Playfair Display (+ system fallbacks). No new font families.
- All internal navigation links must match canonical routes in `client/src/App.jsx` (wouter Switch/Route).
- Minimal diffs, no breaking refactors.

READ FIRST (source of truth)
- Router: `client/src/App.jsx`
- Tokens: `client/src/styles/brand-tokens.css` + `client/src/styles/brand.css`
- Tailwind: `tailwind.config.js`
- Global CSS: `client/src/index.css` (+ `client/src/main.css` if used)
- Find nav UI: search Header/Navbar/Footer/Sidebar/NavLink/Link/a href/CTA buttons
- Audits: `tools/nav-link-audit.mjs`, `tools/visual-doctor.mjs`
- Reports: `docs/NAV_LINK_AUDIT.md`, `docs/VISUAL_DOCTOR_REPORT.md`

TASKS

A) IMPLEMENT REAL MODE TOGGLE UI (BOTH places)
1) Add a Mode control in the Header top-right (segmented control or compact dropdown).
2) Add a “Display / Mode” section inside the Settings page (with short descriptions of each mode).
3) Behavior:
   - Set `document.documentElement.dataset.mode` to: "" | "low-stim" | "reading"
   - Persist to localStorage key `glp-mode`
   - On app start, read localStorage and apply immediately before UI paints (avoid flash).
4) Accessibility:
   - Keyboard reachable
   - aria-pressed or aria-current on the active choice
   - Focus-visible ring uses the gold ring token (no hex)

B) VISUAL CONSISTENCY SWEEP (healing-luxury teal + gold)
1) Buttons: normalize all button variants to a single system:
   - Primary: teal background + ivory text; hover/active via token vars; gold focus ring
   - Secondary: transparent/surface + teal text; soft surface hover; gold focus ring
   - Disabled state: muted surface + muted text; cursor not-allowed
2) Links: unify all links:
   - teal text; underline only on hover; gold focus ring for keyboard focus
3) Cards: unify all cards/surfaces:
   - use semantic surface/bg tokens only
   - consistent radius + soft shadow
   - Low-Stim mode reduces shadows and removes “sparkle” effects (via CSS vars)
4) Icons: standardize:
   - base size 24px
   - consistent stroke/outline style feel
   - no random colors/fills; use current tokens
5) Typography:
   - headings: Playfair Display
   - body/UI: Inter
   - normalize hierarchy (H1/H2/H3 + body/label/caption), line-height, spacing

C) NAV LOCKDOWN (prevent future drift)
1) Treat `client/src/App.jsx` route list as canonical.
2) Ensure every internal nav link (header/footer/sidebar/CTAs/quick links) matches an existing route path exactly.
3) If any nav item points to a missing route, either fix the path or remove the link; do not invent routes unless there is an obvious missing intended page file.

D) VERIFY + DOCUMENT
1) Run: `npm run audit`
2) Update:
   - README.md: “Mode toggle: where it is + how it persists”
   - docs/COLOR_SYSTEM.md: confirm the 3 modes and what changes
3) Click-test in Preview:
   - click every header/footer/sidebar link and confirm no 404s
   - test toggling modes and refreshing to confirm persistence

FINAL RESPONSE REQUIREMENTS
- Confirm A–D complete (checklist)
- Commands run + results summary
- Files changed (1-line summary each)
- Optional improvements (clearly marked OPTIONAL)

Proceed now.