Hard truth

The next improvement is Buddy Engine v1.9 — standardize BuddyOutput.

This is not hardware development.
It is a contract so Buddy can later work across:

* web
* mobile
* onboarding
* journaling
* future physical companion concepts

No AI changes. No route changes. No hardware logic.

⸻

Buddy Engine v1.9 — Standardize BuddyOutput

Paste this into Replit AI:

Upgrade MMHB Buddy Engine to v1.9: standardize BuddyOutput contract for future app/mobile/physical compatibility.
STRICT RULES:
- Do not modify /api/ai/chat
- Do not modify server/routes/buddy.mjs unless only aligning response shape safely
- Do not modify orchestrator, provider, memory, profile, summary, telemetry, crisis logic
- Do not add AI calls
- Do not add memory writes
- Do not change tool execution logic
- Do not replace /start
- Do not delete files
- Do not add hardware logic
- This is a contract/typing/normalization upgrade only
TARGET FILES:
- client/src/lib/avatarState.ts
- client/src/components/avatar/BuddyAvatar.tsx
- docs/architecture/BUDDY_ENGINE.md
OPTIONAL TARGET:
- server/routes/buddy.mjs only if its response already includes buddy output and needs safe alignment
GOAL:
Create one stable BuddyOutput interface that every future Buddy surface can use.
BuddyOutput contract:
{
  state: BuddyState,
  safetyMode: "normal" | "crisis_safe",
  eyeColor: string,
  heartColor: string,
  heartPulse: number,
  motion: "idle" | "slow_glow" | "breathing" | "grounding" | "warm_glow" | "steady" | "sparkle",
  expression: "soft" | "lowered" | "focused" | "grounded" | "bright" | "steady" | "celebratory"
}
PHASE 1 — STANDARDIZE TYPES
In client/src/lib/avatarState.ts:
- Export BuddyState
- Export BuddyMotion
- Export BuddyExpression
- Export BuddySafetyMode
- Export BuddyOutput
- Ensure getBuddyVisualOutput(state) returns the full BuddyOutput contract
PHASE 2 — STATE OUTPUTS
Map states:
calm:
safetyMode normal
eyeColor soft green
heartColor warm rose/red
heartPulse low
motion idle
expression soft
sad:
safetyMode normal
eyeColor muted lavender
heartColor soft purple
heartPulse slow
motion slow_glow
expression lowered
anxious:
safetyMode normal
eyeColor warm amber/green
heartColor warm orange
heartPulse medium
motion breathing
expression focused
overwhelmed:
safetyMode normal
eyeColor muted blue
heartColor grounding blue
heartPulse slow
motion grounding
expression grounded
encouraged:
safetyMode normal
eyeColor bright green
heartColor warm rose
heartPulse medium
motion warm_glow
expression bright
crisis:
safetyMode crisis_safe
eyeColor steady green
heartColor steady green
heartPulse low
motion steady
expression steady
celebrate:
safetyMode normal
eyeColor gold/green
heartColor warm gold
heartPulse medium
motion sparkle
expression celebratory
PHASE 3 — COMPONENT USE
In BuddyAvatar.tsx:
- Use getBuddyVisualOutput(state)
- Avoid hardcoded duplicated color maps if possible
- Keep visual-only responsibility
- No fetch, no AI, no business logic
PHASE 4 — DOC UPDATE
Update docs/architecture/BUDDY_ENGINE.md:
- v1.9 status
- BuddyOutput contract
- future physical compatibility note
- clear statement: no hardware logic yet
PHASE 5 — VERIFY
Run:
npm run dev
Verify:
curl http://0.0.0.0:5000/health
Manual:
- /start loads
- Buddy renders
- all states still render
- reduced motion still respected
- share prompt still works if v1.8 exists
- crisis state still steady and non-flashing
Return:
- files changed
- BuddyOutput contract added
- verification results
- skipped changes

⸻

Shell verification

npm run dev
curl http://0.0.0.0:5000/health
curl -X POST http://0.0.0.0:5000/api/buddy \
-H "Content-Type: application/json" \
-d '{"message":"I feel anxious"}'

⸻

What this improves

Before v1.9:

Buddy visuals = mostly UI state

After v1.9:

Buddy visuals = standardized emotional output contract

That means future systems can safely consume:

state
safetyMode
eyeColor
heartColor
heartPulse
motion
expression

without guessing.

⸻

Next after v1.9

Upgrade Buddy Engine v2.0: use Buddy as a cross-platform visual companion across onboarding, journaling, check-ins, and tools. No AI route changes.

Direct challenge: after v1.9, open /start and confirm Buddy still feels calm, warm, and safe — not technical or robotic.