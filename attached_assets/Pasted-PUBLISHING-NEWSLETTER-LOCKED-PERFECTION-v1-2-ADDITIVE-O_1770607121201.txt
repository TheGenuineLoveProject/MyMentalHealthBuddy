PUBLISHING + NEWSLETTER LOCKED PERFECTION v1.2 (ADDITIVE ONLY)

NON-NEGOTIABLE:
- Additive only: do not rename, delete, move, or refactor existing systems.
- Manual-first: no auto-posting; no spam; no manipulative growth.
- No medical/therapy claims. Trauma-informed, optional language.
- Every step ends with PASS/FAIL evidence.
- Final output MUST include: files changed + commands run + the terminal output showing VERIFIED: PASS.

PRIMARY GOAL:
Make Blog + Newsletter + Publishing robust, auditable, and “cannot regress”:
1) Canonical content + templates
2) Stable routes + safe fallback states
3) Subscribe/unsubscribe pipeline
4) Admin Publishing dashboard (copy-first, human-in-loop)
5) Privacy-first signal logging for publishing actions
6) A single verify command that proves everything works

PHASE 0 — MAP CURRENT REALITY (NO CHANGES)
Print:
- blog index page path(s)
- blog slug page path(s)
- newsletter page path(s)
- server newsletter routes path(s)
- admin publishing/social routes path(s)
- scripts/audit-publishing.mjs path
- any existing analytics routes and admin insights pages

PHASE 1 — CANONICAL CONTENT STRUCTURE (ADDITIVE)
Ensure folders exist:
- content/publishing/blog/
- content/publishing/newsletter/
- content/publishing/templates/

Ensure these template files exist (create if missing):
- content/publishing/templates/weekly-template.md
- content/publishing/templates/welcome-template.md
- content/publishing/templates/crisis-safe-footer.md
- content/publishing/templates/publishing-style-guide.md

If content already exists elsewhere, DO NOT MOVE IT.
Instead:
- create content/publishing/README.md documenting canonical paths
- update loaders to search canonical + legacy locations.

PHASE 2 — BLOG: STABILITY + CTA CONSISTENCY
Requirements:
- /blog must render with 0 posts (gentle empty state)
- /blog sorts by date desc when date exists
- /blog/:slug never crashes; missing slug => Not Found block + Back to Blog
- Add a single reusable CTA component (additive) for:
  - Newsletter CTA
  - Blog CTA
  - Crisis resources CTA (when relevant)
Use optional language and avoid urgency.

PHASE 3 — NEWSLETTER PIPELINE (SAFE)
Ensure endpoints exist and are wired:
- POST /api/newsletter/subscribe
- POST /api/newsletter/unsubscribe

Rules:
- Validate email (basic)
- Rate limit
- Storage preference:
  1) DB: newsletter_subscribers(email unique, status, created_at, unsubscribed_at)
  2) fallback: data/newsletter_subscribers.jsonl (append-only)

Ensure /newsletter page exists and is wired to subscribe endpoint.

PHASE 4 — ADMIN PUBLISHING DASHBOARD (HUMAN-IN-LOOP)
Create or finalize:
- /admin/publishing

Must include:
- List blog posts (from loader)
- List newsletter templates
- Copy buttons for platform-specific captions/CTAs
- Status tagging stored in: data/publishing_status.json
  (draft/approved/published/sent + timestamps)
- Absolutely NO auto-sending and NO auto-posting.

PHASE 5 — PRIVACY-FIRST SIGNALS (PUBLISHING ONLY)
Implement minimal signal logging:
Events allowlist:
- blog_view
- blog_post_view
- newsletter_signup_submit
- newsletter_signup_success
- newsletter_unsubscribe
- pricing_view
- checkout_start

Server:
- POST /api/analytics/event (requireAuth + allowlist + rate limit)
- GET /api/admin/insights/signals (requireAdmin, aggregates last 7 days)

Storage:
- Prefer DB table analytics_events
- Fallback logs/signals.jsonl
No PII: do NOT store email or IP in analytics logs.

Client:
- Add client/src/utils/trackSignalEvent.(js/ts)
- Fire only the allowlisted events on the relevant pages
- Must be non-blocking and dedupe per page load

Admin UI:
- Add /admin/insights (or extend existing admin)
- Show counts by event + top routes + last 50 events (redacted)

PHASE 6 — AUDIT HARDENING
Upgrade scripts/audit-publishing.mjs to enforce:
- templates exist
- frontmatter parse for all posts/templates
- forbidden language scan
- CTA link targets exist (/newsletter /blog /pricing /crisis)
- newsletter endpoints respond (smoke test)
Output EXACTLY one of:
- PUBLISHING_AUDIT: PASS
- PUBLISHING_AUDIT: FAIL (with reasons)

PHASE 7 — COMPLETION LOCK (VERIFY)
Create scripts/verify.mjs (if missing) and package.json scripts:
- "audit:publishing": "node scripts/audit-publishing.mjs"
- "verify": "node scripts/verify.mjs"

verify.mjs must run:
- node scripts/audit-publishing.mjs
- health endpoint check
- npm run build (if exists)
- prints final line:
  VERIFIED: PASS
  or VERIFIED: FAIL (reasons)

REQUIRED FINAL ACTIONS (MUST RUN + PASTE OUTPUT):
1) node scripts/audit-publishing.mjs
2) npm run verify (or node scripts/verify.mjs)
3) curl -s http://localhost:5000/api/health (or correct health endpoint)

FINAL REPORT REQUIRED:
A) Files created/changed (full paths)
B) What changed (bullets)
C) PASS/FAIL checklist
D) Next 3 steps only