You are Replit AI Agent Mode.

TOP-50 PLATFORM PROCESSES PACK #4
(Trust Center + Accessibility + Performance + SEO + Human-Triggered Deep Scan to Prevent Duplicate Work)

Objective:
- Keep “single source of truth” registry system intact (routeKey-only everywhere)
- Add the next 50 high-impact processes (pack #4)
- Add a DEEP SCAN suite that prevents duplicate work:
  - detects duplicate pages/routes/meta entries
  - detects duplicate UI blocks (hardcoded Benefits/Disclaimer/Links)
  - detects duplicate/near-duplicate content chunks across pages
  - detects duplicate routes pointing to same canonicalPath
- Fix build errors and confirm production build passes

DO THIS IN ORDER. NO GUESSWORK. NO MASS REFACTOR.

────────────────────────────────────────────────────────
0) RUN DEEP SCAN FIRST (prevents duplicate work)
If these scripts don’t exist yet, create them now. Then run them BEFORE making content/UI changes.

0A) Create: scripts/deepScan.mjs
PASTE:

---BEGIN PASTE (scripts/deepScan.mjs)---
import fs from "node:fs";
import path from "node:path";
import crypto from "node:crypto";

const ROOT = process.cwd();

const CLIENT_DIR = path.join(ROOT, "client", "src");
const PAGES_DIR = path.join(CLIENT_DIR, "pages");
const COMPONENTS_DIR = path.join(CLIENT_DIR, "components");
const META_DIR = path.join(CLIENT_DIR, "content", "meta");
const REPORT_DIR = path.join(ROOT, "scripts");
const OUT = path.join(REPORT_DIR, ".deep-scan-report.json");

function walk(dir, out = []) {
  if (!fs.existsSync(dir)) return out;
  for (const ent of fs.readdirSync(dir, { withFileTypes: true })) {
    const p = path.join(dir, ent.name);
    if (ent.isDirectory()) walk(p, out);
    else if (/\.(tsx|ts|jsx|js)$/.test(ent.name)) out.push(p);
  }
  return out;
}

function readFile(p) {
  try {
    return fs.readFileSync(p, "utf8");
  } catch {
    return "";
  }
}

function sha(text) {
  return crypto.createHash("sha256").update(text).digest("hex");
}

function normalizeForDupes(code) {
  return (code || "")
    // strip comments
    .replace(/\/\*[\s\S]*?\*\//g, "")
    .replace(/\/\/.*$/gm, "")
    // normalize whitespace
    .replace(/\s+/g, " ")
    // remove common noise
    .replace(/["'`](?:\\.|[^"'`])*["'`]/g, '"STR"')
    .trim();
}

function findHardcodedDupBlocks(code) {
  const flags = [];
  const needles = [
    "not therapy",
    "not medical advice",
    "By using this platform, you confirm you are 18+",
    "Next best step",
    "How this helps",
    "Benefits",
  ];
  for (const n of needles) {
    if ((code || "").toLowerCase().includes(n.toLowerCase())) flags.push(n);
  }
  return flags;
}

function safeRel(p) {
  return path.relative(ROOT, p).replace(/\\/g, "/");
}

function loadRegistryText() {
  // registry is TS; we only scan for obvious duplicate patterns here
  const p = path.join(META_DIR, "routeMetaRegistry.ts");
  return { file: safeRel(p), text: readFile(p) };
}

function extractCanonicalPathLiterals(registryText) {
  // quick heuristic: find occurrences of canonicalPath: "..."
  const re = /canonicalPath\s*:\s*["'`]([^"'`]+)["'`]/g;
  const out = [];
  let m;
  while ((m = re.exec(registryText))) out.push(m[1]);
  return out;
}

function main() {
  fs.mkdirSync(REPORT_DIR, { recursive: true });

  const pages = walk(PAGES_DIR);
  const comps = walk(COMPONENTS_DIR);

  const pageHashes = new Map();
  const nearDupBuckets = new Map();

  const hardcodedBlocks = [];
  const exactDupPages = [];

  for (const file of pages) {
    const raw = readFile(file);
    const norm = normalizeForDupes(raw);
    const h = sha(norm);

    if (pageHashes.has(h)) {
      exactDupPages.push({ hash: h, a: pageHashes.get(h), b: safeRel(file) });
    } else {
      pageHashes.set(h, safeRel(file));
    }

    // near-dup bucket by first 120 chars
    const key = norm.slice(0, 120);
    if (!nearDupBuckets.has(key)) nearDupBuckets.set(key, []);
    nearDupBuckets.get(key).push(safeRel(file));

    const flags = findHardcodedDupBlocks(raw);
    if (flags.length) hardcodedBlocks.push({ file: safeRel(file), flags });
  }

  const nearDupPages = Array.from(nearDupBuckets.entries())
    .filter(([, files]) => files.length >= 3) // 3+ similar starters is a good duplicate signal
    .slice(0, 200)
    .map(([sig, files]) => ({ signature: sig, files }));

  // Component duplicate scan (exact)
  const compHashes = new Map();
  const exactDupComponents = [];
  for (const file of comps) {
    const raw = readFile(file);
    const norm = normalizeForDupes(raw);
    const h = sha(norm);
    if (compHashes.has(h)) exactDupComponents.push({ hash: h, a: compHashes.get(h), b: safeRel(file) });
    else compHashes.set(h, safeRel(file));
  }

  // Registry canonicalPath duplicates heuristic
  const reg = loadRegistryText();
  const canonicalPaths = extractCanonicalPathLiterals(reg.text);
  const canonMap = {};
  for (const p of canonicalPaths) canonMap[p] = (canonMap[p] || 0) + 1;
  const dupCanon = Object.entries(canonMap)
    .filter(([, n]) => n >= 2)
    .map(([p, n]) => ({ canonicalPath: p, count: n }))
    .slice(0, 200);

  const report = {
    generatedAt: new Date().toISOString(),
    counts: {
      pages: pages.length,
      components: comps.length,
      exactDupPages: exactDupPages.length,
      nearDupGroups: nearDupPages.length,
      hardcodedDupBlocks: hardcodedBlocks.length,
      exactDupComponents: exactDupComponents.length,
      dupCanonicalPathsInRegistryHeuristic: dupCanon.length,
    },
    exactDupPages,
    nearDupPages,
    hardcodedDupBlocks,
    exactDupComponents,
    registryHeuristic: {
      file: reg.file,
      dupCanonicalPaths: dupCanon,
      note: "Heuristic only; true registry parse is not performed in this script.",
    },
    actionRules: [
      "If hardcodedDupBlocks > 0, replace in pages with shared components only (SafetyFooter/BenefitsBlock/RelatedLinksBlock).",
      "If exactDupPages > 0, keep ONE canonical file; others become thin wrappers using PageTemplate or redirect routes.",
      "If nearDupGroups > 0, convert to registry-driven meta + single template page.",
      "If dupCanonicalPathsInRegistryHeuristic > 0, ensure each canonicalPath is unique or intentionally aliased.",
    ],
  };

  fs.writeFileSync(OUT, JSON.stringify(report, null, 2));
  console.log(`Deep scan report -> ${safeRel(OUT)}`);
  console.log(`Counts:`, report.counts);
}

main();
---END PASTE---

0B) Add package.json scripts (if not present):
- "scan:deep": "node scripts/deepScan.mjs"

0C) Run:
  npm run scan:deep
Open scripts/.deep-scan-report.json and DO NOT fix anything not listed in its actionRules unless it’s a build failure.

────────────────────────────────────────────────────────
1) TRUST CENTER (Legal-safe, zero-liability stance, registry-driven, shared components)
Create these stubs ONLY if routes don’t exist yet or are flagged by scan:

A) client/src/pages/legal/Disclaimer.jsx
B) client/src/pages/legal/Terms.jsx
C) client/src/pages/legal/Privacy.jsx
D) client/src/pages/legal/Ethics.jsx

Each page must:
- Use PageScaffold
- Clearly state:
  - 18+ only
  - not therapy / not medical advice
  - educational self-reflection + skill-building
  - crisis guidance (local emergency services)
  - no guarantees / results vary
- NO medical claims

Also ensure SafetyFooter links point to these canonical paths:
- /legal/disclaimer
- /legal/terms
- /legal/privacy
- /legal/ethics

Wire routes in router.

Registry:
- Add routeKeys for each legal page, set canonicalPath accordingly
- internalLinks use routeKeys only

────────────────────────────────────────────────────────
2) ACCESSIBILITY PASS (Top 50 platform process pack includes WCAG basics)
Do NOT change global styling; add small safe improvements.

2A) Add Skip Link
Create: client/src/components/a11y/SkipToContent.jsx
- renders an “Skip to content” link that becomes visible on focus
- href="#main"

2B) Ensure main landmark exists
Edit: PageScaffold or PageTemplate layout:
- wrap primary content with:
  <main id="main" role="main">...</main>

2C) Headings
Ensure each page has exactly one H1 (title).
If PageScaffold already renders H1, remove any duplicate H1 from templates (only where flagged by scan).

2D) Buttons and Links
- Ensure any clickable divs are converted to button/link in shared components ONLY.
- RelatedLinksBlock already uses Link; keep.

2E) Form labels for MI sliders
When ModulesPanel adds sliders, ensure label + aria-describedby.

Run:
  npm run build

────────────────────────────────────────────────────────
3) PERFORMANCE PASS (fast wins without refactor)
3A) Memoize registry resolutions
In routeMetaRegistry.ts:
- Add a Map cache for resolved paths and resolved internalLinks
- Cache key = routeKey
- Keep deterministic output

3B) Lazy-load hub link list (optional safe)
If hub links list is big:
- limit to 18 already
- no extra libs

3C) Prevent rerender storms
If PageTemplate recalculates resolveInternalLinks frequently, memoize:
- useMemo(() => resolveInternalLinks(routeKey), [routeKey])

Run:
  npm run build

────────────────────────────────────────────────────────
4) SEO PACK (canonical, OG, sitemap) — HUMAN TRIGGERED
4A) Upgrade SEO component
Edit: client/src/components/seo/SEO.tsx
Ensure:
- <link rel="canonical" href="{origin + canonicalPath}">
- og:url, og:title, og:description
- robots noindex support
- DO NOT hardcode origin; use window.location.origin on client

4B) Add human-triggered sitemap generator
Create: scripts/generate-sitemap.mjs
Goal:
- Export sitemap.xml using registry canonicalPath list (unique only)
- Avoid duplicates automatically
- Write to: public/sitemap.xml (or client/public/sitemap.xml depending on project)

PASTE:

---BEGIN PASTE (scripts/generate-sitemap.mjs)---
import fs from "node:fs";
import path from "node:path";

const ROOT = process.cwd();
const OUT = path.join(ROOT, "public", "sitemap.xml"); // adjust if your build expects client/public
const REG = path.join(ROOT, "client", "src", "content", "meta", "routeMetaRegistry.ts");

function read(p){ try{return fs.readFileSync(p,"utf8")}catch{return ""} }

function extractCanonicalPaths(ts) {
  const re = /canonicalPath\s*:\s*["'`]([^"'`]+)["'`]/g;
  const out = [];
  let m;
  while ((m = re.exec(ts))) out.push(m[1]);
  return out;
}

function unique(arr){ return Array.from(new Set(arr.filter(Boolean))) }

function main(){
  const reg = read(REG);
  const paths = unique(extractCanonicalPaths(reg))
    .filter((p)=>p.startsWith("/"))
    .filter((p)=>!p.includes(":")) // skip parameterized like /hubs/:topic
    .sort();

  const base = process.env.SITE_ORIGIN || "https://TheGenuineLoveProject.com";

  const urls = paths.map((p)=>`  <url><loc>${base}${p}</loc></url>`).join("\n");

  const xml =
`<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
${urls}
</urlset>
`;
  fs.mkdirSync(path.dirname(OUT), { recursive: true });
  fs.writeFileSync(OUT, xml);
  console.log(`Wrote sitemap -> ${OUT}`);
  console.log(`URLs: ${paths.length}`);
}

main();
---END PASTE---

Add script:
- "seo:sitemap": "node scripts/generate-sitemap.mjs"

Run:
  npm run seo:sitemap

────────────────────────────────────────────────────────
5) “NO DUPLICATE WORK” ENFORCEMENT RULES (must follow)
From now on, ALL changes must be guided by:
- scripts/.scan-results.json (your existing scanner)
- scripts/.deep-scan-report.json (new deep scan)

Rule:
- If a file is not in flagged lists OR does not fix a build error, DO NOT edit it.

Create: scripts/.work-guard.md
Include:
- “only touch flagged files”
- “no duplication; add to registry or shared components”
- “routeKey-only everywhere”

────────────────────────────────────────────────────────
6) RUN FULL CHECKS (must pass)
Shell:
  npm run scan:platform
  npm run scan:deep
  npm run build
  npm run seo:sitemap

Quick manual tests:
- /legal/disclaimer renders with PageScaffold + SafetyFooter
- /hubs/anxiety renders RelatedLinks
- Any tool page:
  - SaveButton is present
  - RelatedLinksBlock is present (registry-driven)
  - No hardcoded disclaimers/benefits blocks inside the page file

────────────────────────────────────────────────────────
REQUIRED OUTPUT (you must print this after changes)
1) Changed files list (paths only)
2) Deep scan counts BEFORE vs AFTER (from scripts/.deep-scan-report.json)
3) Build pass confirmation
4) Sitemap URL count output
5) Any remaining flagged duplicates (and which consolidation pattern you’ll use next)

STOP CONDITIONS
- If build fails: fix only the failing file/import paths; rerun npm run build until green.
- If deep scan shows hardcoded dup blocks: replace with shared components in those exact files only.

Then you are ready for TOP-50 PACK #5:
- Registry-driven Search Index (no duplication)
- ModulesPanel interactive (MI rulers + tiny-step + share card) registry-driven
- Content governance: “no claims” linter + wording guard
- Analytics event schema (local-only first) + privacy-safe
- Admin Health Dashboard summary of scans/build/status