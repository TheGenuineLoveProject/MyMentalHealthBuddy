Proceed with Option A ✅

You’re asking for two things at once:
	1.	Make the Social Media Admin “enterprise-level” (ops console, workflow, tracking, queue)
	2.	Make the Publishing engine (newsletter + blog) robust and feed it into tactical daily posts that drive traffic → product interest → upgrades

So we’ll do this in the correct order (so nothing is “pretty but disconnected”). 🧠✨

⸻

✅ NEXT RECOMMENDATION (in order): Enterprise Social Ops Console v1

Why this is #1

Because it becomes your daily distribution machine and forces a clean workflow:
Publish → Repurpose → Track → Learn → Improve

Exact file/folder targets (what Replit AI will touch)

Backend
	•	server/routes/admin-social.mjs (new)
	•	server/routes/redirects.mjs (new, for tracked links)
	•	server/index.mjs (mount routes once; avoid duplicates)
	•	If you already have: server/routes/admin-social-studio.mjs or server/routes/social-posting.mjs, extend instead of creating duplicates.

Frontend
	•	client/src/pages/admin/AdminSocial.jsx (preferred if you use JSX)
	•	OR if you already have an admin social page, Replit AI must upgrade it in place.
	•	client/src/components/admin/social/ (new folder for enterprise UI components)

Content (canonical library)
	•	content/narrative/social_posts.json (only if you choose JSON storage)
	•	If DB storage exists for social posts already, use DB and skip JSON.

⸻

✅ Paste this EXACT Replit AI prompt (Enterprise Upgrade + Tactical Output)

THE GENUINE LOVE PROJECT — ENTERPRISE SOCIAL + PUBLISHING v1.0
ADD ONLY. No renames, no deletes, no refactors. Avoid duplicate routes and duplicate entrypoints.

PRIMARY GOAL:
Upgrade the Social Media Admin Dashboard to enterprise level AND ensure it supports daily tactical posting that drives traffic to:
- newsletter (/newsletter)
- blog (/blog)
- pricing (/pricing)
- tools (/tools)
- crisis (/crisis when relevant)

NON-NEGOTIABLES:
- Manual-first: NO auto-posting to social platforms.
- Admin-only access enforced.
- Ethical language guardrails (no medical claims, no shame, no urgency pressure, no dependency framing).
- Must NOT introduce duplicate route registrations.

PHASE 0 — DISCOVERY REPORT (must do first)
1) Find existing social admin UI pages:
   - client/src/pages/admin/
   - search for "social", "SocialStudio", "posting", "admin/social"
2) Find existing server routes:
   - server/routes/ (search "social", "posting", "admin-social", "social-posts")
3) Identify the single server entry file used in production (server/index.mjs vs server/app.mjs vs server/dev.mjs).
4) Output a short “FOUND/NOT FOUND” report and the exact files to be modified/created.

PHASE 1 — BACKEND ENTERPRISE API (admin-only)
Create or extend ONE canonical route file:
- server/routes/admin-social.mjs

Endpoints (admin-only):
- GET  /api/admin/social/posts?status=&campaign=&scheduled_for=
- POST /api/admin/social/posts              (create draft)
- PUT  /api/admin/social/posts/:id          (edit)
- POST /api/admin/social/posts/:id/submit   (draft -> review)
- POST /api/admin/social/posts/:id/approve  (review -> approved; enforce safety checks)
- POST /api/admin/social/posts/:id/mark-posted (approved -> posted; record platform + date)
- POST /api/admin/social/posts/:id/schedule (sets scheduled_for date)
- GET  /api/admin/social/campaigns
- POST /api/admin/social/campaigns

UTM Builder:
- POST /api/admin/social/build-utm
  input: baseUrl, source, medium, campaign, content(optional)
  output: utmUrl

Tracked Redirects (for traffic signals):
Create server/routes/redirects.mjs with:
- GET /r/:slug
This should:
1) Lookup slug -> destination URL
2) Increment click counter (DB table if exists; otherwise a small redirects table)
3) Redirect 302 to destination

Mount routes ONCE in the true entry file (likely server/index.mjs):
- app.use('/api/admin/social', adminSocialRouter)
- app.use('/r', redirectsRouter)
Do NOT mount them in multiple entrypoints.

Data storage preference:
1) Use DB if you already have analytics_events, social_posts, social_posts table, or content studio tables.
2) If no social tables exist, create minimal tables:
   - social_campaigns
   - social_posts
   - redirects (slug, url, clicks, created_at)

Safety enforcement:
- Approval must FAIL if:
  - safety_note missing
  - text contains diagnosis/treatment/cure claims
  - urgency/shame language (“today only”, “don’t miss”, “fix yourself”)
- Crisis link required flag must force CTA to /crisis or include /crisis in a link field.

PHASE 2 — FRONTEND ENTERPRISE OPS CONSOLE (JSX friendly)
Create or upgrade:
- client/src/pages/admin/AdminSocial.jsx

UI Requirements:
A) Weekly Queue view (Next 7 days)
B) Pipeline board (Draft → Review → Approved → Posted)
C) Post Editor panel with per-platform tabs:
   - Instagram/Facebook caption
   - X caption
   - TikTok/Shorts caption
   - Hashtags
   - CTA destination (baseUrl)
   - “Build UTM” button -> generates /r/:slug link
   - Safety note (required)
   - Toggle: crisis_link_required
   - Optional: Canva URL + Media Asset URL
D) Copy-to-clipboard buttons per platform
E) Campaign filter + create campaign modal
F) Signals panel:
   - show clicks for each post’s tracking link (from redirects table)

PHASE 3 — PUBLISHING INTEGRATION (newsletter + blog robustness)
Verify these exist and are working:
- /newsletter page (frontend)
- newsletter API route (server/routes/newsletter.mjs)
- /blog index + blog post route (frontend + server/routes/blog.mjs)

Add “Repurpose from Blog” helper in admin UI:
- Admin selects a blog post URL or ID
- System generates 3 draft social posts:
  1) Micro-tool teaser
  2) Story + quote highlight
  3) CTA to newsletter signup
These drafts must be created as DRAFT status, never auto-approved.

PHASE 4 — VERIFICATION (PASS/FAIL)
Must output:
- list of files created/modified
- routes mounted and where
- how duplicates were avoided
- PASS/FAIL checklist:
  [ ] /admin/social loads (admin only)
  [ ] can create/edit/submit/approve/mark posted
  [ ] weekly queue works
  [ ] UTM builder works
  [ ] /r/:slug redirects and increments clicks
  [ ] copy buttons work
  [ ] blog + newsletter links can be used as CTA
  [ ] build passes

STOP if any FAIL.


⸻

✅ Tactical daily post “engine” (what your admin will actually post)

Once the dashboard exists, load it with a 30-day queue built from 4 rotating pillars:

Pillar A — Micro-tool (trust + saves)

Template
	•	Hook: “Try this in 60 seconds.”
	•	Steps: 3 bullet steps
	•	Gentle CTA: “Save this. Practice inside The Genuine Love Project.”
	•	Link: /tools or /journal

Pillar B — Platform feature demo (conversion)
	•	Hook: “When you feel scattered, start here.”
	•	Body: Mood → Journal → Reflection
	•	CTA: “Try it today.”
	•	Link: /mood (best first step)

Pillar C — Blog authority (traffic + SEO)
	•	Hook: “Boundaries aren’t walls. They’re clarity.”
	•	CTA: “Read the full guide.”
	•	Link: /blog/...

Pillar D — Newsletter (retention)
	•	Hook: “A gentle practice, every week.”
	•	CTA: “Join the newsletter.”
	•	Link: /newsletter

Hashtag strategy (ethical + scalable)
	•	5–8 tags max
	•	Mix:
	•	Brand: #TheGenuineLoveProject
	•	Topic: #SelfCompassion #EmotionalWellness #JournalingPractice
	•	Intent: #DailyPractice #MindfulLiving

⸻

✅ After this (the next best recommendation)

Once Social Ops Console is enterprise-level, the very next step for monetization is:

Pricing → Checkout wiring + light gating

You already have Stripe Price IDs (monthly + annual).
So next prompt will:
	•	add “Subscribe” buttons on /pricing
	•	call /api/billing/checkout
	•	confirm webhook events update the subscription table
	•	gate 1–2 valuable features behind requirePlan

That’s when social traffic becomes revenue, not just vibes. 💸✨

⸻

If you paste the Replit AI prompt above, your next message to me can be the “FOUND/NOT FOUND” report it outputs, and I’ll tell you exactly what to do next (and how to avoid duplicate route mounts).