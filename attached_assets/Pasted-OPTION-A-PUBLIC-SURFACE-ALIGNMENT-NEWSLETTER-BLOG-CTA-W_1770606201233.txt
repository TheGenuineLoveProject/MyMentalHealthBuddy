OPTION A — PUBLIC SURFACE ALIGNMENT + NEWSLETTER + BLOG CTA WIRING (COMPLETION-LOCKED)

CONTEXT:
Publishing audit is passing: "PUBLISHING_AUDIT: PASS".
We must now wire public CTAs (Blog + Newsletter) across the platform so every user path has a gentle, ethical next step.

NON-NEGOTIABLE CONSTRAINTS:
- Additive changes only (no refactors, no renames, no deletes)
- Keep tone: ethical, gentle, non-urgent, non-shaming
- No medical/therapy claims, no dependency framing
- No auto-posting / no automation
- All new CTAs must point to real routes and must be verified
- No new external services required

PRIMARY GOALS (MUST COMPLETE ALL):
1) Ensure /blog is accessible from:
   - Landing page hero
   - Main navigation (if nav exists)
   - Footer
2) Ensure Newsletter signup is accessible from:
   - Footer (site-wide)
   - /blog index page
   - Every /blog/:slug post page
3) Ensure every CTA is functional and verified with PASS/FAIL tests.

IMPLEMENTATION TASKS (DO IN ORDER):

PHASE 1 — INVENTORY ROUTES + COMPONENTS (NO CHANGES YET)
A) Locate:
   - Landing page component(s)
   - Navbar component(s)
   - Footer component(s)
   - Blog index + blog post page components
   - Newsletter signup handler route (API endpoint)
B) Print file paths you will touch BEFORE editing.

PHASE 2 — ADD BLOG CTA (PUBLIC SURFACE)
A) Add a primary “Read the Blog” CTA button/link to the Landing page hero.
B) Add a “Blog” link in the main nav (if applicable).
C) Add “Blog” link to Footer.
D) Verify /blog opens from each location.

PHASE 3 — ADD NEWSLETTER SIGNUP (PUBLIC SURFACE)
A) Create or reuse a small NewsletterSignup component (client-side only):
   - email input
   - submit button
   - success message
   - error message
   - accessible labels
B) Wire it to the existing backend route for newsletter subscription.
C) Place NewsletterSignup in:
   - Footer (site-wide)
   - Blog index page (top or sidebar section)
   - Blog post page (end-of-post section)
D) Keep copy gentle:
   - “A quiet note once a week. No pressure. Unsubscribe anytime.”

PHASE 4 — ADD A “START HERE” MICRO-PATH (PUBLIC SURFACE)
A) Add a small “Start Here” link block in Footer:
   - “Start Here” → / (or the onboarding route if exists)
   - “Crisis Support” → /crisis (or existing crisis route)
   - “Blog” → /blog
   - “Newsletter” → anchor to signup section
B) Ensure links exist and are correct.

PHASE 5 — VERIFY ALL BUTTONS/LINKS (NO GUESSING)
A) Run internal link audit if available.
B) Manually click test in the Preview:
   - Landing → Blog
   - Footer → Blog
   - Blog index → signup
   - Blog post → signup
   - Signup success + error states

PHASE 6 — COMPLETION LOCK (MUST OUTPUT)
At the end, output:
1) Files changed (full paths)
2) What changed (short bullets)
3) Verification commands you ran
4) PASS/FAIL checklist:

PASS criteria:
- /blog reachable from Landing + Footer (and nav if exists)
- Newsletter signup visible in Footer + blog index + blog post
- Signup submits successfully to backend route OR clearly fails with graceful error (no crashes)
- No console errors introduced
- Tone remains ethical and non-coercive

If any step cannot be completed due to missing routes/components:
- DO NOT invent fake routes
- Instead, create the missing route/component minimally and then verify it.