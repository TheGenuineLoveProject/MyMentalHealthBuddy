SOCIAL MEDIA ADMIN DASHBOARD — HARD EXECUTION MODE

You are Replit AI acting as an elite MIT PhD software engineering + publishing systems team.

PROJECT:
www.TheGenuineLoveProject.com

GOAL (LOCKED):
Enable a social media admin to create, review, and manually publish DAILY social content that promotes the platform — safely, ethically, and without automation risk.

ABSOLUTE CONSTRAINTS (NON-NEGOTIABLE):
1) DO NOT add auto-posting, schedulers, cron jobs, background daemons, or social platform APIs.
2) DO NOT store API keys for Instagram, TikTok, X, Facebook, or YouTube.
3) DO NOT refactor, rename, move, or delete existing systems.
4) Additive, reversible changes only.
5) Avoid duplicates — scan before creating new files.
6) Admin-only access required.
7) Ethical language only: no medical claims, no urgency, no guilt, no dependency framing.

VALUES:
Ethical, loving, pure, kind, all-loving.
Consent-first. Calm. Human-in-the-loop.

────────────────────────
PHASE 0 — BASELINE CHECK (NO CODE CHANGES)
────────────────────────
Run:
- git status
- npm run build
- curl http://localhost:5000/api/health

If any fail → STOP and fix before continuing.

────────────────────────
PHASE 1 — NARRATIVE CONTENT LIBRARY (SOURCE OF TRUTH)
────────────────────────
Create or confirm ONE canonical file:

/content/narrative/social_posts.json

Rules:
- If file exists, EXTEND it (do not replace).
- If missing, CREATE it.

JSON schema (strict):
[
  {
    "id": "string",
    "theme": "reflection | boundaries | emotional-literacy | presence | self-compassion | platform-orientation",
    "instagram_caption": "string",
    "x_caption": "string",
    "video_caption": "string",
    "gentle_cta_url": "/blog | /newsletter | /crisis",
    "safety_note": "string",
    "status": "draft | approved | posted",
    "created_at": "ISO timestamp",
    "posted_at": "ISO timestamp | null"
  }
]

Seed EXACTLY 12 draft posts if fewer exist.
Language rules enforced.

Verify:
- Valid JSON
- No duplicate IDs
- Exactly one canonical file

────────────────────────
PHASE 2 — ADMIN SOCIAL DASHBOARD (CORE FEATURE)
────────────────────────
Create an admin-only page at ONE location:

/admin/social

Requirements:
- Protected by existing admin auth middleware
- Reads from social_posts.json
- Displays list with filters: draft / approved / posted
- Copy-to-clipboard buttons for:
  • Instagram caption
  • X caption
  • Video caption
- Allows:
  • Change status (draft → approved → posted)
  • Set posted_at timestamp manually
- NO publishing automation
- Clear warning banner:
  “Manual publishing only. No automated posting.”

UI rules:
- Calm, utilitarian, no marketing language
- No pressure metrics
- No engagement scoring

────────────────────────
PHASE 3 — SAFE WRITEBACK
────────────────────────
Implement controlled writeback:
- Status updates persist safely
- posted_at only set when status becomes “posted”
- No deletion allowed from UI
- No edits to historical posts once marked “posted”

────────────────────────
PHASE 4 — DAILY ADMIN WORKFLOW VALIDATION
────────────────────────
Verify this exact flow works:

1) Admin logs into /admin/social
2) Views approved posts
3) Copies captions
4) Publishes manually on social platforms
5) Marks post as “posted”
6) Dashboard reflects change

Time to complete ≤ 20 minutes.

────────────────────────
PHASE 5 — ETHICAL & SAFETY ENFORCEMENT
────────────────────────
Ensure:
- No medical or therapeutic claims
- No urgency language
- No guilt/shame framing
- Crisis link used only when appropriate
- Gentle invitation tone everywhere

────────────────────────
PHASE 6 — FINAL VERIFICATION
────────────────────────
Run:
- npm run build
- curl http://localhost:5000/api/health

Create:
docs/SOCIAL_ADMIN_COMPLETE.md

Include:
- What was added
- Where admin accesses it
- Confirmation: NO auto-posting, NO APIs, NO background jobs

END WITH:
STATUS: PASS or FAIL
If FAIL → output exact file paths + minimal fixes required.