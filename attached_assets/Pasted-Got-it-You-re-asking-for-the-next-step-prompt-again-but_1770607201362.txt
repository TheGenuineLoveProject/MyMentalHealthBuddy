Got it ✅ You’re asking for the next step prompt again, but this time I’m going to “lock” it even harder so Replit AI can’t drift or half-finish.

You’re currently in a loop where prompts are big, but completion proof isn’t always enforced. So the next upgrade is:

Option A+ (Locked Execution): “One prompt → one outcome → one proof”

Outcome: Newsletter + Blog + Publishing becomes operational, auditable, and growth-ready with zero guesswork.

Below is the single exact Replit AI prompt you paste next. It forces:
	•	no partial work
	•	no vague “done”
	•	prints exact next actions
	•	ends with VERIFIED: PASS or VERIFIED: FAIL

⸻

✅ EXACT REPLIT AI PROMPT — “PUBLISHING & NEWSLETTER: COMPLETE + PROVE IT” v1.3

PUBLISHING & NEWSLETTER v1.3 — COMPLETE + PROVE IT (ADDITIVE ONLY)

HARD RULES:
- ADDITIVE ONLY: do not rename, delete, move, or refactor existing files.
- If a file already exists, modify minimally and only to connect missing wiring.
- No auto-posting. No auto-sending. Manual-first (human-in-loop).
- No medical claims. No therapy claims. Trauma-informed, optional language.
- Every phase must end with a PRINTED PASS/FAIL and supporting evidence.
- Final must run verification and print: VERIFIED: PASS or VERIFIED: FAIL.

PRIMARY GOAL:
Turn newsletter + blog + publishing into a robust, optimized, refined system:
1) Canonical templates + drafts library
2) /blog and /blog/:slug stability (no crashes, good empty states)
3) /newsletter working signup + unsubscribe endpoints
4) /admin/publishing dashboard (copy-first, approval workflow, status tracking)
5) Audit script that enforces all publishing constraints
6) One verify command that proves completion

PHASE 0 — INVENTORY (NO CHANGES)
1) Print the exact paths of:
   - Blog index page component(s)
   - Blog post/slug page component(s)
   - Newsletter page component(s)
   - Existing newsletter endpoints in server/routes/
   - Any existing blog endpoints in server/routes/
   - Any admin publishing/social routes/pages
   - scripts/audit-publishing.mjs (if exists)
2) Print current health endpoint path.
3) Print whether DB is available/connected (use existing health).

PHASE 1 — CANONICAL CONTENT + TEMPLATES (ADDITIVE)
Ensure these exist (create if missing):
- content/publishing/blog/
- content/publishing/newsletter/
- content/publishing/templates/

Ensure these template files exist (create if missing):
- content/publishing/templates/weekly-template.md
- content/publishing/templates/welcome-template.md
- content/publishing/templates/crisis-safe-footer.md
- content/publishing/templates/publishing-style-guide.md

If content exists in legacy locations, DO NOT MOVE IT.
Instead, add content/publishing/README.md documenting canonical + legacy search fallback.

PHASE 2 — DRAFT LIBRARY (READY-TO-PUBLISH)
Create (if missing):
- content/publishing/newsletter/drafts.json

Add 14 drafts (short, ethical, platform-promoting) with fields:
- id, pillar, title, subject, body_md, cta_url, cta_label, safety_note, status, created_at

Pillars must rotate across:
- Orientation (what GLP is)
- Reflection (a gentle prompt)
- Literacy (feelings wheel / boundaries)
- Practice (1 small exercise)
- Community (affirmation wall)
- Safety (crisis-resources mention when relevant)
- Blog (link to a post)

Rules:
- No urgency
- No shame
- No dependency framing
- Optionality language included

PHASE 3 — NEWSLETTER ENDPOINTS (LIVE)
Ensure endpoints exist and are wired:
- POST /api/newsletter/subscribe
- POST /api/newsletter/unsubscribe

Requirements:
- Validate email
- Rate limit
- Store subscriber:
  Prefer DB table newsletter_subscribers(email unique, status, created_at, unsubscribed_at)
  Else fallback append-only: data/newsletter_subscribers.jsonl

Return clear JSON:
- { ok: true } or { ok:false, error:"..." }

PHASE 4 — /NEWSLETTER PAGE (WIRED)
Ensure /newsletter exists and:
- shows what users get (1x/week)
- includes privacy note
- signup form calls /api/newsletter/subscribe
- success state visible
- unsubscribe instructions visible

PHASE 5 — BLOG STABILITY + CTA
Ensure:
- /blog renders even if 0 posts
- /blog lists posts sorted by date desc if possible
- /blog/:slug never crashes; missing => friendly Not Found + link back
Add a small Newsletter CTA block to blog pages using optional language.

PHASE 6 — ADMIN PUBLISHING DASHBOARD (MANUAL-FIRST)
Create or finalize:
- /admin/publishing  (or /admin/content/publishing if that exists)

Features required:
- Admin-only guard (use existing admin middleware / patterns)
- Tabs: Newsletter Drafts / Blog / Templates
- Copy buttons:
  - Copy subject
  - Copy body_md
  - Copy CTA
- Status controls: draft → approved → sent (manual)
- Stores status in: data/publishing_status.json
No auto sending, no integrations required.

PHASE 7 — AUDIT ENFORCEMENT
Create or update scripts/audit-publishing.mjs to enforce:
- All 4 templates exist
- drafts.json exists and parses
- drafts have required fields
- forbidden language scan (therapy claims, urgency, shame)
- /newsletter route exists
- /blog route exists
Output MUST end with:
- PUBLISHING_AUDIT: PASS
or
- PUBLISHING_AUDIT: FAIL (reasons)

PHASE 8 — VERIFY LOCK (ONE COMMAND PROOF)
Create scripts/verify.mjs and package.json scripts:
- "audit:publishing": "node scripts/audit-publishing.mjs"
- "verify": "node scripts/verify.mjs"

verify.mjs must run:
1) node scripts/audit-publishing.mjs
2) call health endpoint (GET)
3) npm run build (if exists)
Then print final line:
- VERIFIED: PASS
or
- VERIFIED: FAIL (reasons)

REQUIRED FINAL ACTIONS (RUN AND PASTE OUTPUT):
- node scripts/audit-publishing.mjs
- npm run verify
- curl -s http://localhost:5000/api/health  (or correct health path)

FINAL REPORT REQUIRED:
A) Files created/changed (full paths)
B) Evidence: terminal output for audits + verify
C) PASS/FAIL checklist
D) Next 3 steps only (no more)


⸻

✅ After v1.3 is VERIFIED: PASS — the next step (in order)

“Weekly Digest Manual Send Flow” (Resend-ready, still human-controlled):
	1.	Admin picks an approved draft in /admin/publishing
	2.	Copies subject + body → pastes into Resend
	3.	Sends manually
	4.	Marks “sent” in dashboard
	5.	System logs newsletter_sent_manual (admin-only, no PII)

I can give that exact prompt next, but the correct sequence is:
v1.3 → VERIFIED: PASS → then Digest Flow.

This is how you get “Google-grade” execution: proof over vibes 🧠✨