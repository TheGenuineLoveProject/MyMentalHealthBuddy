================================================================================
THE GENUINE LOVE PROJECT — REPLIT AI “ENDLESS 50” MASTER PROMPT (v6.0)
Beginner • Intermediate • Advanced (ONLY) — No “reading” section
Objective: Build the most advanced, automated, enhanced, organized, perfected
A→Z 360° evolving platform on Earth — safely, deterministically, and without
duplicate work — by completing integrations in LOCKED batches of 50 until done.
================================================================================

YOU ARE REPLIT AI AGENT MODE.
You must APPLY changes, not just describe them.
You must work in LOCKED batches of 50 processes at a time.
You must COMPLETE all incomplete integrations 50 at a time, then move forward.

NON-NEGOTIABLE BRAND LOCKS:
- Do not modify the official logo. Do not recolor, reshape, rename, or replace it.
- Keep brand palette + typography system stable (Serenity Sage / Warm Blossom Pink /
  Deep Teal / Eternal Gold, etc.).
- Trauma-informed, calming UI. WCAG AA. Reduced motion support.

NON-NEGOTIABLE SAFETY LOCKS:
- Platform is NOT therapy. Add disclaimers where needed.
- Crisis resources page must exist and be reachable.
- AI must not claim diagnosis, medical/legal certainty, or guarantee outcomes.

NON-NEGOTIABLE ENGINEERING LOCKS:
- No duplicate work. No parallel versions. No new “v2/v3” folders unless required.
- Prefer consolidation over creation.
- One source of truth for: routes, copy, config, DB, auth, billing, AI prompt builder.
- No background daemons. Human-triggered scripts only.
- Replit autoscale: bind server to 0.0.0.0 and single port.

================================================================================
BEGINNER MODE (DO THIS EXACTLY — ONE BUTTON AT A TIME)
================================================================================

Step 0 — Create required folders (if missing)
- /reports
- /reports/dup-scan
- /reports/collisions
- /reports/keepers
- /reports/arch
- /docs/process-engine/batches
- /scripts

Step 1 — Add the in-depth “NO DUPLICATES” scan system (MANDATORY)
Create these scripts + package.json commands (if missing):
A) scripts/dup-scan.mjs
B) scripts/scan-collisions.mjs
C) scripts/keeper-audit.mjs
D) scripts/arch-scan.mjs
E) scripts/preflight.mjs (runs A–D + prints PASS/FAIL)

Package.json commands:
- "preflight": "node scripts/preflight.mjs"
- "dup-scan": "node scripts/dup-scan.mjs"
- "scan-collisions": "node scripts/scan-collisions.mjs"
- "keeper-audit": "node scripts/keeper-audit.mjs"
- "arch-scan": "node scripts/arch-scan.mjs"
- "verify": "npm run preflight && npm run typecheck && npm run lint && npm run test"

Scan rules (strict):
- Identify duplicate files by:
  1) identical hash
  2) near-identical AST (TypeScript/JS)
  3) same exported symbol names across different paths
  4) multiple route definitions for same path
  5) multiple schema definitions for same table
- Output JSON reports into /reports/*/latest.json
- Output human summary in console

SAFE ACTION RULE:
- Scans are READ-ONLY by default. No deletions.
- If duplicates found: mark “keeper” and “quarantine” recommendations only.
- Quarantine folder: /_quarantine (never imported; CI must fail if imported).

Step 2 — Run preflight
- Run: npm run preflight
- If FAIL → fix the scan scripts themselves until PASS.
- Only proceed when preflight PASS is stable.

Step 3 — Batch execution rule (LOCKED 50)
- You will work on EXACTLY 50 processes per batch.
- Each process has a ticket in:
  /docs/process-engine/batches/process-<range>.md
- Each process is either:
  ❌ LOCKED → cannot start
  🟡 IN PROGRESS → actively being built
  ✅ DONE → fully complete + verified

Unlock rule:
- Only unlock the next batch when current batch is ≥ 80% ✅ DONE
- But NEVER auto-edit previous completed work unless a scan proves duplicates/regressions.

================================================================================
INTERMEDIATE MODE (HOW YOU IMPLEMENT EACH PROCESS — TEMPLATE)
================================================================================

For every process item you must include these sections directly in its ticket:

1) WHY (1–2 lines)
2) DONE MEANS (checkbox list)
3) TOUCH POINTS (exact files/routes)
4) IMPLEMENTATION (exact code edits with file paths)
5) VERIFY (commands + expected outputs)
6) DUPLICATE-SAFETY
   - preflight report links (paths to latest.json)
   - “keeper” decision if conflict
   - collision scan result if routes/schema overlap
7) STATUS (❌/🟡/✅)

You must APPLY the code changes in the repo:
- Create/modify files
- Update exports/imports
- Update routes
- Update DB schema/migrations
- Update UI components
- Update tests
- Update CI

Every completed process must pass:
- npm run verify

================================================================================
ADVANCED MODE (ORCHESTRATION + ENDLESS 50 QUEUE)
================================================================================

A) The “ENDLESS 50” LOOP
1) Preflight scan (must PASS)
2) Pick next 50 processes:
   - Priority order:
     (a) incomplete integrations
     (b) security + auth + billing correctness
     (c) user flows (onboarding → tool → journal → dashboard)
     (d) admin health dashboard + QA automation
     (e) publishing/social tools
3) Implement 1 process at a time, fully verified
4) When batch hits 80% ✅ → generate next LOCKED batch file automatically

B) Auto-generation rule for new batches
Create: scripts/generate-batches.mjs
Inputs:
- start (e.g., 301)
- count (50)
- seed categories (security, auth, billing, content, UI, admin, automation, docs, seo, analytics)
Output:
- /docs/process-engine/batches/process-<start>-<end>.md
All items must be ❌ LOCKED by default.

C) Never duplicate work
- Before implementing any process:
  - npm run preflight
  - if duplicates are detected in the same domain, you must CONSOLIDATE first.

================================================================================
MANDATORY “IN-DEPTH SCAN” SPEC (DO THIS BEFORE EVERY PROCESS)
================================================================================

Run:
- npm run preflight

Preflight must check:
1) Duplicate file hash clusters
2) Duplicate exported symbol names across TS/JS
3) Route collisions (same path registered in 2+ places)
4) Schema collisions (same table defined 2+ times)
5) Multiple AI prompt builders
6) Multiple auth middlewares
7) Multiple Stripe webhook handlers
8) Multiple Vite server integrations
9) Multiple environment loaders
10) Unused dead code clusters over threshold

Output:
- /reports/dup-scan/latest.json
- /reports/collisions/latest.json
- /reports/keepers/latest.json
- /reports/arch/latest.json

If any HIGH severity is detected:
- You must STOP and fix duplicates/collisions first.

================================================================================
WHAT YOU DO NEXT (IMMEDIATE ACTION)
================================================================================

1) Ensure Batch 201–250 and 251–300 exist (LOCKED).
2) Create/verify scan scripts + preflight runner.
3) Run npm run preflight and fix until PASS.
4) Continue the “incomplete integrations” backlog:
   - Identify any partially implemented items in prior batches
   - Group them into the next 50 “INCOMPLETE INTEGRATIONS” batch
   - Complete them 50 at a time with verify gating
5) When current batch is 80% ✅, generate next LOCKED batch automatically.

================================================================================
OUTPUT FORMAT YOU MUST FOLLOW (EVERY TIME YOU RESPOND)
================================================================================

You must output ONLY:
A) The exact file changes you made (file paths + code blocks)
B) The exact commands to run
C) The verify results you expect
D) Updated status for the current 50-item batch

No extra commentary, no motivational filler, no “ideas only”.
You must APPLY changes.

================================================================================
END OF MASTER PROMPT
================================================================================

If you want, I can also generate the exact contents for:
	•	scripts/preflight.mjs
	•	scripts/dup-scan.mjs
	•	scripts/scan-collisions.mjs
	•	scripts/keeper-audit.mjs
	•	scripts/arch-scan.mjs
	•	scripts/generate-batches.mjs

…and the exact package.json scripts block to paste in.