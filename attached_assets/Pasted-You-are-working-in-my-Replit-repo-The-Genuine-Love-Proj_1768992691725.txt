You are working in my Replit repo “The Genuine Love Project”.
Goal: Make `npm run test:auth` pass with minimal diffs. Do NOT relax tests. Fix server behavior.

CONTEXT (from failing output):
- Login missing fields test expects HTTP 400 but currently receives 401.
- Logout success test expects message exactly "Logged out" but receives "Logged out."
- Admin access control test expects 403 for non-admin but receives 401.
- Port conflicts may exist; focus on code/test correctness, not deployment.

HARD RULES:
1) Minimal diffs. No refactors.
2) Keep existing security posture for invalid creds (401), but missing fields MUST be 400 per tests.
3) Do not change test expectations unless absolutely necessary (prefer fixing server).
4) Keep response shapes consistent with existing helpers (use existing send* helpers if present).

TASKS:
A) Fix login validation ordering
1) Open `server/routes/auth.mjs` (or wherever POST /api/auth/login is defined).
2) Ensure request body validation runs FIRST:
   - If email or password is missing/empty/invalid type → return HTTP 400.
   - Only after valid input exists should any uniform-auth-failure logic apply.
3) If `loginRateLimit` middleware is applied before validation, change ordering:
   - Validation must run before rate limiting OR rate limiter must only execute when email+password are present.
   - IMPORTANT: the rate limiter must NOT convert missing-field requests into 401.
4) Ensure successful login returns HTTP 200 and includes `token` (tests call `expect(data.token).toBeDefined()`).

B) Fix logout message exactness
1) Find logout handler (likely `POST /api/auth/logout`).
2) Change response JSON message to exactly: "Logged out" (no trailing punctuation).
3) Ensure HTTP 200.

C) Fix admin access control status code
1) Locate admin stats route `/api/admin/stats` (or similar).
2) Confirm middleware chain:
   - `requireAuth` should accept a valid access token and attach `req.user`.
   - `requireAdmin` should return HTTP 403 when authenticated user is not admin.
3) Ensure `requireAuth` parses `Authorization: Bearer <token>` correctly.
4) Verify `verifyAccessToken` works with the token produced by login.
5) Confirm how “admin” is represented (e.g., `user.isAdmin`, `user.role === 'admin'`, etc.) and make `requireAdmin` check that field correctly.

D) Run verification
1) Run: `npm run test:auth`
2) If it fails, fix and rerun until it passes.
3) When green, run: `npm run audit` and ensure it still passes.

OUTPUT REQUIRED:
- List files changed with 1-line explanation each.
- Paste the exact commands run and final pass result.