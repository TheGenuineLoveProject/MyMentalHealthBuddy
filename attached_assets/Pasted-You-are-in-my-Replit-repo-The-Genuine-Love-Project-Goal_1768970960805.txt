You are in my Replit repo “The Genuine Love Project”.

Goal: Fix 3 issues now:
1) Header logo is not perfectly centered.
2) Sign In button is clipped/cut off on top.
3) Icons look too plain AND I can’t login to QA protected pages.

HARD RULES:
- Do NOT rename any existing --glp-* tokens.
- No raw hex outside client/src/styles/brand-tokens.css.
- Fonts only Inter + Playfair Display (+ system fallbacks).
- Minimal diffs. Keep routes canonical in client/src/App.jsx.
- Keep audits passing: npm run audit.

TASKS:
A) HEADER CENTERING (must be mathematically centered)
1) Find the header/nav component used on Landing (search: Header, Navbar, TopNav).
2) Replace layout with a 3-column grid (grid-cols-3):
   - left spacer/controls
   - center logo (justify-self-center)
   - right actions (Sign In)
3) Ensure logo remains centered regardless of right-side button width.

B) SIGN IN CLIPPING FIX (safe area + overflow)
1) Identify why Sign In is cut off:
   - fixed/sticky header with missing safe-area padding
   - parent overflow hidden
   - negative margins / h-screen wrappers
2) Add safe-area CSS vars and prevent top clipping:
   - define --glp-safe-top = env(safe-area-inset-top, 0px)
   - ensure body/main top padding accounts for safe area and header where needed
3) Remove/adjust any overflow-hidden or layout that clips the header area.

C) ICON PREMIUM UPGRADE (single system)
1) Create client/src/components/ui/IconBadge.jsx that uses existing .icon-container classes
2) Update key UI areas (NotFound quick links, landing feature icons, nav icons if present) to wrap icons in IconBadge.
3) Ensure 24px baseline icon sizing and on-brand look without raw colors.

D) QA LOGIN BYPASS (DEV ONLY)
1) Locate auth guard (RequireAuth.jsx / RouteGuard / AuthGuard). Add DEV-only QA bypass:
   - if import.meta.env.DEV and localStorage glp-qa === "1", allow access
   - optional env flag VITE_QA_BYPASS_AUTH=true also allows
2) Add a DEV-only link/button on /login page: “Enable QA Mode”:
   - sets localStorage glp-qa=1 and routes to /dashboard
3) Make sure production behavior is unchanged.

E) VERIFY
1) Run: npm run audit
2) In Preview:
   - confirm header logo is centered
   - confirm Sign In visible (not cut off)
   - enable QA Mode and click several protected routes
3) Report:
   - files changed + 1-line summary each
   - commands run + results
   - where QA Mode is toggled

Proceed now.