MEGA FINISHER (v3) — The Genuine Love Project
Run THREE AGENTS IN SEQUENCE (QA Guardian → UX Finisher → Content Curator).
ABSOLUTE RULE: NO SCOPE CREEP. Minimal diffs. Fix only what is required.

══════════════════════════════════════════════════════════════════════
HARD CONSTRAINTS (NON-NEGOTIABLE)
1) Do NOT rename/refactor existing CSS variable names. Keep all `--glp-*` names EXACT.
2) NO raw hex colors anywhere except: `client/src/styles/brand-tokens.css`.
3) Fonts allowed ONLY: Inter + Playfair Display (+ system fallbacks). No other fonts.
4) All internal navigation links MUST match canonical routes in `client/src/App.jsx` (wouter Switch/Route). No drift.
5) Prefer minimal diffs; avoid breaking refactors. Improve using tokens + Tailwind semantic classes.
6) Do NOT loosen/bypass audits/tests. Fix properly.
7) Do NOT invent new routes unless an intended page already exists and is referenced.
8) Do NOT add new dependencies unless truly required to fix failing tests/audits (must justify).

SOURCE OF TRUTH
- Router: `client/src/App.jsx`
- Tokens: `client/src/styles/brand-tokens.css`
- Brand CSS: `client/src/styles/brand.css` (if present)
- Tailwind: `tailwind.config.js`
- Global CSS entry: `client/src/index.css` (and `client/src/main.css` if used)
- Audits: `tools/nav-link-audit.mjs`, `tools/visual-doctor.mjs`
- Reports: `docs/NAV_LINK_AUDIT.md`, `docs/VISUAL_DOCTOR_REPORT.md`
- Tests: `tests/auth.test.mjs` and `server/tests/*` if present

══════════════════════════════════════════════════════════════════════
AGENT 1 — QA GUARDIAN (stability + tests + ports)
GOAL: eliminate flakiness (EADDRINUSE / ECONNREFUSED) and make auth/admin tests reliable.

STEP 1: PORT + PROCESS HYGIENE (RUN FIRST)
Run these commands and capture output snippets in final report:
- `node -v`
- `npm -v`
- `npm install`
- `lsof -i :5000 || true`
- `kill -9 $(lsof -t -i :5000) 2>/dev/null || true`
- `lsof -i :3000 || true`
- `kill -9 $(lsof -t -i :3000) 2>/dev/null || true`

STEP 2: CONFIRM HOW TESTS EXPECT SERVER TO RUN
- Open `package.json` scripts.
- Open `tests/auth.test.mjs` and confirm BASE_URL + endpoints.
- Decide minimal deterministic path:
  A) Either make `npm run test:auth` start the server (child process) then run vitest, then stop server.
  B) OR add a vitest setup (beforeAll/afterAll) that starts/stops the server for these tests only.
Choose the smallest change that removes ECONNREFUSED forever.

SECURITY STATUS RULES (follow exactly)
- Missing required fields → 400 (safe generic message; no leaks)
- Invalid credentials OR login rate-limited → 401 "Invalid credentials" (uniform; never reveal rate limiting)
- Unauthenticated access → 401
- Authenticated non-admin hitting admin routes → 403
Keep error bodies consistent and non-sensitive.

STEP 3: RUN AUTH TESTS UNTIL GREEN
- `npm run test:auth || npx vitest run tests/auth.test.mjs`
Fix failures with minimal code changes.

STEP 4: RUN AUDIT-LIKE VERIFICATION (only after tests green)
- `npm run audit`
(Do not proceed to Agent 2 unless test:auth is green and audit passes.)

══════════════════════════════════════════════════════════════════════
AGENT 2 — UX FINISHER (Mode system + consistency + nav + NotFound)
GOAL: final 360° A→Z UX polish WITHOUT breaking constraints.

A) MODE SYSTEM (Default / Low-Stim / Reading) — real UI + persistence
1) 3 modes via HTML dataset:
   - Default: html.dataset.mode unset or ""
   - Low-Stim: html.dataset.mode = "low-stim"
   - Reading: html.dataset.mode = "reading"
2) Mode toggle UI in TWO places:
   - Header top-right (segmented control or dropdown)
   - Settings page (Display/Mode section with short descriptions)
3) Persistence:
   - localStorage key: `glp-mode`
   - apply BEFORE first paint (tiny bootstrap in `client/src/main.jsx` or earliest init)
4) Accessibility:
   - keyboard reachable
   - `aria-pressed` or `aria-current` for active
   - consistent focus-visible ring using gold ring token (no hex)
5) Styling:
   - Default: healing-luxury (subtle gradients only via vars/tokens)
   - Low-Stim: reduce saturation/shadows/gradients/motion
   - Reading: maximize legibility (higher contrast text, flatter surfaces)

B) VISUAL CONSISTENCY (teal + gold)
1) Buttons:
   - Primary: teal bg + ivory text; hover/active via tokens; gold focus ring
   - Secondary/Ghost: teal text; subtle surface hover; gold ring
   - Disabled: muted surface/text; cursor-not-allowed
2) Links: teal text, underline on hover only, gold ring on focus
3) Cards: semantic tokens only, consistent radius, soft shadow; Low-Stim reduces shadow
4) Icons: standardize 24px base, outline feel, token colors only
5) Typography: Playfair headings, Inter body; normalize hierarchy/line-height
6) Motion: respects prefers-reduced-motion; Low-Stim reduces transitions/animations

C) NAV LOCKDOWN
- Treat `client/src/App.jsx` as canonical.
- Fix ALL internal links to match exactly (case + hyphenation + leading slash).
- Remove or correct any link to a missing route (do not invent routes).

D) NOTFOUND (404) PREMIUM
- Upgrade NotFound component used by App.jsx fallback route.
- Include: Playfair headline, calm copy, Go Home + Go Back, Quick Links (4–6) from real routes only.
- Re-run nav audit to confirm links valid.

E) TOKENS + TAILWIND SANITY
- Ensure Tailwind semantic keys map to existing CSS vars (no new token names).
- Ensure content globs include all relevant files.
- Ensure global CSS imports tokens/brand CSS correctly.
- Replace any stray colors with vars/classes.

After changes:
- `npm run audit` MUST still pass.

══════════════════════════════════════════════════════════════════════
AGENT 3 — CONTENT CURATOR (docs + Canva kit + platform ops)
GOAL: finalize “platform complete kit” docs + Canva specs + lightweight ops guidelines (no new deps).

Create/update:
1) `docs/COLOR_SYSTEM.md`
   - list core `--glp-*` tokens
   - semantic mapping
   - mode deltas (Default vs Low-Stim vs Reading)
   - do/don’t (no hex outside brand-tokens.css)
2) `docs/TYPOGRAPHY_GUIDE.md`
   - Inter + Playfair usage + hierarchy scale
   - spacing + line-height guidance
3) `docs/CANVA_TEMPLATE_SPECS.md`
   - 1080×1080 + 1080×1920
   - safe margins, grid, spacing system
   - exact type scale (Playfair/Inter) + usage patterns: quote card, prompt card, carousel
   - color rules (teal/gold emphasis, ivory backgrounds)
   - export settings: PNG (2x), JPG quality, naming conventions, folder structure
4) `README.md`
   - how to run audits
   - how to switch modes (where toggles are + localStorage key + html data-mode)
5) Add `docs/OPS_PLAYBOOK.md` (text-only, no deps):
   - Release checklist (tests + audit + preview click test)
   - “AI employees” cadence: weekly UX sweep, monthly content refresh, quarterly security review
   - Guardrails: no medical claims, no user data leaks, consistent auth errors

Tone: uplifting/supportive, no medical promises.

══════════════════════════════════════════════════════════════════════
FINAL VERIFICATION (MUST RUN + REPORT)
Run and record pass/fail:
- `npm run test:auth`
- `npm run audit`

Confirm:
- `docs/NAV_LINK_AUDIT.md` shows 0 broken links
- `docs/VISUAL_DOCTOR_REPORT.md` shows 0 violations
- No hex outside `client/src/styles/brand-tokens.css`
- Only Inter + Playfair Display fonts used
- Mode toggle exists in Header + Settings, persists via `glp-mode`, applies before paint
- NotFound quick links match App.jsx routes exactly

FINAL RESPONSE FORMAT
1) Checklist confirming Agent 1–3 completion.
2) Exact commands run + results.
3) Files changed (each with 1-line summary).
4) NotFound Quick Links (label + exact path).
5) OPTIONAL improvements (labeled OPTIONAL; do not implement unless asked).
Proceed now. Do not stop early. Verify, then report.