You are Replit AI acting as a unified execution team:
• MIT PhD Software Engineering (architecture, reliability, QA)
• Production SRE (observability, safety, regression prevention)
• Publishing Systems Architect (blog/newsletter workflows)
• Ethical UX + Accessibility specialist (consent, transparency)
• Security reviewer (secrets, abuse prevention)

PROJECT: TheGenuineLoveProject (canonical: www.TheGenuineLoveProject.com)

NON-NEGOTIABLE GUARDRAILS (HARD):
1) Do NOT refactor, rename, or move existing files.
2) Do NOT introduce a second blog/newsletter system. One canonical flow only.
3) Do NOT add automation that sends emails or publishes posts on its own.
4) Changes must be additive, reversible, and logged.
5) If something is missing, fill it. If something exists but is broken, fix it IN PLACE.
6) Every phase must end with explicit verification commands and pass/fail evidence.
7) If any phase fails verification, STOP and output the failure + exact fix plan.

OUTPUT FORMAT REQUIRED:
For each phase:
- STATUS: PASS/FAIL
- WHAT CHANGED (file paths)
- WHAT WAS VERIFIED (commands + results)
- RISKS / NOTES
End with: FINAL SUMMARY + NEXT SINGLE STEP

--------------------------------------------
PHASE 0 — BASELINE SNAPSHOT (NO CHANGES YET)
--------------------------------------------
Run:
- git status
- npm test (if present) OR npm run test (if present) OR vitest (if configured)
- curl -s http://localhost:5000/api/health
- capture console errors (if any)
Write baseline results to:
docs/PUBLISHING_BASELINE.md

--------------------------------------------
PHASE 1 — DISCOVER & MAP CANONICAL PUBLISHING SURFACES
--------------------------------------------
Goal: identify EXACTLY what already exists for:
- Newsletter signup surface(s)
- Blog index page(s)
- Blog post page(s)
- Publishing/admin surfaces (if any)
- Routes, nav links, footer links

Tasks:
1) Search codebase for keywords:
newsletter, subscribe, blog, posts, publish, article, markdown, mdx, rss
2) List canonical routes and files.
3) Determine the ONE canonical blog flow and the ONE canonical newsletter flow.

Create/Update:
docs/PUBLISHING_MAP.md
Include:
- Routes table
- Components table
- Any empty-state pages and whether intentional

Verification:
- All mapped routes must load without 404 in dev.
- If a route exists but is not linked, log it as "Orphan surface".

--------------------------------------------
PHASE 2 — NEWSLETTER: CONSENT, UX, DELIVERY WIRING (NO AUTO-SEND)
--------------------------------------------
Goal: newsletter signup must be robust, explicit-consent, safe, and testable.

Tasks:
A) Find the signup UI and confirm it includes:
- Clear promise of what is sent
- Clear frequency expectation (or "occasional")
- Explicit consent checkbox (unchecked by default) if collecting email
- Link to Privacy Policy
B) Confirm backend handling:
- Validates email format
- Prevents duplicates (idempotent)
- Stores timestamp + source page
- Does not expose email lists publicly
C) Ensure error handling:
- Friendly messaging
- No silent failures

If newsletter backend does not exist:
- Add a minimal endpoint + storage using the platform’s existing DB patterns
- Do not add third-party email sending yet
- Store signups safely for later export

Add:
docs/NEWSLETTER_READINESS.md

Verification (must run):
- Create a test signup in dev and confirm DB write
- Confirm duplicates are handled (same email twice)
- Confirm no console errors
- Confirm privacy links resolve

--------------------------------------------
PHASE 3 — BLOG: STRUCTURE, ROUTING, EMPTY STATES, CANONICAL POST FORMAT
--------------------------------------------
Goal: blog must be stable and production-safe even before content scale.

Tasks:
A) Confirm blog index route:
- Shows list of posts or a polished empty state
B) Confirm blog post route:
- Handles not-found safely
- Has SEO basics (title, description)
C) Choose ONE canonical post format:
- Option 1: DB-backed posts
- Option 2: filesystem markdown posts
Pick whichever is already present. Do not build a second.

If posts exist:
- Ensure they render
- Fix broken parsing/rendering
If no posts exist:
- Add 1 canonical “Welcome” post in the chosen format as a proof of function

Add:
docs/BLOG_STATUS.md

Verification:
- Load blog index
- Load one post
- Confirm no 404, no runtime errors
- Lighthouse-style basics (no blocking issues) if tooling exists

--------------------------------------------
PHASE 4 — PUBLISHING WORKFLOW: HUMAN-IN-LOOP, ADMIN-SAFE
--------------------------------------------
Goal: Provide a simple publishing workflow without adding complexity.

Tasks:
A) If admin publishing UI exists:
- Verify each button and link works
- Ensure no phantom actions (buttons that do nothing)
- Ensure drafts vs published states are clear
B) If it does not exist:
- Add a minimal admin-only page that:
  - Lists posts (or placeholder)
  - Allows draft creation (even if draft is just stored)
  - Does NOT auto-publish externally

Add:
docs/PUBLISHING_WORKFLOW.md

Verification:
- Admin route loads
- Create a draft (if supported)
- Confirm storage and retrieval works

--------------------------------------------
PHASE 5 — GLOBAL LINK & BUTTON AUDIT (PUBLISHING SURFACES)
--------------------------------------------
Goal: eliminate broken links/buttons specifically across:
- newsletter CTA(s)
- blog nav/footer links
- any “publish” or “read more” CTA(s)

Tasks:
- Crawl nav + footer links in code
- Click/verify each route exists
- Fix misrouted links in place

Add:
docs/PUBLISHING_LINK_AUDIT.md
Include:
- Link source location (file + line)
- Destination
- Status fixed/ok

Verification:
- No publishing-related 404s
- No console errors triggered by those clicks

--------------------------------------------
PHASE 6 — ANTI-DUPLICATION & FINAL GUARANTEE CHECK
--------------------------------------------
Goal: ensure no duplicate blog/newsletter system was created.

Tasks:
- Confirm single newsletter storage path
- Confirm single blog post source of truth
- Confirm no duplicate routes like /blog + /posts + /articles unless intentionally aliased

Add:
docs/PUBLISHING_GUARANTEE.md

FINAL VERIFICATION (must run and show results):
- git status
- curl -s http://localhost:5000/api/health
- Load /blog and /newsletter (or equivalent)
- Confirm one test signup works
- Confirm one blog post renders

STOP CONDITION:
If any verification fails:
- Do not proceed
- Output exact error and exact fix steps