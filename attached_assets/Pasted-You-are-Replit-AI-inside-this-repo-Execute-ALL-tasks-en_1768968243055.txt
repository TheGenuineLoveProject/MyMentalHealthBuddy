You are Replit AI inside this repo. Execute ALL tasks end-to-end with ZERO drift and ZERO refactors to existing variable names.

GOALS (must complete all):
A) Lock a production-grade design token system using the existing CSS variable names that already exist in this repo (the `--glp-*` vars). Add 3 modes: Default, Low-Stim, Reading.
B) Update Tailwind (JS config) so Tailwind utilities map to those CSS variables (primary/accent/bg/text/ring/surfaces/border).
C) Build an automated Nav Link Audit that:
   - Extracts the authoritative internal route list from `client/src/App.jsx` (wouter <Switch>/<Route path="...">)
   - Scans UI files for internal links (Link/to, href, navigate calls) and compares them to known routes
   - Writes `docs/NAV_LINK_AUDIT.md`
   - Fails CI if any internal link points to a missing route (excluding external URLs, hash links, mailto/tel)
D) Build a “VISUAL DOCTOR” script that fails CI if:
   - any raw hex color (e.g. #fff, #FAF9F7, #2F5D5D, etc.) exists anywhere EXCEPT `client/src/styles/brand-tokens.css`
   - any disallowed inline styles are used (allowlist only: CSS vars usage like `style={{ color: "var(--glp-...)" }}` and layout-only numeric styles for width/height if needed)
   - any new font-family appears beyond Inter + Playfair Display (allow system fallback lists but no new families)
E) Wire everything into package scripts + CI so it runs automatically.

CONSTRAINTS (critical):
- DO NOT rename existing CSS vars. Keep the existing `--glp-*` names. (I already have files like `client/src/styles/brand-tokens.css`, `client/src/styles/brand.css`, and `client/src/index.css` using `--glp-sage`, `--glp-sage-deep`, `--glp-blush`, `--glp-gold`, `--glp-ink`, `--glp-paper` etc.)
- Keep Tailwind config as JavaScript (`tailwind.config.js`), not TS.
- Do not introduce new color hex usage in components; use Tailwind classes mapped to CSS vars OR CSS vars directly.
- Prefer Deep Teal + Gold for primary/accent behavior; keep sage/blush/ivory/charcoal as supporting palette.
- Must keep things compatible with Vite + React and current structure.
- Make changes as small/safe as possible, but complete.

STEP-BY-STEP TASKS:

1) AUTHORITATIVE ROUTE LIST
- Open `client/src/App.jsx`.
- Programmatically treat every `<Route path="...">` as valid internal route (including "/", "/home", etc.). Routes with `:slug` style params are valid patterns (e.g. "/blog/:slug").
- Create `docs/ROUTES.md` containing:
  - a sorted list of all Route paths
  - a short note that this file is generated from App.jsx

2) TOKENS (exact file output)
- Create/overwrite `client/src/styles/brand-tokens.css` so it is the SINGLE source of truth for raw hex colors.
- In `:root`, define these exact brand tokens (use my preferred palette):
  --glp-paper: #FAF9F7;
  --glp-ink: #3A3A3A;
  --glp-sage: #8FBF9F;
  --glp-sage-deep: #2F5D5D;        (this is Deep Teal primary)
  --glp-blush: #F4C7C3;
  --glp-gold: #EAC33B;

- Also define semantic tokens that the app uses as CSS variables (NO raw hex anywhere else):
  --bg: var(--glp-paper);
  --text-1: var(--glp-ink);
  --text-2: rgba(58,58,58,0.72);   (you may compute with rgb helper vars)
  --surface-1: #FFFFFF;            (allowed hex here because this file is the ONLY allowed hex file)
  --surface-2: rgba(250,249,247,0.70);
  --primary: var(--glp-sage-deep);
  --primary-contrast: var(--glp-paper);
  --accent: var(--glp-gold);
  --ring: var(--glp-gold);
  --border: rgba(58,58,58,0.14);

- Add RGB helper vars for rgba usage:
  --glp-ink-rgb: 58,58,58;
  --glp-paper-rgb: 250,249,247;
  --glp-sage-deep-rgb: 47,93,93;
  --glp-gold-rgb: 234,195,59;

- Add 3 modes that override semantic tokens only (do not change base palette):
  Default: `:root` as above
  Low-Stim: `html[data-mode="low-stim"] { ... }` reduce shadow/contrast slightly, soften accent: set `--accent` and `--ring` to a slightly quieter gold computed via rgba or a different CSS var inside this same file.
  Reading: `html[data-mode="reading"] { ... }` maximize legibility: surfaces white, text-2 darker, reduce decorative shadows.

3) GLOBAL CSS WIRING
- Ensure `client/src/index.css` imports or includes `brand-tokens.css` BEFORE Tailwind utilities usage so vars exist.
  - If using CSS imports, do: `@import "./styles/brand-tokens.css";` at top (before @tailwind).
  - Keep `@tailwind base; @tailwind components; @tailwind utilities;`.
- Update `client/src/styles/brand.css` so it does NOT contain raw hex colors anymore.
  - Replace any raw hex usage with `var(--glp-...)` or semantic tokens.
  - Ensure focus-visible uses `var(--ring)` (gold).
- If there are duplicate token blocks across files, remove duplicates and centralize in `brand-tokens.css`.

4) TAILWIND (JS) — EXACT MAPPING TO EXISTING VARS
- Update `tailwind.config.js` to:
  - keep content globs as-is (client/src/**/*.{js,ts,jsx,tsx} etc.)
  - `theme.extend.colors` maps to CSS vars WITHOUT refactoring variable names.
  - Provide both palette names and semantic names:
    colors: {
      // palette
      sage: "var(--glp-sage)",
      blush: "var(--glp-blush)",
      teal: "var(--glp-sage-deep)",   // deep teal primary
      ivory: "var(--glp-paper)",
      charcoal: "var(--glp-ink)",
      gold: "var(--glp-gold)",
      // semantic
      bg: "var(--bg)",
      surface1: "var(--surface-1)",
      surface2: "var(--surface-2)",
      text1: "var(--text-1)",
      text2: "var(--text-2)",
      primary: "var(--primary)",
      accent: "var(--accent)",
      ring: "var(--ring)",
      border: "var(--border)",
    }

- Extend `boxShadow`, `borderRadius`, and `transitionTimingFunction` minimally using CSS vars where helpful.
- Add `ringColor` defaults if needed, but do not break existing classes.
- DO NOT introduce Tailwind plugins unless absolutely necessary.

5) NAV LINK AUDIT (Node script)
- Create `scripts/nav-link-audit.mjs` that:
  a) Parses `client/src/App.jsx` and extracts all `<Route path="...">` strings into a set.
     - Treat "/blog/:slug" as a pattern: it should match links like "/blog/anything".
     - Treat trailing slashes consistently (normalize "/x/" -> "/x" except root).
  b) Scans `client/src/**/*.{js,jsx,ts,tsx}` for internal links from:
     - `<Link to="...">`, `<a href="...">` (only if internal), `navigate("...")`, `history.push("...")` if present.
  c) Classifies each link as:
     - OK (matches a route pattern)
     - BROKEN (no matching route)
     - EXTERNAL (http/https) ignored
     - HASH (starts with #) ignored
     - MAIL/TEL ignored
  d) Outputs `docs/NAV_LINK_AUDIT.md` with:
     - summary counts
     - a table of BROKEN links including file path + line number + link value
     - a section listing all routes extracted from App.jsx
  e) Exits with code 1 if any BROKEN links exist.

6) VISUAL DOCTOR (Node script)
- Create `scripts/visual-doctor.mjs` that:
  a) Recursively scans `client/src`, `shared`, and any UI folders for raw hex colors:
     - Match #RGB/#RRGGBB case-insensitively.
     - Allow them ONLY in `client/src/styles/brand-tokens.css`.
  b) Flags disallowed inline styles:
     - Disallow `style={{ backgroundColor: "#..." }}` etc.
     - Allowlist:
        - style values that reference CSS vars: `var(--glp-...)` or `var(--bg)` etc.
        - purely layout numeric styles like width/height numbers only (optional; keep tight)
  c) Fonts:
     - Scan CSS files for `font-family:` entries.
     - Allow Inter + Playfair Display + system fallbacks only.
     - Flag any other font family tokens.
  d) Output a report to `docs/VISUAL_DOCTOR.md` with violations.
  e) Exit code 1 if any violations exist.

7) PACKAGE SCRIPTS + CI
- Update `package.json` to add:
  - "audit:nav": "node scripts/nav-link-audit.mjs"
  - "audit:visual": "node scripts/visual-doctor.mjs"
  - "audit": "npm run audit:nav && npm run audit:visual"
- Ensure `npm test` or CI pipeline runs `npm run audit`.
- If GitHub Actions exists, update or add `.github/workflows/ci.yml` to run:
  - npm ci
  - npm run build (if present)
  - npm run audit
- Ensure CI fails when audits fail.

8) FIX NAV LINKS (ONLY if broken)
- After implementing audits, run them.
- If broken links exist, fix them by aligning nav/footer/component links to valid routes from `client/src/App.jsx`.
- Do NOT remove routes; fix the links.

9) DELIVERABLES CHECKLIST (must be true)
- No raw hex colors outside `client/src/styles/brand-tokens.css`
- Tailwind colors map to `--glp-*` and semantic vars
- Modes work via `html[data-mode="low-stim"]` and `html[data-mode="reading"]`
- `docs/ROUTES.md`, `docs/NAV_LINK_AUDIT.md`, `docs/VISUAL_DOCTOR.md` exist and are up to date
- CI fails on broken nav links or styling violations

Now implement everything. When finished, summarize:
- Files changed/added
- How to toggle modes (example: set `document.documentElement.dataset.mode = "low-stim"` or "reading")
- How to run audits locally.