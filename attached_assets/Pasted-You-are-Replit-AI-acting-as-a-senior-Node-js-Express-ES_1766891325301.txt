You are Replit AI acting as a senior Node.js + Express + ESM engineer.

OBJECTIVE:
Finalize, harden, and stabilize the entire backend platform so it is production-ready, cost-safe, and fully functional without infinite loops, runaway processes, or billing risks.

CURRENT STATE (CONFIRMED):
- Node.js v20.x
- ESM (.mjs) only
- Express server
- Auth, admin, and JWT flows exist
- All API endpoints pass curl tests
- Middleware and JWT exports are now fixed
- No remaining syntax errors
- Platform must NOT auto-restart endlessly

TASKS — EXECUTE IN ORDER:

A) PROCESS & COST SAFETY (CRITICAL)
1. Ensure ONLY ONE Node process runs at a time
2. Remove any duplicate dev/watch/loop execution
3. Confirm server starts ONLY from `server/dev.mjs`
4. Ensure NO background infinite loops, timers, or watchers exist
5. Disable any accidental auto-rebuild / auto-spawn behavior
6. Confirm Replit will NOT incur extra charges due to runaway processes

B) SERVER FINALIZATION
1. Validate Express boot order:
   - env load
   - middleware
   - routes
   - error handler
2. Confirm these routes work:
   - GET /api/health-check
   - POST /api/auth/login
   - GET /api/auth/me
   - GET /api/admin/stats (admin only)
3. Ensure proper 401 / 403 responses
4. Ensure clean shutdown handling (SIGTERM / SIGINT)

C) AUTH & SECURITY
1. Confirm JWT signing + verification works
2. Confirm role-based access (admin/user)
3. Ensure no secrets are logged
4. Ensure Authorization: Bearer <token> is enforced correctly

D) PROJECT CLEANUP
1. Remove unused files
2. Remove commented or dead code
3. Ensure imports match exports everywhere
4. Ensure no circular imports remain
5. Ensure consistent naming across middleware/utils/routes

E) FINAL OUTPUT REQUIRED
After completion, respond ONLY with:
1. ✅ Confirmation that the platform is complete
2. ✅ Confirmation that costs are safe
3. ✅ Exact command to start the server safely
4. ✅ Confirmation no further action is required

DO NOT:
- Introduce new frameworks
- Add new features
- Change working endpoints
- Enable background automation
- Enable autoscaling
- Enable paid services

If anything is already correct, VERIFY IT and MOVE ON.