TASK: AUTOPILOT BUILD (Config-driven pages for ALL routes)

GOAL:
Make all 119 routes render instantly from /content/routes.js using one config schema, /components/PageTemplate.jsx, and your existing autopilot entry. This must compile and be verifiable via npm scripts.

REQUIREMENTS:
1) /content/routes.js
- Export:
  - export const ROUTE_INDEX = [ ... ]  // raw stubs (route/category/labels/alias/dynamic flags)
  - export const routes = [ ... ]       // fully built configs
  - export function getRouteConfig(pathname) { ... }  // exact match, dynamic match, fallback not found
  - export function matchDynamic(pattern, pathname) { ... }
- Include ALL routes exactly as provided across 13 categories (119 total), including:
  - Landing & Marketing (7)
  - Authentication (6)
  - Dashboard & Core (10)
  - AI & Chat (3)
  - Wellness & Healing Tools (24)
  - Advanced & Mastery (14)
  - Content & Learning (13)
  - Community & Social (3)
  - Support & Resources (5)
  - Legal & Policy (5)
  - Account & Settings (6)
  - Admin (3)
  - System & Utility (7, including /* Not Found)
- Preserve alias support (aliasOf) and dynamic route patterns (/blog/:slug, /community/discussion/:id).
- Provide calm, warm, grounded default copy for all stubs (safe defaults), and allow per-route overrides later.
- No TODOs, no missing exports.

2) /components/PageTemplate.jsx
- Accepts { config }.
- Renders: Layout -> Hero -> Sections -> Module cards grid -> Footer.
- Adds AOS attributes to sections/cards (fade-up/slide-in/scroll-fade).
- Initializes AOS once:true in Layout (or in a safe single place).
- GSAP micro motion ONLY when motion allowed (prefers-reduced-motion safe). Only transform/opacity.

3) Assets safety
- If /public/logo.svg, /public/sacred-pattern.svg, or /public/icons/icon1.svg icon2.svg icon3.svg are missing, create minimal placeholders that match palette and compile.

4) Output format (MANDATORY):
- IMPLEMENTATION CHECKLIST (before)
- FULL FILE CONTENT for every changed/created file (grouped by file path)
- ROUTE COMPLETION REPORT grouped by category listing routes count totals (must equal 119)
- COMPLETED CHECKLIST (after) + DONE GATE verification steps to run in shell:
  - npm run dev
  - npm run verify:routes