# REPLIT AI — A→Z 360 “UNTIL COMPLETE” INFINITE BATCH MASTER PROMPT (v17)
# The Genuine Love Project • APPLY-ALL • Top-50 → Next-50 → Next-50… (UNTIL COMPLETE)
# Beginner / Intermediate / Advanced ONLY (STRICT)

PASTE THIS INTO REPLIT AI AGENT MODE INSIDE THIS REPO.
DO NOT ASK QUESTIONS. DO NOT STOP. DO NOT WAIT FOR CONFIRMATION.
Apply all changes. Work in safe, small patches. After each patch run checks and paste outputs.

================================================================================
0) NON-NEGOTIABLE GUARDRAILS
================================================================================
- ONLY TIERS ALLOWED: Beginner / Intermediate / Advanced
- HARD FAIL if ANY “reading level” language exists anywhere (code, docs, UI text, seed data):
  reading level, reading_level, readingLevel, grade level, grade_level, age level
- If DB already has reading_level: DO NOT rename right now.
  Map reading_level -> tier at the API boundary. UI must only show Beginner/Intermediate/Advanced.

- Replit Autoscale rule: SINGLE PORT only.
  Server must bind 0.0.0.0 and use process.env.PORT, no second listener, no background daemons.

- No secrets committed. Provide .env.example and docs.
- Validate all inputs with Zod.
- Standard API errors:
  { ok:false, error:{ code, message, requestId }, details?:any }
- Accessibility: tap targets >= 44px, keyboard focus visible, color contrast.
- Patch safety: no mass rewrites, no huge refactors, no rename storms.

================================================================================
1) “PLATFORM COMPLETE” EXIT CRITERIA (ALL MUST BE ✅)
================================================================================
ENGINEERING
[ ] npm run content-check ✅
[ ] npm run lint ✅
[ ] npm run typecheck ✅
[ ] npm run build ✅
[ ] npm run test ✅
[ ] Cold start on Replit works ✅ (single port)

CORE PLATFORM
[ ] Auth: signup/login/logout/forgot/reset/email-verify + /me ✅
[ ] RBAC: user/admin + protected routes ✅
[ ] PostgreSQL + Drizzle tables:
    users, sessions, journals, moods, subscriptions, plans, content_items, audit_logs, rate_limits ✅
[ ] AI: safe journaling/chat + crisis resources + rate limits + audit logging + redaction ✅
[ ] Stripe: checkout + portal + webhook verify + plan gating + subscription sync ✅

ADMIN + OPS
[ ] /health + /ready ✅
[ ] requestId middleware ✅
[ ] Admin Analyzer API (admin-only) ✅
[ ] Admin Health Dashboard UI (admin-only) ✅
[ ] Audit trails + export ✅

UX + CONTENT
[ ] Responsive uncluttered UI + consistent spacing/typography ✅
[ ] Tier displays Beginner/Intermediate/Advanced ONLY ✅
[ ] Publishing MVP: admin CRUD + public blog + SEO + sitemap + OG ✅

DEVOPS + AUTOMATION
[ ] GitHub Actions CI: content-check/lint/typecheck/build/test ✅
[ ] Human-triggered scripts: scan → report → verify (idempotent) ✅
[ ] /docs/processes.md + batch docs show exact ✅/🟡/❌ status ✅

================================================================================
2) THE “TOP PLATFORM PROCESSES” LIBRARY (REFERENCE SET)
================================================================================
You must continuously integrate best practices from leading platforms across:
- Reliability: SRE, health/readiness, retries, circuit breakers
- Security: OWASP, secrets, RBAC, audits, CSP, rate limits
- Payments: Stripe best practices + webhook verification
- Content: editorial workflow, SEO, sitemap, OG, RSS
- Product: onboarding, retention loops, UX research, analytics
- Ops: CI/CD, test pyramid, release notes, rollback plan
- Data: migrations, backups, observability, privacy, DSR

You will implement these as “process batches” (50 at a time), each with acceptance criteria.

================================================================================
3) INFINITE BATCH ENGINE (TOP-50 → NEXT-50 → NEXT-50… UNTIL COMPLETE)
================================================================================
Create/maintain these “source of truth” docs:

/docs/processes.md
  - Index of every process batch
  - Current batch pointer
  - Completion scoreboard
  - Links to batch files
  - “Next patch” pointer

/docs/process-batches/batch-001.md   (Processes 1–50)
/docs/process-batches/batch-002.md   (51–100) generated ONLY after batch-001 is ✅
/docs/process-batches/batch-003.md   (101–150) generated ONLY after batch-002 is ✅
...continue generating in increments of 50 until COMPLETE criteria is met.

Each process entry MUST contain:
- Process #: Name
- Why it matters (1–2 lines)
- “Done means” acceptance criteria
- Implementation file paths
- Verification steps
- Status ✅/🟡/❌

================================================================================
4) PATCH ORDER (STRICT — DO NOT SKIP)
================================================================================
PATCH 1: Tier enforcement + banned-word removal + content-check gate
PATCH 2: Single-port + /health + /ready + error handler + requestId
PATCH 3: Drizzle schema completion + DB wiring + migrations doc
PATCH 4: Auth + sessions + RBAC + protected routing
PATCH 5: Stripe billing + webhook verify + plan gating
PATCH 6: AI safety + rate limits + audit logs + crisis response + redaction
PATCH 7: Admin Analyzer API + Admin Health Dashboard UI
PATCH 8: Publishing MVP + SEO + sitemap + OG + RSS
PATCH 9: CI/CD + tests + scan/report/verify automation scripts
PATCH 10: Batch-001 finalization ✅ + lock docs
THEN generate Batch-002 and repeat this patch loop until COMPLETE.

================================================================================
5) REQUIRED FILES (AUTO-CREATE IF MISSING)
================================================================================
Docs:
  /docs/processes.md
  /docs/process-batches/batch-001.md
  /docs/security.md
  /docs/ops.md
  /docs/ai-safety.md
  /docs/migrations.md
  /docs/release-notes.md

Scripts:
  /scripts/content-check.mjs
  /scripts/scan.mjs
  /scripts/report.mjs
  /scripts/verify.mjs

Server:
  /server/middleware/requestId.ts
  /server/middleware/rateLimit.ts
  /server/middleware/auth.ts
  /server/middleware/rbac.ts
  /server/middleware/errorHandler.ts
  /server/routes/health.ts
  /server/routes/auth.ts
  /server/routes/ai.ts
  /server/routes/billing.ts
  /server/routes/admin.ts
  /server/routes/content.ts

Frontend minimum pages:
  /src/pages/Login.tsx
  /src/pages/Signup.tsx
  /src/pages/ForgotPassword.tsx
  /src/pages/ResetPassword.tsx
  /src/pages/Journal.tsx
  /src/pages/Pricing.tsx
  /src/pages/AdminDashboard.tsx
  /src/pages/Blog.tsx
  /src/pages/Help.tsx

================================================================================
6) EXECUTION LOOP (RUN UNTIL COMPLETE — DO NOT ASK QUESTIONS)
================================================================================
LOOP A — SCAN + BASELINES (RUN & PASTE OUTPUTS)
1) Repo snapshot
  ls -la
  node -v && npm -v
  cat package.json

2) Hard fail check for banned tier language
  rg -n "reading\\s*level|grade\\s*level|age\\s*level|reading_level|readingLevel|grade_level" . || true

3) Port & listener checks
  rg -n "listen\\(|createServer\\(|0\\.0\\.0\\.0|process\\.env\\.PORT|PORT" server src . || true

4) Feature presence scan
  rg -n "drizzle|DATABASE_URL|postgres|pg" . || true
  rg -n "stripe|STRIPE_|webhook|checkout|portal" . || true
  rg -n "openai|crisis|rate\\s*limit|redact|prompt injection|moderation" . || true
  rg -n "admin|dashboard|analyzer|audit" . || true
  rg -n "blog|content|sitemap|metadata|og:" . || true

5) Install + baseline commands
  npm ci || npm install
  npm run -s content-check || true
  npm run -s lint || true
  npm run -s typecheck || true
  npm run -s build || true
  npm run -s test || true

LOOP B — APPLY NEXT PATCH (IN ORDER, FULLY)
- Implement the next patch completely.
- Keep changes minimal and safe.

LOOP C — VERIFY AFTER EACH PATCH (PASTE OUTPUTS)
  npm run -s content-check
  npm run -s lint
  npm run -s typecheck
  npm run -s build
  npm run -s test

LOOP D — REQUIRED REPORT AFTER EACH PATCH
1) ✅ Changed files (paths)
2) 🧪 Commands + outputs (pasted)
3) 🔎 Click-test routes:
   /  /login  /signup  /journal  /pricing  /admin  /blog  /help
4) ✅ Update /docs/processes.md + batch file statuses
5) Immediately continue next patch

================================================================================
7) PATCH #1 — DO THIS NOW (TIER ENFORCEMENT + CONTENT CHECK) — EXACT BUILD
================================================================================
A) Create /scripts/content-check.mjs with these rules:
- FAIL if any banned tier language exists anywhere.
- FAIL if any tier value exists outside Beginner/Intermediate/Advanced in:
  - UI labels, docs, seed data, API contract docs
- PASS if:
  - All tier values normalize to exactly: Beginner | Intermediate | Advanced

B) Add to package.json:
  "content-check": "node scripts/content-check.mjs"

C) Add /docs/tier-policy.md describing:
- Only Beginner/Intermediate/Advanced display
- Legacy mapping strategy if DB uses reading_level

D) Implement tier mapping at API boundary:
- Any persisted field reading_level (if present) maps to tier:
    reading_level in ["beginner","intermediate","advanced"] -> Title Case
    numeric -> bucket into Beginner/Intermediate/Advanced (documented)
- UI NEVER shows reading_level.

E) Verify:
  npm run -s content-check

================================================================================
8) BATCH-001 (PROCESSES 1–50) — IMPLEMENT FULLY BEFORE NEXT BATCH
================================================================================
A) Product & UX (1–10)
1 Onboarding flow (tier selection + goals)
2 Personalization settings (preferences)
3 Navigation + search (MVP)
4 Design tokens + component consistency
5 Accessibility baseline (keyboard/focus/alt/labels)
6 Feedback capture (bug/idea form)
7 Progressive disclosure (reduce overwhelm)
8 Help/FAQ page + crisis resources page
9 i18n scaffolding (structure, not translations)
10 Performance budgets (basic lighthouse targets + bundle checks doc)

B) Auth/Security/Privacy (11–20)
11 Session strategy (httpOnly cookie or token) + /me
12 Password reset + email verify
13 RBAC (admin/user) + route protection
14 Rate limiting middleware (per-ip + per-user)
15 Zod validation in every route
16 Security headers (helmet) + CSP baseline doc
17 Audit logs for sensitive actions
18 Disclaimers: “not therapy” + crisis info visible
19 Data export/delete scaffolding (account settings)
20 Secrets management + env docs

C) Data & Reliability (21–30)
21 PostgreSQL wired with Drizzle (no mock DB)
22 Migration docs (push/generate workflow)
23 Backup guidance doc (ops)
24 Idempotent APIs + unified error format
25 No background job policy (human-triggered scripts only)
26 /health and /ready endpoints
27 Feature flags (simple env flag system)
28 Caching strategy doc (server + client)
29 Privacy-friendly analytics events schema
30 Disaster recovery checklist doc

D) AI Safety & Quality (31–40)
31 Crisis detection + safe response template + resources
32 Prompt injection hardening rules
33 Fallback responses (network/provider down)
34 Consent + transparency UI (AI usage disclosure)
35 Log redaction for PII
36 AI rate limits + quotas
37 Non-diagnostic policy enforcement (supportive only)
38 Prompt tests/evals (unit tests for guardrails)
39 Human escalation guidance (seek professional)
40 Output constraints (length, format, boundaries)

E) Monetization & Growth (41–50)
41 Stripe checkout + customer portal
42 Webhook verify + subscription sync
43 Plan gating in UI + API
44 Ethical pricing UX + clear cancel flows
45 Email capture optional + double opt-in design
46 SEO: sitemap + meta + OG + robots + canonical
47 Publishing pipeline: draft/review/publish with roles
48 Social share assets generator (MVP templates)
49 Admin analytics dashboard (MVP)
50 CI/CD + release notes discipline + versioning doc

================================================================================
9) AFTER BATCH-001 ✅ — AUTO-GENERATE BATCH-002 (51–100) THEN IMPLEMENT
================================================================================
Only after batch-001 is ✅:
1) Generate /docs/process-batches/batch-002.md with processes 51–100 focusing on:
- Observability (structured logs, metrics, traces)
- Alerts + dashboards (SLOs)
- Integration + e2e tests
- Editorial tooling & moderation
- Advanced admin tools (audit explorer, exports)
- Performance: code splitting, caching, image optimization
- Privacy/compliance workflows (DSR, retention)
- First i18n locale rollout
- Backup/restore drills
- Growth automation (opt-in segmentation, lifecycle emails)

2) Repeat patch loop until batch-002 ✅
3) Generate batch-003 (101–150) and continue
Repeat until COMPLETE exit criteria is all ✅.

================================================================================
10) START NOW
================================================================================
RUN LOOP A (SCAN) and paste outputs.
Then implement PATCH #1 completely, verify, and proceed patch #2 onward.
After batch-001 ✅, auto-generate batch-002 and continue until COMPLETE.