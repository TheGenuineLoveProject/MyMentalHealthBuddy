Hard truth

The Replit AI result shows Step 1 is now mostly fixed:

✅ /api/auth/user fixed
✅ token-aware API client added
✅ admin route protected correctly
✅ admin dashboard returns 401 without token
✅ session path is no longer missing

Now proceed in this exact order.

Paste this into Replit AI next

Continue from the verified session/admin fix.
Complete these phases in order with DRY-RUN first, smallest-safe edits only, and verification after each phase.
PHASE 1 — FINAL VERIFY SESSION / ADMIN LOGIN
Do not rewrite admin.mjs.
Verify:
- Login stores token using mmhb_token
- Refresh restores session through /api/auth/user
- Admin user routes to /admin
- Non-admin user routes to /dashboard
- /api/admin/dashboard returns 200 with real admin token
- /api/admin/dashboard returns 401/403 without token
If broken, fix only the exact failing link.
PHASE 2 — MAKE ADMIN DASHBOARD ELITE
Create or polish the Admin Command Center UI.
Target route: /admin
Requirements:
- visual dashboard cards
- system health panel
- AI telemetry panel
- user/session status panel
- route status panel
- deployment readiness panel
- self-healing controls section
- clear loading/error states
- mobile/tablet responsive
- uses existing /api/admin/dashboard and existing admin endpoints
Do not delete backend routes.
PHASE 3 — CONNECT AI USER MEMORY SAFELY
Do not rewrite orchestrator.
Connect authenticated user identity to AI calls only through existing memory/profile system.
Requirements:
- authenticated userId passed to /api/ai/chat
- guest fallback remains working
- memory/profile/summary behavior preserved
- no private data exposed in UI
- crisis logic unchanged
PHASE 4 — EMOTIONAL INTELLIGENCE LAYER
Verify existing emotionInference.mjs and responsePolicy wiring.
Add only missing safe improvements:
- clearer inferred state telemetry
- fallback from module → emotional tone when inference is empty
- no crisis override
- no diagnosis
- no therapy replacement claims
PHASE 5 — UPGRADE BUDDY REACTIONS
Improve BuddyAvatar only.
Requirements:
- subtle mouth movement
- soft arm/palm movement
- eye blink
- state-based reactions
- no flashing
- respects prefers-reduced-motion
- crisis state remains steady and calm
- no backend changes unless already required
Return after each phase:
- files changed
- what was fixed
- verification commands
- verification result
- skipped risky changes

Shell commands

npm run dev

Then verify:

curl http://0.0.0.0:5000/health
curl http://0.0.0.0:5000/api/auth/user
curl http://0.0.0.0:5000/api/admin/dashboard

Expected:

* /health → 200
* /api/auth/user → 200 or valid unauth response, not 404
* /api/admin/dashboard without token → 401/403
* /api/admin/dashboard with admin token → 200

Do not replace server/routes/admin.mjs.