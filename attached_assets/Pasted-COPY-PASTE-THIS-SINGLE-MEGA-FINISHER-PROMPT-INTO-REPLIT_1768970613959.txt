COPY/PASTE THIS SINGLE “MEGA FINISHER” PROMPT INTO REPLIT AI (AGENT MODE)

You are working in my Replit repo for “The Genuine Love Project”.
We already have audits passing (nav-link audit + visual doctor). Now perform ONE final 360° A→Z platform completion pass: implement missing UX polish (mode toggle + consistency), perfect navigation + NotFound experience, and verify everything by running audits and documenting results.

══════════════════════════════════════════════════════════════════════
NON-NEGOTIABLE RULES (HARD CONSTRAINTS)
1) Do NOT rename/refactor my existing CSS variable names. Keep my current `--glp-*` names exactly.
2) No raw hex colors anywhere except: `client/src/styles/brand-tokens.css`.
3) Fonts allowed only: Inter + Playfair Display (+ system fallbacks). Do not introduce any other font families.
4) All internal navigation links must match canonical routes defined in `client/src/App.jsx` (wouter Switch/Route). No drift.
5) Prefer minimal diffs; avoid breaking refactors. Improve via tokens + Tailwind semantic classes.
6) Do NOT loosen or bypass existing audits; fix issues properly.

══════════════════════════════════════════════════════════════════════
READ FIRST (SOURCE OF TRUTH)
- Canonical router: `client/src/App.jsx`
- Tokens: `client/src/styles/brand-tokens.css`
- Brand CSS: `client/src/styles/brand.css` (if present)
- Tailwind: `tailwind.config.js` (JS)
- Global CSS entry: `client/src/index.css` (and `client/src/main.css` if used)
- Nav components: search for Header/Navbar/Footer/Sidebar/NavLink/Link/a href/CTA buttons
- Audits: `tools/nav-link-audit.mjs`, `tools/visual-doctor.mjs`
- Reports: `docs/NAV_LINK_AUDIT.md`, `docs/VISUAL_DOCTOR_REPORT.md`
- Workflows: `.github/workflows/*` (audit/nav/visual)

══════════════════════════════════════════════════════════════════════
MISSION: SINGLE-PASS 360° A→Z FINISHER (DO ALL)

A) MODE SYSTEM (Default / Low-Stim / Reading) — REAL UI + persistence
1) Ensure 3 modes exist via HTML dataset:
   - Default: html.dataset.mode = "" (or not set)
   - Low-Stim: html.dataset.mode = "low-stim"
   - Reading: html.dataset.mode = "reading"
2) Implement a Mode Toggle UI in TWO places (BOTH):
   - Header top-right (compact segmented control or dropdown)
   - Settings page (Display/Mode section with short descriptions)
3) Persistence:
   - localStorage key: `glp-mode`
   - Apply on app start BEFORE UI paints (avoid flash). If needed, add a tiny bootstrap in `client/src/main.jsx` or earliest possible client init to set dataset ASAP.
4) Accessibility:
   - keyboard reachable
   - `aria-pressed` / `aria-current` on active option
   - consistent focus-visible ring using the gold ring token (no hex)
5) Mode styling rules:
   - Default: full “healing luxury” (subtle gradients allowed only via tokens/vars)
   - Low-Stim: reduce saturation + shadows + decorative gradients + motion
   - Reading: maximize legibility (high contrast text, flatter surfaces, minimal decoration)

B) VISUAL CONSISTENCY SWEEP (healing-luxury teal + gold power pair)
Unify look/feel across the entire app:
1) Buttons (normalize to one system):
   - Primary: teal background + ivory text; hover/active via tokens; gold focus ring
   - Secondary/Ghost: teal text; subtle surface hover; gold focus ring
   - Disabled: muted surface + muted text; cursor-not-allowed
   - Ensure consistent padding, radius, icon gap, and transitions (reduced in Low-Stim)
2) Links:
   - teal text
   - underline on hover only
   - gold focus ring visible on keyboard focus
3) Cards/Surfaces:
   - use semantic surface/bg tokens only
   - consistent radius + soft shadow
   - Low-Stim reduces/turns off shadows and decorative gradients via CSS vars
4) Icons:
   - standardize to 24px base
   - unify style (outline feel)
   - no random fills/colors; use tokens/vars
5) Typography:
   - Headings: Playfair Display
   - Body/UI: Inter
   - Normalize hierarchy (H1/H2/H3 + body/label/caption), line-height, spacing
   - Avoid low-contrast text; ensure comfortable line-height
6) Motion/Effects:
   - Respect prefers-reduced-motion globally
   - Low-Stim mode reduces transitions/animations using CSS variables or conditionals

C) NAVIGATION LOCKDOWN (ZERO DRIFT)
1) Treat route list in `client/src/App.jsx` as canonical.
2) Find ALL internal links across app:
   - header nav, footer nav, sidebar nav
   - CTA buttons, quick links, cards linking to pages
3) Every internal link path must exactly match an existing route path in App.jsx:
   - same case, same hyphenation, correct leading slash
4) If a link points to a missing route:
   - fix to correct existing route OR remove it
   - do NOT invent routes unless there is an obviously intended missing page file already present and referenced

D) PERFECT THE NOTFOUND (404) EXPERIENCE
1) Locate the NotFound component used by the fallback route in `client/src/App.jsx`.
2) Upgrade it to premium healing-luxury:
   - Playfair headline, calm supportive body copy
   - Primary button: “Go Home” (teal + ivory, gold focus ring)
   - Secondary: “Go Back” (ghost/outline, gold focus ring)
   - Add “Try these” / “Quick Links” (4–6 items max) that ONLY use real routes from App.jsx
   - Ensure layout is crisp on mobile/tablet (safe margins, centered, no squished typography)
3) Re-run nav audit afterward to confirm quick links are valid.

E) TOKENS + TAILWIND SANITY (NO REFACTOR, JUST ALIGNMENT)
1) Ensure Tailwind semantic keys map to existing CSS vars (no new token names):
   - bg/surface/text/muted/border/primary/accent/ring
   - plus explicit brand keys already used (sage/blossom/teal/gold/ivory/charcoal)
2) Ensure Tailwind content globs include all relevant files.
3) Ensure global CSS entry imports tokens/brand css correctly.
4) Replace any stray color definitions with vars/classes if found.

F) DOCS: FINAL “PLATFORM COMPLETE” KIT
Create or update:
1) `docs/COLOR_SYSTEM.md`
   - list core `--glp-*` tokens
   - semantic mapping
   - what changes in Default vs Low-Stim vs Reading
   - do/don’t rules (no hex outside brand-tokens.css)
2) `docs/TYPOGRAPHY_GUIDE.md`
   - Inter + Playfair usage
   - scale and hierarchy
   - spacing and line-height guidance
3) `docs/CANVA_TEMPLATE_SPECS.md`
   - 1080×1080 and 1080×1920
   - safe margins, grid, spacing system
   - exact type scale (Playfair/Inter) + usage patterns (quote cards, prompt cards, carousel)
   - color rules (teal/gold emphasis, ivory backgrounds)
4) `README.md`
   - “How to run audits”: `npm run audit`, `npm run nav:audit`, `npm run visual:doctor`
   - “How to switch modes”: where toggle is + localStorage key + html data-mode

G) VERIFY (MUST ACTUALLY RUN COMMANDS + CONFIRM)
1) Run in Shell:
   - `npm run audit`
2) Confirm reports updated:
   - `docs/NAV_LINK_AUDIT.md` shows 0 broken links
   - `docs/VISUAL_DOCTOR_REPORT.md` shows 0 violations
3) Manual click-test (Preview):
   - click every header/footer/sidebar link (if present)
   - confirm no 404s
   - test mode toggle: switch modes → refresh → confirm persistence

══════════════════════════════════════════════════════════════════════
ACCEPTANCE CRITERIA (MUST BE TRUE)
- `npm run audit` passes.
- No raw hex colors exist outside `client/src/styles/brand-tokens.css`.
- No font families beyond Inter + Playfair Display (+ system fallbacks).
- Mode toggle exists in Header AND Settings, persists via `glp-mode`, applies before paint.
- NotFound page is on-brand and its quick links all match routes in `client/src/App.jsx`.
- All internal links match canonical routes (no drift).

══════════════════════════════════════════════════════════════════════
FINAL RESPONSE FORMAT (REQUIRED)
When finished, respond with:
1) A checklist confirming completion of A–G.
2) Exact commands run and their results (pass/fail).
3) Files changed (each with a 1-line summary).
4) Quick Links used on NotFound (label + exact path).
5) OPTIONAL improvements (clearly labeled OPTIONAL; do not implement unless asked).

Proceed now. Do not stop early. Verify, then report.