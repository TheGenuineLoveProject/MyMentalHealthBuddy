Yes. The right move now is a soft paywall, not a hard gate.

Decision

Do this in order:

1. add server-side paywall policy
2. add usage counter
3. attach paywall hint to successful non-crisis responses
4. render inline upgrade card on /start
5. add paywall telemetry
6. do not gate the first relief experience

⸻

Hard rules

Never show paywall:

* on first session
* on crisis flow
* during a tool step
* before a successful response

Show paywall only:

* after 3 successful non-crisis sessions
* when user tries a premium-only tool
* after trust has been earned

⸻

Exact file-by-file implementation sequence

1. Create paywall policy

server/ai/paywallPolicy.mjs

export function shouldShowPaywall({
  isCrisis = false,
  successfulSessions = 0,
  dailySessions = 0,
  requestedPremium = false
}) {
  if (isCrisis) return false;
  if (requestedPremium) return true;
  if (successfulSessions >= 3) return true;
  if (dailySessions >= 3) return true;
  return false;
}
export function getPaywallReason({
  successfulSessions = 0,
  dailySessions = 0,
  requestedPremium = false
}) {
  if (requestedPremium) return "premium_feature";
  if (successfulSessions >= 3) return "value_proven";
  if (dailySessions >= 3) return "daily_limit";
  return null;
}

⸻

2. Create usage counter

server/utils/usageCounter.mjs

import fs from "fs";
import path from "path";
const DIR = path.resolve("data/usage");
function ensureDir() {
  if (!fs.existsSync(DIR)) fs.mkdirSync(DIR, { recursive: true });
}
function sanitize(id) {
  return String(id || "anonymous").replace(/[^a-zA-Z0-9_-]/g, "_");
}
function getFile(id) {
  return path.join(DIR, `${sanitize(id)}.json`);
}
export function readUsage(id) {
  try {
    ensureDir();
    const file = getFile(id);
    if (!fs.existsSync(file)) {
      return {
        successfulSessions: 0,
        dailySessions: 0,
        lastDay: new Date().toISOString().slice(0, 10)
      };
    }
    const data = JSON.parse(fs.readFileSync(file, "utf-8"));
    const today = new Date().toISOString().slice(0, 10);
    if (data.lastDay !== today) {
      data.dailySessions = 0;
      data.lastDay = today;
    }
    return data;
  } catch {
    return {
      successfulSessions: 0,
      dailySessions: 0,
      lastDay: new Date().toISOString().slice(0, 10)
    };
  }
}
export function incrementSuccessfulSession(id) {
  const data = readUsage(id);
  data.successfulSessions += 1;
  data.dailySessions += 1;
  ensureDir();
  fs.writeFileSync(getFile(id), JSON.stringify(data, null, 2));
  return data;
}

⸻

3. Attach paywall decision in AI route

server/routes/ai.mjs

Add imports:

import { incrementSuccessfulSession } from "../utils/usageCounter.mjs";
import { shouldShowPaywall, getPaywallReason } from "../ai/paywallPolicy.mjs";

Then after a successful non-crisis result, before res.json(...), add:

const identity =
  req.dbUserId ||
  req.headers["x-guest-id"] ||
  "anonymous";
const isCrisis = Boolean(result?.response?.isCrisis);
if (result?.ok && !isCrisis && result?.response?.reply) {
  const usage = incrementSuccessfulSession(identity);
  result.response.paywall = {
    show: shouldShowPaywall({
      isCrisis,
      successfulSessions: usage.successfulSessions,
      dailySessions: usage.dailySessions
    }),
    reason: getPaywallReason({
      successfulSessions: usage.successfulSessions,
      dailySessions: usage.dailySessions
    })
  };
}

⸻

4. Render soft paywall on /start

In your working client/src/pages/Start.tsx, after rendering the response, add:

{response?.paywall?.show && (
  <div className="mt-6 rounded-xl border p-4">
    <h3 className="font-semibold">Keep your momentum going</h3>
    <p className="mt-2 text-sm opacity-80">
      Unlock deeper tools, more personalized support, and longer-term insights when you’re ready.
    </p>
    <div className="mt-4 flex gap-3">
      <button className="rounded-lg px-4 py-2 border">
        Unlock Pro
      </button>
      <button className="rounded-lg px-4 py-2">
        Keep using free tools
      </button>
    </div>
  </div>
)}

If your response is nested, use the exact path your component already uses, for example:

response?.response?.paywall?.show

Use the real one from your file.

⸻

5. Add paywall telemetry

Where your frontend already sends telemetry, add:

* paywall_shown
* paywall_clicked

Example:

if (response?.paywall?.show) {
  track("paywall_shown", {
    reason: response.paywall.reason
  });
}

On Unlock Pro click:

track("paywall_clicked", {
  reason: response?.paywall?.reason || null
});

⸻

Exact shell commands

mkdir -p data/usage
touch server/ai/paywallPolicy.mjs
touch server/utils/usageCounter.mjs
npm run dev

⸻

Verification sequence

Run these with straight apostrophes only:

curl -X POST http://0.0.0.0:5000/api/ai/chat -H "Content-Type: application/json" -H "x-guest-id: paywalltest1" -d '{"message":"I feel anxious"}'
curl -X POST http://0.0.0.0:5000/api/ai/chat -H "Content-Type: application/json" -H "x-guest-id: paywalltest1" -d '{"message":"I feel overwhelmed"}'
curl -X POST http://0.0.0.0:5000/api/ai/chat -H "Content-Type: application/json" -H "x-guest-id: paywalltest1" -d '{"message":"I keep overthinking"}'

Expected:

* first session: paywall.show = false
* second session: likely still false
* third successful non-crisis session: paywall.show = true

Also verify crisis:

curl -X POST http://0.0.0.0:5000/api/ai/chat -H "Content-Type: application/json" -H "x-guest-id: paywalltest2" -d '{"message":"I want to hurt myself"}'

Expected:

* crisis response
* no paywall shown

⸻

Rollback plan

If anything feels off:

git checkout -- server/ai/paywallPolicy.mjs server/utils/usageCounter.mjs server/routes/ai.mjs client/src/pages/Start.tsx
npm run dev

If not using git cleanly, copy backups before edits.

⸻

Replit AI prompt

Paste this exactly:

Implement soft paywall safely without changing working AI behavior.
Goals:
1. Show upgrade only after repeated value
2. Never show paywall on crisis
3. Never interrupt the first successful relief experience
4. Keep core free tools available
Constraints:
- Preserve all working AI routes, crisis behavior, telemetry, memory, profile, summary, provider, and tool execution
- Smallest safe changes only
- No changes to responsePolicy/provider logic unless strictly necessary
- Keep HTTP transport in server/routes
- Keep AI-domain decision helpers in server/ai
- Keep low-level usage storage helper in server/utils
Implement:
A. Add server/ai/paywallPolicy.mjs
B. Add server/utils/usageCounter.mjs
C. After successful non-crisis responses in server/routes/ai.mjs, increment usage and attach:
   response.paywall = { show, reason }
D. On /start UI, render a soft inline upgrade card only when response.paywall.show === true
E. Add telemetry events for:
   - paywall_shown
   - paywall_clicked
Before edits return:
- exact files to edit
- exact rollback plan
- verification checklist
After edits verify:
- first successful session shows no paywall
- third successful non-crisis session shows paywall
- crisis flow never shows paywall
- existing / and /start still work

⸻

Best next move after this

After paywall is working, do this next:

1. launch content pack
2. public soft launch
3. observe telemetry
4. optimize from real users

If you want the next step after paywall, say build the exact launch content pack.