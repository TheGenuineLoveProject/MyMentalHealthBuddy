TASK: CATEGORY RUN — LANDING ROUTES UPGRADE (ONE PASS, NO TODOs)

SCOPE:
Upgrade ALL Landing & Marketing routes at once, using route-specific overrides for:
- "/"
- "/healing"
- "/pricing"
- "/landing"
- "/original-home"
AND aliases:
- "/home" aliasOf "/"
- "/welcome" aliasOf "/"
Keep ALL other routes on category presets (do not touch them except if required to fix compilation).

HARD RULES:
- Keep config-driven system intact: /content/routes.js -> getRouteConfig() -> PageTemplate -> Sacred components.
- Do NOT rewrite the architecture. Only add Landing overrides + ensure presets still power the rest.
- Output must compile. No TODOs. No partial solutions.

REQUIREMENTS:
1) In /content/routes.js:
   A) Confirm Landing category preset exists and is calm, rich-but-gentle:
      - clarity → safety → action
      - 3–4 default sections max
      - 3–6 modules max
      - motion.level = "rich" but still subtle (no abrupt motion)
   B) Add explicit overrides for the Landing routes listed above:
      For each route override, set:
      - title, description (SEO)
      - hero.title, hero.copy (warm, grounded, gentle, evidence-based)
      - hero.primaryCta + hero.secondaryCta (clear, non-pushy)
      - sections: 3–4 max, each with eyebrow/title/subtitle/variant and optional bullets/cards
      - modules/cards: 3–6 max with icon/title/text
      - motion.level appropriate:
        "/" "rich"
        "/healing" "calm"
        "/pricing" "minimal-calm" (no hype, transparent)
        "/landing" "rich"
        "/original-home" "calm" (nostalgic legacy, still coherent)
   C) Ensure aliases:
      - "/home" and "/welcome" are aliasOf "/"
      - They must inherit "/" config and not duplicate content
   D) Ensure getRouteConfig handles:
      - exact match
      - aliasOf resolution
      - dynamic patterns remain untouched
      - NotFound fallback remains untouched

2) Copy standards (Landing voice):
- Warm, grounded, gentle, evidence-based
- Short paragraphs, reassuring language, no pressure
- Avoid grandiose claims; emphasize safety, choice, pacing
- CTAs like: "Start gently", "Explore at your pace", "See pricing", "Begin a practice"

3) Output requirements:
- Provide FULL updated /content/routes.js (entire file)
- Provide a "LANDING UPGRADE REPORT":
  - list each upgraded landing route + its hero title + CTAs
  - confirm aliases resolve correctly
  - confirm all other routes still use presets (no overrides added)

DONE GATE (MUST PASS):
✅ npm run dev works
✅ no console errors
✅ getRouteConfig returns correct config for /, /healing, /pricing, /landing, /original-home
✅ /home and /welcome inherit "/" via aliasOf
✅ no other categories changed beyond presets
FAIL THE RUN if any DONE GATE item is not satisfied.