CORE MASTER COMMAND v3 — THE GENUINE LOVE PROJECT
(DETERMINISTIC PATCH + SKILLS + SUBAGENTS + EXECUTABLES + MCP SPEC)

PRIMARY GOAL:
Patch ONLY the files listed in scripts/.scan-results.json (flagged),
and add shared modules that remove duplication and keep build stable:
SEO + SafetyFooter(18+) + Benefits + PageScaffold + BenefitsBank + Dynamic Context
Then add reusable Skill Packs (MI + 12 Practices + Safe NLP)
Then add deterministic executables + subagent specs + MCP spec (no new deps unless already present)

ABSOLUTE RULES:
1) You may modify ONLY:
   A) files explicitly listed in scripts/.scan-results.json
   B) new shared modules/components created by this prompt
   C) scripts/brand-lock.sh (required)
   D) new scripts under scripts/ (human-triggered only)
   E) new spec files under agents/ (text only)
2) DO NOT mass refactor. DO NOT rename routes. DO NOT delete files.
3) DO NOT paste long disclaimers in every page. Use shared SafetyFooter + Scaffold.
4) LEGAL SAFE LANGUAGE:
   - Never claim therapy, diagnosis, treatment, cure, guarantee.
   - Use: education, self-reflection, wellness skill-building.
   - Always include 18+ notice via SafetyFooter.
   - Crisis guidance: “If you are in immediate danger, contact local emergency services.”
5) No plagiarism. No copyrighted text.
6) Deterministic: no randomness. Same inputs must yield same outputs.

STEP 0 — Load scan targets
- Open scripts/.scan-results.json
- Extract lists for: missing-seo, missing-safety, missing-benefits, contains-disallowed
- Only patch those file paths.

STEP 1 — Fix git hook blocker (required)
Create file: scripts/brand-lock.sh
Content:
#!/usr/bin/env bash
echo "brand-lock: ok"
exit 0
Make executable (chmod +x scripts/brand-lock.sh).

STEP 2 — Create single policy source (new file)
Create: client/src/policy/platformPolicy.ts
Must export:
- AGE_REQUIREMENT_TEXT (18+)
- NOT_THERAPY_TEXT
- NOT_MEDICAL_ADVICE_TEXT
- FORBIDDEN_CLAIMS array (cure, diagnose, treat, guarantee, etc.)
- CRISIS_TEXT (generic emergency guidance)

STEP 3 — Create static prompt bank (new file)
Create: client/src/content/prompts/staticPrompts.ts
Export short variant strings:
- refineNoticeVariants[]
- encouragementVariants[]
- nextStepVariants[]
- ctaVariants[]
All legally safe, calm brand tone.

STEP 4 — Create deterministic dynamic context (new file)
Create: client/src/content/context/buildPageContext.ts
Export:
buildPageContext(routeKey: string) => {
  title: string,
  description: string,
  category?: string,
  benefitKeys: string[],
  relatedRoutes: string[],
  noIndex?: boolean
}
Must be deterministic.

STEP 5 — Create BenefitsBank (new file)
Create: client/src/content/benefits/benefitsBank.ts
Export:
- getBenefitsForRoute(routeKey: string): string[]
- getBenefitsForCategory(category: string): string[]
Must return stable, varied, short bullets (no medical promises).
Pages must use .slice(0,3) deterministically.

STEP 6 — Create shared backbone components (new files)
Create:
A) client/src/components/seo/SEO.tsx
   - document.title
   - meta description
   - og:title/og:description
   - robots noindex when requested

B) client/src/components/safety/SafetyFooter.tsx
   - imports platformPolicy
   - renders: 18+ notice + not therapy + not medical advice + crisis text + legal links

C) client/src/components/marketing/BenefitsBlock.tsx
   - renders “How this helps” + bullets

D) client/src/components/layout/PageScaffold.tsx
   Props:
   - routeKey (string)
   - title?, description?, benefitsBullets?, noIndex?
   Behavior:
   - uses buildPageContext(routeKey) when title/desc not provided
   - renders SEO + header + BenefitsBlock + children + SafetyFooter

STEP 7 — Patch ONLY flagged pages
For each flagged file in missing-seo/missing-safety/missing-benefits:
- Wrap return UI in <PageScaffold routeKey="..." ...>
- Ensure benefitsBullets uses getBenefitsForRoute(routeKey).slice(0,3)
- Do NOT duplicate disclaimers in the page body.
- Keep existing page content; minimal edits only.

STEP 8 — Fix contains-disallowed ONLY in flagged files
For each flagged file in contains-disallowed:
- Replace placeholder/TODO/return null with:
  - PageScaffold + refineNotice (from staticPrompts) + one clear next action + navigation link
- Remove unsafe claim phrases; replace with safe wording (skills practice, reflection, habit building).

STEP 9 — Add Skills Engine (new files)
Create:
A) client/src/skills/skillRegistry.ts
   - defines Skill type
   - exports getSkillPack(packId)

B) client/src/skills/packs/mi.ts
   - OARS prompts
   - importance/confidence rulers (0–10)
   - change talk prompts
   Educational only.

C) client/src/skills/packs/twelvePractices.ts
   - 12 Practices (mind–body–soul transformation)
   Each includes: reflection + micro-action + safety note.

D) client/src/skills/packs/nlpReframe.ts
   - safe “reframing language templates”
   - no manipulation language, no guarantees.

E) client/src/skills/renderSkill.tsx
   - renders a pack consistently (beginner/intermediate/advanced)

IMPORTANT:
Do not inject skills into every page yet unless that page is flagged.
Only create the reusable system now.

STEP 10 — Viral + monetization hooks (new file; non-invasive)
Create: client/src/components/share/ShareCardPrompt.tsx
- “Save this tool”
- “Share a 2-minute win”
No external APIs.

STEP 11 — Subagent specs (new text files only)
Create folder agents/
Create:
- agents/layout-agent.md
- agents/seo-agent.md
- agents/safety-agent.md
- agents/content-agent.md
- agents/growth-agent.md
Each must define:
- mission
- allowed files
- forbidden edits
- checklist
- deterministic output format

STEP 12 — Executables (human-triggered scripts; new)
Create:
A) scripts/plan-patches.mjs
   - reads scripts/.scan-results.json
   - outputs a patch plan to console (does NOT edit files)

B) scripts/verify-platform.mjs
   - runs scan + build
   - prints summary

STEP 13 — MCP servers integration (safe)
IF repo already contains MCP config:
- integrate tool gateway interface in existing pattern
ELSE:
- create agents/mcp-tools-spec.md describing:
  - proposed tool registry (scan, verify, content generation)
  - deterministic rules
  - no background autonomy
Do NOT add dependencies.

VERIFY:
Run:
- npm run scan:platform
- npm run build
Fix ONLY issues caused by new imports/paths/types in touched files. No refactors.

REQUIRED OUTPUT:
1) List of changed files (paths only)
2) Patched counts by category
3) New shared modules created
4) New agent specs created
5) Scan summary after patch
6) Build success confirmation