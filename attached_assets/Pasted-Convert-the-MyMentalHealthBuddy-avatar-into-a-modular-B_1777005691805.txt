Convert the MyMentalHealthBuddy avatar into a modular Buddy Engine, built beside the current working platform.

Rules:
- Do not delete existing files.
- Do not replace /start.
- Do not modify /api/ai/chat.
- Preserve crisis behavior, telemetry, memory, profile, summary, provider, orchestrator.
- Healing domain only. No sales, no diagnosis, no therapy replacement claims.

Create:

1. client/src/components/avatar/BuddyAvatar.tsx
Pure visual component.
Props:
- state
- size optional

Render:
- white rounded robot body
- black face screen
- soft green eyes
- glowing heart
- circular base
- gentle animations
- state classes:
  calm, sad, anxious, overwhelmed, encouraged, crisis, celebrate

No fetch. No AI. No business logic.

2. client/src/lib/avatarState.ts
Export:
- BuddyState type
- resolveBuddyState(input)
- getBuddyVisualOutput(state)

BuddyOutput:
{
  eyeColor,
  heartColor,
  heartPulse,
  motion
}

3. client/src/hooks/useBuddy.ts
- manages buddy state
- calls POST /api/buddy
- stores messages
- resets state to calm after 60 seconds idle
- never touches /api/ai/chat

4. server/routes/buddy.mjs
Route:
POST /api/buddy

Behavior:
- validate message
- resolve emotional state
- crisis short-circuit server-side
- return:
  { ok:true, text, state }

Crisis response must include:
988 Suicide & Crisis Lifeline by calling or texting 988.

Rules:
- no diagnosis
- no monetization
- no business logic
- 2–3 sentences max
- validate + ground + one small step

5. Mount route safely in server/app.mjs:
app.use("/api", buddyRoutes)

6. Add docs:
docs/architecture/BUDDY_ENGINE.md

Include:
- ownership model
- healing rules
- state model
- future physical avatar compatibility
- verification commands

Verification:
curl http://0.0.0.0:5000/health

curl -X POST http://0.0.0.0:5000/api/buddy \
-H "Content-Type: application/json" \
-d '{"message":"I feel anxious"}'

Expected:
{ ok:true, state:"anxious", text:"..." }

Do not connect Buddy to /start yet. Only build and verify the isolated Buddy Engine.