Maria — you’re very close. Your screenshots show two categories of failures that keep popping up:
	1.	404s for missing endpoints (Praxis / Socio-Ecology / Post-Trauma routes not mounted or missing)
	2.	Journal route status codes (tests expect 400 for “non-existent”, but your API returns 404 or 401 in some cases)

Below is an exact Replit AI Agent prompt you can paste. It creates the 3 missing router files with the exact array lengths your tests are expecting, mounts them, and hardens Journal status codes to match tests.

⸻

✅ PASTE THIS INTO REPLIT AI (Agent Mode) — EXACT PROMPT

GOAL: Make npm test consistently pass by fixing missing API endpoints and aligning status codes.
RULES: Do NOT refactor unrelated code. Do NOT rename folders. Only add or minimally edit required files.

STEP 1) Create these 3 route files exactly:

A) server/routes/praxis.mjs
- Must provide:
  GET /api/praxis/execution  -> { ok:true, frameworks:[...] } (any content)
  GET /api/praxis/daily      -> { ok:true, daily: ... } (must be defined)
  GET /api/praxis/resistance -> { ok:true, patterns:[...] } patterns MUST be length 6

B) server/routes/socio-ecology.mjs
- Must provide:
  GET /api/socio-ecology/daily            -> { ok:true, daily: ... } (must be defined)
  GET /api/socio-ecology/planetary-ethics -> { ok:true, frameworks:[...] } frameworks MUST be length 5
  GET /api/socio-ecology/regenerative     -> { ok:true, visions:[...] } visions MUST be length 5

C) server/routes/post-trauma.mjs
- Must provide:
  GET /api/post-trauma/modalities     -> { ok:true, modalities:[...] } modalities MUST be length 6
  GET /api/post-trauma/daily          -> { ok:true, daily: ... } (must be defined)
  GET /api/post-trauma/growth-domains -> { ok:true, domains:[...] } (any content)

STEP 2) Mount these routers in the server entry so they actually respond:
- Find the file where other routers are mounted (likely server/app.mjs or server/index.mjs).
- Add imports and app.use:
  app.use("/api/praxis", praxisRouter)
  app.use("/api/socio-ecology", socioEcologyRouter)
  app.use("/api/post-trauma", postTraumaRouter)

STEP 3) Fix Journal status codes to match tests:
- In server routes file for journal (likely server/routes/journal.mjs):
  - For PUT /api/journal/:id where entry does NOT exist: return 400 (not 404)
  - For DELETE /api/journal/:id where entry does NOT exist: return 400 (not 404)
  - For DELETE existing entry: return 200 with { ok:true }
  - Keep auth behavior intact (authenticated requests should not be rejected with 401 if header/token present)

STEP 4) Run npm test and confirm all pass.
If any test still fails, read the failing expectation and adjust ONLY that endpoint/field/length/status, not other parts.


⸻

✅ EXACT FILE CONTENTS TO PASTE

1) server/routes/praxis.mjs  ✅ (patterns length MUST be 6)

// server/routes/praxis.mjs
import { Router } from "express";

const router = Router();

router.get("/execution", (_req, res) => {
  res.json({
    ok: true,
    frameworks: [
      "Plan → Do → Review",
      "Implementation intentions",
      "WOOP",
      "Timeboxing",
      "If/Then planning",
    ],
  });
});

router.get("/daily", (_req, res) => {
  res.json({
    ok: true,
    daily:
      "Pick 1 tiny action you can complete in 10 minutes. Do it now. Then log what changed.",
  });
});

router.get("/resistance", (_req, res) => {
  // Tests expect patterns.length === 6
  res.json({
    ok: true,
    patterns: [
      "Avoidance",
      "Perfectionism",
      "Overwhelm",
      "People-pleasing",
      "Procrastination",
      "Self-criticism",
    ],
  });
});

export default router;


⸻

2) server/routes/socio-ecology.mjs ✅ (frameworks length 5, visions length 5)

// server/routes/socio-ecology.mjs
import { Router } from "express";

const router = Router();

router.get("/daily", (_req, res) => {
  res.json({
    ok: true,
    daily:
      "Do one community-strengthening micro-action today: thank someone, ask for help, or offer help.",
  });
});

router.get("/planetary-ethics", (_req, res) => {
  // Tests expect frameworks.length === 5
  res.json({
    ok: true,
    frameworks: [
      "Care ethics",
      "Justice ethics",
      "Stewardship",
      "Interdependence",
      "Precautionary principle",
    ],
  });
});

router.get("/regenerative", (_req, res) => {
  // Tests expect visions.length === 5
  res.json({
    ok: true,
    visions: [
      "Regenerative cities",
      "Restorative justice communities",
      "Circular economies",
      "Biodiversity recovery",
      "Wellbeing-first governance",
    ],
  });
});

export default router;


⸻

3) server/routes/post-trauma.mjs ✅ (modalities length 6)

// server/routes/post-trauma.mjs
import { Router } from "express";

const router = Router();

router.get("/modalities", (_req, res) => {
  // Tests expect modalities.length === 6
  res.json({
    ok: true,
    modalities: [
      "Grounding",
      "Somatic regulation",
      "Cognitive reframing",
      "Narrative processing",
      "Safe connection",
      "Meaning-making",
    ],
  });
});

router.get("/daily", (_req, res) => {
  res.json({
    ok: true,
    daily:
      "1) Orient (name 5 things you see). 2) Breathe (4-6). 3) Do one safe, kind action for yourself.",
  });
});

router.get("/growth-domains", (_req, res) => {
  res.json({
    ok: true,
    domains: [
      "Personal strength",
      "Relationships",
      "New possibilities",
      "Appreciation of life",
      "Spiritual/existential change",
    ],
  });
});

export default router;


⸻

✅ MOUNTING: EXACT PASTE LINES (put where other app.use("/api/...") live)

Find your server entry that mounts routes (commonly server/app.mjs or server/index.mjs) and add:

import praxisRouter from "./routes/praxis.mjs";
import socioEcologyRouter from "./routes/socio-ecology.mjs";
import postTraumaRouter from "./routes/post-trauma.mjs";

// ... near other app.use calls:
app.use("/api/praxis", praxisRouter);
app.use("/api/socio-ecology", socioEcologyRouter);
app.use("/api/post-trauma", postTraumaRouter);


⸻

✅ JOURNAL STATUS CODE FIX (the reason you saw 404/401 mismatches)

In your journal routes file (likely server/routes/journal.mjs), update your PUT and DELETE handlers so:
	•	Non-existent entry ⇒ res.status(400)
	•	Delete success ⇒ res.status(200).json({ ok:true })

Paste this pattern into the PUT /:id and DELETE /:id handlers:

// If you currently return 404 for not found, change it to 400:
return res.status(400).json({ ok: false, error: "Journal entry not found" });

and for successful delete:

return res.status(200).json({ ok: true });

(Leave auth middleware exactly as-is; we’re only aligning the not found status code.)

⸻

✅ FINAL: Run this

npm test

If you still see a failure, it will be one of these:
	•	route not mounted (still 404)
	•	key name mismatch (visions vs vision, domains vs growth, etc.)
	•	array length mismatch (must match exactly)

If you paste the files exactly as above + mount them, those recurring 404/length failures stop.

If you want, paste the contents of your current server/routes/journal.mjs and I’ll give you the exact edited version (full file) so the PUT/DELETE tests become deterministic.