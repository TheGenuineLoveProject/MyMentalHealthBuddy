“Lock to generator pattern” using a micro detection (flattened vs nested)

This makes the mapping match your generator even if you later switch styles.

TASK: Improve content/routeFileMap.js to auto-detect whether generated pages are flattened filenames or nested folders, and then map routes accordingly.

REQUIREMENTS:
	1.	Implement detectGeneratedLayout() that checks for a small set of sentinel candidates (existsSync):
	•	client/src/pages/generated/account-profile.jsx (flattened)
	•	client/src/pages/generated/account/profile.jsx (nested)
	•	client/src/pages/generated/blog-[param].jsx OR client/src/pages/generated/blog/[slug].jsx
	•	client/src/pages/generated/community-discussion-[param].jsx OR client/src/pages/generated/community/discussion/[id].jsx
	2.	Choose one layout strategy deterministically:
	•	Prefer nested if nested sentinel exists
	•	Else prefer flattened if flattened sentinel exists
	•	Else default to flattened (since generator screenshot shows flattened patterns like blog-[param].jsx)
	3.	Export export const GENERATED_LAYOUT = "nested"|"flat"; after detection.
	4.	Ensure routeToGeneratedFile(route) uses GENERATED_LAYOUT and stays pure/deterministic after initial detection.

DONE GATE:
	•	On first run it chooses the correct layout for this repo
	•	Mapping for /blog/:slug, /community/discussion/:id, /404, /* matches real files if present

OUTPUT: full updated content/routeFileMap.js.

⸻

Prompt 3 — Tiny override table that matches your exact screenshot (best starting default)

If your generator already outputs these (your screenshot shows blog-[param].jsx and community-discussion-[param].jsx):

TASK: Implement the smallest possible deterministic override map matching the current generator output.

DO THIS EXACTLY:
	•	Generated root: client/src/pages/generated/
	•	Use flat filenames (hyphen-joined)
	•	Use [param] placeholder files exactly:
	•	/blog/:slug → blog-[param].jsx
	•	/community/discussion/:id → community-discussion-[param].jsx
	•	Edge:
	•	/404 → 404.jsx
	•	/* → 404.jsx
	•	Home:
	•	/ → index.jsx
	•	/home → home.jsx
	•	/welcome → welcome.jsx

IMPLEMENT: Create content/routeFileMap.js with:
	•	ROUTE_FILE_OVERRIDES containing the exact entries above
	•	routeToGeneratedFile(route) that:
	•	returns overrides when present
	•	otherwise strips leading /, replaces / with -, removes empty, appends .jsx

THEN: Update scripts/print-routes-diff.mjs to use this mapping first for file links.

OUTPUT: full contents of both files.

⸻

One-line “force the best alias behavior” add-on (optional)

If you want aliases to always deep-link to canonical file when both exist:

Add rule: “If route has aliasOf, link to canonical route’s generated file when it exists; otherwise link to the alias path.”
