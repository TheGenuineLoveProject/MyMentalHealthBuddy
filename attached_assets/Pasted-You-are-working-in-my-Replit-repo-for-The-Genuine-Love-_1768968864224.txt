You are working in my Replit repo for “The Genuine Love Project”. Goal: finish a complete A→Z platform polish pass with a strict brand system (teal + gold), enforce design-token discipline, and guarantee navigation links match the real route list. Do NOT rename my existing CSS variable names (keep my current `--glp-*` and any already in use). Prefer minimal diffs; no breaking refactors.

PRIMARY SOURCE OF TRUTH (read these first):
- Router: `client/src/App.jsx` (wouter Switch/Route + RouteGuard)
- Design tokens: `client/src/styles/brand-tokens.css` (and any existing brand css: `client/src/styles/brand.css`)
- Tailwind: `tailwind.config.js`
- Global CSS entry: `client/src/index.css` (and/or `client/src/main.css` if used)
- Existing audit tools: `tools/nav-link-audit.mjs`, `tools/visual-doctor.mjs`
- Existing reports: `docs/NAV_LINK_AUDIT.md`, `docs/VISUAL_DOCTOR_REPORT.md`
- Existing workflows: `.github/workflows/nav-audit.yml`, `.github/workflows/visual-doctor.yml` (and/or `audit.yml`)

WHAT TO COMPLETE (A→Z CHECKLIST)
A) Brand Tokens + Modes (Default / Low-Stim / Reading)
1) Ensure `client/src/styles/brand-tokens.css` contains:
   - Core brand color tokens (teal, gold, sage, blush, paper/ivory, ink/charcoal) using my current `--glp-*` naming.
   - Semantic tokens mapped to those colors: background/surface/text/border/primary/accent/ring etc. Use CSS variables only (no raw hex outside brand-tokens.css).
2) Implement 3 visual modes controlled by attribute on `<html>`:
   - Default: normal contrast, teal primary, gold accent/ring.
   - Low-Stim: reduced saturation + reduced decorative shadows; keep readability.
   - Reading: max legibility (clean white surfaces, stronger text contrast, minimal gradients).
   Use `html[data-mode="low-stim"] { ... }` and `html[data-mode="reading"] { ... }`.

B) Tailwind Integration (JS only)
1) Update `tailwind.config.js` (JS) so Tailwind colors map to my existing CSS variables (NOT new names).
2) Provide semantic color keys I can use everywhere:
   - `bg`, `surface`, `text`, `muted`, `border`, `primary`, `accent`, `ring`
   - also keep explicit brand keys: `sage`, `blossom`, `teal`, `gold`, `ivory`, `charcoal`
3) Ensure Tailwind `content` globs include all relevant paths (client/src/**/*.{js,jsx,ts,tsx} etc).
4) Add sensible radii and shadows using CSS vars (and disable/soften for low-stim mode via CSS vars).

C) Router + Navigation Link Perfection (must match real routes)
1) Parse the actual route list in `client/src/App.jsx` and treat it as canonical.
2) Find all navigation UIs (header, sidebar, footer, buttons) and:
   - Ensure internal links exactly match existing route paths (case, hyphenation, leading slash).
   - Remove or fix any links that point to routes that do not exist.
   - If a link must exist but route is missing, create a placeholder page with consistent design and add the route (only if clearly intended).
3) Ensure NotFound route exists and looks on-brand (calm, helpful CTA back home).

D) Automated Audits (must pass)
1) NAV LINK AUDIT:
   - Ensure `tools/nav-link-audit.mjs` scans:
     a) canonical routes from `client/src/App.jsx`
     b) all internal links in JSX/TSX (href/to/path props)
   - Output markdown report to `docs/NAV_LINK_AUDIT.md`
   - Exit non-zero if broken links exist (CI fail).
2) VISUAL DOCTOR:
   - Ensure `tools/visual-doctor.mjs` fails CI if any raw hex colors are used outside `client/src/styles/brand-tokens.css`.
   - ALSO enforce (new):
     - no inline style color values that include hex/rgb/hsl unless using CSS vars
     - allowlist only these font families: Inter, Playfair Display (and system fallbacks)
     - fail if any NEW font-family appears outside allowlist
   - Output markdown report to `docs/VISUAL_DOCTOR_REPORT.md`
   - Exit non-zero on violations.
3) Package scripts:
   - Ensure `package.json` has scripts:
     - `"nav:audit": "node tools/nav-link-audit.mjs"`
     - `"visual:doctor": "node tools/visual-doctor.mjs"`
     - `"audit": "npm run nav:audit && npm run visual:doctor"`
4) GitHub Actions workflows:
   - Ensure workflows run on PR + push and run `npm ci` then `npm run audit`.
   - Keep separate workflows if already present; otherwise create one unified `audit.yml`.

E) Visual Refinement Pass (teal + gold power pair)
1) Replace any stray colors with tokens + Tailwind semantic classes.
2) Ensure consistent:
   - Button styles (primary teal, focus ring gold, hover subtle)
   - Link styles (teal text, underline on hover, visible focus)
   - Cards (soft surfaces, minimal shadow, roomy spacing)
   - Icons (thin outline feel; consistent size; avoid random fills)
3) Typography:
   - Headings: Playfair Display
   - Body/UI: Inter
   - No new typefaces.
   - Normalize hierarchy (H1/H2/H3 + body/label/caption), and ensure good line-height.
4) Accessibility:
   - Ensure text contrast is acceptable (avoid gold body text on ivory).
   - Ensure focus-visible rings are consistent and visible.

F) Documentation: “Platform Complete” kit
Create/Update these docs:
1) `docs/COLOR_SYSTEM.md` — tokens, semantic mapping, do/don’t, mode behavior
2) `docs/TYPOGRAPHY_GUIDE.md` — scale + usage + spacing
3) `docs/CANVA_TEMPLATE_SPECS.md` — exact Canva specs:
   - 1080×1080 and 1080×1920
   - safe margins, grid, spacing system
   - exact type scale (Playfair + Inter sizes) and rules for quote cards, prompt cards, carousel slides
   - color usage rules (teal/gold emphasis, ivory backgrounds)
4) Update `README.md` with “How to run audits” and “How to switch modes” (data-mode on html).

ACCEPTANCE CRITERIA (must verify, not assume)
1) `npm run audit` passes locally (in Replit shell).
2) `docs/NAV_LINK_AUDIT.md` is updated and shows 0 broken internal links.
3) `docs/VISUAL_DOCTOR_REPORT.md` is updated and shows 0 violations.
4) No raw hex colors exist outside `client/src/styles/brand-tokens.css`.
5) No fonts beyond Inter + Playfair Display (plus system fallbacks) are used.
6) Navigation and footer links exactly match routes defined in `client/src/App.jsx`.

FINAL OUTPUT REQUIRED
When done, respond with:
- a brief checklist confirming each section A–F is complete
- the exact commands you ran and their results
- a list of files changed with 1-line summary each
- any remaining optional improvements (clearly marked “optional”)

Proceed now.