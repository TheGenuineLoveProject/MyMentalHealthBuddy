✅ TL;DR: Here’s your complete package:
	1.	LandingPage.tsx with HeroSection, AboutSection, JoinSection, SacredFooter, and SacredPattern
	2.	JoinSection.tsx — CTA form with divine glow
	3.	A full Webflow-ready HTML + CSS version for export/deployment

Everything is sacred, animated, and branded to the 100 billionth power.

⸻

✅ 1. LandingPage.tsx — Complete React Composition

// LandingPage.tsx
import React from 'react';
import HeroSection from './HeroSection';
import AboutSection from './AboutSection';
import JoinSection from './JoinSection';
import SacredFooter from './SacredFooter';
import SacredPattern from './SacredPattern';

const LandingPage = () => (
  <>
    <SacredPattern />
    <HeroSection />
    <AboutSection />
    <JoinSection />
    <SacredFooter />
  </>
);

export default LandingPage;


⸻

✅ 2. JoinSection.tsx — CTA Form with Divine Glow

// JoinSection.tsx
import React from 'react';
import styled, { keyframes } from 'styled-components';
import { brandColors, brandFonts } from './theme';

const glow = keyframes`
  0%, 100% {
    box-shadow: 0 0 20px rgba(244, 199, 195, 0.3);
  }
  50% {
    box-shadow: 0 0 40px rgba(244, 199, 195, 0.5);
  }
`;

const Section = styled.section`
  background: ${brandColors.softWhite};
  padding: 6rem 2rem;
  text-align: center;
  font-family: ${brandFonts.primary};
`;

const Title = styled.h2`
  font-family: ${brandFonts.display};
  font-size: 2.5rem;
  margin-bottom: 1rem;
  color: ${brandColors.charcoal};
`;

const Subtitle = styled.p`
  font-size: 1.1rem;
  margin-bottom: 2rem;
  color: ${brandColors.deepTeal};
`;

const Form = styled.form`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
  max-width: 400px;
  margin: 0 auto;
`;

const Input = styled.input`
  width: 100%;
  padding: 0.75rem 1rem;
  border: 2px solid ${brandColors.dustyRose};
  border-radius: 8px;
  font-size: 1rem;
`;

const Button = styled.button`
  padding: 0.75rem 2rem;
  background: ${brandColors.dustyRose};
  color: ${brandColors.charcoal};
  border: none;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  animation: ${glow} 4s ease-in-out infinite;

  &:hover {
    background: ${brandColors.sageGreen};
    color: ${brandColors.softWhite};
  }
`;

const JoinSection = () => (
  <Section>
    <Title>Join the Movement</Title>
    <Subtitle>Sign up to receive sacred updates, tools, and love from the heart of the project.</Subtitle>
    <Form>
      <Input type="email" placeholder="Enter your email" required />
      <Button type="submit">Join Now</Button>
    </Form>
  </Section>
);

export default JoinSection;


⸻

✅ 3. 🌐 Full Webflow / HTML Exportable Version

<!-- index.html -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>The Genuine Love Project</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins&family=Cormorant+Garamond:wght@600&display=swap" rel="stylesheet" />
  <style>
    :root {
      --sage-green: #8fbf9f;
      --dusty-rose: #f4c7c3;
      --deep-teal: #2f5d5d;
      --soft-white: #faf9f7;
      --charcoal: #3a3a3a;
      --golden-yellow: #eac33b;
    }

    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, var(--sage-green), var(--dusty-rose), var(--golden-yellow));
      overflow-x: hidden;
    }

    section {
      padding: 4rem 2rem;
      text-align: center;
    }

    h1, h2 {
      font-family: 'Cormorant Garamond', serif;
      color: var(--charcoal);
    }

    p {
      color: var(--deep-teal);
    }

    .cta-form input {
      padding: 0.75rem;
      width: 100%;
      max-width: 300px;
      border: 2px solid var(--dusty-rose);
      border-radius: 8px;
      margin-bottom: 1rem;
    }

    .cta-form button {
      background: var(--dusty-rose);
      border: none;
      padding: 0.75rem 2rem;
      font-weight: bold;
      border-radius: 8px;
      cursor: pointer;
    }

    footer {
      background: var(--charcoal);
      color: var(--soft-white);
      padding: 2rem 1rem;
      position: relative;
    }

    .glow-bar {
      height: 5px;
      width: 100%;
      background: var(--golden-yellow);
      box-shadow: 0 0 30px var(--golden-yellow);
      margin-bottom: 1rem;
    }

    .sacred-bg {
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      z-index: -1;
      pointer-events: none;
      overflow: hidden;
    }

    .sacred-bg svg {
      position: absolute;
      top: 50%; left: 50%;
      width: 80vmin;
      height: 80vmin;
      transform: translate(-50%, -50%);
      animation: pulse 6s ease-in-out infinite;
      fill: none;
      stroke: rgba(255,255,255,0.08);
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1) translate(-50%, -50%); opacity: 0.15; }
      50% { transform: scale(1.05) translate(-50%, -50%); opacity: 0.25; }
    }
  </style>
</head>
<body>

  <div class="sacred-bg">
    <svg viewBox="0 0 100 100">
      <circle cx="50" cy="50" r="30"/>
      <circle cx="50" cy="50" r="20"/>
      <circle cx="50" cy="50" r="10"/>
      <polygon points="50,15 85,85 15,85"/>
    </svg>
  </div>

  <section>
    <h1>The Genuine Love Project</h1>
    <p>Healing hearts. Awakening minds. Through sacred visuals + gentle code.</p>
  </section>

  <section>
    <h2>About Us</h2>
    <p>We design healing-centered interfaces that invite calm, wholeness, and truth into the web experience.</p>
  </section>

  <section class="cta-form">
    <h2>Join the Movement</h2>
    <p>Receive sacred updates and design downloads.</p>
    <input type="email" placeholder="Enter your email" required />
    <br />
    <button>Join Now</button>
  </section>

  <footer>
    <div class="glow-bar"></div>
    <p>© 2026 The Genuine Love Project. All rights reserved.</p>
  </footer>
</body>
</html>


⸻

✅ You now have:

Component	Purpose
LandingPage.tsx	Full React page composed of all sections
JoinSection.tsx	Glowing form with CTA
SacredPattern.tsx	Background animated sacred SVG
index.html	Webflow-compatible exportable version


⸻

🔁 Next Options:

a. Want me to add TestimonialsSection.tsx with soft fade scroll effects?
b. Add multi-step onboarding flow or signup animation?

Let’s keep radiating healing. What’s next?