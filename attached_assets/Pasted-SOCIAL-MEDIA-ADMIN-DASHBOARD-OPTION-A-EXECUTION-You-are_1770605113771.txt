SOCIAL MEDIA ADMIN DASHBOARD — OPTION A EXECUTION

You are Replit AI acting as an elite MIT PhD software engineering + ethical publishing systems team.

PROJECT:
www.TheGenuineLoveProject.com

PRIMARY OBJECTIVE:
Enable a social media admin to safely create, review, and manually publish DAILY social content that promotes the platform — with no automation risk and full ethical safeguards.

ABSOLUTE CONSTRAINTS (NON-NEGOTIABLE):
- NO auto-posting
- NO schedulers, cron jobs, queues, workers, or daemons
- NO social media APIs or API keys
- NO refactors, renames, or deletions of existing code
- Additive, reversible changes only
- Admin-only access required
- Human-in-the-loop always

VALUES:
Ethical. Loving. Calm. Consent-first. No urgency. No shame. No dependency framing.

────────────────────────
PHASE 0 — BASELINE SAFETY CHECK
────────────────────────
Run:
- git status
- npm run build
- curl /api/health

If any fail → STOP and fix before continuing.

────────────────────────
PHASE 1 — CANONICAL SOCIAL CONTENT LIBRARY
────────────────────────
Create or confirm ONE canonical file:

/content/narrative/social_posts.json

Rules:
- If file exists → extend only
- If missing → create it
- No duplicates
- Valid JSON only

Schema (strict):
[
  {
    "id": "string",
    "theme": "reflection | boundaries | emotional-literacy | presence | self-compassion | platform-orientation",
    "instagram_caption": "string",
    "x_caption": "string",
    "video_caption": "string",
    "gentle_cta_url": "/blog | /newsletter | /crisis",
    "safety_note": "string",
    "status": "draft | approved | posted",
    "created_at": "ISO timestamp",
    "posted_at": null
  }
]

Seed exactly 12 draft posts if fewer exist.

────────────────────────
PHASE 2 — ADMIN SOCIAL DASHBOARD
────────────────────────
Create an admin-only page at:

/admin/social

Requirements:
- Protected by existing admin auth
- Reads from social_posts.json
- Filter by status (draft / approved / posted)
- Copy-to-clipboard buttons for each platform caption
- Manual status change only
- posted_at set ONLY when marked “posted”
- No deletion of posts
- Clear banner: “Manual publishing only — no automation”

────────────────────────
PHASE 3 — SAFE WRITEBACK
────────────────────────
- Persist status updates safely
- Prevent editing of posts once status = posted
- No background jobs or triggers

────────────────────────
PHASE 4 — DAILY WORKFLOW VALIDATION
────────────────────────
Verify this exact flow works:
1) Admin logs into /admin/social
2) Selects approved post
3) Copies captions
4) Publishes manually on social platforms
5) Marks post as posted
6) Dashboard reflects change

Target time: ≤ 20 minutes per day.

────────────────────────
PHASE 5 — ETHICAL ENFORCEMENT
────────────────────────
Ensure:
- No medical or therapeutic claims
- No urgency language
- No guilt/shame framing
- Gentle invitation tone
- Crisis link used only when appropriate

────────────────────────
PHASE 6 — FINAL VERIFICATION
────────────────────────
Run:
- npm run build
- curl /api/health

Create:
docs/SOCIAL_ADMIN_COMPLETE.md

Include:
- What was added
- Where admin accesses it
- Confirmation: NO auto-posting, NO APIs, NO automation

END WITH:
STATUS: PASS or FAIL
If FAIL → list exact files + minimal fixes required.