Upgrade MMHB Buddy Engine to v1.5: tool-specific avatar expressions only.

STRICT RULES:
- Do not modify /api/ai/chat
- Do not modify server/routes/buddy.mjs
- Do not modify orchestrator, provider, memory, profile, summary, telemetry, crisis logic
- Do not change tool execution logic
- Do not replace /start
- Do not delete files
- Only improve BuddyAvatar visual state mapping on /start

TARGET FILE:
client/src/pages/Start.tsx

OPTIONAL TARGET:
client/src/components/avatar/BuddyAvatar.tsx only if visual classes need support.

GOAL:
BuddyAvatar should visually match the active tool:
- box_breathing → breathing visual
- thought_reframe → focused/encouraged visual
- emotional_checkin → soft/sad visual
- overload_reset → grounded/overwhelmed visual
- relationship_repair → warm/encouraged visual
- pattern_interrupt → focused/encouraged visual

PHASE 1 — ADD TOOL-TO-BUDDY MAPPER

In Start.tsx, add helper:

function mapToolToBuddyState(toolId = "", modules = []) {
  if (toolId === "box_breathing") return "anxious";
  if (toolId === "grounding_54321") return "overwhelmed";
  if (toolId === "thought_reframe") return "encouraged";
  if (toolId === "emotional_checkin") return "sad";
  if (toolId === "overload_reset") return "overwhelmed";
  if (toolId === "relationship_repair") return "encouraged";
  if (toolId === "pattern_interrupt") return "encouraged";

  if (modules.includes("anxiety")) return "anxious";
  if (modules.includes("emotional_processing")) return "sad";
  if (modules.includes("cognitive_reframe")) return "encouraged";
  if (modules.includes("self_regulation")) return "overwhelmed";

  return "calm";
}

PHASE 2 — APPLY AFTER AI RESPONSE

When /api/ai/chat returns:

const nextBuddyState = mapToolToBuddyState(
  response?.tool?.tool?.id || response?.tool?.id || "",
  response?.modules || []
);

setBuddyState(nextBuddyState);

PHASE 3 — ADD TOOL HELPER COPY

Near BuddyAvatar, show text based on active tool:

const buddyToolCopy = {
  box_breathing: "Follow Buddy’s breath.",
  grounding_54321: "Let Buddy help you ground.",
  thought_reframe: "Buddy is helping you think clearly.",
  emotional_checkin: "Buddy is staying gentle with you.",
  overload_reset: "Buddy is helping you make this smaller.",
  relationship_repair: "Buddy is helping you prepare with care.",
  pattern_interrupt: "Buddy is helping you break the loop."
};

Render only when toolId exists.

PHASE 4 — KEEP STATE RECOVERY

Preserve v1.4 behavior:
- non-crisis states recover to calm
- crisis state never auto-transitions
- reduced motion respected
- no flashing

PHASE 5 — ADD TELEMETRY EVENT

Add additive-only event:
buddy_tool_expression

Metadata:
{
  toolId,
  buddyState
}

Do not log message text.

PHASE 6 — VERIFY

Run:
npm run dev

Manual tests:
1. Calm Me Down
Expected:
- toolId box_breathing
- Buddy state anxious/breathing
- helper says Follow Buddy’s breath.

2. Help Me Think Clearly
Expected:
- toolId thought_reframe
- Buddy state encouraged/focused

3. Understand This Feeling
Expected:
- toolId emotional_checkin
- Buddy state sad/soft

4. Crisis content
Expected:
- crisis-safe Buddy behavior preserved
- no auto recovery out of crisis

Return:
- files changed
- mapping added
- verification results
- skipped changes