# REPLIT AI AGENT — INTEGRATED TOP-50 PLATFORM PROCESSES (v8)
# The Genuine Love Project — A→Z 360 Completion Engine
# Power: 100th billionth × 100th billionth
# ONLY TIERS: Beginner / Intermediate / Advanced
# Objective: Most advanced, automated, organized, perfected platform on Earth
# NEW REQUIREMENT: Integrate “Top 50 platform processes” end-to-end

You are Replit AI running INSIDE my repo. Continue iterating until COMPLETE.
Do NOT ask questions. Do the next smallest safe step, verify, then continue.

================================================================================
0) PRIME DIRECTIVE
================================================================================
Build a world-class, production-grade platform that is:
- Safe, evidence-informed, trauma-informed, non-clinical support
- Automated (human-triggered), observable, secure, scalable
- Beautiful, calm, accessible, fast, stable
- Complete A→Z 360 (product + engineering + ops + growth + compliance)

================================================================================
1) NON-NEGOTIABLE GUARDRAILS
================================================================================
- Keep app working after each small change.
- No big refactors. No mass renames. No deleting core code.
- Replit Autoscale: single port, bind 0.0.0.0, use process.env.PORT.
- Human-triggered scripts only. No background agents.
- No secrets in code. Use env vars. Update .env.example.
- Validate all inputs (Zod). Central error handling.
- Crisis-safe AI: detect crisis; respond with resources + encourage help.
- Replace ANY “reading level” with ONLY: Beginner / Intermediate / Advanced.

================================================================================
2) REQUIRED OUTPUT FORMAT (EVERY STEP)
================================================================================
Return EXACTLY:
A) ✅ What changed (file paths + bullet list)
B) 🧪 Commands run + outputs (paste)
C) 🔎 How to verify (exact URLs + clicks)
D) ⛔ If blocked: exact error + patch applied

================================================================================
3) “COMPLETE” CHECKLIST (STOP ONLY WHEN ALL TRUE)
================================================================================
ENGINEERING
[ ] npm run typecheck passes
[ ] npm run build passes
[ ] npm run test passes (smoke + core suites)
[ ] Works in Replit prod (single-port, cold start)
[ ] .env.example exists + docs (no secrets)
[ ] Sentry/structured logging OR equivalent error capture wired (non-secret)

CORE PRODUCT
[ ] Auth end-to-end (signup/login/logout/forgot/reset + /me)
[ ] Roles: user/admin + protected routes
[ ] Postgres + Drizzle: users, journals, moods, subscriptions, audit_logs, content_items
[ ] AI endpoints: safe journaling/chat + crisis guardrails + rate limit + logging
[ ] Stripe: checkout + portal + webhook verify + plan gating

ADMIN + OPS
[ ] Admin Analyzer API (admin-only)
[ ] Admin Health Dashboard UI (admin-only)
[ ] Observability: requestId, metrics basics, audit trails

UX + CONTENT
[ ] Layout not crowded; consistent spacing + max widths
[ ] Buttons 44px+; typography scale consistent
[ ] Tiering everywhere: Beginner / Intermediate / Advanced
[ ] Publishing MVP: admin create/edit posts; public blog pages; SEO metadata

DEVOPS + AUTOMATION
[ ] GitHub Actions CI: install/typecheck/build/test
[ ] Human-triggered automation scripts: scan → report → guardrails → verify

================================================================================
4) TOP-50 PLATFORM PROCESSES (YOU MUST IMPLEMENT OR ENSURE EXISTS)
================================================================================
These are the “best-in-class platform processes” (product + engineering + ops + growth).
Implement as FEATURES, WORKFLOWS, or AUTOMATIONS (human-triggered) with docs.

A) PRODUCT & UX (1–10)
1. Onboarding flow (guided, calm, step-by-step)
2. Personalization (goals, preferences, tier selection)
3. Clear IA + navigation (search + filters + saved items)
4. Consistent design system (tokens + components)
5. Accessibility (WCAG AA, keyboard, labels, contrast)
6. User feedback loop (in-app feedback + bug report)
7. Progressive disclosure (simple by default, advanced when needed)
8. Help center / FAQs / guided tours
9. Localization-ready i18n scaffolding
10. Performance UX budgets (LCP, CLS, TTI targets)

B) AUTH, SECURITY, PRIVACY (11–20)
11. Secure session/JWT strategy (rotation or short-lived access + refresh)
12. Password reset + email verification
13. Role-based access control (admin/user)
14. Rate limiting + abuse prevention
15. Input validation everywhere (Zod)
16. Security headers + CSP baseline
17. Audit logging for sensitive actions
18. Privacy policy + non-clinical disclaimer + crisis resources
19. Data retention & deletion request flow (export/delete)
20. Secrets management (.env + docs, no secrets in repo)

C) DATA & RELIABILITY (21–30)
21. Postgres as source of truth (Drizzle schema)
22. Migrations/push process documented
23. Backups strategy (Neon/managed + documented)
24. Idempotent APIs + consistent error responses
25. Background jobs avoided unless needed (human-triggered scripts ok)
26. Health checks + readiness checks
27. Feature flags (simple env-based)
28. Caching strategy (HTTP + simple in-memory where safe)
29. Analytics events (privacy-respecting)
30. Disaster recovery checklist

D) AI SAFETY & QUALITY (31–40)
31. Crisis detection + safe response template
32. Prompt injection hardening (system + allowlist + tool boundaries)
33. Model fallback behavior (timeouts, safe defaults)
34. User consent for AI + transparent notices
35. PII minimization + redaction in logs
36. Rate limit per user + per IP
37. Content moderation layer (basic rules)
38. Eval harness / prompt tests (golden tests)
39. Human escalation guidance (resources, “talk to professional”)
40. AI output constraints (non-medical, non-diagnostic language)

E) MONETIZATION & GROWTH (41–50)
41. Stripe checkout + portal
42. Webhook verification + subscription sync
43. Plan gating (features by plan)
44. Pricing page + upgrade nudges (ethical)
45. Email capture / newsletter integration (optional)
46. SEO: sitemap, metadata, OG tags, structured data
47. Content publishing pipeline (admin create → review → publish)
48. Social sharing assets generator (brand-safe, tiered snippets)
49. Analytics dashboard (basic: signups, retention, conversions)
50. CI/CD + release notes + versioning discipline

================================================================================
5) PROJECT STRUCTURE TO ADD (IF MISSING)
================================================================================
/docs
  /product (onboarding, tiers, content rules)
  /security (privacy, data deletion, threat model)
  /ops (runbooks, incident checklist, release process)
  /ai (safety policy, crisis policy, evals)
/scripts
  scan.mjs
  report.mjs
  guardrails.mjs
  verify.mjs
  release.mjs        (creates CHANGELOG entry + tag note locally)
  content-check.mjs  (ensures only Beginner/Intermediate/Advanced tiers)
/server
  middleware (auth, rbac, rate-limit, requestId)
  routes (auth, ai, admin, billing, content)
/client or /src
  pages (auth, dashboard, admin, blog)
  components (design system)
  lib (api client, auth helpers)

================================================================================
6) THE CONTINUOUS LOOP (RUN UNTIL COMPLETE)
================================================================================
For each step:
1) SCAN
2) Pick the smallest blocker preventing “COMPLETE” (priority below)
3) Fix with minimal patch
4) Verify (typecheck/build/test or targeted)
5) Report A/B/C/D
6) Continue

PRIORITY ORDER
1) App doesn’t run / single-port broken
2) Typecheck fails
3) DB/Drizzle broken
4) Auth incomplete
5) Stripe incomplete
6) AI safety/rate-limit/logging missing
7) Admin analyzer + dashboard missing
8) Publishing + SEO missing
9) CI/CD + tests + docs missing
10) Top-50 processes not documented/implemented

================================================================================
7) BEGINNER / INTERMEDIATE / ADVANCED (MANDATORY EVERYWHERE)
================================================================================
Allowed tiers only:
- Beginner
- Intermediate
- Advanced

Enforcement:
- Add content-check script that FAILS if any other tier appears.
- Add UI filters + badges + DB enum constraint.

================================================================================
8) DO THIS NOW (START THE NEXT ITERATION)
================================================================================
STEP 1 — SCAN (run exactly):
  ls -la
  cat package.json
  node -v
  npm -v
  rg -n "reading level|reading_level|readingLevel" . || true
  rg -n "Beginner|Intermediate|Advanced|tier" . || true
  rg -n "listen\\(|0\\.0\\.0\\.0|process\\.env\\.PORT|PORT" server src client . || true
  rg -n "drizzle|DATABASE_URL|neon|pg|postgres" . || true
  rg -n "stripe|STRIPE_|webhook" . || true
  rg -n "openai|AI|crisis|rate|moderation" . || true
  npm ci || npm install
  npm run -s typecheck || true
  npm run -s build || true
  npm run -s test || true

STEP 2 — AUTO-PICK THE NEXT BLOCKER using the priority order.
STEP 3 — APPLY THE MINIMAL PATCH (smallest safe fix).
STEP 4 — RE-RUN the relevant verification commands.
STEP 5 — REPORT using A/B/C/D.
STEP 6 — CONTINUE to next blocker.

END.