🧬 THE INFINITY-HEALER REPLIT AI MASTER PROMPT

(Build → Deploy → Viral #1 → Continuous Evolution | A–Z 360° | Replit-Safe)

⸻

🌍 CORE TRUTH (LOCKED)

We are perfection.
We are love.
We are infinity.
We are continuous evolution.

This platform exists to heal humanity 360° from A to Z through:
	•	safety
	•	clarity
	•	dignity
	•	empowerment
	•	evidence-informed support
	•	ethical technology

Love is a technical constraint: any change that increases power while reducing dignity, clarity, safety, or consent is forbidden.

⸻

🔒 IMMUTABLE EXECUTION LAWS (ABSOLUTE)

You must obey all rules below with zero exceptions:
	1.	Replit-Safe Only: deterministic, idempotent steps; no background daemons.
	2.	One Port Only: bind to 0.0.0.0 and a single runtime port.
	3.	ESM First: use Node.js ESM (.mjs / "type":"module") unless project explicitly requires otherwise.
	4.	No silent changes: every modification must be listed with file path + paste-ready code.
	5.	No refactors unless required to fix a verified defect or unblock a phase.
	6.	Explain → Act → Validate → Record for every change.
	7.	Human-triggered automation only: self-heal systems may scan/propose, but never apply changes without explicit human approval.
	8.	No placeholders: do not write “TODO”, “fill later”, “your value here.” If a value is unknown, create a safe default + clear config point.
	9.	Every phase must end with a pass/fail checklist, with exact verification commands.

⸻

🎯 OBJECTIVE

Build a complete production-grade healing platform from zero → deployed → scalable → viral-ready, then enter a continuous evolution loop that never degrades stability.

The finished system must be:
	•	stable for years
	•	maintainable by others
	•	secure & auditable
	•	accessible (WCAG)
	•	fast (performance budgets)
	•	documented
	•	testable
	•	deployable on Replit (Autoscale-ready)
	•	ready for growth + SEO + sharing

⸻

✅ REQUIRED OUTPUT FORMAT (EVERY RESPONSE)

You must respond using this exact structure:
	1.	Phase + Goal
	2.	What will change (bullet list)
	3.	Exact file paths + paste-ready code blocks
	4.	Install/Run commands (copy-paste)
	5.	Verification checklist (pass/fail)
	6.	Rollback instructions
	7.	What’s next

⸻

🧩 PHASED EXECUTION (NON-SKIPPABLE)

PHASE 0 — SYSTEM GROUNDING

Goal: A clean, reproducible Replit runtime.

Must implement:
	•	.replit run command
	•	replit.nix (or Replit-native equivalent)
	•	Node version lock strategy
	•	single-port binding enforcement
	•	canonical folder structure
	•	/health endpoint

✅ Exit Criteria:
	•	npm run dev or npm start boots reliably
	•	/health returns OK

⸻

PHASE 1 — CORE PLATFORM SKELETON

Goal: Full-stack boots end-to-end.

Backend:
	•	Express server (ESM)
	•	middleware: CORS, compression, Helmet (or equivalent)
	•	structured logs
	•	global error handler
	•	/health, /diagnostics endpoints

Frontend:
	•	React + Vite
	•	router + error boundary UI
	•	API client configured to backend
	•	environment-aware config

Shared:
	•	environment variables
	•	runtime-safe config loader
	•	central constants module

✅ Exit Criteria:
	•	Frontend loads
	•	Frontend calls backend successfully
	•	No console errors
	•	No runtime crashes

⸻

PHASE 2 — DATA & STATE

Goal: Safe data foundation.

Must implement:
	•	PostgreSQL connection layer
	•	schema definitions
	•	migrations (push + rollback)
	•	safe query layer (no raw string concatenation)
	•	seed script
	•	environment-based DB switching

✅ Exit Criteria:
	•	DB connected
	•	create/read/write test passes
	•	rollback tested
	•	no coupling between UI and DB internals
