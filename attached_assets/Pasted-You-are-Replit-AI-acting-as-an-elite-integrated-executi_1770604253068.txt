You are Replit AI acting as an elite integrated execution team:
- MIT PhD Software Engineering (architecture, QA, reliability)
- Publishing Systems Engineer (blog + newsletter + content workflow)
- Narrative Systems Architect (single story spine)
- UX QA & Link Integrity Auditor (buttons/routes)
- Ethical Growth Engineer (no manipulation, trust-first)
- Safety & Accessibility Reviewer (trauma-informed, consent-first)

PROJECT:
www.TheGenuineLoveProject.com

VALUES (HARD CONSTRAINTS):
We are ethical, loving, pure, kind, all-loving.
Therefore:
- No urgency, fear, guilt, shame, coercion, hype, or misleading promises
- No medical/therapy claims or “guaranteed healing” language
- Manual-only distribution: NO auto-email campaigns, NO auto-posting, NO schedulers/daemons
- Consent-first language, clear boundaries, and crisis accessibility
- No invasive tracking or fingerprinting; no storing IPs for analytics

ABSOLUTE ENGINEERING RULES:
1) Do NOT refactor, rename, move, or delete existing files or routes.
2) Additive & reversible changes only.
3) Do NOT create parallel blog/newsletter systems; use the canonical existing routes/pages.
4) Avoid duplicate work: scan before adding anything new.
5) Every phase must end with verification and a PASS/FAIL status.
6) If any phase FAILS: STOP and output a surgical patch list (files + exact edits) and rerun verification.

OUTPUT FORMAT REQUIRED:
For each phase, print:
- STATUS: PASS/FAIL
- FILES CHANGED (exact paths)
- COMMANDS RUN + RESULTS
- WHAT WAS FIXED / ADDED
End with: FINAL SUMMARY + NEXT SINGLE STEP.

========================================================
PHASE 0 — BASELINE & NO-DUPLICATE SCAN (NO CODE CHANGES)
========================================================
Run:
- git status
- npm run build
- curl -s http://localhost:5000/api/health
- node scripts/prevent-duplicates.mjs (if present)
- node scripts/soft-launch-smoke.mjs (if present)

Write:
docs/NARRATIVE_AMPLIFICATION_BASELINE.md

If build or health fails: STOP and fix first.

========================================================
PHASE 1 — CANONICAL NARRATIVE SPINE (SOURCE OF TRUTH)
========================================================
Create:
docs/NARRATIVE_SPINE.md

It must include:
A) One-sentence purpose (plain, grounded)
B) What we are (3 bullets)
C) What we are not (3 bullets)
D) What users can expect (3 bullets, non-medical)
E) Who it is for / not for (clear boundaries)
F) Tone rules (calm, kind, invitational)
G) Forbidden language list (urgency/guilt/fear/shame/guarantees)
H) Safety statement + link/reference to /crisis

Verification:
- File exists
- No medical claims
- Consistent with public pages

========================================================
PHASE 2 — BLOG HARDENING (PUBLISHING FOUNDATION)
========================================================
Identify canonical blog system (routes/pages/components) already present.
Do NOT create a second blog system.

Ensure these routes work:
- /blog (index)
- /blog/:slug (post view) OR the existing equivalent

Create or ensure 5 canonical posts exist in the canonical system:
1) Welcome to The Genuine Love Project
2) What This Is / What This Is Not
3) How to Use This Gently
4) A 5-Minute Daily Practice
5) Safety, Consent & Boundaries

Rules:
- Trauma-informed, calm, non-clinical
- No outcome promises
- Include gentle CTA to /newsletter
- Include /crisis reference where appropriate

Verification:
- Navigate to /blog and each post route (no 404)
- No console errors
- Links inside posts resolve

========================================================
PHASE 3 — NEWSLETTER HARDENING (CONSENT-FIRST, MANUAL)
========================================================
Identify canonical newsletter signup page + backend route.
Do NOT create a second newsletter system.

Ensure:
- email validation
- storage of signup (existing DB table or existing storage pattern)
- NO auto-campaign sending

Create DRAFT templates only (not auto-sent):
- Welcome Draft
- Weekly Reflection Draft
- Community Note Draft

Store drafts in the existing content system (or a minimal new drafts file under content/ if no system exists):
content/newsletter/drafts.json

Verification:
- Signup flow stores record
- No email auto-sends
- Drafts exist and are viewable by admin (if admin UI exists)

========================================================
PHASE 4 — 12 SOCIAL NARRATIVE POSTS (MANUAL, MULTI-PLATFORM)
========================================================
Create:
content/narrative/social_posts.json

Requirements:
- Valid JSON
- EXACTLY 12 objects
Each object:
- id
- theme
- hook
- instagram_caption
- tiktok_caption
- youtube_shorts_description
- x_post
- gentle_cta_url (must be /blog or /newsletter or /crisis)
- safety_note (string, can be empty)

Constraints:
- No urgency language
- No guilt/shame/fear
- No “guaranteed healing”
- No medical claims
- Must match NARRATIVE_SPINE

Verification:
- JSON parses
- Exactly 12 objects
- gentle_cta_url routes exist

========================================================
PHASE 5 — ADMIN NARRATIVE LIBRARY (NO AUTO-POSTING)
========================================================
If admin UI exists:
Add admin-only page:
- /admin/narrative

It must:
- list the 12 posts
- provide copy buttons
- allow status tags: draft / review / approved / posted (manual)
- show a warning banner: “Manual publishing only. No automated posting.”

If no admin UI exists:
Create minimal admin-only page that reads social_posts.json and displays it.

Verification:
- Admin protected
- Loads without console errors
- Status persists after refresh

========================================================
PHASE 6 — LINK + BUTTON INTEGRITY SWEEP (PUBLIC FIRST)
========================================================
Scan public surfaces for broken destinations:
- header nav
- footer links
- “Read more” buttons
- “Subscribe” and “Blog” CTAs
- marketing hero CTAs

Fix ONLY by:
- pointing to valid internal routes
- adding graceful fallbacks if route missing (simple page + links back)
Do NOT redesign.

Verification:
- Click through top public pages (home, about, blog, newsletter, crisis, contact)
- No 404s
- No console errors

========================================================
PHASE 7 — FINAL QA + NO-DUPE GUARANTEE
========================================================
Run:
- git status
- npm run build
- curl -s http://localhost:5000/api/health
- node scripts/prevent-duplicates.mjs (if present)
- node scripts/soft-launch-smoke.mjs (if present)

Write:
docs/NARRATIVE_AMPLIFICATION_COMPLETE.md

Include:
- What was added
- What was fixed
- What was intentionally not changed
- Confirmation: no automation added, manual-only publishing preserved

END WITH:
STATUS: PASS/FAIL
NEXT SINGLE RECOMMENDED STEP (choose ONE)