You are Replit AI acting as a senior full-stack engineer. Implement the following tasks EXACTLY, in this order, using ONLY JavaScript (not TypeScript) and our existing project structure. Do not refactor component code unless required for tokens to work. Preserve existing variable names prefixed with --glp-*. Produce diffs and create any missing files.

PROJECT CONTEXT (do not change):
- Tailwind config file exists: tailwind.config.js (JS)
- Brand tokens exist at: client/src/styles/brand-tokens.css
- Brand CSS exists at: client/src/styles/brand.css
- Tailwind entry file exists: client/src/index.css
- App is Vite/React and uses JS/JSX (not TS). Some *.d.ts files may exist; ignore them.

PRIMARY BRAND PAIR:
- Deep Teal = #2F5D5D (primary action color)
- Gold = #EAC33B (focus ring + accent, NOT body text)

TASK A — TOKENS (client/src/styles/brand-tokens.css)
1) Update brand-tokens.css to be the SINGLE SOURCE OF TRUTH.
2) Keep existing raw brand variables (do not delete):
   --glp-sage, --glp-sage-deep, --glp-blush, --glp-gold, --glp-ink, --glp-paper and any existing *-rgb helpers.
3) Ensure --glp-sage-deep is set to Deep Teal #2F5D5D.
4) Ensure --glp-gold is set to Gold #EAC33B.
5) Ensure --glp-paper is Ivory #FAF9F7 and --glp-ink is Charcoal #3A3A3A.
6) Add semantic tokens (mapped to the above) WITH EXACT NAMES:
   --glp-bg, --glp-surface, --glp-surface-2,
   --glp-text, --glp-text-muted,
   --glp-primary, --glp-primary-foreground,
   --glp-accent, --glp-accent-foreground,
   --glp-border, --glp-ring,
   --glp-radius-1, --glp-radius-2,
   --glp-shadow-1,
   --glp-motion-fast, --glp-motion-med, --glp-ease.
   Use reasonable values; ring must map to gold; text must map to charcoal; bg must map to ivory.
7) Add 3 modes using attribute selectors (do not use .dark for these modes):
   - Default mode: :root (already)
   - Low-stim: html[data-mode="low-stim"] { ... }:
     - reduce decorative shadow (set --glp-shadow-1 to none or minimal)
     - slightly soften accent (gold) if desired but keep recognizable
     - keep readability intact
   - Reading: html[data-mode="reading"] { ... }:
     - set surfaces to #FFFFFF
     - set text-muted darker for legibility
     - remove decorative gradients (if any are token-driven)
8) Do NOT hardcode colors inside components. Only tokens file may contain hex values.

TASK B — CSS IMPORT ORDER (client/src/index.css)
1) Ensure brand-tokens.css is imported ONCE and BEFORE tailwind directives.
   The top of index.css must be:
   @import "./styles/brand-tokens.css";
   @tailwind base;
   @tailwind components;
   @tailwind utilities;
2) If brand.css is needed, import it AFTER the tailwind directives unless it contains tokens. (Tokens must remain only in brand-tokens.css.)

TASK C — TAILWIND THEME EXTEND (tailwind.config.js)
Update tailwind.config.js (JS) theme.extend with:
1) colors mapped to our CURRENT CSS VAR NAMES exactly:
   sage: "var(--glp-sage)"
   blossom: "var(--glp-blush)"
   teal: "var(--glp-sage-deep)"  // this is deep teal
   ivory: "var(--glp-paper)"
   charcoal: "var(--glp-ink)"
   gold: "var(--glp-gold)"

2) Add semantic colors that map to semantic tokens:
   bg: "var(--glp-bg)"
   surface: "var(--glp-surface)"
   "surface-2": "var(--glp-surface-2)"
   text: "var(--glp-text)"
   "text-muted": "var(--glp-text-muted)"
   primary: "var(--glp-primary)"
   "primary-foreground": "var(--glp-primary-foreground)"
   accent: "var(--glp-accent)"
   "accent-foreground": "var(--glp-accent-foreground)"
   border: "var(--glp-border)"
   ring: "var(--glp-ring)"

3) Extend borderRadius using tokens:
   glp1: "var(--glp-radius-1)"
   glp2: "var(--glp-radius-2)"

4) Extend boxShadow:
   glp1: "var(--glp-shadow-1)"

5) Extend fontFamily to match brand:
   serif: ["Playfair Display", "ui-serif", "Georgia", "serif"]
   sans: ["Inter", "ui-sans-serif", "system-ui", "sans-serif"]

6) Add fontSize scale matching Canva:
   tglp-title: ["56px", { lineHeight: "1.15" }]
   tglp-subtitle: ["36px", { lineHeight: "1.2" }]
   tglp-heading: ["32px", { lineHeight: "1.25" }]
   tglp-subheading: ["28px", { lineHeight: "1.35" }]
   tglp-section: ["24px", { lineHeight: "1.35" }]
   tglp-body: ["18px", { lineHeight: "1.65" }]
   tglp-quote: ["20px", { lineHeight: "1.6" }]
   tglp-caption: ["14px", { lineHeight: "1.5" }]

7) Ensure content globs remain unchanged unless clearly wrong.

TASK D — NAV LINK AUDIT SCRIPT (Node, JS)
1) Create: tools/nav-link-audit.mjs
2) Script requirements:
   - Scan these directories (if they exist): client/src, client, docs
   - Extract internal links from:
     href="..."; href={'...'}; to="..."; to={'...'}; Link to="..."
   - Ignore:
     - mailto:, tel:, http://, https://, # anchors, javascript:
     - links containing placeholders like { } if they are dynamic (skip)
   - Determine app routes by parsing react-router usage:
     - Find <Route path="..."> and <Route path={'...'}>
     - Also detect createBrowserRouter route objects if present (path: "...")
   - Consider valid internal paths:
     - "/" always valid if home exists
     - Any extracted internal link must match a discovered route OR be a static file under client/public or client/src/assets (check filesystem existence for obvious asset links).
3) Output:
   - Create docs/NAV_LINK_AUDIT.md
   - Include:
     - timestamp
     - counts: routes found, links found, broken links found
     - tables: broken links with file+line+link
     - tables: all routes list
4) Exit codes:
   - If broken links found: process.exit(1)
   - Else: process.exit(0)

TASK E — PACKAGE SCRIPT
1) Update package.json to add:
   "nav:audit": "node tools/nav-link-audit.mjs"
If package.json is not present or is generated differently, locate the main package.json and update it.

TASK F — CI (GitHub Actions)
1) Create: .github/workflows/nav-audit.yml
2) Workflow:
   - trigger on push and pull_request
   - setup node 18+
   - npm ci (or npm install if lockfile missing)
   - npm run nav:audit
3) Ensure CI fails if nav audit fails.

TASK G — VERIFICATION + REPORT
After implementing, produce a final report in the chat with:
1) A checklist showing each task A–F as DONE.
2) The exact commands to run locally in Replit shell:
   - npm run nav:audit
   - npm run build (if available)
3) Show the first ~30 lines of:
   - client/src/index.css
   - tailwind.config.js (relevant theme.extend section)
4) Confirm docs/NAV_LINK_AUDIT.md was generated and where to find it.

IMPORTANT CONSTRAINTS
- Do not convert files to TypeScript.
- Do not rename --glp-* variables.
- Do not introduce a new token naming scheme.
- Keep changes minimal outside the specified files.
- If there is an existing dark mode system using .dark, do not delete it; do not mix it with the 3 modes. The 3 modes must be data-mode based.

Now implement everything.