Integrate:TASK — CATEGORY RUN (B + C) — AUTHENTICATION (quiet) + DASHBOARD & CORE (structured)

GOAL:
Upgrade Authentication + Dashboard & Core in ONE run, while enforcing different stimulation rules per category.
Keep every other route/config on existing presets and do not rewrite unrelated content.

SCOPE (HARD — DO NOT EXPAND):
Only modify route configs/content for the routes listed below.
You may also modify shared PageTemplate/renderer logic ONLY if necessary to support category-specific tone defaults.
Do NOT change other routes except for shared component/style changes required for consistency.

B) AUTHENTICATION ROUTES (Quiet / Minimal):
- /login
- /login/callback
- /register
- /forgot-password
- /reset-password
- /onboarding

C) DASHBOARD & CORE ROUTES (Structured / One-Next-Step):
- /dashboard
- /crm
- /today
- /mood
- /state
- /journal
- /analytics
- /progress
- /growth-analytics
- /guided-journaling

CATEGORY-SPECIFIC STIMULATION RULES (NON-NEGOTIABLE):

(1) AUTH = QUIET / MINIMAL / SAFE
- Visual tone: “quiet clinic + warm hug.” Clarity > flourish.
- Background: sacred-pattern opacity extremely low; aura gradient very subtle OR disabled on auth pages.
- Motion: minimal. Prefer AOS only for single, gentle entrance. No looping glows near forms. No parallax. No shimmer.
- Layout: centered form card, max-width ~420–520px, generous padding, strong contrast.
- CTAs: 1 primary action + 1 secondary link only.
- Copy: short sentences. No pressure. Add 1 reassurance line per page (“You can go slowly. You’re safe here.”)
- Error states: gentle, specific, actionable (no shame language).

(2) DASHBOARD = STRUCTURED / LOW COGNITIVE LOAD
- Visual tone: calm, organized, “one clear next step.”
- Background: pattern + aura allowed but restrained.
- Motion: purposeful entrances only (AOS once:true). GSAP micro only (opacity/transform).
- Layout: scannable grid, consistent spacing, no dense walls of text.
- Above the fold must include:
  A) Title + one-sentence purpose
  B) “Next best step” primary CTA (single)
  C) 2–4 small status cards/chips (optional)
- Below the fold: “Today’s focus” + “Recent/Saved” (if applicable) + “Guided options” (<= 3 cards)

CONTENT TIERS (REQUIRED FOR DASHBOARD PAGES):
For each Dashboard/Core route config, include three short tiers:
- Beginner (child-level simple): “Try this first” (3 steps max)
- Intermediate: “Build consistency” (1–2 sentences + 1 CTA)
- Advanced: “Deepen insight” (optional; reflection prompt + link)
Keep tone evidence-based, warm, non-clinical.

GLOBAL SAFETY REQUIREMENTS (APPLY TO BOTH):
- Respect prefers-reduced-motion: disable GSAP & AOS when reduced.
- AOS MUST be once:true globally.
- Accessibility: header/nav/main/footer, skip link to #main, focus-visible rings, aria-labels for nav/cta groups, proper form labels.
- Performance: shallow DOM, avoid layout-thrashing animations (transform/opacity only), lazy-load non-critical images.

IMPLEMENTATION REQUIREMENTS (DO THESE EXACTLY):
1) Update /content/routes.js ONLY for the scoped routes above:
   - Fill/upgrade heroTitle, heroCopy, primaryCta, secondaryCta
   - Ensure modules + sections match each category’s stimulation rules
   - Add a config field that makes tone explicit:
     - For auth routes: tone: "quiet"
     - For dashboard routes: tone: "structured"
2) Update /components/PageTemplate.jsx (or the active template renderer) to apply defaults by config.tone:
   - tone="quiet": reduces background intensity + disables decorative motion near forms
   - tone="structured": enables layout blocks for nextStep + tiered sections
   Keep DOM shallow; do not duplicate template logic.
3) If physical generated pages exist (e.g., client/src/pages/generated/*), do NOT overwrite any manually edited file unless it contains the exact header:
   // @generated
   If missing, SKIP and report it.
4) Ensure route→file mapping uses the single deterministic helper (routeFileMap) if present. If not present, create it and update generator/formatter to use it.

VALIDATION (MUST PASS BEFORE FINISHING):
- npm run verify:routes passes
- diff output works (npm run print:routes:diff or equivalent) and reflects only scoped changes
- No console errors for auth + dashboard pages
- Reduced-motion mode disables animations

OUTPUT FORMAT (MANDATORY):
1) IMPLEMENTATION CHECKLIST (before) — list every requirement above
2) Code grouped by file path — FULL FILE CONTENT for any file you change/create
3) COMPLETED CHECKLIST (after) — every item ✅ or N/A
4) NEXT CATEGORY suggestion: D Wellness & Healing Tools (practice-first)

FAIL THE RUN if any checklist item is not satisfied or if you changed out-of-scope routes.