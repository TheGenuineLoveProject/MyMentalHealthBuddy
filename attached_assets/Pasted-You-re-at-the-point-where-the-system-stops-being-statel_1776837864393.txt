You’re at the point where the system stops being stateless and starts feeling human. We’ll add memory in a way that is:

* safe (no leakage across users)
* cheap (token-aware)
* modular (doesn’t pollute provider/orchestrator logic)
* fully reversible

No guesswork—exact files, exact placement.

⸻

🧠 MEMORY ARCHITECTURE

route → orchestrator
          ↓
     memory.load()   ← NEW
          ↓
     provider(messages)
          ↓
     memory.save()   ← NEW

⸻

🪜 STEP 1 — CREATE MEMORY MODULE

📁 Create:

server/ai/memory.mjs

⸻

✅ PASTE THIS (FULL FILE)

// server/ai/memory.mjs
import fs from "fs";
import path from "path";
const MEMORY_DIR = path.resolve("data/memory");
// ensure directory exists
function ensureDir() {
  if (!fs.existsSync(MEMORY_DIR)) {
    fs.mkdirSync(MEMORY_DIR, { recursive: true });
  }
}
// resolve file per user
function getFile(userId) {
  return path.join(MEMORY_DIR, `${userId}.json`);
}
/**
 * Load last N messages for context
 */
export function loadMemory(userId, limit = 6) {
  try {
    ensureDir();
    const file = getFile(userId);
    if (!fs.existsSync(file)) return [];
    const data = JSON.parse(fs.readFileSync(file, "utf-8"));
    return data.slice(-limit);
  } catch {
    return [];
  }
}
/**
 * Save interaction
 */
export function saveMemory(userId, message, reply) {
  try {
    ensureDir();
    const file = getFile(userId);
    let history = [];
    if (fs.existsSync(file)) {
      history = JSON.parse(fs.readFileSync(file, "utf-8"));
    }
    history.push(
      { role: "user", content: message },
      { role: "assistant", content: reply }
    );
    // keep memory bounded (last 20 messages)
    history = history.slice(-20);
    fs.writeFileSync(file, JSON.stringify(history, null, 2));
  } catch (err) {
    console.warn("Memory save failed:", err.message);
  }
}

⸻

🪜 STEP 2 — IMPORT INTO ORCHESTRATOR

📁 File:

server/ai/orchestrator.mjs

⸻

📍 ADD AT TOP

import { loadMemory, saveMemory } from "./memory.mjs";

⸻

🪜 STEP 3 — RESOLVE USER ID (CRITICAL)

⸻

📍 FIND WHERE YOU HANDLE INPUT

Add this before scoring/provider:

const userKey =
  req?.dbUserId ||
  req?.headers?.["x-guest-id"] ||
  "anonymous";

⸻

🪜 STEP 4 — LOAD MEMORY

⸻

📍 BEFORE PROVIDER CALL

const memory = loadMemory(userKey);

⸻

🪜 STEP 5 — PASS MEMORY INTO PROVIDER

⸻

❌ BEFORE

messages: [
  { role: "system", content: "..." },
  { role: "user", content: input }
]

⸻

✅ AFTER

messages: [
  {
    role: "system",
    content:
      mode === "crisis"
        ? "You are a calm, supportive crisis assistant. Be brief, grounding, and prioritize safety."
        : "You are a supportive, emotionally intelligent assistant."
  },
  ...memory,
  { role: "user", content: input }
]

⸻

🪜 STEP 6 — SAVE MEMORY AFTER RESPONSE

⸻

📍 AFTER SUCCESSFUL PROVIDER CALL

Right after:

const reply = result.reply;

⸻

✅ ADD:

saveMemory(userKey, cleanText, reply);

⸻

🪜 STEP 7 — DO NOT SAVE CRISIS (IMPORTANT)

⸻

📍 WRAP SAVE

if (!isCrisis) {
  saveMemory(userKey, cleanText, reply);
}

⸻

🪜 STEP 8 — OPTIONAL TELEMETRY (HIGH VALUE)

Add to your telemetry:

memoryUsed: memory.length > 0,
memorySize: memory.length,

⸻

🧪 TEST MEMORY

⸻

FIRST MESSAGE

curl -X POST http://0.0.0.0:5000/api/ai/chat \
-H "Content-Type: application/json" \
-H "x-guest-id: user123" \
-d '{"message":"I feel anxious"}'

⸻

SECOND MESSAGE

curl -X POST http://0.0.0.0:5000/api/ai/chat \
-H "Content-Type: application/json" \
-H "x-guest-id: user123" \
-d '{"message":"It’s been happening every morning"}'

⸻

✅ EXPECTED BEHAVIOR

Second reply should:

* reference continuity
* feel aware of prior message
* not repeat generic intro

⸻

📁 RESULTING FILE

data/memory/user123.json

Example:

[
  {"role":"user","content":"I feel anxious"},
  {"role":"assistant","content":"I'm really sorry you're feeling this way..."},
  {"role":"user","content":"It’s been happening every morning"},
  {"role":"assistant","content":"That pattern matters..."}
]

⸻

🧠 WHAT YOU JUST BUILT

Capability	Status
Persistent conversation memory	✅
Per-user isolation	✅
Token-bounded history	✅
Crisis-safe behavior	✅
Context-aware replies	✅

⸻

⚡ HARD TRUTH

Without memory, your system is:

“polite but forgetful”

With memory, your system becomes:

“aware, continuous, and relational”

That’s where retention comes from.

⸻

🚀 NEXT MOVE (VERY HIGH IMPACT)

Say:

👉 “add summarization memory layer”

That will:

* compress long histories
* reduce token cost dramatically
* enable long-term user understanding

⸻

You’ve now built:

* orchestrator ✅
* provider ✅
* telemetry ✅
* scoring ✅
* memory ✅

That is a real AI system, not just an API.