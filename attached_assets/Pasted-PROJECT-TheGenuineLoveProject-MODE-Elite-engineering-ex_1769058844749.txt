PROJECT: TheGenuineLoveProject
MODE: Elite engineering execution. Strict sequencing. Zero scope creep. Ship-grade reliability.

YOU MUST RUN 3 AGENTS IN ORDER:
1) QA Guardian → 2) UX Finisher → 3) Content Curator
No agent may start until the prior agent’s “PASS” section is complete and verified.

GLOBAL CONTRACT (NON-NEGOTIABLE)
1) ZERO scope creep:
   - Only repair/fix/complete/upgrade what exists.
   - You may add ONLY the minimum missing glue required to make existing features work: login/admin login, navigation, styling consistency, build stability, safety page.
   - If user asked for "social media automation / AI employees": ONLY repair existing automation code; do NOT invent a brand-new growth system unless files already exist. If missing, create a stub doc listing recommended next steps WITHOUT implementing it.
2) Maintain these files at repo root:
   - FIX_LEDGER.md (issue → root cause → patch → verification command(s) → expected output)
   - ENV_CHECKLIST.md (env var → where used → purpose → how to set on Replit)
   - .env.example (safe placeholders; never secrets)
3) Never hardcode credentials/secrets; use Replit Secrets or .env.
4) Every change must include verification (command or manual checklist).
5) Content rules:
   - No medical claims, no guaranteed outcomes. Provide supportive, grounded guidance.
   - Must include SAFETY.md + an in-app Safety page route.
6) Quality rules:
   - Keep diffs minimal, consistent, typed where applicable, no duplicate files.
   - Prefer composable utilities and a single source of truth for theme tokens.

ACCEPTANCE CRITERIA (ALL MUST PASS)
A) Clean install works.
B) Build works.
C) Tests pass (if present). If none exist, add minimal smoke checks.
D) Lint/typecheck pass if scripts exist.
E) Login works in dev.
F) Admin login route works OR is correctly gated with explicit messaging.
G) Navigation has no dead internal links; add friendly 404 page.
H) Visual system is cohesive: tokens for color/typography/spacing; accessible focus indicators; reduced motion support.
I) Safety page exists and is reachable from footer/help.

PHASE 0 — DISCOVERY (DO FIRST)
Output:
- Framework + router + auth method + DB + styling system.
- Scripts from package.json/pyproject.
- Route map and where login/admin live.
- List of failing commands (if any) with exact error excerpts.
Write:
- FIX_LEDGER.md (Discovery section)
- ENV_CHECKLIST.md
- .env.example
- tools/release_gate.sh (ship/no-ship)
- tools/env_audit.js and tools/link_scan.js (if Node present)

PHASE 1 — AGENT 1: QA GUARDIAN (STABILITY + AUTH)
Allowed edits ONLY: backend/auth/api, db/migrations, config/scripts, tests, env files, utilities.
Tasks:
1) Make tools/release_gate.sh pass as far as possible.
2) Fix login + admin login:
   - Identify auth flow, callbacks, sessions/cookies/JWT, DB user records, role flags.
   - If roles missing, implement MINIMAL role flag + guard only (no full RBAC).
3) Repair duplicates/incomplete files; delete dead code only if provably unused.
4) Add minimal smoke checks:
   - server starts
   - login endpoint exists
   - protected route returns 401 when unauthenticated
5) Update FIX_LEDGER.md for every fix.

Deliverable:
- “QA PASS” section with exact commands and expected results.

PHASE 2 — AGENT 2: UX FINISHER (VISUALS + NAV + A11Y)
Allowed edits ONLY: UI components, theme tokens, CSS/Tailwind, layout, navigation, pages, accessibility attributes, state components.
Tasks:
1) Create a single source of truth for design tokens:
   - CSS variables OR Tailwind theme extension.
2) Apply tokens to:
   - buttons/inputs/cards/nav/modals/toasts
   - spacing/typography consistency
3) Fix navigation links:
   - ensure hrefs match real routes
   - add friendly 404 and empty/loading/error states
4) Accessibility basics:
   - visible focus
   - keyboard navigation for menus/forms
   - reduced motion support
5) Update FIX_LEDGER.md with verification checklist + link scan results.

Deliverable:
- “UX PASS” section with route list confirmed and manual checklist.

PHASE 3 — AGENT 3: CONTENT CURATOR (ONBOARDING + SAFETY + CLARITY)
Allowed edits ONLY: content, onboarding text, help/FAQ/safety pages, microcopy, metadata.
Tasks:
1) Add/Update:
   - Onboarding: what this is, how to use, privacy expectations
   - Safety: not medical advice; crisis guidance; encourage professional support
   - FAQ: account/login/admin; troubleshooting
2) Ensure copy is supportive, grounded, and avoids guarantees.
3) Structure content for localization (central constants or markdown directory).
4) Update FIX_LEDGER.md.

Deliverable:
- “CONTENT PASS” section with content map.

FINAL — RELEASE GATE (MUST RUN AND REPORT)
Run:
- bash tools/release_gate.sh
Report:
- PASS/FAIL + paste outputs
- Next actions if FAIL (bounded, minimal)
Now begin Phase 0. Do not skip.