MODE: CORE MASTER COMMAND v3.0 (Safe Real Work: fix, build, test)
GOAL: Fix ALL current failing tests and API routes WITHOUT refactoring unrelated files.
RULES:
- Do NOT rename folders, do NOT move files, do NOT delete files.
- Only minimal edits required to make tests pass.
- Preserve existing endpoints and behavior unless tests require changes.
- Every fix MUST include: exact file path + exact code to paste + exact placement instructions.
- After each change, run: npm test
- Stop only when: “Test Files 0 failed” and all tests pass.

STEP 0 — BASELINE
1) Run tests:
   - Command: npm test
2) Identify the EXACT failing test names and error outputs.
3) For each failure, map it to:
   - Missing route (404)
   - Wrong status code (expected 200 but got 400/401/404)
   - Wrong response shape (expected body.data.* but missing)
   - Wrong array length assertions (expected N items)
   - Auth middleware mismatch (401 vs expected 400/200)

STEP 1 — FIX JOURNAL ROUTES (KNOWN FAILURES)
A) Open file: server/routes/journal.mjs
B) Replace the ENTIRE file with the exact code below (full overwrite):
---BEGIN PASTE---
import { Router } from "express";
import jwt from "jsonwebtoken";
import crypto from "crypto";

const router = Router();
const journalStore = new Map();

const isProd = process.env.NODE_ENV === "production";
const ACCESS_SECRET =
  process.env.JWT_SECRET || (isProd ? null : "dev_secret_not_for_production");

function requireAuth(req, res, next) {
  try {
    const header = req.headers.authorization || "";
    const token = header.startsWith("Bearer ") ? header.slice(7) : null;
    if (!token) return res.status(401).json({ ok: false, message: "Unauthorized." });

    const payload = jwt.verify(token, ACCESS_SECRET);
    req.user = payload;
    return next();
  } catch {
    return res.status(401).json({ ok: false, message: "Unauthorized." });
  }
}

// POST /api/journal
router.post("/", requireAuth, async (req, res) => {
  const { title, content } = req.body || {};
  if (!title || String(title).trim().length === 0) {
    return res.status(400).json({ ok: false, message: "Title is required." });
  }

  const id = crypto.randomUUID();
  const now = new Date().toISOString();

  const entry = {
    id,
    title: String(title).trim(),
    content: content == null ? "" : String(content),
    createdAt: now,
    updatedAt: now,
    userId: req.user?.id || "test-user",
  };

  journalStore.set(id, entry);
  return res.status(200).json({ ok: true, data: entry });
});

// GET /api/journal
router.get("/", requireAuth, async (req, res) => {
  const userId = req.user?.id || "test-user";
  const entries = Array.from(journalStore.values()).filter((e) => e.userId === userId);
  return res.status(200).json({ ok: true, data: entries });
});

// PUT /api/journal/:id  (tests expect 400 for missing, NOT 404)
router.put("/:id", requireAuth, async (req, res) => {
  const { id } = req.params;
  const { title, content } = req.body || {};

  if (!id || id === "undefined" || id === "null") {
    return res.status(400).json({ ok: false, message: "Invalid journal id." });
  }

  const existing = journalStore.get(id);
  if (!existing) {
    return res.status(400).json({ ok: false, message: "Journal entry not found." });
  }

  if (title != null) {
    const t = String(title).trim();
    if (t.length === 0) {
      return res.status(400).json({ ok: false, message: "Title cannot be empty." });
    }
    existing.title = t;
  }
  if (content != null) {
    existing.content = String(content);
  }

  existing.updatedAt = new Date().toISOString();
  journalStore.set(id, existing);

  return res.status(200).json({ ok: true, data: existing });
});

// DELETE /api/journal/:id (tests expect 400 for missing, NOT 404)
router.delete("/:id", requireAuth, async (req, res) => {
  const { id } = req.params;

  if (!id || id === "undefined" || id === "null") {
    return res.status(400).json({ ok: false, message: "Invalid journal id." });
  }

  if (!journalStore.has(id)) {
    return res.status(400).json({ ok: false, message: "Journal entry not found." });
  }

  journalStore.delete(id);
  return res.status(200).json({ ok: true });
});

export default router;
---END PASTE---

STEP 2 — ENSURE JOURNAL ROUTER IS MOUNTED
1) Open server/app.mjs (or server/index.mjs depending on project)
2) Confirm it contains something like:
   import journalRouter from "./routes/journal.mjs";
   app.use("/api/journal", journalRouter);
3) If missing, add it near other app.use routes, then save.

STEP 3 — FIX ALL REMAINING 404 API FAILURES (MISSING ENDPOINTS)
For each failing test that shows “expected 404 to be 200”:
1) Identify the endpoint path from the test output, e.g.
   /api/praxis/execution
   /api/praxis/daily
   /api/socio-ecology/daily
   /api/socio-ecology/regenerative
2) Search in server/routes for the matching router file:
   - If file exists but endpoint missing: add minimal router.get(...) or router.post(...) with correct response shape.
   - If router file missing: create it under server/routes/<name>.mjs and mount it.
3) Response format MUST match:
   - Always include { ok: true, ... } for successful responses
   - If the test expects arrays: return arrays under the exact key (frameworks, visions, patterns, modalities, etc.)
   - If the test checks length, return EXACT number of items expected.

STEP 4 — FIX ARRAY LENGTH ASSERTIONS (EXPECTED N BUT GOT M)
If tests show errors like “expected 6 to be 5”:
1) Open the route file that returns that array.
2) Adjust the returned array to EXACT length expected in test.
3) Do NOT change test files. Fix the API responses.

STEP 5 — AUTH STATUS CONSISTENCY
If tests show “expected 401 to be 200”:
1) The test is likely calling with authHeader(authToken).
2) Ensure the endpoint uses requireAuth middleware ONLY if the test expects auth.
3) If the test expects public endpoint, remove requireAuth from that endpoint ONLY.

STEP 6 — RUN & CONFIRM
Run:
- npm test
If any failures remain:
- Fix the next failing item ONLY, then rerun.
Repeat until:
- Test Files 0 failed
- All tests pass

OUTPUT REQUIREMENT:
After fixes, print:
1) List of files changed
2) Why each fix was needed (1 sentence)
3) Final npm test output summary line showing all passed