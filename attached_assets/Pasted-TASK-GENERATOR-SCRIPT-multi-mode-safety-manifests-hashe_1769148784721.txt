TASK: GENERATOR SCRIPT — multi-mode + safety + manifests + hashes (must match existing output pattern)

CONTEXT:
Your generator output pattern is:
client/src/pages/generated/...

GOAL:
Create/upgrade a deterministic page generator that can optionally create physical files for routes, even though autopilot already works.

MUST-HAVE MODES:
--mode=all
--mode=landing
--mode=category --category="Wellness & Healing Tools"
--mode=range --from=A --to=C
--mode=verify
verify also supports --fix (local only) to regenerate and rewrite sha files

SOURCE OF TRUTH:
- Category letter mapping MUST live in /content/categoryOrder.js
- Routes source MUST be /content/routes.js

CRITICAL OVERWRITE RULE:
Generator MUST refuse to overwrite any existing page unless the first line contains:
  // @generated
If not present, skip and report it.

OUTPUT FILES (required):
- reports/routes.generated.json (manifest of generated files + route mapping)
- reports/routes.generated.sha256 (stable hash of manifest)
- reports/routes.config.snapshot.json (canonical snapshot of the route configs)
- reports/routes.config.sha256 (stable hash of config snapshot)
Snapshot MUST include per-route hashes so we can pinpoint which route changed.

ADD deterministic route→file mapping:
- Implement scripts/route-to-file.mjs that matches your current generator naming (see screenshot):
  client/src/pages/generated/home.jsx
  client/src/pages/generated/welcome.jsx
  client/src/pages/generated/404.jsx
  client/src/pages/generated/blog-[param].jsx
  client/src/pages/generated/community-discussion-[param].jsx
Include explicit mappings for /, /404, /*, /blog/:slug, /community/discussion/:id.

ADD/UPDATE npm scripts:
- gen:pages:all
- gen:pages:landing
- gen:pages:category
- gen:pages:range
- verify:routes
- verify:routes:fix

OUTPUT REQUIREMENTS:
1) IMPLEMENTATION CHECKLIST (before)
2) FULL FILE CONTENTS for every new/changed script and config file
3) Print exact shell commands for each mode
4) COMPLETED CHECKLIST (after)

DONE GATE:
✅ Generator writes stable JSON (2-space) with stable key ordering
✅ Refuses overwrite without // @generated
✅ Writes all reports + hashes
✅ verify mode exits non-zero on drift; --fix can update locally
If any fail, fix before finishing.