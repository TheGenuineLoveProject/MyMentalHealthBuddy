You are working inside my existing Replit project for “The Genuine Love Project”.

GOAL:
1) Implement the finalized GLP design token system with Deep Teal + Gold as the priority pair, using my existing CSS variable names prefixed with --glp-* (NO refactors required).
2) Update tailwind.config.js (JavaScript, not TypeScript) to map both palette + semantic tokens, including ring/border/surface/text tokens.
3) Add 3 visual modes: Default, Low-Stim, Reading, controlled by <html data-mode="..."> and implemented in CSS tokens (no JS required to switch modes).
4) Add a Nav Link Audit script (Node) that:
   - scans client/src + client + docs for href/to links,
   - detects SPA routes from router definitions,
   - writes docs/NAV_LINK_AUDIT.md,
   - exits with code 1 if any broken internal links/routes/static files exist.
5) Wire the audit into package.json scripts and add GitHub Actions workflow to fail CI on broken links.
6) Verify everything is wired correctly with a step-by-step checklist and proof.

CONSTRAINTS:
- I am using tailwind.config.js (JS). Keep it JS.
- My current token variable names are already in use: --glp-sage, --glp-blush, --glp-sage-deep, --glp-paper, --glp-ink, --glp-gold, plus *-rgb helpers.
- Do NOT rename existing variables or require refactoring component classes.
- Ensure brand-tokens.css is imported exactly once and before Tailwind directives.
- Do not break existing dark mode if present; keep it as-is unless it conflicts. If conflicts exist, document them and fix safely.

IMPLEMENTATION TASKS (DO THESE EXACTLY):
A) TOKENS
1. Replace client/src/styles/brand-tokens.css with the finalized content:
   - core palette values
   - RGB helper variables
   - semantic tokens: --glp-bg, --glp-surface, --glp-surface-2, --glp-text, --glp-text-muted, --glp-primary, --glp-primary-foreground, --glp-accent, --glp-accent-foreground, --glp-border, --glp-ring, radius tokens, shadow token, motion tokens
   - modes:
     html[data-mode="low-stim"] {...}
     html[data-mode="reading"] {...}

2. In client/src/index.css:
   - add: @import "./styles/brand-tokens.css";
   - ensure this import is ABOVE:
     @tailwind base;
     @tailwind components;
     @tailwind utilities;

B) TAILWIND
3. Replace tailwind.config.js with an updated version that:
   - keeps existing palette color keys (sage/blossom/teal/ivory/charcoal/gold mapped to var(--glp-*))
   - adds semantic keys: bg, surface, surface-2, text.DEFAULT, text.muted, primary.DEFAULT/foreground, accent.DEFAULT/foreground, border, ring
   - extends borderRadius sm/md from CSS vars
   - adds boxShadow 1 from CSS var
   - adds transitionDuration fast/med and transitionTimingFunction soft from CSS vars

C) NAV LINK AUDIT
4. Create tools/nav-link-audit.mjs exactly as specified:
   - Node script walking scan dirs: client/src, client, docs
   - ignores node_modules/dist/build/.git/.cache etc
   - extracts href="..." and to="..." and to={"..."} links
   - extracts routes from router files using patterns:
     path: "/x"
     <Route path="/x" ...>
   - treats internal absolute paths as routes unless file extension suggests static asset
   - writes docs/NAV_LINK_AUDIT.md summary + list of broken links and warnings
   - exits 1 if broken links exist

5. In package.json:
   - add script: "nav:audit": "node tools/nav-link-audit.mjs"

6. Add GitHub Actions workflow:
   - .github/workflows/nav-audit.yml
   - runs on push to main + pull_request
   - runs npm ci, then npm run nav:audit
   - fails if broken links exist

D) VERIFICATION / PROOF
7. After making changes:
   - show a concise “Files changed” list with paths
   - show the exact lines added to client/src/index.css
   - confirm tailwind.config.js is JS and exports correctly
   - run (or simulate in the console output) these commands:
     - npm run nav:audit
   - confirm docs/NAV_LINK_AUDIT.md was created
   - provide a final completion checklist with ✅ next to each item

IMPORTANT OUTPUT REQUIREMENTS:
- Do not ask me questions. Decide and proceed.
- If there is already an existing token file or import, fix duplicates cleanly.
- If my project uses a different index.css location, find the correct Tailwind entry file and apply import there.
- If router detection is incomplete, still implement the script and document limitations in docs/NAV_LINK_AUDIT.md warnings.

Now execute all tasks and provide proof of completion.