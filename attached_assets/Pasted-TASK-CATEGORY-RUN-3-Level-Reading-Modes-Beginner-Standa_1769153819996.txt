TASK: CATEGORY RUN — 3-Level Reading Modes (Beginner / Standard / Deep)
Goal: Add a platform-wide 3-level reading mode system that works for ALL routes rendered via config-driven autopilot, without breaking existing routes.

NON-NEGOTIABLES:
- Next.js (pages router), React
- CSS Modules only
- AOS once:true, GSAP off under prefers-reduced-motion
- Accessibility: semantic landmarks + skip link + focus-visible + ARIA
- Copy must be original and legally safer: no diagnosis, no medical/treatment claims, no guarantees, no plagiarism, no quotes.

INCLUSIVE LANGUAGE / POLITICAL RESPECT (MANDATORY):
- Use person-first, non-stigmatizing language (e.g., “people who…” not labels).
- Avoid assumptions about identity, culture, ability, or background.
- Avoid moralizing terms (“should”, “weak”, “normal/abnormal”).
- Avoid infantilizing tone; “Beginner” must be respectful and empowering.
- Avoid politically charged framing; keep tone welcoming and neutral.

SCOPE (FILES TO CREATE/UPDATE):
1) /content/routes.js
2) /components/PageTemplate.jsx
3) /components/Layout.jsx (+ styles if needed)
4) NEW: /components/ReadingLevelToggle.jsx + /components/ReadingLevelToggle.module.css
5) NEW: /content/readingLevels.js (constants + helpers)
6) OPTIONAL: /styles/globals.css (add .srOnly utility if missing)

DATA MODEL REQUIREMENTS:
A) Per-route defaults in /content/routes.js:
- Add field: defaultReadingLevel: "standard" (or "beginner" for onboarding/auth/welcome pages, "deep" for advanced tools)
- Add field: readingLevelsEnabled: true (default true, allow disabling for legal/system pages if desired)

B) Copy variants:
- For each route config, hero + sections must support variants:
  hero: {
    title: { beginner, standard, deep },
    subtitle: { beginner, standard, deep },
    primaryCta: { label: { beginner, standard, deep }, href: "..." },
    secondaryCta: { label: { beginner, standard, deep }, href: "..." }
  }
- Sections:
  sections: [
    {
      id,
      eyebrow: { beginner, standard, deep },
      title: { beginner, standard, deep },
      subtitle: { beginner, standard, deep },
      variant,
      cards: [
        { icon, title: { beginner, standard, deep }, text: { beginner, standard, deep } }
      ],
      bullets: {
        beginner: [...],
        standard: [...],
        deep: [...]
      }
    }
  ]

C) Fallback rule:
- If a variant is missing for a field, fallback to standard.
- If standard missing, fallback to first available string.

READING LEVEL BEHAVIOR:
1) UI toggle (global, per user):
- Render a ReadingLevelToggle in Layout (top-right in nav or below nav).
- Control must be a <fieldset> with <legend> (visually hidden ok).
- Use radio inputs:
  - value="beginner" label "Beginner"
  - value="standard" label "Standard"
  - value="deep" label "Deep"
- Must be keyboard navigable (Tab + Arrow keys).
- Must show focus-visible ring.
- Must announce current selection for screen readers.
- Include a short helper text: “Choose the level of detail you prefer.”

2) Persistence + URL:
- Reading level resolution order:
  a) URL query ?level=beginner|standard|deep (if present)
  b) localStorage "glp_reading_level" (if present)
  c) route.defaultReadingLevel
  d) "standard"
- When user changes toggle:
  - update state
  - set localStorage
  - update URL query (shallow routing) without full reload
- SSR-safe: do not access window/localStorage until useEffect.

3) PageTemplate rendering:
- PageTemplate receives config + resolved readingLevel
- Use helper getVariant(field, level) to pick correct text.
- Render hero/sections/cards using resolved variant strings.

COPY SAFETY RULES (STRICT):
- Use supportive, autonomy-respecting phrasing: “You can try…”, “If it helps…”
- Allowed: “may help”, “some people find…”, “often used for…”
- Never: “cures”, “treats”, “guarantees”, “clinically proven to fix you”
- Add a brief educational disclaimer (global footer or practice pages):
  “Educational support only — not medical advice.” + link to /crisis
- No references to named universities/experts unless explicitly provided with permission and citations.

BEGINNER/STANDARD/DEEP COPY GUIDELINES:
- Beginner:
  - respectful + empowering (not childish)
  - 1–2 short sentences per block
  - plain language, avoid jargon; define any necessary terms in 5–10 words
- Standard:
  - warm, grounded, evidence-friendly tone
  - 2–4 bullets per section max
  - clear next step CTA
- Deep:
  - slightly more detail, one short definition per section
  - include “Why this might help” + “When to pause/stop” lines for practices
  - still calm, still short paragraphs

IMPLEMENTATION STEPS:
1) Create /content/readingLevels.js:
- export const READING_LEVELS = ["beginner","standard","deep"]
- export function normalizeReadingLevel(input)
- export function getVariant(value, level): handles string OR {beginner,standard,deep}
- export function resolveReadingLevel({ routerQuery, stored, routeDefault })

2) Create /components/ReadingLevelToggle.jsx:
- Props: value, onChange
- Accessible fieldset/legend, radio group
- aria-label="Reading level"
- Calm segmented-control UI via CSS Modules

3) Update /components/Layout.jsx:
- Add reading level state
- On mount: load localStorage if no ?level
- Watch router query changes
- Render <ReadingLevelToggle />
- Keep skip link and sacred background layers intact

4) Update /components/PageTemplate.jsx:
- Accept { config }
- Read readingLevel from Layout (prop drilling) OR via a tiny context (choose simplest)
- Use getVariant on hero/sections/cards/bullets
- Ensure minimal DOM nesting

5) Update /content/routes.js:
- Add defaultReadingLevel and readingLevelsEnabled for every route stub
- Generate starter variants (beginner/standard/deep) that are respectful + inclusive and compile.
- DO NOT leave TODOs; all stubs must run.

OUTPUT FORMAT (MANDATORY):
1) IMPLEMENTATION CHECKLIST (before)
2) Code grouped by file path (FULL file contents for changed/created files)
3) COMPLETED CHECKLIST (after) with ✅ or N/A for:
   - routes.js includes defaults + variants + fallback
   - toggle is accessible (fieldset/legend/radios/focus-visible)
   - persistence works (query + localStorage)
   - PageTemplate uses variants correctly
   - SSR safe
   - inclusive language rules applied
4) Provide quick manual test steps:
   - visit /?level=beginner, toggle to deep, refresh, verify persistence

DONE GATE — FAIL IF ANY NOT MET:
✅ npm run dev works (no build errors)
✅ no console errors on load
✅ toggle works via keyboard + screen reader semantics
✅ URL query level works and persists to localStorage
✅ missing variant falls back to standard without crashing
✅ AOS once:true and GSAP reduced-motion safe remain intact
✅ no medical claims, no plagiarism, no fake endorsements