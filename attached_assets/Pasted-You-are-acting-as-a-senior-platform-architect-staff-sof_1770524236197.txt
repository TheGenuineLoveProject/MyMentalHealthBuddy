You are acting as a senior platform architect, staff software engineer, and production-readiness auditor.

Your task is to COMPLETE, HARDEN, and FORMALIZE the current platform state — without refactoring, renaming, deleting, or restructuring existing files.

GLOBAL CONSTRAINTS (NON-NEGOTIABLE):
- DO NOT remove or rename any existing files, routes, components, env vars, or Stripe products
- DO NOT introduce dark patterns, urgency tactics, or coercive monetization
- All changes must be additive, reversible, and minimal
- subscription_status ("free" | "pro") remains the single canonical source of truth
- The platform must remain trauma-informed, calm, and non-extractive

CURRENT VERIFIED STATE (DO NOT RE-IMPLEMENT):
- Stripe products & prices exist and are live
- Webhooks write canonical subscription_status
- users.subscription_status column exists and defaults to "free"
- /api/auth/user returns subscriptionStatus
- AuthContext exposes subscriptionStatus and isPro
- Feature access map exists
- PlanGate.jsx exists and is wired
- Billing page reads from context
- AI chat soft limits enforced server-side
- Visual Pro indicators exist
- No console errors, no failing routes

OBJECTIVE:
Finalize the platform by adding STRUCTURAL COMPLETION LAYERS, not features.

EXECUTE THE FOLLOWING STEPS IN ORDER:

STEP 1 — PLATFORM STATUS SNAPSHOT
- Create a single machine-readable status report (JSON or JS object) describing:
  - subscription pipeline state
  - billing readiness
  - webhook integrity
  - frontend gating readiness
  - server enforcement readiness
- Place it in a safe location (e.g. /docs/platformStatus.js or similar)
- No UI changes

STEP 2 — FEATURE ACCESS ASSERTION LAYER
- Add a lightweight helper (no new context) that allows any component to assert:
  - "is this feature allowed for this user?"
- This must read ONLY from:
  - featureAccess config
  - subscriptionStatus from context
- No conditional rendering changes yet

STEP 3 — STRIPE SAFETY & FAILURE HARDENING
- Add defensive handling for:
  - duplicate webhook delivery
  - delayed Stripe events
  - missing subscription records
- Ensure idempotency and logging only (no retries, no blocking)

STEP 4 — DEPLOYMENT READINESS CHECK
- Add a non-public endpoint or script that verifies:
  - Stripe keys exist
  - Webhook secret exists
  - Database connectivity
  - subscription_status column integrity
- This must be safe to run in production and return a simple pass/fail object

STEP 5 — DOCUMENTATION FOR FUTURE YOU
- Add a concise internal document explaining:
  - how subscription state flows end-to-end
  - how to safely add a new Pro feature
  - how to roll back monetization safely
- This is for maintainability, not marketing

STEP 6 — FINAL VERIFICATION
- Re-run sanity checks
- Confirm:
  - no UI regressions
  - no new API surface exposed
  - no new Stripe products created
  - no changes to pricing behavior

OUTPUT REQUIREMENTS:
- After execution, output a FINAL VERIFICATION REPORT with:
  - What was added
  - Why it is safe
  - What was explicitly not changed
  - Confirmation that the platform is production-ready

Do NOT ask follow-up questions.
Do NOT suggest growth hacks or marketing.
Proceed methodically and conservatively.