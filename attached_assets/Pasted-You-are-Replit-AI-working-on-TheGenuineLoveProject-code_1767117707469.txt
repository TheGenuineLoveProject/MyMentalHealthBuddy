You are Replit AI working on TheGenuineLoveProject codebase.

GOAL:
- Perform an A→Z 360° audit and implement fixes for: missing utility files, metadata/deployment readiness, security hardening, UI visual upgrade, integration verification, performance improvements, SEO, and publishing foundation.

RULES:
- Follow Replit best practices: one web server port, bind 0.0.0.0, do not break dev server, do not introduce background daemons.
- No secrets in code. Use .env for secrets.
- Keep changes minimal but complete, with clear file diffs.
- After each change, run: npm test (if exists), npm run build (if exists), npm run dev, and report results.

TASKS (DO IN ORDER):
1) Repo scan:
   - Identify missing utility functions referenced in code (e.g., cn, class merge, formatting, env asserts).
   - Add them under src/lib/* and update imports.

2) Deployment metadata:
   - Ensure site-wide metadata: title, description, OpenGraph, Twitter, favicon, canonical URL.
   - Add robots.txt and sitemap.xml.
   - Validate base URL config for production.

3) Security:
   - Confirm Helmet, CORS policy, rate limiting for auth + AI endpoints.
   - Add server-side validation (Zod) for all POST routes.
   - Add request logging + error handler (no PII in logs).
   - Confirm cookies/session settings are secure in production.

4) Integration verification:
   - Add scripts/verify-integrations.ts to check DB connectivity, OpenAI key presence, Stripe key presence, and health endpoints.
   - Add npm script "verify".

5) UI upgrade:
   - Apply the brand system: Serenity Sage + Eternal Gold accent.
   - Improve typography, spacing scale, and button hierarchy.
   - Replace low-quality images with optimized assets where applicable.
   - Improve accessibility (focus states, contrast, aria labels).

6) Performance:
   - Add image optimization guidance, lazy loading, route-level code splitting if applicable.
   - Reduce bundle bloat and report largest modules.

7) Output:
   - Provide a CHANGELOG list of files changed.
   - Provide the exact commands I should run to confirm.