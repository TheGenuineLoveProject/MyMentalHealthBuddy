You are working in my Replit repo “The Genuine Love Project”.
Goal: make auth/admin stable locally + in deployed preview, keep tests/audits green, and eliminate flaky port/deploy issues.

HARD RULES
- Do not change existing `--glp-*` CSS var names.
- No raw hex colors anywhere except `client/src/styles/brand-tokens.css`.
- Fonts only Inter + Playfair Display (with system fallbacks).
- Prefer minimal diffs; no broad refactors.
- Do NOT weaken tests; fix the implementation.

TASKS (DO ALL)
A) Confirm test contract for auth endpoints
1) Open `tests/auth.test.mjs` and list expected endpoints + expected status codes/messages.
2) Verify the server routes match exactly (paths + methods). If mismatch exists, fix server wiring (not tests).

B) Fix status-code semantics (if any are still failing)
- Missing login fields must return 400 (validation error).
- Invalid credentials must return 401 (auth error) with uniform message (security).
- Non-admin accessing admin routes must return 403 (authorized but forbidden).
- Unauthenticated requests to protected routes should return 401.
- Logout should return `{ message: "Logged out" }` exactly (no trailing punctuation) if tests expect it.

C) Fix “deployment could not be reached” / 500s
1) Ensure `npm run dev` starts exactly one server process.
2) Ensure port usage is consistent and documented (what runs on 3000/5000 etc).
3) If a port collision occurs, add safe prestart logic or update dev script to avoid EADDRINUSE.
4) Add a minimal health check endpoint `/api/health` that returns 200 reliably.

D) Verification
Run and paste results:
- `npm run test:auth` (or the project’s auth test command)
- `npm run audit` (nav-link audit + visual doctor)
- `npm run lint` (if present)
Confirm: tests pass, audit passes, no new warnings introduced.

OUTPUT FORMAT (REQUIRED)
1) What was failing and why (bullet list).
2) Files changed (each with 1-line reason).
3) Exact commands run + pass/fail output summary.
4) Any remaining TODOs (should be none).
Proceed now.