Role: You are a senior Node.js (ESM) + Express + Drizzle/DB debugger for Replit.

Project context
	•	Replit workspace: TheGenuineLoveProject
	•	Node: v20.19.3
	•	Backend is ESM only (.mjs)
	•	Entry: node server/dev.mjs
	•	Goal: working auth middleware + admin protection + successful curl tests

What I need from you
	1.	Fix server/middleware/auth.mjs completely (exports + behavior)
	2.	Fix server/utils/jwt.mjs exports so imports stop crashing
	3.	Ensure /api/admin/stats works with requireAuth + requireAdmin
	4.	Clean rebuild of server/dev.mjs so app is always defined before routes are used
	5.	Provide curl commands that WILL succeed (login → token → admin stats)

Observed errors (verbatim)
	•	ReferenceError: app is not defined in server/dev.mjs (previously around line 6 when calling app.use(...))
	•	SyntaxError: The requested module './middleware/auth.mjs' does not provide an export named 'auth'
	•	SyntaxError: The requested module '../utils/jwt.mjs' does not provide an export named 'verifyAccessToken'
	•	When calling /api/admin/stats without a token: {"error":"Unauthorized","message":"Authentication required"} (expected)
	•	Login attempts currently return {"message":"Invalid credentials"} (I need a deterministic way to create/test an admin user)

Current route expectation
	•	GET /api/admin/stats should:
	•	401 if no token
	•	401 if token invalid
	•	403 if user is not admin
	•	200 and return JSON like { users: <number>, auditLogs: <number> } for admin

Hard requirements
	•	Keep everything ESM (import/export), no CommonJS.
	•	Do NOT rely on “file save hot reload” for backend. Assume I will restart workflow after changes.
	•	I want exact code edits (full file contents) for:
	•	server/middleware/auth.mjs
	•	server/utils/jwt.mjs
	•	server/routes/admin.mjs
	•	server/routes/auth.mjs (only if needed)
	•	server/dev.mjs

What you must do first (diagnostic steps)
	1.	Open and inspect these files and report mismatches:
	•	server/middleware/auth.mjs
	•	server/utils/jwt.mjs
	•	server/routes/admin.mjs
	•	server/routes/auth.mjs
	•	server/dev.mjs
	2.	Confirm exactly which exports exist in jwt.mjs and auth.mjs, and update imports/exports so they match.

Deliverables
A) ✅ Paste the corrected full contents of each file (listed above).
B) ✅ Provide working curl commands in this exact sequence:
	1.	Health check
	2.	Create admin user (if not present) OR tell me exact seed command/route
	3.	Login admin and capture token
	4.	Call /api/admin/stats with the token and show expected output

Use localhost:5000 and JSON content-type.

Extra
If “Something went wrong” appears in Replit UI while doing this, tell me whether it’s a Replit UI issue vs my app crash, and what logs to check.

⸻

✅ IMPORTANT (so you don’t get “bash: command not found” again)
	•	Paste that block into Replit AI Chat (the assistant panel), not into the Shell.
	•	Only paste curl commands into the Shell.
