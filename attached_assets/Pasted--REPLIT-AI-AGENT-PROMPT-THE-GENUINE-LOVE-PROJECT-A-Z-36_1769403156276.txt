# REPLIT AI AGENT PROMPT — THE GENUINE LOVE PROJECT
# “A→Z 360 COMPLETE” — NEXT POWERFUL / ROBUST / SOLID PHASE (v2)
# Power: 100th billionth × 100th billionth
# Style: Beginner / Intermediate / Advanced ONLY (NO “reading level”)
# Mode: CORE MASTER COMMAND v3.0 (SAFE REAL WORK: fix, build, deploy)

You are Replit AI running INSIDE my repo. Your job is to COMPLETE the platform from A→Z 360 in safe, verifiable steps.
You must execute in sequence, never breaking the build, never leaking secrets, and always validating after each phase.

=====================================================================
0) NON-NEGOTIABLE SAFETY RULES
=====================================================================
- No large refactors. No mass renames. No deleting core features.
- Keep app runnable at all times.
- Replit Autoscale rules: one public port, bind 0.0.0.0, avoid background watchers.
- Never hardcode secrets. Use env vars only.
- Every phase ends with: typecheck + build + quick smoke test.
- Content tiers ONLY: Beginner / Intermediate / Advanced (remove/avoid “reading level”).

=====================================================================
1) OUTPUT FORMAT (MANDATORY)
=====================================================================
After EACH phase, respond with:
A) ✅ What changed (file paths)
B) 🧪 Commands run + results (copy output)
C) 🔎 How to verify in UI (exact clicks/URLs)
D) ⛔ If blocked: exact error + the fix applied

=====================================================================
2) A→Z 360 COMPLETION MAP (PHASE ORDER — DO NOT SKIP)
=====================================================================
PHASE 1  Baseline Inventory + Remove “reading level”
PHASE 2  Auth End-to-End (user + admin)
PHASE 3  Database/Drizzle Real Persistence (no mocks)
PHASE 4  AI Support (safe prompts + crisis fallback + rate limit)
PHASE 5  Stripe Billing + Webhooks (verified + stored)
PHASE 6  Admin Health Dashboard (real diagnostics + logs)
PHASE 7  Security Hardening (CSP, CSRF strategy, validation)
PHASE 8  UX & Accessibility (spacing, button size, a11y)
PHASE 9  Quality Gate (tests + CI)
PHASE 10 Docs + Launch Checklist (DONE state)

=====================================================================
PHASE 1 — BASELINE INVENTORY + “READING LEVEL” REMOVAL
=====================================================================
1.1 Inventory
- Print tree (max depth 4)
- Capture package scripts
Commands:
  ls -la
  find . -maxdepth 4 -type f | sed 's|^\./||' | sort > /tmp/filelist.txt
  cat package.json
  (optional) cat client/package.json || true
  (optional) cat server/package.json || true

1.2 Baseline checks (do NOT fix yet; just record)
  node -v
  npm -v
  npm run -s typecheck || true
  npm run -s build || true
  npm run -s lint || true
  npm test -s || true

1.3 Remove “reading level” everywhere
- Search for these terms:
  "reading level", "reading-level", "reading_level", "grade level", "easy reading", "literacy"
Commands:
  rg -n "reading level|reading-level|reading_level|grade level|easy reading|literacy" .

If any matches exist:
- Replace UI/content labels so ONLY these tiers remain:
  Beginner / Intermediate / Advanced
- Do not change meaning; just remove the “reading level” concept.
- Update any types/enums/constants accordingly.
- Update any tests/docs referencing it.

1.4 Validate Phase 1 changes
  npm run -s typecheck
  npm run -s build

Deliverable at end of PHASE 1:
- Baseline report + list of “reading level” removals.

=====================================================================
PHASE 2 — AUTH END-TO-END (USER + ADMIN) — “SOLID CORE”
=====================================================================
Goal: Signup/Login/Logout/Forgot/Reset + protected routes.

2.1 Locate auth code:
  rg -n "auth|login|signup|register|jwt|session|bcrypt|forgot|reset|password" .

2.2 Server requirements (use existing style; fix it, don’t reinvent):
Endpoints required:
  POST /api/auth/signup
  POST /api/auth/login
  POST /api/auth/logout
  POST /api/auth/forgot-password
  POST /api/auth/reset-password
  GET  /api/auth/me

Rules:
- If app already uses JWT: keep JWT but ensure httpOnly cookie if possible.
- Hash passwords with bcrypt.
- Validate inputs with Zod.
- Add rate limit to auth endpoints.
- Prevent account enumeration on forgot-password.
- Add middleware: requireAuth + requireAdmin.

2.3 DB tables required:
- users (id, email, passwordHash, role, createdAt)
- password_resets (id, userId, tokenHash, expiresAt, usedAt, createdAt)

2.4 Frontend routes/pages required:
- /signup
- /login
- /forgot-password
- /reset-password
- /dashboard (protected)
- /admin (admin protected)

2.5 Admin seed rule:
- Use env ADMIN_EMAIL
- On first boot, if a user matches ADMIN_EMAIL, ensure role = admin.

2.6 Validate Phase 2:
  npm run -s typecheck
  npm run -s build
Smoke test (manual):
- Signup new user → login → dashboard loads → logout works
- Admin user → /admin loads

=====================================================================
PHASE 3 — DATABASE + DRIZZLE REAL PERSISTENCE (NO MOCKS)
=====================================================================
Goal: all key features persist.

3.1 Confirm DATABASE_URL is used
  rg -n "DATABASE_URL|drizzle|neon|postgres|pg" .

3.2 Required domain tables:
- journal_entries (userId, title, body, mood, tags, createdAt)
- subscriptions (userId, stripeCustomerId, stripeSubId, status, plan, currentPeriodEnd)
- audit_logs (actorUserId, action, metaJson, createdAt)

3.3 Migrations:
- Ensure drizzle-kit scripts exist in package.json:
  db:push or db:migrate
- If missing: add scripts using drizzle-kit.

3.4 Add DB health endpoint:
  GET /api/health/db (returns ok + latency)

Validation:
- run migration/push
- create user + journal entry and confirm it reloads

=====================================================================
PHASE 4 — AI SUPPORT (SAFE, RELIABLE, NON-CLINICAL)
=====================================================================
Goal: AI journaling + chat with guardrails.

4.1 Endpoints required:
  POST /api/ai/chat
  POST /api/ai/journal-reflection
  POST /api/ai/mood-insights

4.2 Safety:
- crisis keyword detector (self-harm, suicide, harm to others, etc.)
- if crisis detected: return crisis resources + encourage professional help
- always include “not therapy/medical advice” disclaimer
- rate limit per IP/user
- structured logs: requestId, userId(optional), latency, status

4.3 Validation:
- curl test normal
- curl test crisis phrase (must trigger safe response)
- build/typecheck

=====================================================================
PHASE 5 — STRIPE BILLING + VERIFIED WEBHOOKS
=====================================================================
Goal: checkout + webhook verification + subscription sync.

Env required:
- STRIPE_SECRET_KEY
- STRIPE_WEBHOOK_SECRET
- STRIPE_PRICE_BASIC / PLUS / PREMIUM (or your existing prices)
- APP_URL

Endpoints required:
  POST /api/billing/create-checkout-session
  POST /api/billing/create-portal-session
  POST /api/billing/webhook  (raw body + signature verify)
  GET  /api/billing/status

Frontend required:
- /pricing (3 tiers) → “Upgrade” triggers checkout
- Dashboard shows plan status

Validation:
- build/typecheck
- webhook verifies signature (no “raw body” bugs)

=====================================================================
PHASE 6 — ADMIN HEALTH DASHBOARD (REAL DIAGNOSTICS)
=====================================================================
Goal: admin sees health + logs + content tool MVP.

Server:
  GET /api/admin/health (admin-only)
Includes:
- uptime
- db ok + latency
- stripe configured
- ai configured
- recent errors count
- build metadata (commit if available)

Frontend:
- Admin dashboard cards + “Run Diagnostics” button
- Publishing tool MVP:
  - create draft post in DB (content_posts table)
  - list drafts

Validation:
- admin-only enforced
- UI renders no console errors

=====================================================================
PHASE 7 — SECURITY HARDENING (MINIMAL, SOLID)
=====================================================================
- Helmet + basic CSP
- CORS locked to APP_URL
- Zod validation on all write endpoints
- central error handler (no stack leaks in prod)
- CSRF strategy if cookies are used
- audit_logs populated for sensitive actions

Validation:
- typecheck/build
- auth + ai + billing still work

=====================================================================
PHASE 8 — UX / ACCESSIBILITY (FIX “CROWDED / TINY BUTTONS”)
=====================================================================
Requirements:
- button min height 44px
- consistent spacing scale
- better layout breathing room
- empty/loading states
- keyboard navigation + focus visible
- aria labels where needed

Validation:
- open / /pricing /dashboard /admin
- no layout overflow, no tiny buttons

=====================================================================
PHASE 9 — QUALITY GATE (TESTS + CI)
=====================================================================
- minimal tests:
  - auth flow tests
  - db health test
  - crisis detector test
- GitHub Actions:
  - install
  - typecheck
  - build
  - test

Validation:
- npm test passes
- workflow valid

=====================================================================
PHASE 10 — DOCS + DONE CHECKLIST
=====================================================================
- README: setup, env vars, run, db migrate, stripe notes
- DISCLAIMER.md, PRIVACY.md, SECURITY.md
- LAUNCH_CHECKLIST.md with checkboxes

=====================================================================
START NOW (NO DISCUSSION)
=====================================================================
DO PHASE 1 immediately.
Return the PHASE 1 baseline report and the “reading level” removal results.
Then proceed to PHASE 2 automatically.

END.