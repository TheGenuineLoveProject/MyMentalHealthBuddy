Perfect — this is the right next move. We’ll add a provider layer without breaking anything you already have.

⸻

🧠 HARD TRUTH

Right now your orchestrator still “knows too much” about OpenAI.
We’re going to move execution out so the orchestrator only decides what to do, not how to do it.

⸻

🎯 GOAL

Create:

server/ai/provider.mjs

Then wire it into your orchestrator with minimal change.

⸻

🪜 STEP 1 — CREATE FILE

📁 Path (exact)

server/ai/provider.mjs

⸻

✍️ STEP 2 — PASTE THIS EXACT CODE

// server/ai/provider.mjs
const DEFAULT_MODEL = process.env.AI_MODEL || "gpt-4o-mini";
const FALLBACK_MODEL = "gpt-4o-mini"; // safe fallback
const TIMEOUT_MS = 12000;
const MAX_RETRIES = 2;
/**
 * Core provider call with timeout
 */
async function callOpenAIWithTimeout(openai, payload) {
  return Promise.race([
    openai.chat.completions.create(payload),
    new Promise((_, reject) =>
      setTimeout(() => reject(new Error("AI timeout")), TIMEOUT_MS)
    )
  ]);
}
/**
 * Main provider function
 */
export async function callAIProvider({
  openai,
  input,
  mode = "normal",
  risk = { level: "low" }
}) {
  if (!openai) {
    throw new Error("OpenAI client not available");
  }
  let lastError = null;
  const modelsToTry = [
    DEFAULT_MODEL,
    ...(DEFAULT_MODEL !== FALLBACK_MODEL ? [FALLBACK_MODEL] : [])
  ];
  for (const model of modelsToTry) {
    for (let attempt = 0; attempt <= MAX_RETRIES; attempt++) {
      try {
        const start = Date.now();
        const response = await callOpenAIWithTimeout(openai, {
          model,
          messages: [
            {
              role: "system",
              content:
                mode === "crisis"
                  ? "You are a calm, supportive crisis assistant. Be brief, grounding, and prioritize safety."
                  : "You are a supportive, emotionally intelligent assistant."
            },
            {
              role: "user",
              content: input
            }
          ],
          temperature: risk.level === "high" ? 0.2 : 0.6
        });
        const latency = Date.now() - start;
        return {
          ok: true,
          modelUsed: model,
          latency,
          reply: response.choices?.[0]?.message?.content || ""
        };
      } catch (err) {
        lastError = err;
        console.warn(`AI attempt failed (model=${model}, try=${attempt})`, err.message);
        // retry same model first
        if (attempt < MAX_RETRIES) continue;
        // otherwise break to try next model
        break;
      }
    }
  }
  // total failure
  return {
    ok: false,
    error: lastError?.message || "AI provider failed"
  };
}

⸻

🪜 STEP 3 — MODIFY ORCHESTRATOR (SMALL CHANGE ONLY)

📁 Open:

server/ai/orchestrator.mjs

⸻

🔝 ADD IMPORT AT TOP

import { callAIProvider } from "./provider.mjs";

⸻

🔍 FIND THIS (your current OpenAI call)

Something like:

const response = await openai.chat.completions.create(...)

⸻

❌ DELETE THAT BLOCK

⸻

✅ REPLACE WITH THIS

const aiResult = await callAIProvider({
  openai,
  input: cleanText,
  mode: "normal",
  risk
});
if (!aiResult.ok) {
  return {
    ok: false,
    status: 500,
    stage: "ai",
    response: {
      error: "AI unavailable. Please try again."
    }
  };
}

⸻

🔁 THEN USE:

reply: aiResult.reply

instead of old response parsing

⸻

🧪 STEP 4 — TEST (SAME AS BEFORE)

curl -X POST http://0.0.0.0:5000/api/ai/chat \
-H "Content-Type: application/json" \
-H "x-guest-id: t1" \
-d '{"message":"I feel anxious"}'

⸻

✅ EXPECTED

Same behavior as before:

* normal reply ✔️
* crisis still short-circuits ✔️
* no crashes ✔️

⸻

🔥 WHAT YOU JUST ADDED

Capability	Status
Timeout protection	✅
Retry logic	✅
Fallback model	✅
Centralized AI execution	✅
Orchestrator decoupled	✅

⸻

🚀 NEXT (AFTER THIS WORKS)

We can now layer:

1. Cost tracking
2. Streaming responses
3. Multi-model routing (GPT + Claude fallback)
4. Response caching
5. Adaptive model selection (based on risk)

⸻

🙌 YOU’RE BUILDING THIS CORRECTLY

This is now:

👉 real production architecture

⸻

👉 When done, say:

“provider working”

or paste error — I’ll fix fast ⚡