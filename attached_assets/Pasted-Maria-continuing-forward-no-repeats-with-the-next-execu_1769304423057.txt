Maria — continuing forward (no repeats) with the next “execution layer”: concrete Replit AI prompts + file-by-file build plan that turns the framework into working, reusable components, guards, scanners, growth tools, and performance upgrades.

Everything below stays inside your non-negotiables:
	•	Educational only (no medical claims, no diagnosis, no guarantees)
	•	18+ gating
	•	Pause/stop everywhere
	•	/crisis everywhere wellness appears
	•	DRY-RUN first
	•	Non-destructive
	•	Original writing only
	•	Human-in-the-loop
	•	Replit-safe (no background workers)

⸻

PHASED IMPLEMENTATION MAP (A→Z, 360°)

Phase 1 — SAFETY + CONSISTENCY (highest ROI)
	1.	Global disclaimer + /crisis routing + pause/stop
	2.	18+ gating (UI + server guard)
	3.	Microcopy library + rotation helper
	4.	Benefits tokens + page “BenefitBlock” component
	5.	“Method tiles” (MI / Strengths+PIE / NLP) as reusable blocks

Phase 2 — TOOLING + PATHWAYS
	6.	12-Phase Self-Alignment Path™ pages + navigation
	7.	Reflection tool upgrades (MI prompts + NLP reframes + opt-out)
	8.	Shareable Reflection Cards (watermark + URL, no claims)

Phase 3 — PERFORMANCE + GROWTH + MONETIZATION
	9.	Repo scanner + “incomplete page” report
	10.	SEO + schema + internal linking + content ops
	11.	Creator studio (prompt packs) + tier gating
	12.	Anti-scrape deterrence + premium watermarking + rate limits

⸻

A) REPLIT AI MASTER PROMPT — DRY-RUN + PATCH (COPY/PASTE)

Paste this into Replit AI. It forces the assistant to (1) scan, (2) propose, (3) patch, (4) validate, (5) document — and STOP.

YOU ARE:
Permanent System Steward for TheGenuineLoveProject.com (Aaliyah Draws Art LLC — Maria Landa)

NON-NEGOTIABLES:
- DRY-RUN FIRST (no changes until plan is shown)
- Non-destructive (never delete without permission)
- Human-in-the-loop (no autonomous execution)
- Educational wellness support only (no diagnosis, no medical claims, no treat/cure/heal promises)
- 18+ only gating required
- Every wellness tool includes: pause/stop + opt-out + /crisis link
- Original writing only (zero plagiarism)
- WCAG AA accessibility
- Replit-safe (no background workers; user-triggered actions only)

GOAL:
Implement the platform-wide Safety + Consistency System:
1) Global disclaimer + /crisis + pause/stop coverage
2) 18+ gating (UI + server guard)
3) Benefits Engine (token library + BenefitBlock component)
4) Microcopy library with rotation helper (non-repetitive)
5) Method tiles for MI, Strengths/PIE, and Ethical NLP

STEP 1 — DRY-RUN REPORT:
Scan repo and output:
- Route/page list and where layout/footer lives
- Where auth/signup is implemented
- Where wellness tools live
- Missing disclaimers, missing /crisis links, missing pause/stop
- Proposed files to add/edit with exact paths
- Risks and minimal-change plan

STEP 2 — PATCH (only after DRY-RUN plan is printed):
Add these new files (or adapt to project structure):
- src/lib/safety.ts (disclaimer strings, banned phrases, crisis path)
- src/lib/microcopy.ts (library + rotation helper)
- src/lib/benefits.ts (token library + pick helpers)
- src/components/safety/SafetyFooterStrip.tsx
- src/components/benefits/BenefitBlock.tsx
- src/components/methods/MITile.tsx
- src/components/methods/StrengthsPIETile.tsx
- src/components/methods/NLPTile.tsx
- src/components/consent/AgeGateModal.tsx (for first wellness action)
- server/middleware/requireAdult.ts (or equivalent) and wire to protected routes

Then inject:
- SafetyFooterStrip in global layout/footer
- BenefitBlock at top of Home, Pricing, Onboarding, Dashboard, Tools
- Method tiles where appropriate

STEP 3 — VALIDATE:
Run typecheck/build and smoke test:
Home, Pricing, Signup/Login, Dashboard, at least 2 tool flows, /crisis

STEP 4 — DOCUMENT:
Create docs/safety-and-copy.md explaining:
- Required disclaimers
- Microcopy rules
- Banned phrase list
- 18+ gating behavior
- Where to place BenefitBlock and method tiles

STOP.


⸻

B) READY-TO-PASTE CODE (CORE FILES)

If you prefer to implement manually, paste these files exactly (adjust src/ vs client/ paths to match your repo).

1) src/lib/safety.ts

// Educational-only safety constants (no medical claims)
export const CRISIS_PATH = "/crisis";

export const SAFETY_DISCLAIMER_SHORT =
  "Educational wellness support — not medical advice.";

export const SAFETY_DISCLAIMER_LONG =
  "This platform provides educational wellness tools only and is not a substitute for professional medical, mental health, or crisis care.";

export const AGE_18_PLUS_LINE =
  "By continuing, you confirm you are 18+ and understand this platform provides educational wellness tools, not medical or mental health treatment.";

export const PAUSE_STOP_LINE = "You’re in control. Pause or stop anytime.";

export const CRISIS_LINE = `Need urgent help? Visit ${CRISIS_PATH}.`;

// Guardrails: phrases to avoid across copy to reduce liability risk
export const BANNED_CLAIM_PHRASES = [
  "treat",
  "cure",
  "heal your trauma",
  "diagnose",
  "therapy",
  "clinically proven",
  "guarantee",
  "medical advice",
];

2) src/lib/benefits.ts

export type BenefitToken =
  | "Agency"
  | "Boundaries"
  | "Calm"
  | "Direction"
  | "Emotional literacy"
  | "Focus"
  | "Gentle habits"
  | "Honesty"
  | "Identity"
  | "Judgment-free"
  | "Kindness"
  | "Language clarity"
  | "Meaning"
  | "Nervous system regulation"
  | "Ownership"
  | "Purpose"
  | "Quality attention"
  | "Resilience"
  | "Self-respect"
  | "Trust"
  | "Understanding"
  | "Values"
  | "Wellbeing literacy"
  | "Your pace"
  | "Zero pressure";

export const BENEFIT_TOKENS: BenefitToken[] = [
  "Agency",
  "Boundaries",
  "Calm",
  "Direction",
  "Emotional literacy",
  "Focus",
  "Gentle habits",
  "Honesty",
  "Identity",
  "Judgment-free",
  "Kindness",
  "Language clarity",
  "Meaning",
  "Nervous system regulation",
  "Ownership",
  "Purpose",
  "Quality attention",
  "Resilience",
  "Self-respect",
  "Trust",
  "Understanding",
  "Values",
  "Wellbeing literacy",
  "Your pace",
  "Zero pressure",
];

export function pickBenefits(tokens: BenefitToken[], count: number): BenefitToken[] {
  const unique = Array.from(new Set(tokens));
  return unique.slice(0, Math.max(1, Math.min(count, unique.length)));
}

3) src/lib/microcopy.ts

export type MicrocopyCategory =
  | "consent"
  | "encouragement"
  | "boundaries"
  | "progress"
  | "errors"
  | "cta";

export const MICROCOPY: Record<MicrocopyCategory, string[]> = {
  consent: [
    "You’re in charge here.",
    "Pause, skip, or stop anytime.",
    "Choose the gentlest option that still feels supportive.",
    "No pressure — small is valid.",
  ],
  encouragement: [
    "That makes sense.",
    "You’re not alone in this.",
    "One kind step is enough.",
    "Even one honest sentence counts.",
  ],
  boundaries: [
    "What would self-respect look like here?",
    "What’s a ‘good enough’ boundary for today?",
    "You can care and still say no.",
  ],
  progress: [
    "Consistency over intensity.",
    "Gentle momentum is still momentum.",
    "Skip days are allowed.",
  ],
  errors: [
    "Nothing is broken. Let’s try again.",
    "Not loaded yet — refresh or come back in a minute.",
    "No entries yet. Want a tiny start?",
  ],
  cta: [
    "Start gently",
    "Try a 60-second reset",
    "Take one small step",
    "Continue when you’re ready",
    "Save this for later",
  ],
};

// Deterministic-ish rotation so copy feels consistent but not repetitive.
// Seed can be route name + day-of-year, etc.
export function rotateMicrocopy(category: MicrocopyCategory, seed: number): string {
  const arr = MICROCOPY[category];
  if (!arr || arr.length === 0) return "";
  const idx = Math.abs(seed) % arr.length;
  return arr[idx];
}

4) src/components/benefits/BenefitBlock.tsx

import React from "react";
import { CRISIS_PATH, SAFETY_DISCLAIMER_SHORT, PAUSE_STOP_LINE } from "@/lib/safety";
import type { BenefitToken } from "@/lib/benefits";

export function BenefitBlock({
  benefits,
  seconds = "30–90 seconds",
}: {
  benefits: BenefitToken[];
  seconds?: string;
}) {
  return (
    <section aria-label="Benefits" className="rounded-xl border p-4">
      <p className="text-sm">
        <strong>What you’ll get:</strong> {benefits.join(" • ")}
      </p>
      <p className="text-sm">
        <strong>Time:</strong> {seconds}
      </p>
      <p className="text-sm">
        <strong>Control:</strong> {PAUSE_STOP_LINE}
      </p>
      <p className="text-sm">
        <strong>Note:</strong> {SAFETY_DISCLAIMER_SHORT}{" "}
        <a className="underline" href={CRISIS_PATH}>
          If you’re in crisis, visit {CRISIS_PATH}.
        </a>
      </p>
    </section>
  );
}

5) src/components/safety/SafetyFooterStrip.tsx

import React from "react";
import { AGE_18_PLUS_LINE, SAFETY_DISCLAIMER_SHORT, CRISIS_PATH } from "@/lib/safety";

export function SafetyFooterStrip() {
  return (
    <div className="w-full border-t py-3 text-xs opacity-90">
      <div className="mx-auto max-w-5xl px-4 flex flex-col gap-1">
        <span>18+ only • {SAFETY_DISCLAIMER_SHORT} • Pause/stop anytime • <a className="underline" href={CRISIS_PATH}>{CRISIS_PATH}</a></span>
        <span className="opacity-80">{AGE_18_PLUS_LINE}</span>
      </div>
    </div>
  );
}


⸻

C) 12-PHASE PATHWAY — PAGE + COMPONENT (SAFE, OPTIONAL)

Replit AI Prompt: Add the 12-Phase Path

DRY-RUN FIRST.
Add a new route/page: /path/self-alignment
Implement a component that renders the 12 phases with:
- Optional, self-paced language
- Pause/stop line in each phase
- /crisis link in header and footer
- No “12-step” naming
- No clinical promises

Then add a “Try the 12-Phase Path” card on Dashboard and Tools index pages.
Validate route works and is accessible (headings, lists, focus states).
Stop.


⸻

D) “INCOMPLETE PAGES” SCANNER (REPLIT-SAFE, HUMAN-TRIGGERED)

Replit AI Prompt: Add a manual scanner script

DRY-RUN FIRST.
Create scripts/scanIncomplete.mjs that:
- Recursively scans src/ (and server/ if present)
- Flags TODO, FIXME, "coming soon", placeholder lorem
- Flags empty React components returning null
- Flags routes missing BenefitBlock, /crisis, or pause/stop language
- Outputs a markdown report to reports/incomplete-pages.md
- Does NOT modify existing files
Add npm script: "scan:incomplete": "node scripts/scanIncomplete.mjs"
Stop after adding script + docs.

If you want, I can also give you the full scanIncomplete.mjs code (it’s ~120 lines) — but the prompt above is usually enough for Replit AI to generate correctly.

⸻

E) VIRAL + MONETIZATION TOOLING — EXACTLY WHAT TO BUILD NEXT

1) Reflection Cards (viral loop #1)

Replit AI Prompt

DRY-RUN FIRST.
Add a “Generate Reflection Card” feature after journaling completion:
- Card contains: user-selected quote (not medical), chosen value, tiny next step, watermark: TheGenuineLoveProject.com
- Include: Educational disclaimer + /crisis link (small)
- Provide share options: download image OR copy link to /share/:id
- Ensure user can edit before sharing (consent)
- No outcome claims
- User-triggered only, no background jobs
Stop after implementation + validation.

2) Weekly “Your Week in Genuine Love” recap (viral loop #2)

Replit AI Prompt

DRY-RUN FIRST.
Add a weekly recap page /recap (manual user-triggered generation):
- Top values practiced
- Most used tool
- One “gentle win” (user-written)
- One shareable recap card
No scheduling; user clicks “Generate recap”.
Stop.


⸻

F) ANTI-COPY / ANTI-SCRAPE (SAFE, REALISTIC)

You asked: “Ensure competitors/hackers can’t copy/paste pages.”
Truth: no website can prevent copying 100%, but we can raise the cost and protect premium content.

Replit AI Prompt: Deterrence + rate limits + watermarking

DRY-RUN FIRST.
Implement anti-scrape deterrence:
- Rate-limit requests to HTML routes (basic IP limiter)
- Add signed URLs for premium assets
- Watermark all exported/share cards
- Add Terms link + copyright notice in footer
Optional: disable selection/context menu ONLY inside premium content container
Do not break accessibility (keyboard, screen readers).
Stop.


⸻

G) ONE STEP ONLY TO RUN RIGHT NOW

To keep you on track without overwhelm, do this next:

✅ Run the DRY-RUN Completion Audit (the big prompt in section A).
That will produce the exact list of incomplete pages + missing safety + missing benefits blocks + performance risks in your actual repo.

When you paste the DRY-RUN report back here, I’ll transform it into:
	•	Phase 1 patch (copy-paste code blocks with exact file paths)
	•	Phase 2 pathway + card system
	•	Phase 3 performance + growth + monetization
…while staying non-destructive and 18+ enforced.

If you want me to give you the full scripts/scanIncomplete.mjs right now, tell me your repo’s top folders (e.g., client/, src/, server/) and I’ll output the exact script in one paste.