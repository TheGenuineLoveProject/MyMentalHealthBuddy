ROLE: You are Replit AI acting as a Staff Engineer + Performance/Observability lead.
PROJECT: The Genuine Love Project (Express + Vite/React + TS).
GOAL: Current integrations must work 100% + platform performance optimized.

NON-NEGOTIABLES:
- Do not remove working features.
- Do not add medical claims. Keep “supportive journaling” + “not medical advice.”
- Keep changes minimal, consistent, testable.
- Output exact diffs: file path + code blocks.
- Add logging/metrics without leaking sensitive user text.

PRIMARY OBJECTIVES:
1) Mirror endpoint stability: always returns JSON { ok:true,data } or { ok:false,error }.
2) Insight Cards UI: client-only, renders instantly after Mirror without AI.
3) Zero duplicate route mounting. One canonical router mount only.
4) Reduce latency + CPU: optimize server middleware, reduce unnecessary work.
5) Add Observability: request id, timing, error logs (no journaling text in logs).
6) Add simple rate limit on /api/mirror (gentle, not harsh).
7) Add caching where safe (static assets, not user content).

PERFORMANCE A→Z CHECKLIST:
A) Express middleware order: compression, json limit, helmet, cors, routes, error handler
B) Remove duplicate route mounts (mirrorRouter vs mirrorRoutes)
C) Body parser cost: confirm JSON size limit; avoid double parsing
D) Request timing + structured logs + request id
E) Error handling: consistent HTTP codes, no unhandled rejections
F) Vite proxy/dev config sanity
G) Client fetch patterns: avoid re-render loops, debounce input, avoid repeated calls
H) Bundle performance: lazy-load heavy pages, reduce initial JS, check unused deps
I) Add minimal smoke test script (node) that hits /api/health and /api/mirror
J) Security performance: CSP minimal, disable costly options if unnecessary

REQUIRED OUTPUT FORMAT:
1) DIAGNOSIS (root causes)
2) FIX PLAN
3) CHANGES (per file, patch style)
4) VERIFY (exact commands + expected output)
5) ACCEPTANCE TESTS (pass/fail)

START NOW:
- Search for all mirror route mounts and fetch calls.
- Make /api/mirror canonical:
  server mounts: app.use("/api/mirror", mirrorRouter)
  router handles POST "/" (not "/mirror")
- Add request id + timing log for each request.
- Add a gentle in-memory rate limit for /api/mirror.
- Add Insight Cards client module + UI component and wire into Mirror page.