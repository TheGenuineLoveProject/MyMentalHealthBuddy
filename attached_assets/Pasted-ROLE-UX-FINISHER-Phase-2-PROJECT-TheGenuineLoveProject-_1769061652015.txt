ROLE: UX FINISHER (Phase 2)
PROJECT: TheGenuineLoveProject (Replit)
MISSION: Upgrade the entire platform’s visuals + UX to a calm, premium, uplifting experience. Fix navigation links, add missing UI states, and enforce an accessible design system. Do NOT change backend/auth/db logic.

NON-NEGOTIABLE SCOPE LOCK (NO SCOPE CREEP)
1) Only edit UI/UX files (components, pages, styles, assets, layout). Do not modify API behavior, auth logic, DB schema, or server routes.
2) If a “fix” requires backend changes, STOP and write a TODO in FIX_LEDGER.md (do not implement).
3) Keep diffs minimal. Reuse existing components. No new major features.
4) After each change set, run verification commands and record results.

OUTPUT FORMAT REQUIRED
- For each step: list file paths changed + why + how verified.
- End with “UX PASS” checklist + command outputs.

DISCOVERY (DO FIRST)
A) Detect UI stack:
   - Next.js pages vs app router, or Vite/React.
   - Tailwind vs CSS modules vs styled-components.
B) Locate:
   - Global stylesheet entry point (pages/_app.*, app/layout.*, src/main.*).
   - Navigation component(s) and routes directory.
C) Print:
   - A short route inventory (login, dashboard, legal, settings, content pages).
   - Any existing theme tokens or styling conventions.

DESIGN SYSTEM (SINGLE SOURCE OF TRUTH)
Create/confirm ONE token source:
Option 1: CSS variables (preferred if not Tailwind).
- Create: styles/tokens.css and styles/base.css
- Must include: brand palette, neutral palette, typography scale, spacing scale, radii, shadows, focus ring, reduced motion.
Option 2: Tailwind theme extension
- Extend tailwind.config.* with the same tokens.

Then import globally ONCE:
- pages router: pages/_app.* → import "../styles/base.css"
- app router: app/layout.* → import "../styles/base.css"
- Vite: src/main.* → import "./styles/base.css"

COMPONENT POLISH (PRIORITY ORDER)
Apply tokens consistently to these surfaces (only where they exist):
1) Navigation: top bar / sidebar / footer
2) Auth screens: login/register/reset (inputs + buttons + error states)
3) Dashboard: cards/panels/sections spacing + typography
4) Buttons: primary / secondary / ghost / destructive variants
5) Forms: input/textarea/select, validation messages, disabled/loading states
6) Feedback: toast/alert/banner if present; otherwise implement minimal inline alerts

ACCESSIBILITY BASELINE (MUST)
- Visible :focus-visible ring for all interactive elements
- Keyboard navigation for menus and dialogs (if present)
- Sufficient contrast for text and buttons
- prefers-reduced-motion support
- Add aria-labels where icons-only controls exist

NAVIGATION LINKS (MUST FIX)
Run:
- node tools/route_map.cjs
- node tools/link_scan.cjs
Fix:
- Dead internal hrefs (update paths or add minimal placeholder pages IF they’re clearly intended routes)
- Add friendly 404 page
- Add empty/loading/error UI states for major pages

CONTENT PRESENTATION (UI ONLY)
- Improve microcopy presentation (headings, spacing, readability) but do not change meaning.
- Ensure footer/help includes link to Safety page (if already exists). If not, add a UI link placeholder and add TODO in FIX_LEDGER.md.

DOCUMENTATION + LEDGER (MUST)
Create/update:
- docs/UX_TOKENS.md (token rules + examples)
- FIX_LEDGER.md (each issue → root cause → fix → verification)

VERIFICATION (RUN AND PASTE OUTPUT)
- node tools/smoke_http.cjs
- node tools/route_map.cjs
- node tools/link_scan.cjs
- If build script exists: npm run build
- Quick manual checklist:
  - Login page renders, inputs look consistent
  - Dashboard loads, cards readable
  - Nav links work, 404 works
  - Focus ring visible
  - Reduced motion honored

DELIVERABLES
1) Calm premium theme tokens applied across the platform
2) Buttons/inputs/cards/nav consistent
3) Dead links removed; 404 + UI states added
4) A11Y baseline complete
5) docs/UX_TOKENS.md + FIX_LEDGER.md updated
6) End with “UX PASS” including command outputs

BEGIN NOW.