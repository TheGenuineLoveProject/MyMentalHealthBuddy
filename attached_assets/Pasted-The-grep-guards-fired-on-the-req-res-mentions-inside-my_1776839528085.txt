The grep guards fired on the req/res mentions inside my new contract comment — false positives. The smoke test handles that correctly (it strips comments first), but the inline grep in the shell didn't. Also a stray apostrophe broke the curl loop. Re-running cleanly:
Everything passes — and the AI summary is dramatically better than the heuristic. Marking the task complete and running code review per project policy:
One severe finding — the await blocks the HTTP response for ~1.8s every time summarization triggers. Easy fix: fire-and-forget since saveMemory already has its own try/catch.
Task #6 complete
Latency fix verified: turn 8 returned in 1.38s instead of the ~3s+ it would have been with the blocking await. The summarizer still ran in the background (2.48s, 810 input / 129 output tokens) and the summary file was written successfully — proving the fire-and-forget pattern works without losing durability.

What landed
Item	Status
await saveMemory → fire-and-forget with .catch()	✅ Sub-second response restored
Contract comment block at top of orchestrator	✅ Documents the function shape and bans req/res/next
summarizeWithAI wired into telemetry (route /ai/internal/summarize, purpose: "summary")	✅ Cost separable from chat in dashboards
Dead heuristic summarizeHistory removed	✅
Indentation fix on saveMemory declaration	✅
scripts/test-orchestrator.mjs smoke test	✅ All 21 assertions pass: empty/crisis/normal contracts + source-level grep guards (with comment-stripping so the contract block doesn't trigger false positives)
AI summary genuinely semantic	✅ — see real output above: "Anxiety related to performance and public speaking", "Tools to improve sleep quality and establish a calming bedtime routine" — not template prose
Crisis path still skips load + save	✅ (smoke test asserts no files written)
Summary cost (you asked earlier)
The summarizer ran on a real conversation: $0.000552 per summarization call, triggered roughly every 12 turns. So a heavy user with 100 turns of conversation pays ~$0.005 in summarization cost over the whole session. Cheap.

Architect findings I deferred (with rationale)
Race condition on concurrent saves for same user — Real bug. With async saveMemory and sync fs.writeFileSync, two in-flight requests for the same user can race. Out of scope for this task; would need either an in-memory per-user lock or moving to async file I/O with atomic temp-file+rename. Worth its own task before this hits production scale.
Telemetry route label aesthetics (task:summarize vs /ai/internal/summarize) — Cosmetic. Current label works; if you wire up Datadog/New Relic later and the label collides with route metrics, easy 1-line fix.
Smoke test doesn't assert summarization triggering — Would need to seed a 12-message memory file before the test runs. Reasonable to add, but the existing live e2e covered it conclusively.
Next per advisor
"Add user profile extraction layer" — produces structured {emotional_patterns, triggers, relationship_themes, coping_strategies, support_needs, risk_flags} JSON per user, kept separate from short-term memory and summary text, with append/update semantics rather than overwrite. Want me to plan it out as a new task?