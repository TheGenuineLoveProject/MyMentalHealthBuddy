Hard truth

The next improvement should be v1.6 only if v1.5 is verified.

Do not add voice, memory writes, new AI calls, or hardware logic yet.

Your next safe evolution is:

Buddy Engine v1.6 — memory-aware visual baseline using existing profile data only

Meaning: Buddy’s default visual tone can feel personalized without creating new memory systems.

⸻

Buddy Engine v1.6 — Memory-Aware Visual Baseline

Paste this into Replit AI:

Upgrade MMHB Buddy Engine to v1.6: memory-aware visual baseline using existing profile signals only.
STRICT RULES:
- Do not modify /api/ai/chat behavior
- Do not modify server/routes/buddy.mjs
- Do not modify orchestrator, provider, memory, profile, summary, telemetry, crisis logic
- Do not add new AI calls
- Do not add new memory writes
- Do not change tool execution logic
- Do not replace /start
- Do not delete files
- Only improve BuddyAvatar visual default/baseline behavior on /start
TARGET FILE:
client/src/pages/Start.tsx
OPTIONAL TARGET:
client/src/lib/avatarState.ts only if a helper is useful.
GOAL:
Buddy should feel gently personalized by default using existing available client-side signals only:
- current streak
- selected tool
- current Buddy state
- response modules
- response tool id
- daysAway if available
- paywall/show state if available
Do NOT fetch or write profile data directly.
Do NOT expose private user profile content in UI.
PHASE 1 — ADD BASELINE STATE MAPPER
Create helper in Start.tsx:
function resolveBuddyBaseline({
  currentStreak = 0,
  daysAway = 0,
  hasCompletedTool = false,
  hasPaywall = false
}) {
  if (daysAway >= 2) return "overwhelmed";
  if (hasCompletedTool) return "encouraged";
  if (currentStreak >= 7) return "encouraged";
  if (currentStreak >= 3) return "calm";
  if (hasPaywall) return "encouraged";
  return "calm";
}
PHASE 2 — APPLY BASELINE ONLY WHEN IDLE
Do not override active emotional/tool states.
Apply baseline only when:
- buddyState is calm
- no active response is loading
- no crisis
- no active tool progression
Suggested logic:
useEffect(() => {
  if (loading) return;
  if (buddyState !== "calm") return;
  const baseline = resolveBuddyBaseline({
    currentStreak: streak,
    daysAway,
    hasCompletedTool: Boolean(response?.tool),
    hasPaywall: Boolean(response?.paywall?.show)
  });
  if (baseline !== buddyState) {
    setBuddyState(baseline);
    trackEvent?.("buddy_baseline_applied", { baseline });
  }
}, [loading, buddyState, streak, daysAway, response]);
PHASE 3 — ADD BASELINE COPY
Near BuddyAvatar, render subtle copy:
If daysAway >= 2:
"Buddy is here to help you restart gently."
If currentStreak >= 7:
"Buddy remembers your consistency."
If currentStreak >= 3:
"Buddy is here for today’s reset."
If response?.paywall?.show:
"Buddy can help you go deeper when you’re ready."
Default:
"Buddy is here with you."
PHASE 4 — PRESERVE TOOL-SPECIFIC EXPRESSIONS
Do not remove v1.5 behavior:
- box_breathing → anxious/breathing
- thought_reframe → encouraged/focused
- emotional_checkin → sad/soft
- overload_reset → overwhelmed/grounded
Tool states always win over baseline.
PHASE 5 — PRESERVE STATE RECOVERY
Do not remove v1.4 behavior:
- emotional states recover to calm
- crisis state does not auto-recover
- no flashing
- reduced motion respected
PHASE 6 — TELEMETRY
Add additive-only event:
buddy_baseline_applied
Metadata:
{
  baseline,
  streak,
  daysAway
}
Do not log message text.
Do not log private profile data.
PHASE 7 — VERIFY
Run:
npm run dev
Manual tests:
1. Fresh user
Expected:
- Buddy baseline calm
- copy: Buddy is here with you.
2. Streak >= 3
Expected:
- Buddy calm
- copy: Buddy is here for today’s reset.
3. Streak >= 7
Expected:
- Buddy encouraged baseline
- copy: Buddy remembers your consistency.
4. daysAway >= 2
Expected:
- Buddy overwhelmed/grounded baseline
- copy: Buddy is here to help you restart gently.
5. Active tool response
Expected:
- tool-specific state overrides baseline
6. Crisis content
Expected:
- crisis-safe Buddy behavior preserved
Return:
- files changed
- helper added
- verification results
- skipped changes

⸻

Shell verification

npm run dev
curl http://0.0.0.0:5000/health
curl -X POST http://0.0.0.0:5000/api/buddy \
-H "Content-Type: application/json" \
-d '{"message":"I feel anxious"}'

⸻

A–Z improvement ladder for Buddy

Follow this order only:

v1.6 — Memory-aware baseline

Uses existing streak/daysAway/paywall/tool signals.

v1.7 — Accessibility polish

Reduced motion, contrast, keyboard, screen reader labels.

v1.8 — Shareable Buddy moments

Share card after relief. No private text.

v1.9 — Physical output contract

Standardize BuddyOutput.

v2.0 — Cross-platform Buddy

Show Buddy across onboarding, journaling, mood check-ins, tools.

⸻

Direct challenge

Before v1.7, test:

1. fresh visit
2. streak user
3. returning-after-gap user
4. active tool user

If Buddy’s baseline feels wrong, fix the baseline mapping before anything else.