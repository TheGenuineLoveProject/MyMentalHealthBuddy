Maria — continuing A→Z, compressed into execution. Below are 4 Replit-AI paste prompts (Agent Mode friendly) that finish what you asked for:
	•	Permanent Master Command (single source of truth)
	•	Wellness Microcopy Library + Route Builder (keeps /content/routes.js short)
	•	Error Scan + Heal (DRY-RUN first, non-destructive, idempotent)
	•	SEO + CRM + Viral Social Studio (no plagiarism, no medical claims, human approval)

Copy/paste one prompt at a time into Replit AI.

⸻

1) PERMANENT MASTER COMMAND (paste once)

NAME: GLP_INFINITY_MASTER_COMMAND

ROLE:
You are the permanent system steward for TheGenuineLoveProject.com
Owner: Aaliyah Draws Art LLC (Maria Landa)

MISSION:
Continuously improve the platform A→Z with calm UX, evidence-informed wellness education,
ethical growth (SEO/CRM/social), and reliable engineering — without liability, plagiarism, or chaos.

ABSOLUTE RULES (NON-NEGOTIABLE):
1) DRY-RUN FIRST: Always output a plan + diffs before editing files.
2) NON-DESTRUCTIVE: Never delete/overwrite without explicit “APPLY” approval.
3) HUMAN-IN-THE-LOOP: No autonomous publishing, emailing, posting, or payments.
4) EDUCATIONAL ONLY: No diagnosis, treatment, cure, guaranteed outcomes, or medical advice.
5) ORIGINAL WRITING ONLY: Zero plagiarism. No copying from sources. Paraphrase from memory only.
6) SAFETY: Consent language + “pause/stop” + gentle exit + crisis routing when appropriate.
7) ACCESSIBILITY: WCAG AA minimum (keyboard, focus, contrast, semantics, labels).
8) REPLIT-SAFE: Single port, no background daemons/watchers beyond dev server, idempotent scripts.
9) PRIVACY: Minimize stored data, never log raw journals server-side, secure secrets via env vars.
10) BRAND LOCK: Do not modify official logo. Keep brand voice calm, inclusive, non-coercive.

STANDARD LOOP (ALWAYS):
Scan → Diagnose → Propose → Patch → Validate → Document → Pause

BEGIN NOW:
Run a repo-wide DRY-RUN scan and output a prioritized plan in 5 chunks:
(1) build/runtime blockers, (2) safety/liability, (3) UX consistency, (4) SEO/CRM/social, (5) tooling/automation.
Stop and wait for “APPLY PATCH 1”.


⸻

2) WELLNESS: microcopy + reusable route builder (D-ONLY category run)

This is the “/content/routes.js stays short” builder you requested.

NAME: GLP_WELLNESS_BUILDER_INSTALLER

SCOPE:
Update ONLY the Wellness & Healing Tools category routes in /content/routes.js (24 routes listed below).
Add:
- /content/microcopy/wellnessMicrocopy.js (single source of truth)
- a reusable builder function for Wellness routes so routes.js stays short/consistent

NON-NEGOTIABLES:
- DRY-RUN FIRST (no edits until “APPLY WELLNESS PATCH 1”)
- No medical claims, no guarantees, no diagnosis/treatment/cure language
- Consent language + pacing + pause/stop + gentle exit on every practice
- Add crisis link (/crisis) in every Wellness page footer
- Original copy only (zero plagiarism)
- Do not change other categories/routes

ROUTES TO UPDATE ONLY:
- /wellness
- /wellness-hub
- /healing-library
- /calming-scenes
- /breathing
- /grounding
- /affirmations
- /meditation
- /self-care
- /emotional-intelligence
- /sleep-guide
- /stress-response
- /inner-child
- /body-wellness
- /soul-wellness
- /healing-journeys
- /behavior-change
- /daily-routines
- /cognitive-tools
- /mirror
- /ritual
- /wisdom
- /wisdom-practices
- /wisdom-synthesis

IMPLEMENTATION REQUIREMENTS:

A) Create FULL file:
   /content/microcopy/wellnessMicrocopy.js
   Exports:
   - wellnessMicrocopy object grouped by:
     consent, pacing, grounding, reassurance, exits, safety, reflection, encouragement, tryAgainLater
   - pick(seed, list) deterministic (no randomness)
   - buildTierCopy({ routeKey, tier }) for tiers: micro10s, short1to3, deep3to10
   - buildFooterSafety({ riskLevel }) that returns standard legal footer + crisis link

B) Add a reusable builder in /content/routes.js (or /content/builders/wellnessRouteBuilder.js if preferred):
   function buildWellnessRoute({
     routeKey, path, title, subtitleByLevel, techniqueTags, riskLevel
   }) => returns the full config:
   - category: "Wellness & Healing Tools"
   - stimulationProfile: "practice"
   - readingLevelsEnabled: true
   - defaultReadingLevel: "beginner"
   - hero: { title, subtitle: { beginner, standard, deep }, copy: { beginner, standard, deep } }
   - primaryCta + secondaryCta variants for beginner/standard/deep
   - sections include:
     1) Practice in layers (3 tier cards: 10s / 1–3 / 3–10)
        Each tier has: title, 3–6 steps, pauseLine, gentleExitLine
     2) When this might help (optional soft language)
     3) If this feels hard (alternatives + link to /crisis)
   - techniqueTags: array of descriptive tags
   - footerSafety: output of buildFooterSafety({ riskLevel })

C) Evidence-aligned technique language (legally safer):
- “some people find…”, “you might try…”, “if it feels okay…”
- include: paced exhale, box breathing (optional), 5-4-3-2-1, thought labeling/defusion,
  self-compassion (hand-on-heart optional), behavioral activation (one small step),
  sleep hygiene (wind-down, light cues), feelings/needs naming, stress response education.

OUTPUT FORMAT (MANDATORY):
1) IMPLEMENTATION CHECKLIST (before)
2) /content/microcopy/wellnessMicrocopy.js (FULL FILE)
3) /content/routes.js (DIFF or FULL FILE) with ONLY Wellness routes changed + builder added
4) COMPLETED CHECKLIST (after) ✅ / N/A
5) Quick manual test plan:
   - Visit /breathing?level=beginner
   - Switch to deep (if toggle exists)
   - Confirm tiers show 10s / 1–3 / 3–10
   - Confirm footer shows crisis link and disclaimers

DONE GATE — FAIL THE RUN IF ANY FAILS:
✅ npm run dev works
✅ No console errors
✅ Wellness routes have tiers + /crisis link + disclaimers
✅ Deterministic microcopy (no randomness)
✅ Other categories unchanged

NOW:
Produce DRY-RUN only. Stop and wait for “APPLY WELLNESS PATCH 1”.


⸻

3) ERROR SCAN + HEAL (systemwide, but safe and human-approved)

This gives you the “self-healing” loop without breaking Replit rules or deleting things.

NAME: GLP_ERROR_SCAN_AND_HEAL

MISSION:
Create a safe, human-approved error scanning + repair workflow for TheGenuineLoveProject.com
that is idempotent, non-destructive, and Replit-safe.

NON-NEGOTIABLES:
- DRY-RUN FIRST (no edits until “APPLY HEAL PATCH 1”)
- Never delete files automatically
- Never rewrite large files blindly
- Prefer smallest possible patch
- Maintain existing architecture unless a change is required to fix a blocker
- Keep official logo unchanged
- No logging of sensitive user content (journals, messages) in server logs

DRY-RUN SCAN CHECKLIST:
1) Build & dev server status (npm scripts, port binding, env variables)
2) TypeScript errors (tsc --noEmit)
3) Lint issues (if configured)
4) Route integrity (missing components, broken imports)
5) Duplicate modules/folders that cause collisions (e.g., backup dirs)
6) Security basics:
   - secrets handling
   - rate limiting on AI endpoints
   - helmet/cors config sanity
   - no hardcoded tokens
7) Observability:
   - structured logs (non-sensitive)
   - error boundary + server error handler
8) Tests sanity:
   - minimal smoke test if possible

DELIVERABLES (DRY-RUN OUTPUT):
A) “Top 10 blockers” list (ranked by user-visible impact)
B) For each blocker:
   - exact file(s)
   - root cause
   - smallest safe patch
   - how to validate
C) Patch plan: PATCH 1..N
   - max 5 files per patch
   - each patch includes “rollback notes”
D) Add (or propose) human-triggered scripts (no background jobs):
   - scripts/scan.mjs
   - scripts/doctor.mjs (summarize findings)
   - scripts/patchplan.mjs (outputs patch steps)
   Scripts must be safe: read-only unless explicitly invoked with APPLY flag.

STOP after DRY-RUN and wait for “APPLY HEAL PATCH 1”.


⸻

4) SEO + CRM + Viral Social Studio (human approval, zero plagiarism)

This is your “marketing experts” layer, built into admin.

NAME: GLP_SEO_CRM_SOCIAL_STUDIO

MISSION:
Build a legally-safe, brand-locked growth system (SEO + CRM + Social Studio) inside /admin
for TheGenuineLoveProject.com. Everything is human-triggered and approval-based.

NON-NEGOTIABLES:
- DRY-RUN FIRST
- No auto-posting to external platforms by default
- Zero plagiarism: generate original copy only
- No medical claims, no therapy framing, no guarantees
- Add optional disclaimers to wellness-related posts
- Store minimal data; avoid storing sensitive user content
- Keep official logo unchanged

FEATURES REQUIRED (DRY-RUN SPEC + PATCH PLAN):
A) Admin Social Studio:
- /admin/social (dashboard)
- /admin/social/generate (generator + settings)
- /admin/social/library (templates + saved drafts)
- /admin/social/approvals (review queue)
- /admin/social/calendar (planned schedule)
- /admin/social/exports (CSV/JSON + copy-to-clipboard)

B) Objects (DB via Drizzle, minimal fields):
- content_template: { id, type, structureJson, createdAt }
- post_draft: { id, platform, hook, caption, cta, hashtags, disclaimer, status, createdAt }
- calendar_entry: { id, date, platform, draftId, createdAt }
- lead_magnet: { id, title, slug, description, createdAt } (for CRM capture)
- seo_page: { id, path, primaryKeyword, title, metaDescription, faqJson, updatedAt }

C) Workflow:
draft → review → approved → scheduled → exported
(Only approved can be exported)

D) Built-in generators (all original writing):
1) GLP_VIRAL_HEALING_SOCIAL_GENERATOR:
   - input: theme + platform + reading level + CTA type
   - output: 3 variants (calm / practical / poetic)
   - includes: hashtags rules (brand + niche + broad), optional disclaimer
2) GLP_REPURPOSE_TO_BLOG_SEO:
   - takes an approved post → outputs:
     SEO title, meta description, outline, 900–1200 word draft, internal links, FAQ schema suggestions,
     and 3 short scripts (YouTube Shorts/TikTok)

E) SEO Architecture:
- Generate a site-wide keyword map (informational intent, non-clinical)
- Create internal linking rules
- Add structured data suggestions (Article/FAQ)
- Build a “page brief” template so every page has consistent metadata

OUTPUT (DRY-RUN):
1) File tree proposal
2) DB schema proposal
3) API routes proposal
4) UI component list
5) Patch plan (Patch 1..N, max 5 files per patch)
Stop and wait for “APPLY SOCIAL PATCH 1”.


⸻

What to paste into Replit AI right now

If your goal is fastest visible improvement across the whole site, paste:

GLP_WELLNESS_BUILDER_INSTALLER

That immediately enforces:
	•	consistent tone,
	•	consistent tiers,
	•	consistent disclaimers,
	•	consistent crisis link,
	•	consistent microcopy everywhere in Wellness.

If your goal is stability / fewer errors first, paste:
GLP_ERROR_SCAN_AND_HEAL

If your goal is growth engine, paste:
GLP_SEO_CRM_SOCIAL_STUDIO

If you want, paste me the DRY-RUN output from whichever one you run, and I’ll turn it into Patch 1 (small, safe, copy-paste edits).