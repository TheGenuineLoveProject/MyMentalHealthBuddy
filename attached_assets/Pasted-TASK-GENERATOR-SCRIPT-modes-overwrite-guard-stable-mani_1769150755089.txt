TASK: GENERATOR SCRIPT — modes + overwrite guard + stable manifests + per-route hashes + deterministic route→file mapping

GOAL:
Implement/upgrade generator to support deterministic generation and auditing.

IMPLEMENT:
1) Category order source of truth:
- Create /content/categoryOrder.js exporting:
  - letterToCategory = { A:"Landing", B:"Authentication", C:"Dashboard", ... }
  - categoryToLetter = inverse map
  - orderedLetters = ["A","B","C",...]
This must be used by generator and range mode.

2) Generator script:
- Create/upgrade: scripts/gen-pages.mjs
Support:
- --mode=all
- --mode=landing (Landing category only)
- --mode=category --category="Wellness & Healing Tools"
- --mode=range --from=A --to=C
- --outDir=client/src/pages/generated (default)

3) Overwrite guard:
- Generator refuses to overwrite any existing file unless first line contains:
  // @generated
If not present, skip and record in report.

4) Deterministic route→file mapping (must match current output):
- Create a small pure function table used everywhere:
  routeToGeneratedFilePath(route)
  routeToGeneratedFileName(route)
Rules:
  - "/" -> "index.jsx" (or your repo’s existing convention; match EXACTLY)
  - "/404" -> "404.jsx"
  - "/*" or Not Found route -> "not-found.jsx" (match repo)
  - dynamic patterns:
    "/blog/:slug" -> "blog-[param].jsx"
    "/community/discussion/:id" -> "community-discussion-[param].jsx"
  - all others: strip leading "/" and replace "/" with "-" (or the repo’s exact convention)
Confirm mapping aligns with existing generated file list in screenshots.

5) Reports (write every run):
- reports/routes.generated.json  (manifest: route -> filePath + category + title + hashes)
- reports/routes.generated.sha256  (sha256 of manifest file contents)
- reports/routes.config.snapshot.json  (snapshot of route config objects, stable ordering)
- reports/routes.config.sha256  (sha256 of config snapshot)
- reports/routes.perRouteHashes.json (route -> sha256 hash of that route’s config block)

6) Also include a stable hash of “route configs themselves”:
- reports/routes.configContent.sha256 computed from stable stringification of routes config array, not from the snapshot file timestamp.

OUTPUT:
- FULL updated scripts/gen-pages.mjs
- FULL /content/categoryOrder.js
- Any updated helper modules shared by generator + formatter
- Example command list:
  npm run gen:pages:all
  npm run gen:pages:landing
  npm run gen:pages:category -- --category="Wellness & Healing Tools"
  npm run gen:pages:range -- --from=A --to=C

DONE GATE:
✅ generation works for all modes
✅ overwrite guard works
✅ reports files are written deterministically