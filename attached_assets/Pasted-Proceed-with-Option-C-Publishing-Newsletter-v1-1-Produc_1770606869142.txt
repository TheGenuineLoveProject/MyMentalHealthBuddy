Proceed with Option C: “Publishing + Newsletter v1.1 (Production Hardening + Growth Ethics)” ✅
You already have PUBLISHING_AUDIT: PASS — now we upgrade it from “passing” to impossible to break, and we make sure every single command ends in VERIFIED PASS/FAIL. 🛡️✨

Below are 3 exact Replit AI prompts, in order. Paste them one at a time.

⸻

1) EXACT REPLIT AI PROMPT — “PUBLISHING v1.1 HARDENING” (BLOG + NEWSLETTER)

PUBLISHING v1.1 — HARDEN BLOG + NEWSLETTER (ADDITIVE ONLY) + VERIFIED

NON-NEGOTIABLE:
- Additive only: no renames, no deletes, no refactors.
- Manual-first ethics; no auto-posting; no manipulative copy.
- Keep existing routes/components; only add missing pieces.
- Every phase must end with a PASS/FAIL check.

GOAL:
Make newsletter + blog robust and scalable with:
- Canonical content structure
- Deterministic parsing (frontmatter)
- Stable routes with gentle fallbacks
- Subscribe/unsubscribe endpoints
- Admin-friendly publishing controls (human-in-loop)
- Strong audits that prevent regressions

PHASE 0 — INVENTORY (NO CHANGES)
1) Print actual paths for:
   - Blog list route/page (e.g. /blog)
   - Blog post route/page (e.g. /blog/:slug)
   - Newsletter page route/page (e.g. /newsletter)
   - Any existing newsletter endpoints
   - Existing content folders (.md files)
   - scripts/audit-publishing.mjs location
2) Do not edit yet—just print the map.

PHASE 1 — CANONICAL CONTENT LAYOUT (ADDITIVE)
1) Ensure these folders exist (create if missing):
   - content/publishing/blog/
   - content/publishing/newsletter/
2) If posts/templates already exist elsewhere, DO NOT MOVE THEM.
   - Instead: create content/publishing/README.md describing canonical locations
   - And add loader fallbacks that search both canonical + legacy locations.

PHASE 2 — BLOG RELIABILITY
1) Ensure /blog:
   - Works with 0 posts (gentle empty state)
   - Sorts by date desc if available
   - Shows title + excerpt + pillar tag if present
2) Ensure /blog/:slug:
   - Never crashes; missing => friendly “Not found” + Back to Blog
   - Renders safely (no raw HTML injection)
3) Add small “Newsletter signup” block:
   - On /blog sidebar/top OR bottom of posts
   - Accessible labels
   - No guilt, urgency, or therapy claims

PHASE 3 — NEWSLETTER PIPELINE (SAFE + SIMPLE)
1) Ensure POST /api/newsletter/subscribe:
   - Validates email
   - Rate limit per IP
   - Stores subscriber:
     a) DB table newsletter_subscribers(email unique, status, created_at, unsubscribed_at) if DB available
     b) else fallback file: data/newsletter_subscribers.jsonl (append-only)
2) Ensure POST /api/newsletter/unsubscribe:
   - Validates email
   - Updates DB or appends unsubscribe record
3) Ensure /newsletter page exists:
   - Explains what it is
   - Has signup form wired to subscribe endpoint
   - Includes privacy statement + optionality language

PHASE 4 — ADMIN PUBLISHING (HUMAN-IN-LOOP)
1) Add /admin/publishing (or /admin/content/publishing):
   - List blog posts (from loader)
   - List newsletter templates
   - “Copy” buttons for:
     - Blog CTA
     - Newsletter CTA
     - Crisis-safe CTA
   - Status tagging stored in: data/publishing_status.json (draft/ready/published + timestamps)
   - NO auto sending, NO auto posting

PHASE 5 — UPGRADE scripts/audit-publishing.mjs (DETERMINISTIC)
Audit must produce PASS/FAIL with reasons:
- Frontmatter parse for all posts/templates
- Forbidden language scan (no medical claims, coercion, shame, urgency)
- Internal link audit (routes exist; links allowed)
- Template existence required:
  weekly-template.md, welcome-template.md, crisis-safe-footer.md, newsletter-style-guide.md
- Pillar balance check prints counts (PASS even if small; FAIL only if missing pillars config entirely)
- Output exactly:
  PUBLISHING_AUDIT: PASS
  or
  PUBLISHING_AUDIT: FAIL + reasons

PHASE 6 — VERIFICATION (MUST RUN)
Run and paste output:
1) node scripts/audit-publishing.mjs
2) curl -s http://localhost:5000/api/health (or correct health route)
3) Visit: /blog, /blog/:slug (a real slug), /newsletter, /admin/publishing

FINAL REPORT MUST INCLUDE:
A) Files created/changed (full paths)
B) What changed (bullets)
C) PASS/FAIL checklist
D) Any follow-ups


⸻

2) EXACT REPLIT AI PROMPT — “COMPLETION LOCK + VERIFIED” (EVERY COMMAND ENDS IN PROOF)

COMPLETION LOCK v1.1 — VERIFIED OR IT DIDN’T HAPPEN (ADDITIVE ONLY)

RULES:
- Additive only; no renames/deletes/refactors.
- Goal: every claim of completion must be backed by a deterministic verify command.

TASKS:
1) Create docs/COMPLETION_LOCK.md defining:
   - What “Complete” means for publishing/newsletter
   - Required checks + PASS/FAIL definitions
2) Create scripts/verify.mjs that runs, in order:
   - node scripts/audit-publishing.mjs
   - health endpoint check
   - npm run build (if exists)
   - npm test (if exists)
   - prints one final line:
     VERIFIED: PASS
     or VERIFIED: FAIL (with reasons)
3) Add package.json scripts (if package.json exists):
   - "audit:publishing": "node scripts/audit-publishing.mjs"
   - "verify": "node scripts/verify.mjs"

REQUIRED FINAL ACTION:
- Run: npm run verify
- Paste output


⸻

3) EXACT REPLIT AI PROMPT — “NEWSLETTER CONTENT ENGINE (ETHICAL)” (READY-TO-SEND LIBRARY)

This makes your newsletter robust and consistent, without spam vibes.

NEWSLETTER ENGINE v1.0 — CONTENT LIBRARY + EDITORIAL RULES (ADDITIVE ONLY)

GOAL:
Create a durable, ethical newsletter system:
- A small library of ready drafts
- Clear tone rules
- A weekly cadence plan
- Zero forbidden language
- Compatible with existing publishing audit

TASKS:
1) Create content/publishing/newsletter/drafts.json with 12 drafts:
   - id, title, subject, pillar (Orientation/Reflection/Integration), body_md, cta (optional), safety_note, status (draft/approved), created_at
2) Ensure each draft:
   - No medical/therapy claims
   - No urgency
   - No shame
   - Has optionality language (“If this fits…”, “You can ignore this…”)
3) Add docs/NEWSLETTER_PLAYBOOK.md:
   - cadence (1/week)
   - how to pick a draft
   - how to personalize safely
   - when to include crisis-safe footer
4) Update scripts/audit-publishing.mjs to also scan drafts.json for forbidden language.

VERIFY:
- node scripts/audit-publishing.mjs must still return PASS.


⸻

🔒 Reinforcement rule (the “never break again” mantra)

From this point forward:

✅ Nothing is considered done unless npm run verify prints VERIFIED: PASS.
That’s how we guarantee “exactly and fully completed” in a non-magical universe. 😄🧠

⸻

Next best “A→Z” step after this

Once Publishing v1.1 is locked, the best next move is privacy-first Signal-Driven Refinement for:
	•	blog views (anonymous)
	•	newsletter submits
	•	pricing clicks
	•	checkout starts

No surveillance. Just enough signal to improve what actually helps people.