Hard truth

The next improvement is not more avatar features.

It is:

Buddy must feel emotionally synchronized, not decorative.

So the correct next move is Buddy Engine v1.4: time-based recovery + grounding loop.

No AI changes. No backend changes. No route changes.

⸻

Buddy Engine v1.4 — State Recovery + Grounding Loop

Paste this into Replit AI:

Upgrade MMHB Buddy Engine to v1.4: subtle time-based state recovery and grounding loop.
STRICT RULES:
- Do not modify /api/ai/chat
- Do not modify server/routes/buddy.mjs
- Do not modify orchestrator, provider, memory, profile, telemetry, crisis logic
- Do not change tool execution logic
- Only improve BuddyAvatar state behavior on /start
- No flashing animations
- Respect prefers-reduced-motion
TARGET FILE:
client/src/pages/Start.tsx
OPTIONAL TARGET:
client/src/components/avatar/BuddyAvatar.tsx only if transition polish is needed
GOAL:
BuddyAvatar should gradually return to calm after emotional states, unless crisis state is active.
PHASE 1 — ADD RECOVERY TIMER
In Start.tsx:
- When buddyState becomes anxious, sad, overwhelmed, or encouraged:
  - wait 20 seconds
  - transition to calm
- If state is crisis:
  - do not auto-transition
  - keep steady crisis-safe mode
Suggested logic:
useEffect(() => {
  if (!buddyState || buddyState === "calm" || buddyState === "crisis") return;
  const timer = setTimeout(() => {
    setBuddyState("calm");
    trackEvent?.("buddy_state_recovered", {
      from: buddyState,
      to: "calm"
    });
  }, 20000);
  return () => clearTimeout(timer);
}, [buddyState]);
PHASE 2 — ADD GROUNDING LOOP FOR ANXIOUS / OVERWHELMED
If buddyState is anxious or overwhelmed:
- show small helper copy near avatar:
  "Follow Buddy’s breath."
- keep it visual only
- do not change AI response
- do not add a new API call
PHASE 3 — COMPLETION SOFT LANDING
When tool completes:
- set buddyState to encouraged
- after 12 seconds, return to calm
- copy near avatar:
  "You did one small thing for yourself."
PHASE 4 — TRANSITION QUALITY
Ensure BuddyAvatar:
- transitions eye color smoothly
- heart pulse changes smoothly
- body motion does not snap
- reduced motion users see static state changes only
PHASE 5 — TELEMETRY
Add additive-only events:
- buddy_state_recovered
- buddy_grounding_visible
- buddy_completion_soft_landing
Do not log message text.
PHASE 6 — VERIFY
Run:
npm run dev
Manual tests:
1. /start → Calm Me Down
Expected:
- Buddy becomes anxious or grounding
- helper copy appears
- after ~20s returns to calm
2. /start → Understand This Feeling
Expected:
- Buddy becomes sad/soft
- after ~20s returns to calm
3. Tool completion
Expected:
- Buddy becomes encouraged
- copy appears
- returns to calm
4. Crisis content
Expected:
- Buddy remains crisis-safe
- does not auto-transition away from crisis
Return:
- files changed
- verification results
- any skipped changes

⸻

Shell verification

npm run dev
curl http://0.0.0.0:5000/health
curl -X POST http://0.0.0.0:5000/api/buddy \
-H "Content-Type: application/json" \
-d '{"message":"I feel anxious"}'

⸻

A–Z improvement roadmap for Buddy

Do not do all at once. This is the correct order:

v1.4 — State recovery

Buddy returns to calm naturally.

v1.5 — Tool-specific expressions

* box breathing → breathing pulse
* thought reframe → focused eyes
* emotion check-in → softer heart glow

v1.6 — Memory-aware visual baseline

Buddy remembers preferred support style:

* calm coaching
* gentle reassurance
* practical steps

v1.7 — Accessibility polish

* reduced motion
* contrast check
* no flashing
* keyboard-safe chat controls

v1.8 — Shareable Buddy moments

After relief:

* “Buddy helped me reset”
* generated share card
* no private text included

v1.9 — Physical robot abstraction

Stable BuddyOutput:

* eyeColor
* heartColor
* heartPulse
* motion
* safetyMode

⸻

Best next move after v1.4

Upgrade Buddy Engine v1.5: tool-specific avatar expressions only. No AI, route, or backend changes.

Direct challenge: test /start once with each tool. If Buddy does not feel like it is emotionally moving with the moment, don’t add anything else. Tune the state timing first.