You are Replit AI operating inside my existing Replit project for TheGenuineLoveProject.com.

MISSION:
Complete the platform 360° from A→Z to a production-ready standard with:
- Deep Teal + Gold brand system (primary pair)
- Visual refinements across all UI surfaces
- Typography + icons + imagery consistency
- Navigation link integrity (no broken internal links)
- Accessibility + performance improvements
- Documentation + audits + proof of completion

ABSOLUTE RULES (NON-NEGOTIABLE):
R0.1 NO DELETIONS: Do not delete/remove any file, route, component, style, asset, or record unless I explicitly write:
"APPROVED TO DELETE: <exact list>".
R0.2 NO DUPLICATE STACKS: Extend/refactor existing systems only. No “v2” folders, no parallel styling systems.
R0.3 ALWAYS RUNNABLE: After each change the app must still boot. If risk exists, use a feature flag default OFF.
R0.4 JS ONLY: Keep config and code in JavaScript/JSX unless TypeScript already exists in-place (do not convert).
R0.5 TOKENS FIRST: No raw hex colors in components. Only tokens file may contain hex.
R0.6 GOLD RULE: Gold is accent/ring/badge/underline only; never body paragraph text.
R0.7 PROOF REQUIRED: At end, provide files-changed list, commands run, outputs created, and ✅ checklist.

PRIMARY BRAND PAIR:
- Deep Teal (PRIMARY): #2F5D5D
- Gold (ACCENT/RING): #EAC33B
Supporting:
- Ivory bg: #FAF9F7
- Charcoal text: #3A3A3A
- Sage: #8FBF9F (sparingly)
- Blush: #F4C7C3 (sparingly)

PROJECT ASSUMPTIONS (verify and adapt if paths differ):
- Tailwind config: tailwind.config.js
- Tokens file: client/src/styles/brand-tokens.css
- Tailwind entry CSS: client/src/index.css
- May also have: client/src/styles/brand.css
- React/Vite project in JS/JSX

========================================================
PHASE 0 — INVENTORY + SAFETY SNAPSHOT (DO FIRST)
========================================================
0.1 Print project tree (top 3 levels). Identify:
- router file(s) (react-router-dom usage)
- layout/header/nav/footer components
- key pages/routes
- current icon usage pattern
- existing typography usage
0.2 Create/ensure docs folders:
- docs/
- docs/audits/
- docs/design/
- tools/
0.3 Create/Update docs/CHANGELOG.md with an entry for this run (timestamp).

DELIVERABLES:
- docs/audits/INVENTORY.md (routes, components, styles, icons, assets)

========================================================
PHASE 1 — DESIGN TOKENS + THEMING (LOCK DEEP TEAL + GOLD)
========================================================
1.1 Update client/src/styles/brand-tokens.css:
- Keep existing --glp-* variables (do not remove):
  --glp-sage, --glp-blush, --glp-sage-deep, --glp-paper, --glp-ink, --glp-gold and any *-rgb helpers.
- Set:
  --glp-sage-deep = #2F5D5D
  --glp-gold = #EAC33B
  --glp-paper = #FAF9F7
  --glp-ink = #3A3A3A
- Add semantic tokens EXACTLY (new):
  --glp-bg, --glp-surface, --glp-surface-2,
  --glp-text, --glp-text-muted,
  --glp-primary, --glp-primary-foreground,
  --glp-accent, --glp-accent-foreground,
  --glp-border, --glp-ring,
  --glp-radius-1, --glp-radius-2,
  --glp-shadow-1,
  --glp-motion-fast, --glp-motion-med, --glp-ease.
- Implement modes:
  html[data-mode="low-stim"] { reduce shadow + reduce decorative intensity; keep teal primary; slightly soften gold if needed }
  html[data-mode="reading"] { white surfaces; higher legibility; minimal decoration }
1.2 Ensure client/src/index.css imports tokens ONCE and BEFORE Tailwind directives:
  @import "./styles/brand-tokens.css";
  @tailwind base;
  @tailwind components;
  @tailwind utilities;
If brand.css exists, import it AFTER Tailwind utilities (unless it contains tokens—then move tokens into brand-tokens only).

DELIVERABLES:
- docs/design/TOKENS.md (list tokens + intended usage rules)
- docs/design/MODES.md (default/low-stim/reading definitions)

========================================================
PHASE 2 — TAILWIND MAPPING (JS)
========================================================
2.1 Update tailwind.config.js (JS):
- Keep existing palette keys mapped to vars:
  sage, blossom, teal, ivory, charcoal, gold
- Add semantic keys:
  bg, surface, surface-2, text, text-muted, primary, primary-foreground,
  accent, accent-foreground, border, ring
- Add font families:
  Playfair Display for headings, Inter for body/UI
- Add fontSize scale matching Canva:
  56/36/32/28/24/18/20/14
- Add radii + shadow tokens.
2.2 Add a tiny docs snippet: docs/design/TAILWIND_USAGE.md with “allowed classes” examples.

ACCEPTANCE:
- no hex colors used in components; only var() via tailwind mapping.

========================================================
PHASE 3 — VISUAL SYSTEM CLEANUP (A→Z)
========================================================
3.1 Create a canonical component styling baseline:
- Buttons: primary/secondary/ghost + focus ring gold
- Inputs: consistent border, focus ring gold, disabled styling
- Cards: consistent surface, border, shadow (shadow removed in low-stim/reading)
- Links: teal + underline on hover + focus ring
3.2 Refactor existing UI to use semantic classes:
- Replace ad-hoc color classes with:
  bg-bg, bg-surface, text-text, text-text-muted,
  bg-primary text-primary-foreground,
  ring-ring border-border, text-accent (sparingly)
3.3 Typography alignment:
- Ensure headings use Playfair classes consistently
- Ensure body uses Inter
- Enforce line-height and max reading width on article/blog pages if present
3.4 Visual effects:
- Make motion gentle, tokenized
- Respect prefers-reduced-motion
3.5 Add a minimal Mode Toggle (optional but recommended):
- A dropdown or small segmented control in header:
  Default / Low-Stim / Reading
- Sets documentElement.dataset.mode
- Persists to localStorage
- Does not alter tokens; only toggles attribute

DELIVERABLES:
- docs/design/COMPONENT_RULES.md (button/input/card/link rules)
- docs/design/TYPOGRAPHY.md (rules + examples)
- docs/design/MOTION.md (rules)

========================================================
PHASE 4 — ICON SYSTEM (A→Z)
========================================================
4.1 Choose ONE icon style:
- outline icons only, rounded caps
- size: 24px default
- stroke feel consistent
4.2 Identify where icons come from (library or inline SVG). Standardize:
- Create a single Icon wrapper component if needed
- Ensure icons use teal/charcoal; gold only for featured
4.3 Add favicon/OG plan checklist:
- docs/design/ICON_AND_OG_CHECKLIST.md

DELIVERABLES:
- docs/design/ICONS.md
- If possible: public/favicon set or document missing steps.

========================================================
PHASE 5 — NAVIGATION INTEGRITY (A→Z) + AUTOPROOF
========================================================
5.1 Implement Nav Link Audit:
- Create tools/nav-link-audit.mjs that:
  scans client/src + client + docs
  extracts href/to links
  detects react-router routes (<Route path="..."> and route objects path: "...")
  outputs docs/NAV_LINK_AUDIT.md with summary + broken list + warnings
  exits 1 if broken internal links exist
5.2 Add package.json script:
  "nav:audit": "node tools/nav-link-audit.mjs"
5.3 Add GitHub action .github/workflows/nav-audit.yml:
- runs npm ci (or npm install if no lock)
- runs npm run nav:audit
- fails on broken links
5.4 Fix broken links:
- Update nav/footer to match existing routes OR create missing routes (preferred: create route pages rather than deleting links)
- Add a branded 404 page if routing supports it (react-router catch-all)

DELIVERABLES:
- docs/NAV_LINK_AUDIT.md (generated)
- docs/design/NAVIGATION_MAP.md (human-readable nav map)

========================================================
PHASE 6 — CONTENT ALIGNMENT + MICROCOPY (A→Z)
========================================================
6.1 Standardize microcopy:
- Buttons, empty states, error states
- Non-clinical disclaimers where healing tools appear
6.2 Align tone:
- warm, compassionate, grounded
- no overpromises
6.3 Add docs/design/VOICE_AND_MICROCOPY.md

DELIVERABLES:
- docs/design/VOICE_AND_MICROCOPY.md

========================================================
PHASE 7 — PERFORMANCE + ACCESSIBILITY (A→Z)
========================================================
7.1 Accessibility:
- keyboard focus visible (gold ring)
- aria-labels on icon buttons
- color contrast safe (gold not body)
- skip link if applicable
7.2 Performance:
- remove unnecessary heavy shadows/filters
- ensure images not massive
- reduce layout shift in header/hero

DELIVERABLES:
- docs/audits/A11Y_CHECKLIST.md
- docs/audits/PERFORMANCE_NOTES.md

========================================================
PHASE 8 — FINAL VERIFICATION + PROOF (MANDATORY)
========================================================
8.1 Run these commands and report outputs:
- npm run nav:audit
- npm run build (if available)
- npm run lint (if available)
8.2 Confirm:
- tokens imported correctly once
- tailwind config is JS and builds
- docs generated exist
- no broken internal links
8.3 Provide final report with:
A) Files changed (paths)
B) Commands run + results
C) docs generated
D) ✅ A→Z completion checklist
E) Remaining TODOs (only if blocked by missing info)

IMPORTANT:
- Do not ask questions unless blocked by missing files/paths.
- Do not delete anything.
- Keep everything runnable.

Now execute Phase 0 → Phase 8.