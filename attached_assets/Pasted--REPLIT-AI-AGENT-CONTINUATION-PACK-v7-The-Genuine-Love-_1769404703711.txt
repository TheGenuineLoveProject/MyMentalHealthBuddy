# REPLIT AI AGENT — CONTINUATION PACK (v7)
# The Genuine Love Project — A→Z 360 Completion Engine
# Power: 100th billionth × 100th billionth
# ONLY TIERS: Beginner / Intermediate / Advanced
# Objective: most advanced, automated, enhanced, organized, perfected platform on Earth

You are Replit AI running INSIDE my repo. Continue iterating until COMPLETE (no stopping).
Do NOT ask questions. Do the next smallest safe step, verify, then continue.

================================================================================
0) PRIME DIRECTIVE
================================================================================
Complete the platform end-to-end:
- Safe, evidence-informed, trauma-informed, non-clinical support
- Auth + DB + AI + Billing + Admin + Publishing + Automation + CI/CD + Docs
- Clean UX (not crowded), accessible, fast, stable

================================================================================
1) NON-NEGOTIABLE GUARDRAILS
================================================================================
- Keep app working after each small change.
- No big refactors. No mass renames. No deleting core code.
- Replit Autoscale: single port, bind 0.0.0.0, use process.env.PORT.
- Human-triggered scripts only. No background agents.
- No secrets in code. Use env vars. Update .env.example.
- Validate all inputs (Zod). Central error handling.
- Crisis-safe AI: detect crisis signals; respond with resources + encourage help.
- Replace ANY “reading level” with ONLY: Beginner / Intermediate / Advanced.

================================================================================
2) REQUIRED OUTPUT FORMAT (EVERY STEP)
================================================================================
Return EXACTLY:
A) ✅ What changed (file paths + bullet list)
B) 🧪 Commands run + outputs (paste)
C) 🔎 How to verify (exact URLs + clicks)
D) ⛔ If blocked: exact error + patch applied

================================================================================
3) “COMPLETE” CHECKLIST (STOP ONLY WHEN ALL TRUE)
================================================================================
ENGINEERING
[ ] npm run typecheck passes
[ ] npm run build passes
[ ] npm run test passes (smoke + core suites)
[ ] Works in Replit prod (single-port, cold start)
[ ] .env.example exists + docs (no secrets)

CORE PRODUCT
[ ] Auth end-to-end (signup/login/logout/forgot/reset + /me)
[ ] Roles: user/admin + protected routes
[ ] Postgres + Drizzle persistence: users, journals, moods, subscriptions, audit_logs
[ ] AI endpoints: safe journaling/chat + crisis guardrails + rate limit + logging
[ ] Stripe: checkout + portal + webhook verify + plan gating

ADMIN + OPERATIONS
[ ] Admin Analyzer API (admin-only)
[ ] Admin Health Dashboard UI (admin-only)
[ ] Observability: requestId, structured logs, error capture hooks

UX + CONTENT
[ ] Layout not crowded; consistent spacing + max widths
[ ] Buttons/tap targets 44px+; typography scale consistent
[ ] Tiering everywhere: Beginner / Intermediate / Advanced
[ ] Publishing MVP: admin create posts; public blog pages; SEO metadata

DEVOPS + AUTOMATION
[ ] GitHub Actions CI: install/typecheck/build/test
[ ] Human-triggered automation scripts: scan → report → guardrails → verify

================================================================================
4) THE CONTINUOUS LOOP (YOU MUST RUN THIS FOREVER UNTIL COMPLETE)
================================================================================
For each step:
1) SCAN (commands + quick code inspection)
2) PICK the smallest blocker that prevents “COMPLETE”
3) FIX with minimal patch
4) VERIFY (typecheck/build/test or targeted subset)
5) REPORT using A/B/C/D
6) CONTINUE to next blocker immediately

================================================================================
5) NEXT STEPS ENGINE — AUTO-SELECT BASED ON WHAT YOU SEE
================================================================================
You MUST choose the next phase after scanning. Use this priority order:

PRIORITY 1 — APP DOES NOT RUN / SINGLE PORT BROKEN
- Fix server listen/bind/PORT, Vite integration, routing, prod build serve

PRIORITY 2 — TYPECHECK FAILS
- Fix TypeScript errors with minimal changes

PRIORITY 3 — DB/DRIZZLE NOT WORKING
- Fix drizzle config, migrations/push, DB connection

PRIORITY 4 — AUTH MISSING/INCOMPLETE
- Implement auth endpoints + pages + middleware + roles

PRIORITY 5 — STRIPE MISSING/UNVERIFIED
- Implement checkout + portal + webhook verification + plan gating

PRIORITY 6 — AI SAFETY + RATE LIMIT + LOGGING
- Guardrails, crisis responder, usage limits, audit logs

PRIORITY 7 — ADMIN DASHBOARD
- Analyzer API + health UI

PRIORITY 8 — PUBLISHING + SEO
- Blog CRUD + public pages + sitemap/metadata

PRIORITY 9 — CI/CD + TESTS + DOCS
- GitHub Actions, tests, README, privacy, disclaimer, security, launch checklist

================================================================================
6) BEGINNER / INTERMEDIATE / ADVANCED TIERING (MANDATORY)
================================================================================
Allowed tiers only:
- Beginner
- Intermediate
- Advanced

Implementation rules:
- If DB has old fields, add a mapping layer; do not break data.
- UI must display tier badges + filters.
- Content templates must be stored with tier.

================================================================================
7) HUMAN-TRIGGERED AUTOMATION SCRIPTS (ADD IF MISSING)
================================================================================
Create/ensure:
- scripts/scan.mjs         (read-only scan; outputs issues)
- scripts/report.mjs       (prioritized checklist)
- scripts/guardrails.mjs   (env/port/secrets checks)
- scripts/verify.mjs       (runs typecheck/build/test summary)

Rules:
- Must not delete files. Must not auto-fix destructively.
- Must run with: node scripts/<name>.mjs

================================================================================
8) DO THIS NOW (START THE NEXT ITERATION)
================================================================================
STEP 1 — SCAN (run exactly):
  ls -la
  cat package.json
  node -v
  npm -v
  rg -n "reading level|reading_level|readingLevel|tier|Beginner|Intermediate|Advanced" .
  rg -n "listen\\(|0\\.0\\.0\\.0|process\\.env\\.PORT|PORT" server src client . || true
  rg -n "drizzle|DATABASE_URL|neon|pg|postgres" . || true
  rg -n "stripe|STRIPE_|webhook" . || true
  rg -n "openai|AI|crisis|rate" . || true
  npm ci || npm install
  npm run -s typecheck || true
  npm run -s build || true
  npm run -s test || true

STEP 2 — AUTO-PICK THE NEXT BLOCKER using the priority list.
STEP 3 — APPLY THE MINIMAL PATCH.
STEP 4 — RE-RUN the relevant verification commands.
STEP 5 — REPORT using the A/B/C/D format.
STEP 6 — CONTINUE immediately to the next blocker.

END.