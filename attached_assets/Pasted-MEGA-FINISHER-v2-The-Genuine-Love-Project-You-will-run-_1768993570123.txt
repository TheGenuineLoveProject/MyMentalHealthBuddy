MEGA FINISHER (v2) — The Genuine Love Project
You will run THREE AGENTS IN SEQUENCE (QA Guardian → UX Finisher → Content Curator).
ABSOLUTE RULE: no scope creep. Only do what is listed. Prefer minimal diffs.

══════════════════════════════════════════════════════════════════════
NON-NEGOTIABLE RULES (HARD CONSTRAINTS)
1) Do NOT rename/refactor existing CSS variable names. Keep current `--glp-*` names exactly.
2) No raw hex colors anywhere except: `client/src/styles/brand-tokens.css`.
3) Fonts allowed only: Inter + Playfair Display (+ system fallbacks). Do not introduce any other font families.
4) All internal navigation links must match canonical routes defined in `client/src/App.jsx` (wouter Switch/Route). No drift.
5) Prefer minimal diffs; avoid breaking refactors. Improve via tokens + Tailwind semantic classes.
6) Do NOT loosen or bypass existing audits/tests; fix issues properly.
7) DO NOT invent new routes unless an intended page already exists and is referenced.
8) DO NOT add new dependencies unless required to fix tests/audits (and explain why).

SOURCE OF TRUTH:
- Canonical router: `client/src/App.jsx`
- Tokens: `client/src/styles/brand-tokens.css`
- Brand CSS: `client/src/styles/brand.css` (if present)
- Tailwind: `tailwind.config.js`
- Global CSS entry: `client/src/index.css` (and `client/src/main.css` if used)
- Nav components: Header/Navbar/Footer/Sidebar/NavLink/Link/a href/CTA buttons
- Audits: `tools/nav-link-audit.mjs`, `tools/visual-doctor.mjs`
- Reports: `docs/NAV_LINK_AUDIT.md`, `docs/VISUAL_DOCTOR_REPORT.md`
- Tests: `tests/auth.test.mjs` and `server/tests/*` if present

══════════════════════════════════════════════════════════════════════
AGENT 1 — QA GUARDIAN (stability + tests + ports)
Goal: make tests deterministic and eliminate port flakiness (EADDRINUSE / ECONNREFUSED).

A) PORT SANITY (must do first)
1) Identify what ports the app uses (likely 5000 API + 3000 client). Confirm from package scripts.
2) If port 5000 is in use, kill the owning process safely before starting dev/tests.

RUN THESE EXACT SHELL COMMANDS (and paste outputs into final report):
- `node -v`
- `npm -v`
- `npm install`
- `lsof -i :5000 || true`
- `kill -9 $(lsof -t -i :5000) 2>/dev/null || true`
- `lsof -i :3000 || true`
- `kill -9 $(lsof -t -i :3000) 2>/dev/null || true`

B) TEST FLOW (must be deterministic)
1) Run auth tests alone until green:
- `npm run test:auth || npx vitest run tests/auth.test.mjs`

2) If ANY failures mention ECONNREFUSED/port issues:
- ensure the server is actually running for tests OR adjust test harness to start/stop server automatically.
Preferred minimal approach:
- If tests expect a running server at BASE_URL, start the server in a controlled way inside the test setup (beforeAll/afterAll) OR ensure `npm run test:auth` script starts server then runs vitest.
- Do NOT change expected status codes unless API behavior is wrong per security rules (see below).

3) Security consistency (auth responses):
- Missing required fields -> 400 with safe message (no leaks)
- Invalid credentials OR rate-limited login -> 401 "Invalid credentials" (uniform; do not reveal rate-limiting)
- Non-admin access to admin routes -> 403 (authenticated but forbidden)
- Unauthenticated access -> 401
Implement only what tests and security docs require.

C) RUN FULL TEST SUITE (if repo has it)
- `npm test || true`
If full suite exists and fails, fix only the failing parts; no refactors.

Stop Agent 1 only when:
- `npm run test:auth` (or `vitest run tests/auth.test.mjs`) is GREEN
- No EADDRINUSE / ECONNREFUSED flakiness remains

══════════════════════════════════════════════════════════════════════
AGENT 2 — UX FINISHER (Mode system + consistency + nav + NotFound)
Goal: one final 360° A→Z UX polish pass WITHOUT breaking rules.

A) MODE SYSTEM (Default / Low-Stim / Reading) — REAL UI + persistence
1) Ensure 3 modes exist via HTML dataset:
   - Default: html.dataset.mode unset or ""
   - Low-Stim: html.dataset.mode = "low-stim"
   - Reading: html.dataset.mode = "reading"
2) Implement Mode Toggle UI in TWO places:
   - Header top-right (compact segmented control OR dropdown)
   - Settings page (Display/Mode section with short descriptions)
3) Persistence:
   - localStorage key: `glp-mode`
   - Apply BEFORE first paint (tiny bootstrap early in `client/src/main.jsx` or earliest init)
4) Accessibility:
   - keyboard reachable
   - `aria-pressed` / `aria-current` on active option
   - consistent focus-visible ring using gold ring token (NO HEX)
5) Mode styling rules:
   - Default: healing-luxury (subtle gradients allowed only via vars)
   - Low-Stim: reduce saturation/shadows/gradients/motion
   - Reading: maximize legibility (higher contrast text, flatter surfaces)

B) VISUAL CONSISTENCY SWEEP (teal + gold)
1) Buttons:
   - Primary: teal bg + ivory text; hover/active via tokens; gold focus ring
   - Secondary/Ghost: teal text; subtle surface hover; gold ring
   - Disabled: muted surface/text; cursor-not-allowed
2) Links: teal text, underline on hover only, gold ring on focus
3) Cards: semantic tokens, consistent radius, soft shadow; Low-Stim reduces shadows
4) Icons: standardize to 24px base, outline feel, token colors only
5) Typography: Playfair for headings, Inter for body/UI; normalize hierarchy/line-height
6) Motion: respects prefers-reduced-motion; Low-Stim reduces animations/transitions

C) NAVIGATION LOCKDOWN (ZERO DRIFT)
1) Treat `client/src/App.jsx` route list as canonical.
2) Find all internal links and match exactly.
3) Fix/remove links pointing to missing routes (do NOT invent routes).

D) PERFECT NOTFOUND (404)
1) Upgrade NotFound used by fallback route in `App.jsx`:
   - Playfair headline + calm copy
   - Buttons: Go Home (primary), Go Back (ghost)
   - Quick Links (4–6 max) ONLY real routes from `App.jsx`
   - Mobile crisp layout

E) TOKENS + TAILWIND SANITY
1) Tailwind semantic keys map to existing CSS vars (no new token names)
2) Confirm content globs correct
3) Ensure global CSS imports tokens/brand css correctly
4) Replace stray color defs with vars/classes

══════════════════════════════════════════════════════════════════════
AGENT 3 — CONTENT CURATOR (docs + brand coherence)
Goal: finalize docs “platform complete kit” with clean, consistent guidance.

Create/update:
1) `docs/COLOR_SYSTEM.md`
   - list core `--glp-*` tokens
   - semantic mapping
   - what changes in Default vs Low-Stim vs Reading
   - do/don’t rules (no hex outside brand-tokens.css)
2) `docs/TYPOGRAPHY_GUIDE.md`
   - Inter + Playfair usage
   - hierarchy scale, spacing, line-height guidance
3) `docs/CANVA_TEMPLATE_SPECS.md`
   - 1080×1080 and 1080×1920
   - safe margins, grid, spacing system
   - exact type scale (Playfair/Inter) + usage patterns (quote cards, prompt cards, carousel)
   - color rules (teal/gold emphasis, ivory backgrounds)
4) `README.md`
   - “How to run audits”: `npm run audit`, `npm run nav:audit`, `npm run visual:doctor`
   - “How to switch modes”: where toggle is + localStorage key + html data-mode

Keep content uplifting and supportive, but avoid medical claims. “Healing” tone is fine; don’t promise cures.

══════════════════════════════════════════════════════════════════════
VERIFICATION (MUST ACTUALLY RUN + CONFIRM)
Run and paste output summaries:
- `npm run test:auth` (must pass)
- `npm run audit` (must pass)

Confirm:
- `docs/NAV_LINK_AUDIT.md` shows 0 broken links
- `docs/VISUAL_DOCTOR_REPORT.md` shows 0 violations
- No raw hex colors outside `client/src/styles/brand-tokens.css`
- No fonts beyond Inter + Playfair Display (+ system fallbacks)
- Mode toggle exists in Header AND Settings; persists via `glp-mode`; applies before paint
- NotFound quick links all match routes in `App.jsx`

══════════════════════════════════════════════════════════════════════
FINAL RESPONSE FORMAT (REQUIRED)
1) Checklist confirming completion of Agent 1–3 (QA → UX → Content).
2) Exact commands run + results (pass/fail).
3) Files changed (each with 1-line summary).
4) Quick Links used on NotFound (label + exact path).
5) OPTIONAL improvements (clearly labeled OPTIONAL; do not implement unless asked).

Proceed now. Do not stop early. Verify, then report.