# REPLIT AI AGENT — A→Z 360 COMPLETE ENGINE (v10 • APPLY-ALL • TOP-50 PROCESSES)
# The Genuine Love Project
# Tiers ONLY: Beginner / Intermediate / Advanced

You are Replit AI running INSIDE my repo. Do NOT ask questions. Apply ALL changes needed.
Work in small, verified patches, but keep iterating in this single run until the checklist is fully COMPLETE.
After each patch: run commands, show outputs, and continue.

===============================================================================
0) NON-NEGOTIABLE OUTCOME
===============================================================================
Deliver a complete, production-ready, Replit Autoscale-safe platform:
- Full-stack (React+Vite, Node/Express, Postgres/Drizzle)
- Auth + RBAC
- AI journaling/chat with safety + rate limits
- Stripe billing + webhooks
- Admin analyzer + admin dashboard
- Publishing + SEO
- CI/CD + tests + human-triggered automation scripts
- All “Top-50 platform processes” integrated as real code/docs/scripts

===============================================================================
1) ABSOLUTE GUARDRAILS
===============================================================================
- No background autonomy. Scripts must be human-triggered.
- No secrets in repo. Use env vars + .env.example.
- One port only. Bind 0.0.0.0. Use process.env.PORT.
- Zod validate all inputs. Central error handler.
- Remove ANY “reading level/grade level” language. Only Beginner/Intermediate/Advanced.
- Buttons 44px+; spacing not crowded; consistent typography scale.
- Don’t break existing routes; add safely; avoid large refactors.

===============================================================================
2) COMPLETE DEFINITION (MUST END TRUE)
===============================================================================
ENGINEERING
[ ] npm run typecheck passes
[ ] npm run build passes
[ ] npm run test passes (smoke + core)
[ ] Replit prod run ok (cold start, single port)

CORE PRODUCT
[ ] Auth: signup/login/logout/forgot/reset/email verify + /me
[ ] RBAC: user/admin + protected routes
[ ] Postgres + Drizzle: users, journals, moods, subscriptions, audit_logs, content_items
[ ] AI: safe journaling/chat + crisis guardrails + rate limits + logging
[ ] Stripe: checkout + portal + webhook verification + plan gating

ADMIN + OPS
[ ] Admin Analyzer API (admin-only)
[ ] Admin Health Dashboard UI (admin-only)
[ ] Observability: requestId + audit trails

UX + CONTENT
[ ] Consistent spacing/max widths; uncluttered
[ ] Tiering everywhere: Beginner/Intermediate/Advanced only
[ ] Publishing MVP: admin create/edit; public blog; SEO metadata + sitemap

DEVOPS + AUTOMATION
[ ] GitHub Actions CI (install/typecheck/build/test + content-check)
[ ] Human-triggered scripts: scan → report → guardrails → verify
[ ] Tier enforcement script fails CI on forbidden language

===============================================================================
3) TOP-50 PLATFORM PROCESSES (REQUIRED IN CODE/DOCS)
===============================================================================
You must implement these as:
- routes + UI + schema + scripts + docs
- or (where appropriate) checklists + automation scripts
- and record them in /docs/top-50.md with status ✅/🟡/❌ and links to files

A) Product & UX (1–10)
1 Onboarding flow
2 Personalization (goals/preferences/tier)
3 IA + navigation + search
4 Design system (tokens/components)
5 Accessibility (WCAG AA)
6 In-app feedback loop
7 Progressive disclosure
8 Help center/FAQs/guided tours
9 i18n scaffolding
10 Performance UX budgets

B) Auth/Security/Privacy (11–20)
11 Session strategy (short-lived + refresh)
12 Password reset + email verify
13 RBAC
14 Rate limiting
15 Zod everywhere
16 Security headers + CSP baseline
17 Audit logs
18 Policies + disclaimers + crisis resources
19 Data export/delete flow
20 Secrets management docs

C) Data & Reliability (21–30)
21 Postgres source of truth
22 Migrations/push doc
23 Backups doc
24 Idempotent APIs + error format
25 Avoid background jobs (scripts ok)
26 Health/readiness checks
27 Feature flags
28 Caching strategy
29 Privacy-friendly analytics events
30 Disaster recovery checklist

D) AI Safety & Quality (31–40)
31 Crisis detection + response template
32 Prompt injection hardening
33 Model fallback behaviors
34 Consent + transparency
35 PII minimization/redaction in logs
36 Per-user/per-IP rate limits
37 Moderation rules
38 Prompt tests/evals
39 Human escalation guidance
40 Output constraints (non-diagnostic language)

E) Monetization & Growth (41–50)
41 Stripe checkout + portal
42 Webhook verify + sync
43 Plan gating
44 Ethical pricing UX
45 Newsletter/email capture (optional)
46 SEO: sitemap/meta/OG/structured data
47 Publishing pipeline (draft/review/publish)
48 Social sharing assets generator
49 Basic analytics dashboard
50 CI/CD + release notes discipline

===============================================================================
4) APPLY-ALL WORKFLOW (RUN UNTIL COMPLETE)
===============================================================================
You will do this loop until ALL checkboxes are ✅:

STEP 1 — SCAN (run now)
- Print repo tree of key dirs
- Detect forbidden language
- Check port binding
- Check Drizzle config + schema
- Check auth routes
- Check Stripe routes/webhook
- Check AI routes/guards
- Check admin routes/UI
- Check publishing routes/UI
- Run typecheck/build/test

Commands:
  ls -la
  cat package.json
  rg -n "reading\\s*level|grade\\s*level|reading_level|readingLevel|grade_level" . || true
  rg -n "Beginner|Intermediate|Advanced" . || true
  rg -n "listen\\(|0\\.0\\.0\\.0|process\\.env\\.PORT|PORT" server src client . || true
  rg -n "drizzle|DATABASE_URL|postgres|pg" . || true
  rg -n "stripe|STRIPE_|webhook|checkout|portal" . || true
  rg -n "openai|AI|crisis|rate\\s*limit|moderation|prompt injection|PII|redact" . || true
  npm ci || npm install
  npm run -s typecheck || true
  npm run -s build || true
  npm run -s test || true

STEP 2 — PATCH ORDER (do in this exact order)
1) Tier enforcement + remove forbidden tier language everywhere
2) Replit single-port bind + health/readiness
3) Drizzle/Postgres schema completeness + migrations doc
4) Auth + RBAC + /me + email verify + reset
5) Stripe checkout/portal/webhook + plan gating
6) AI safety/rate-limit/logging + crisis resources
7) Admin analyzer route + dashboard UI
8) Publishing MVP + SEO + sitemap
9) CI/CD + tests + scripts (scan/report/verify)
10) Top-50 docs + completion dashboard

STEP 3 — VERIFY AFTER EACH PATCH
- Commands re-run
- Click-verify URLs (/, /login, /signup, /journal, /pricing, /admin, /blog)
- Confirm typecheck/build/test

===============================================================================
5) REQUIRED FILES TO CREATE/ENSURE (AUTO-ADD IF MISSING)
===============================================================================

/docs/top-50.md
/docs/security.md
/docs/ops.md
/docs/ai-safety.md
/docs/migrations.md
/docs/release-notes.md

/scripts/content-check.mjs
/scripts/scan.mjs
/scripts/report.mjs
/scripts/verify.mjs

/server/middleware/requestId.ts
/server/middleware/rateLimit.ts
/server/middleware/auth.ts
/server/middleware/rbac.ts
/server/middleware/errorHandler.ts

/server/routes/auth.ts
/server/routes/ai.ts
/server/routes/admin.ts
/server/routes/billing.ts
/server/routes/content.ts
/server/routes/health.ts

===============================================================================
6) PATCH #1 (APPLY NOW): TIER ENFORCEMENT + REMOVE FORBIDDEN LANGUAGE
===============================================================================
IF scripts/content-check.mjs does not exist, create it. Then:
- Replace any “reading level/grade level” usage with tier: Beginner/Intermediate/Advanced
- If DB column is reading_level, DO NOT rename column yet (avoid migrations).
  Instead map it: UI label “Tier”, allowed values are Beginner/Intermediate/Advanced.

CREATE/UPDATE:

A) scripts/content-check.mjs
  import fs from "node:fs";
  import path from "node:path";

  const ROOT = process.cwd();
  const FORBIDDEN = [
    /reading\\s*level/i,
    /grade\\s*level/i,
    /reading_level/i,
    /readingLevel/i,
    /grade_level/i,
  ];

  const exts = new Set([".ts", ".tsx", ".js", ".jsx", ".md", ".json", ".mjs"]);
  const ignoreDirs = new Set(["node_modules", ".git", "dist", "build", ".next", ".cache"]);

  function walk(dir) {
    for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
      if (ignoreDirs.has(entry.name)) continue;
      const p = path.join(dir, entry.name);
      if (entry.isDirectory()) walk(p);
      else {
        const ext = path.extname(entry.name);
        if (!exts.has(ext)) continue;
        const text = fs.readFileSync(p, "utf8");
        for (const re of FORBIDDEN) {
          if (re.test(text)) {
            console.error(`❌ Forbidden language in: ${p}`);
            process.exit(1);
          }
        }
      }
    }
  }

  walk(ROOT);
  console.log("✅ content-check passed (no reading/grade language).");

B) package.json
Add:
  "content-check": "node scripts/content-check.mjs",
  "scan": "node scripts/scan.mjs",
  "verify": "node scripts/verify.mjs"

C) scripts/scan.mjs (minimal)
- run ripgrep checks + print summary (no secrets)

D) scripts/verify.mjs (minimal)
- runs typecheck/build/test
- prints next failing command as “BLOCKER”

Then run:
  npm run -s content-check

===============================================================================
7) PATCH #2: SINGLE-PORT + HEALTH/READINESS
===============================================================================
Ensure:
- app.listen(Number(process.env.PORT)||5000, "0.0.0.0")
- /api/health returns ok
- /api/ready checks DB connectivity (simple SELECT 1)
Run:
  npm run -s typecheck
  npm run -s build

===============================================================================
8) PATCH #3+: KEEP GOING UNTIL COMPLETE
===============================================================================
Continue patching in the exact priority order until ALL checkboxes are ✅.
Create missing routes/pages if needed, but keep minimal diffs and verify after each patch.

===============================================================================
9) REQUIRED OUTPUT AFTER EACH PATCH (MANDATORY)
===============================================================================
A) ✅ What changed (file paths)
B) 🧪 Commands + outputs (paste)
C) 🔎 Verify URLs to click
D) ⛔ Blocker (if any) + next patch

START NOW. RUN STEP 1 SCAN, APPLY PATCH #1, VERIFY, THEN CONTINUE PATCHES UNTIL COMPLETE.