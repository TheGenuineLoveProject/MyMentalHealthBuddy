The advisor's note is mostly validation plus a warning: stop adding detection, start measuring. That matches the discipline in your preferences ("DRY-RUN FIRST", "if unsure, ask ONE clarifying question"). Two things worth flagging before I touch anything:

1. The one concrete code lever they slipped in — a defensive fallback for when inference returns nothing but a module did fire:

const PRIMARY_STATE = inferredStates[0] ?? modules[0];
Right now, when inference is weak (no indirect phrasing detected) but moduleRouter selected, say, anxiety_regulation or relationship, the tone falls through to default. That's a 5-line change in responsePolicy.mjs — purely additive, can't break the crisis lock, can't break what already works.

2. The "measure, don't build" pivot. You actually already have most of the substrate for this:

aiTelemetry.mjs is logging inferredStates, policy, toolId, moduleCount per call
events.jsonl whitelist already captures first_response_success, paywall_shown, paywall_clicked, streak_incremented
What's missing is a read surface — nothing aggregates these yet, so you can't actually answer "did users continue after the first line?" without grepping JSONL by hand
So the honest next moves, ranked smallest→biggest:

A. Tiny safety net (~10 min): the ?? modules[0] fallback above. Low risk, addresses the "anxiety branch under-mirrors when profile is sparse" gap the previous review already raised.
B. Add 2 missing telemetry events (~20 min): first_line_continued (user sends a 2nd message after first AI reply) and tool_completed (user reaches step N of N). These are the exact signals the advisor said to watch and you literally cannot measure them yet.
C. Admin metrics endpoint (~1 hr): GET /api/admin/metrics/funnel that aggregates events.jsonl + ai_telemetry.jsonl into the 4 numbers (validation continuation, tool completion, share rate, streak return). Read-only, admin-gated, no UI yet — just JSON you can curl.
Before I start: which of A / B / C do you want, or all three? I'd recommend A + B together (safety net + the missing events) and hold C until you actually have a few days of data to aggregate. But your call.
Tasks
Ready for review
Restore app from commit e3a322d0 into branch restore-recovered-app