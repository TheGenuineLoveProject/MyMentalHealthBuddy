You are working in the Replit project: TheGenuineLoveProject.
Goal: Fix all build/deploy blockers and fully apply the Genuine Love Project brand system.

NON-NEGOTIABLE RULES
- Do not delete features.
- Do not change ports or break Replit Autoscale rules.
- Do not add secrets to git. Use .env only.
- Make minimal, safe diffs. After each change, run the checks listed below.

PHASE 1 — FIX THE HARD BLOCKER (EJSONPARSE)
1) Open package.json and make it valid JSON.
   - Remove any pasted JavaScript, terminal output, or stray characters.
   - Ensure commas/quotes are correct.
2) Validate with: node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('package.json OK')"

PHASE 2 — PRODUCTION SESSIONS (remove MemoryStore warning)
Implement Postgres-backed sessions using connect-pg-simple.
1) Install: npm i connect-pg-simple
2) Update server/index.mjs (or server/index.ts) session config:
   - Use PG connection (DATABASE_URL)
   - Set cookie secure/sameSite properly for production
   - Keep dev experience working locally
3) Ensure the app still boots in Replit dev + deploy.

PHASE 3 — BRAND SYSTEM (Serenity Sage + fonts + visuals)
Apply this brand identity everywhere:

Brand:
- Name: The Genuine Love Project
- Tagline: Live in Genuine Love

Colors (exact):
- Serenity Sage: #8FBF9F
- Warm Blossom Pink: #F4C7C3
- Deep Teal: #2F5D5D
- Ivory Light: #FAF9F7
- Charcoal Deep: #3A3A3A
- Eternal Gold: #D4AF37

Fonts:
- Headings: Playfair Display
- Body/UI: Inter

Tasks:
1) Create/Update shared/brand.mjs (and shared/brand.ts if TS exists) exporting these constants.
2) Update Tailwind theme to include these colors and map primary/secondary/accent to the brand.
3) Update client CSS variables (shadcn/ui style) to use Serenity Sage as primary, Deep Teal as foreground/accent, Ivory as background, Charcoal as text, and Gold as highlight.
4) Update landing/hero UI copy to match brand voice:
   - compassionate, credible, transformational
5) Ensure logo in public/brand/ is used in the header/hero (fallback to text if not found).

PHASE 4 — VERIFY
Run:
- npm install
- npm run build (or the project’s build command)
- npm run start (or Replit run)
Confirm:
- No JSON parse errors
- No crash loops
- Home page renders and brand colors appear
- Sessions do NOT use MemoryStore in production mode

OUTPUT REQUIRED
- List every file changed
- Paste the final versions of the changed files
- Paste command outputs for the validation commands