ROGER_FINALIZE_TGLP_v1 (The Genuine Love Project)

Goal:
- Keep the app working (Landing renders).
- Eliminate remaining build/runtime errors.
- Centralize and enforce brand system (no hardcoded strings/colors).
- Finish Auth + DB + AI + Billing wiring (production-safe).
- Produce a “Launch Ready” checklist + verify build.

Rules:
- Do not expose .env values.
- Do not break existing routes/UI that already render.
- After each phase: run commands + report results + list files changed.

PHASE 0 — BASELINE SNAPSHOT
1) Run:
   - node -v
   - npm -v
   - npm run -s dev (confirm working)
   - npm run -s build (capture errors)
2) Paste outputs into docs/ops/PHASE0_BASELINE.md

PHASE 1 — BRAND SINGLE SOURCE OF TRUTH (NON-NEGOTIABLE)
1) Ensure shared/brand.ts (or .mjs) exports:
   - name, tagline
   - colors: primary, secondary, accent, background,I,
   - typography: headings/body
   - logo paths (public/brand/logo.svg, favicon.svg, og-image.svg)
2) Ensure client uses only BRAND:
   - Replace all hardcoded “The Genuine Love Project”, “Live in Genuine Love”
   - Replace all hardcoded hex colors with BRAND.colors.*
3) Update brand guard so it FAILS CI if violations exist.
4) Run: npm run -s brand:guard and fix until clean.

PHASE 2 — AUTH + SESSION HARDENING (PRODUCTION SAFE)
1) Confirm:
   - secure session cookies in prod
   - trust proxy in prod
   - SESSION_SECRET required in prod (no defaults)
2) Add “/api/auth/me” and ensure UI uses it to hydrate session state.
3) Add route guards for dashboard pages.

PHASE 3 — DATABASE + DRIZZLE CONSISTENCY
1) Confirm Drizzle config works and migrations apply cleanly:
   - drizzle-kit generate/push (whatever repo uses)
2) Confirm tables exist:
   - users
   - journals
   - aiMessages (or equivalent)
3) Ensure server routes use DB layer (no mock storage).

PHASE 4 — AI CHAT RELIABILITY + SAFETY UX
1) Confirm OpenAI SDK installed and imported correctly.
2) Implement:
   - normal chat route
   - crisis-safe route (returns resources + safe language; no therapy claims)
3) Persist messages to aiMessages (or existing table).
4) Add rate limit + requestId logging.

PHASE 5 — STRIPE BILLING HARDENING
1) Strict webhook signature verification.
2) Allowlist price IDs; prevent client-side tampering.
3) Customer portal route.
4) Pro gating enforced server-side.

PHASE 6 — BUILD/TEST/DEPLOY VERIFICATION
1) Run:
   - npm run -s test || true
   - npm run -s build
   - npm run -s start (production mode)
2) Fix all failures.
3) Write docs/LAUNCH_READY.md:
   - env vars required (names only)
   - run commands
   - deployment checklist
   - monitoring checklist