{
  "name": ".",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "diagnose": "node scripts/diagnose.mjs || echo diagnose stub",
    "heal": "node scripts/autoheal-json.mjs",
    "verify": "node scripts/verify.mjs || echo verify stub",
    "server:start": "npm --prefix apps/server run dev",
    "client:start": "npm --prefix apps/client run dev",
    "start:all": "concurrently -k \"npm --prefix apps/server run dev\" \"npm --prefix apps/client run start\"",
    "start:heal": "npm run heal && (npm ci --prefer-offline --no-audit || npm i --prefer-offline --no-audit); npm run start:all",
    "heal:json": "node scripts/autoHealJson.mjs",
    "fix": "node scripts/autoheal-json.mjs",
    "build": "npm --prefix apps/client run build && npm --prefix apps/server run build",
    "build:production": "npm --prefix apps/client run build && npm --prefix apps/server run build",
    "start": "NODE_ENV=production npm --prefix apps/server run start",
    "db:push": "npm --prefix apps/server run db:push"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^7.0.3",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  },
  "workspaces": [
    "apps/*",
    "ops/*",
    "shared/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@hookform/resolvers": "^5.2.2",
    "@stripe/react-stripe-js": "^5.3.0",
    "@stripe/stripe-js": "^8.3.0",
    "@tanstack/react-query": "^5.90.7",
    "@types/pg": "^8.15.6",
    "connect-pg-simple": "^10.0.0",
    "drizzle-kit": "^0.31.6",
    "drizzle-orm": "^0.44.7",
    "drizzle-zod": "^0.8.3",
    "express-session": "^1.18.2",
    "lucide-react": "^0.552.0",
    "openai": "^6.8.1",
    "pg": "^8.16.3",
    "react-icons": "^5.5.0",
    "stripe": "^19.2.1",
    "web-vitals": "^5.1.0",
    "wouter": "^3.7.1"
  }
}
