# Public-Safe SSR Remediation Governance Plan — Phase H2.7 (ANALYSIS ONLY)

> **Mode:** PUBLIC-SAFE SSR REMEDIATION SEQUENCING — analysis only, no runtime change, no wrapper, no centralization, no migration.
> **Sources:** `codex/storage/storageUsageAudit.json`, `codex/storage/storageHotspotClusters.json`, `codex/storage/storageStabilizationCandidates.json`, `codex/storage/storageSsrHydrationBlockers.json`.
> **Generated at:** 2026-05-30T23:26:24.663Z.
> **Scope:** Exactly the 17 public-safe files from H2.6. Protected/excluded/never-touch domains are out of scope.
> **No runtime change · no wrapper · no centralization · no migration.** This is a FUTURE sequencing plan only.

## Summary

| Metric | Value |
| --- | --- |
| Public-safe files | 17 |
| Independent candidates | 17 |
| Parallel-safe candidates | 17 |
| Shared-key coordination groups | 0 |
| Highest-confidence candidates (score ≥80) | 6 |
| By blocker profile | {"ssr-only":5,"hydration-only":2,"ssr+hydration":10} |
| Safety score range | 0–95 |

## Safest-first remediation ordering

| Order | File | Score | Blocker profile | Independent |
| --- | --- | --- | --- | --- |
| 1 | client/src/components/lumi/LumiCompanion.jsx | 95 | ssr-only | yes |
| 2 | client/src/pages/pathways/CalmPlan.jsx | 95 | ssr-only | yes |
| 3 | client/src/components/WellnessScore.jsx | 90 | ssr-only | yes |
| 4 | client/src/components/layout/Header.jsx | 85 | hydration-only | yes |
| 5 | client/src/lumi-memory/state/memoryStore.ts | 85 | ssr-only | yes |
| 6 | client/src/components/ui/CalmModeToggle.jsx | 80 | hydration-only | yes |
| 7 | client/src/components/GoalProgress.jsx | 70 | ssr+hydration | yes |
| 8 | client/src/components/SelfCompassion.jsx | 70 | ssr+hydration | yes |
| 9 | client/src/components/SleepTracker.jsx | 70 | ssr+hydration | yes |
| 10 | client/src/components/StressMonitor.jsx | 70 | ssr+hydration | yes |
| 11 | client/src/components/WorryTimeScheduler.jsx | 70 | ssr+hydration | yes |
| 12 | client/src/pages/GrowthAnalyticsPage.tsx | 70 | ssr-only | yes |
| 13 | client/src/components/AccessibilityToolbar.jsx | 65 | ssr+hydration | yes |
| 14 | client/src/components/DigitalDetox.jsx | 65 | ssr+hydration | yes |
| 15 | client/src/components/ValuesExplorer.jsx | 60 | ssr+hydration | yes |
| 16 | client/src/components/HabitTracker.jsx | 5 | ssr+hydration | yes |
| 17 | client/src/components/SelfCareChecklist.jsx | 0 | ssr+hydration | yes |

## Lowest-regret remediation path (independent, safest-first)

1. `client/src/components/lumi/LumiCompanion.jsx`
2. `client/src/pages/pathways/CalmPlan.jsx`
3. `client/src/components/WellnessScore.jsx`
4. `client/src/components/layout/Header.jsx`
5. `client/src/lumi-memory/state/memoryStore.ts`
6. `client/src/components/ui/CalmModeToggle.jsx`
7. `client/src/components/GoalProgress.jsx`
8. `client/src/components/SelfCompassion.jsx`
9. `client/src/components/SleepTracker.jsx`
10. `client/src/components/StressMonitor.jsx`
11. `client/src/components/WorryTimeScheduler.jsx`
12. `client/src/pages/GrowthAnalyticsPage.tsx`
13. `client/src/components/AccessibilityToolbar.jsx`
14. `client/src/components/DigitalDetox.jsx`
15. `client/src/components/ValuesExplorer.jsx`
16. `client/src/components/HabitTracker.jsx`
17. `client/src/components/SelfCareChecklist.jsx`

## Highest-confidence future candidates (score ≥ 80)

- `client/src/components/lumi/LumiCompanion.jsx` — score 95
- `client/src/pages/pathways/CalmPlan.jsx` — score 95
- `client/src/components/WellnessScore.jsx` — score 90
- `client/src/components/layout/Header.jsx` — score 85
- `client/src/lumi-memory/state/memoryStore.ts` — score 85
- `client/src/components/ui/CalmModeToggle.jsx` — score 80

## Independent & parallel-safe candidates

**Independent (no shared-key coupling):** `client/src/components/lumi/LumiCompanion.jsx`, `client/src/pages/pathways/CalmPlan.jsx`, `client/src/components/WellnessScore.jsx`, `client/src/components/layout/Header.jsx`, `client/src/lumi-memory/state/memoryStore.ts`, `client/src/components/ui/CalmModeToggle.jsx`, `client/src/components/GoalProgress.jsx`, `client/src/components/SelfCompassion.jsx`, `client/src/components/SleepTracker.jsx`, `client/src/components/StressMonitor.jsx`, `client/src/components/WorryTimeScheduler.jsx`, `client/src/pages/GrowthAnalyticsPage.tsx`, `client/src/components/AccessibilityToolbar.jsx`, `client/src/components/DigitalDetox.jsx`, `client/src/components/ValuesExplorer.jsx`, `client/src/components/HabitTracker.jsx`, `client/src/components/SelfCareChecklist.jsx`

**Parallel-safe (can be remediated simultaneously):** `client/src/components/lumi/LumiCompanion.jsx`, `client/src/pages/pathways/CalmPlan.jsx`, `client/src/components/WellnessScore.jsx`, `client/src/components/layout/Header.jsx`, `client/src/lumi-memory/state/memoryStore.ts`, `client/src/components/ui/CalmModeToggle.jsx`, `client/src/components/GoalProgress.jsx`, `client/src/components/SelfCompassion.jsx`, `client/src/components/SleepTracker.jsx`, `client/src/components/StressMonitor.jsx`, `client/src/components/WorryTimeScheduler.jsx`, `client/src/pages/GrowthAnalyticsPage.tsx`, `client/src/components/AccessibilityToolbar.jsx`, `client/src/components/DigitalDetox.jsx`, `client/src/components/ValuesExplorer.jsx`, `client/src/components/HabitTracker.jsx`, `client/src/components/SelfCareChecklist.jsx`

## Dependency relationships (shared-key coordination — soft)

_None — every public-safe file owns a distinct key set; all are independent._

## Per-file remediation governance

| Order | File | Domain | Keys | SSR blocker | Hydration blocker | Smallest additive pattern | Blast radius | Reversibility | Safety score | Rollback complexity | Migration readiness after | Why low-risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | client/src/components/lumi/LumiCompanion.jsx | lumi-ux | (dynamic) | bare localStorage access without typeof-window guard | none detected | Wrap the access in a `typeof window !== 'undefined'` guard, or move the read into useEffect. | local (single touch) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 95 | trivial (restore single guarded line; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (lumi-ux); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 2 | client/src/pages/pathways/CalmPlan.jsx | ux-preferences | glp_calm_plan | bare localStorage access without typeof-window guard | none detected | Wrap the access in a `typeof window !== 'undefined'` guard, or move the read into useEffect. | local (single touch) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 95 | trivial (restore single guarded line; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (ux-preferences); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 3 | client/src/components/WellnessScore.jsx | wellness-progress | last_wellness_score | bare localStorage access without typeof-window guard | none detected | Wrap the access in a `typeof window !== 'undefined'` guard, or move the read into useEffect. | single-file (few touches) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 90 | trivial (restore single guarded line; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (wellness-progress); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 4 | client/src/components/layout/Header.jsx | ux-preferences | glp-mode | none detected | storage read inside useState initializer (server/client value mismatch) | Move the initial read out of the initializer: default-init state, then hydrate via useEffect + setState after mount (two-pass). | local (single touch) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 85 | trivial (restore single guarded line; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (ux-preferences); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 5 | client/src/lumi-memory/state/memoryStore.ts | lumi-ux | (dynamic) | bare localStorage access without typeof-window guard | none detected | Wrap the access in a `typeof window !== 'undefined'` guard, or move the read into useEffect. | single-file (few touches) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 85 | trivial (restore single guarded line; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (lumi-ux); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 6 | client/src/components/ui/CalmModeToggle.jsx | ux-preferences | glp-calm-mode | none detected | storage read inside useState initializer (server/client value mismatch) | Move the initial read out of the initializer: default-init state, then hydrate via useEffect + setState after mount (two-pass). | single-file (few touches) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 80 | trivial (restore single guarded line; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (ux-preferences); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 7 | client/src/components/GoalProgress.jsx | wellness-progress | wellness_goals | bare localStorage access without typeof-window guard | storage read inside useState initializer (server/client value mismatch) | Default-initialize state to a deterministic value, then read storage in a post-mount useEffect under a `typeof window !== 'undefined'` guard (covers SSR + hydration in one pass). | single-file (few touches) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 70 | low (two-pass init revert is mechanical; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (wellness-progress); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 8 | client/src/components/SelfCompassion.jsx | tools-monitors | self_compassion_data | bare localStorage access without typeof-window guard | storage read inside useState initializer (server/client value mismatch) | Default-initialize state to a deterministic value, then read storage in a post-mount useEffect under a `typeof window !== 'undefined'` guard (covers SSR + hydration in one pass). | single-file (few touches) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 70 | low (two-pass init revert is mechanical; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (tools-monitors); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 9 | client/src/components/SleepTracker.jsx | tools-monitors | sleep_history | bare localStorage access without typeof-window guard | storage read inside useState initializer (server/client value mismatch) | Default-initialize state to a deterministic value, then read storage in a post-mount useEffect under a `typeof window !== 'undefined'` guard (covers SSR + hydration in one pass). | single-file (few touches) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 70 | low (two-pass init revert is mechanical; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (tools-monitors); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 10 | client/src/components/StressMonitor.jsx | tools-monitors | stress_monitor_data | bare localStorage access without typeof-window guard | storage read inside useState initializer (server/client value mismatch) | Default-initialize state to a deterministic value, then read storage in a post-mount useEffect under a `typeof window !== 'undefined'` guard (covers SSR + hydration in one pass). | single-file (few touches) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 70 | low (two-pass init revert is mechanical; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (tools-monitors); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 11 | client/src/components/WorryTimeScheduler.jsx | tools-monitors | worry_time_data | bare localStorage access without typeof-window guard | storage read inside useRef initializer | Default-initialize state to a deterministic value, then read storage in a post-mount useEffect under a `typeof window !== 'undefined'` guard (covers SSR + hydration in one pass). | single-file (few touches) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 70 | low (two-pass init revert is mechanical; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (tools-monitors); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 12 | client/src/pages/GrowthAnalyticsPage.tsx | analytics | (dynamic) | bare localStorage access without typeof-window guard | none detected | Wrap the access in a `typeof window !== 'undefined'` guard, or move the read into useEffect. | single-file (multiple touches) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 70 | trivial (restore single guarded line; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (analytics); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 13 | client/src/components/AccessibilityToolbar.jsx | ux-preferences | glp-a11y-settings | bare localStorage access without typeof-window guard | storage read inside useRef initializer | Default-initialize state to a deterministic value, then read storage in a post-mount useEffect under a `typeof window !== 'undefined'` guard (covers SSR + hydration in one pass). | single-file (few touches) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 65 | low (two-pass init revert is mechanical; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (ux-preferences); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 14 | client/src/components/DigitalDetox.jsx | tools-monitors | digital_detox_data | bare localStorage access without typeof-window guard | storage read inside useRef initializer | Default-initialize state to a deterministic value, then read storage in a post-mount useEffect under a `typeof window !== 'undefined'` guard (covers SSR + hydration in one pass). | single-file (few touches) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 65 | low (two-pass init revert is mechanical; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (tools-monitors); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 15 | client/src/components/ValuesExplorer.jsx | tools-monitors | values_explorer_data | bare localStorage access without typeof-window guard | storage read inside useState initializer (server/client value mismatch) | Default-initialize state to a deterministic value, then read storage in a post-mount useEffect under a `typeof window !== 'undefined'` guard (covers SSR + hydration in one pass). | single-file (multiple touches) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 60 | low (two-pass init revert is mechanical; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (tools-monitors); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 16 | client/src/components/HabitTracker.jsx | wellness-progress | habit_streaks, habits_completed_today, habits_last_date, wellness_habits | bare localStorage access without typeof-window guard | storage read during component init/render path; storage read inside useState initializer (server/client value mismatch) | Default-initialize state to a deterministic value, then read storage in a post-mount useEffect under a `typeof window !== 'undefined'` guard (covers SSR + hydration in one pass). | single-file (multiple touches) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 5 | low (two-pass init revert is mechanical; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (wellness-progress); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |
| 17 | client/src/components/SelfCareChecklist.jsx | wellness-progress | selfcare_completed, selfcare_date, selfcare_streak, selfcare_total_points | bare localStorage access without typeof-window guard | storage read during component init/render path | Default-initialize state to a deterministic value, then read storage in a post-mount useEffect under a `typeof window !== 'undefined'` guard (covers SSR + hydration in one pass). | single-file (multiple touches) | high (additive guard / two-pass init; revert by restoring original line or checkpoint) | 0 | low (two-pass init revert is mechanical; or checkpoint rollback) | Becomes a qualified lowest-blast-radius stabilization candidate (H2.5 gates) once SSR + hydration guards are added. | public, non-sensitive domain (wellness-progress); isolated single-file change; additive guard / two-pass init only — no API or data-shape change; fully reversible; no shared-key coupling |

### Verification requirements (apply per file, in a future authorized phase)

- **`client/src/components/lumi/LumiCompanion.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/pages/pathways/CalmPlan.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/components/WellnessScore.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/components/layout/Header.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/lumi-memory/state/memoryStore.ts`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/components/ui/CalmModeToggle.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/components/GoalProgress.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/components/SelfCompassion.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/components/SleepTracker.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/components/StressMonitor.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/components/WorryTimeScheduler.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/pages/GrowthAnalyticsPage.tsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/components/AccessibilityToolbar.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/components/DigitalDetox.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/components/ValuesExplorer.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/components/HabitTracker.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.
- **`client/src/components/SelfCareChecklist.jsx`**: npm run verify:all green (build + scans); route still returns 200; no React hydration warning in console (server/client parity); storage read/write value parity (persisted value identical before/after); behavior parity: storage-available path unchanged; storage-unavailable path degrades gracefully (no throw); reduced-motion / a11y unaffected on the page.

---

**Scope note:** Governance/sequencing only. No runtime code modified, no storage replaced, no wrapper/library added, nothing centralized or migrated. Plan covers ONLY the 17 public-safe files; auth, healing, crisis, journal, provider, billing, dashboard, admin, and chat are excluded. Crisis routing and business↔healing separation are unaffected. Companion docs: `docs/governance/storage/`, `codex/storage/`.
