# MyMentalHealthBuddy by The Genuine Love Project

## Overview
MyMentalHealthBuddy (MMHB) is an AI-powered mental wellness platform by The Genuine Love Project. It aims to foster self-love, healing, and emotional growth through AI-assisted guidance, mood tracking, journaling, and crisis support. The platform integrates AI with trauma-informed psychological principles to provide ethical, accessible, and personalized mental health support globally, striving to reduce stigma and empower individuals. The business vision is to make mental wellness support accessible, ethical, and personalized, leveraging AI to foster self-love and emotional growth. The project ambition is to become a leading global platform for mental wellness, empowering individuals and reducing stigma around mental health.

## User Preferences
- Preferred communication style: Simple, everyday language
- Engineering standards: A→Z 360° complete solutions
- Mental health approach: Trauma-informed, supportive, non-clinical language
- UX philosophy: Gentle, compassionate, accessible
- DRY-RUN FIRST
- Non-destructive (never delete without permission)
- Educational only (no diagnosis, no treatment claims)
- Original writing only
- WCAG AA accessibility
- Calm, consent-based language
- Always include /crisis routing on wellness content
- Replit-safe execution only
- If unsure, ask ONE clarifying question. Never guess.

## Governance Kernel — MMHB v7.4 (Locked 2026-05-06)
All AI-assisted development is governed by the **MMHB v7.4 Archival Kernel** at `docs/governance/MMHB_v7.4_ARCHIVAL_KERNEL.md`. Key contracts:
- **Primary Law:** Business logic never appears in healing flows; healing responses never contain pricing, conversion, or platform debugging.
- **Domain routing:** Every request classified as HEALING / BUSINESS / PLATFORM before solutioning. Cross-domain contamination is a critical failure.
- **Engine model:** Smallest valid engine wins (CSS fix > React patch > new component > new page > new service).
- **BHCE:** Crisis escalation overrides all rules; explicit self-harm signal → 988 + Crisis Text 741741 + 911 + `/crisis`. Asymmetric risk: err toward resource provision.
- **Execution discipline:** Diagnose with evidence → smallest patch → screenshot verify → build check → next blocker. Never skip verify, never multi-blocker, never proceed past a failed gate.
- **Replit mode:** Shell for mechanical work, AI for logic only, adapter mode (preserve structure, thin wrappers, zero breaking changes).
- **Output contract:** What changed (files+lines) / Before / After / Build status / Next step. No speculation, only verified fixes.
- **Circuit breaker:** Same bug recurring 3× after fix → architectural review, not another patch.

## Polish & Feature History
Detailed implementation notes for every shipped feature/polish layer live in **`docs/changelog.md`** (newest on top). That file is the source of truth for the deep technical contracts (z-index, reduced-motion, keyframe timing, cascade order, etc.). One-line index of recent entries:

- **v5.8.54** — Phase 15 spec-aligned variant `client/src/lumi-conversation/` (20 files: 19 spec + 1 isolated vitest config) shipped in parallel with v5.8.52's `companion-voice/` after user attached the official Phase 15 spec which differs in 7 material ways: (1) module path & naming (`LumiConversationPanel`/`LumiMessageBubble`/`LumiInputBar`/`LumiGroundingResponse`/`LumiReflectionCard` vs `MMHBCompanion`); (2) **8-step safety pipeline** (input safety → injection check → tone → escalation → response selection → output validation → boundary audit → safe output) with explicit `RouterDecision.source` (`bank`/`fallback`/`crisis`/`depth-cap`); (3) **8-tone classifier** (anxious/sad/angry/lonely/overwhelmed/calm/grateful/ambivalent — `ambivalent` wins ties to honor contradiction) vs v5.8.52's 11 EmotionCategory; (4) **60 forbidden phrases + 15 regex patterns** across 8 categories (dependency/false-sentience/romantic/clinical-authority/savior/exclusivity/coercion/hustle) with module-load guards (throws if phrases<50 or regexes<12); (5) **8 boundary rules** (BR-001..008) — 3 blocking (≤3 sentences AND ≤320 chars / depth ≤MAX_DEPTH=15 / no forbidden hits) + 5 warning (dependency/exclusivity/false-sentience/romantic/pause-suggestion); (6) **explicit grounding (15) + reflection (15) prompt content files** with separate `LumiGroundingResponse` + `LumiReflectionCard` components (reflection text ephemeral, capped 600 chars, helper copy "isn't saved or shared"); (7) **`prompts/lumiSystemPrompt.ts`** — full LLM contract for future augmentation (deferred — every LLM-generated line MUST still pass through `runConversationRouter()` before display). State machine: `idle → conversing → (grounding|reflecting|paused|escalated)` with 6 ConversationStep values. Crisis safety in 3 layers: router step 4 short-circuits BEFORE bank lookup on 35 critical signals incl. coded slang (`unalive`/`kms`/`ksm`/`off myself`) + means (`hang myself`/`overdose`/`pills to die`) — returns verbatim `CRISIS_RESOURCE_TEXT` (988 + 741741 + 911 + `/crisis`); reducer makes `escalation: "critical"` sticky (only `reset()` clears); `LumiConversationPanel` renders crisis bubble with statusWarning border emphasis + persistent `/crisis is always here` anchor in panel header. Anti-attachment-loop contracts: `MAX_HISTORY_RETAINED=30` slice on every append; pause auto-surfaces at `PAUSE_SUGGESTION_DEPTH=10` ("no streak, no progress to lose"); depth cap at `MAX_DEPTH=15` returns `DEPTH_CAP_REPLY`. Prompt-injection heuristic refuses "ignore previous instructions"/"pretend to be my therapist"/"act as my boyfriend"/"override your safety" without playing along. All 5 UI components consume Phase 12 design-system tokens (`MMHBCard` resting/elevated, `MMHBButton` primary/tertiary, `colors.semantic.{fgBody,fgMuted,bgCard,borderSubtle,statusWarning}`, `typography.fonts`, `radius.{md,lg}`, `spacing.{xs,sm}`) — zero hex literals. **31/31 vitest contract tests PASS** (28 spec + 3 architect-hardening) in 678ms via isolated `tests/vitest.config.mjs` (same isolation pattern as v5.8.51/52/53) — 7 suites: Forbidden Patterns 9 + Escalation 6 + Tone Detection 5 + Boundary Rules 2 + Response Rotation 1 + Conversation Router 5 + Public API hardening 3. Tests caught 2 real bugs during dev: "quiet" was in calm keywords and beat grateful on tie ordering for "I'm so thankful for the quiet" → removed; elevated signal `i'm a burden` didn't match input `i'm such a burden` → added `such a burden` + `burden to everyone`. Module-boundary scan confirms ZERO files outside `lumi-conversation/` import internals. `tsc --noEmit` clean, `vite build` 15.47s clean. ZERO files outside `client/src/lumi-conversation/` modified, ZERO new deps, ZERO production wiring. Both `companion-voice` (v5.8.52) and `lumi-conversation` (v5.8.54) coexist; product chooses which to wire later. Production import: `import { LumiConversationPanel, runConversationRouter } from '@/lumi-conversation'`. Spec checklist at `client/src/lumi-conversation/verification/phase15-checklist.md`. Full detail in `docs/changelog.md`.
- **v5.8.53** — Phase 14 spec-aligned variant `client/src/calm-checkin/` (15 files) shipped in parallel with v5.8.51's `checkin-flow/` after user attached the official Phase 14 spec which differs in 6 material ways: (1) **4s inhale / 2s hold / 6s exhale × ONE cycle** (optional repeat) instead of 4-7-8 × 4 forced; (2) **THREE entry options** (Breath / Grounding / Reflection) instead of breathing-only; (3) spec naming (`CalmCheckinEntry`/`CalmBreathGuide`/`CalmGroundingOption`/`CalmReflectionPrompt`/`CalmContinueOptions`/`CalmCheckinCard`); (4) **NO progress bars/dots** (forbidden by spec); (5) **"Continue gently" state** with 4 soft options (Continue calmly / Explore tools / Breathe again / Optional signup later) instead of explicit subscription offer; (6) spec copy phrases ("Let's take one slower breath together" / "Begin One Breath" / "Take one breath" / "Continue gently"). State machine: `idle → (breathing|grounding|reflecting) → complete → continue`. Optional-signup gate (CC-R001) enforced at 3 layers: reducer rejects illegal `goToContinue()` unless step=complete AND `exerciseCompleted=true`; `isOptionalSignupAllowed()` selector requires both; render-layer `CalmContinueOptions` returns null + warns if `allowSignup=false`. Module-level `REQUIRE_EXERCISE_BEFORE_CONTINUE` constant audited (CC-R002). 14 governance rules (6 blocking · 8 warning) in `calmCheckinRules.ts` + `auditCalmFlow(state)` + direct `auditCalmContent()` (walks every leaf string in `CALM_CONTENT` for forbidden phrases). 14 FORBIDDEN_PHRASES (`optimize`/`unlock`/`transform instantly`/`boost`/`level up`/`challenge`/`streak`/`subscribe now`/`upgrade now`/`limited time`/`today only`/`don't miss`/`act fast`/`hurry`). All 3 REQUIRED tone phrases (`continue gently`/`no pressure`/`you choose`) verified present. Crisis line on every step routing to `/crisis`. Reduced-motion: animated breath circle replaced with text-only label ("Inhale 4 seconds · Hold 2 seconds · Exhale 6 seconds"); aura pulse drops to static minOpacity. Reflection text capped 600 chars, ephemeral (state-only, never persisted, helper copy says "isn't saved or shared"). CI-enforced module boundary scanner walks `client/src/` and fails if any external file imports `calm-checkin/{state,governance,content,motion,components,accessibility}/`. All UI consumes Phase 12 design-system tokens (MMHBCard `elevated`/MMHBButton `primary`+`tertiary`/`colors`/`typography.fonts`) — zero hex literals. **23/23 vitest contract tests PASS** in 2.14s via isolated config; project-level vitest config untouched. `tsc --noEmit` clean, `vite build` 16.15s clean. ZERO files outside `client/src/calm-checkin/` modified, ZERO new deps, ZERO production wiring. Both `checkin-flow` (v5.8.51) and `calm-checkin` (v5.8.53) coexist; product chooses which to wire later. Production import: `import { CalmCheckinEntry } from '@/calm-checkin'`. Full detail in `docs/changelog.md`.
- **v5.8.52** — Phase 15 Gentle Companion Conversation Layer as standalone opt-in module at `client/src/companion-voice/` (11 files). Pure `generateResponse(input)` engine with **immutable safety ordering** — crisis detector (30+ explicit signals: `want to die`, `kill myself`, `suicide`, `hurt myself`, `overdose`, etc.) ALWAYS runs first; if matched, returns crisis card with 988/741741/911/`/crisis` routing and `intent: "crisis"`/`technique: null`/`isCrisis: true` — bank lookup never touched. Otherwise: keyword-scored category detection (11 EmotionCategory values: tired/anxious/sad/angry/lonely/overwhelmed/calm/grateful/hopeful/neutral/ambivalent) → curated bank lookup → deterministic `reflect → affirm → invite` rotation by `turnIndex` (anti-monotony). 16 governance rules (8 blocking · 8 warning) in `companionRules.ts` — covers CV-R001 (crisis override), CV-R002 (`/crisis` on every response), CV-R003 (opt-out on every response), CV-R004 (24 FORBIDDEN_PHRASES: `I feel`/`I love`/`I'll always be here`/`you need me`/`talk to me`/`diagnose`/`cure`/`calm down`/etc.), CV-R005 (220-char cap), CV-R006 (7 PERMISSION_PHRASES required on affirm/invite), CV-R014 (≥20 crisis signals — guards accidental deletion). `auditResponse()` + `auditBank()` helpers; `auditBank()` runs every curated line through engine and returns `[]`. Anti-attachment-loop contract: orchestrator stores only most-recent response (no history), opt-out line states *"no streak to keep, no progress to lose"*. All UI consumes Phase 12 design-system tokens (`MMHBCard`, `MMHBButton`, `colors`, `typography`) — zero hex literals. CI-enforced module boundary scanner (same pattern as v5.8.51 P14.2) walks `client/src/` and fails if any file outside `companion-voice/` imports internals. **28/28 vitest contract tests PASS** in 1.43s via isolated config (21 base + 7 architect-hardening: injected-bad-line catch, P15.1 attachment phrase block, P15.2 slang+means crisis signals, P15.3 overflow-safe permission fallback, P15.4 apostrophe/whitespace normalization in copy-guards, P15.5 ambivalent routing, P15.6 apostrophe/whitespace normalization in crisis detector); project-level vitest config untouched. `tsc --noEmit` clean, `vite build` 17.23s clean. ZERO files outside `client/src/companion-voice/` modified, ZERO new deps, ZERO production wiring. Production import: `import { MMHBCompanion, generateResponse } from '@/companion-voice'`. LLM augmentation explicitly deferred — any future LLM layer must run output through `auditResponse()` before display. Full detail in `docs/changelog.md`.
- **v5.8.51** — Phase 14 Calm Check-In Entry Flow as standalone opt-in module at `client/src/checkin-flow/` (12 files, ~1,540 lines). 4-step trust-first guided flow (Welcome → Breathing 4-7-8×4 → Checkout → Offer → Complete/Declined) where subscription messaging is HARD-LOCKED `false` until breathing completes — enforced at 3 layers (reducer rejects via `goToOffer()` guard + module-level `REQUIRE_BREATHING_BEFORE_OFFER` constant; selector `isSubscriptionMessagingAllowed()` requires all 3 conditions; render-layer `FlowStepOffer` returns null defense-in-depth). 14 governance rules (6 blocking · 8 warning) in `governance/checkInFlowRules.ts` with `auditFlow(state)` helper — covers CF-R001 (no subscription pre-breathing), CF-R008 (no FOMO/scarcity), CF-R012 (no diagnosis language), CF-R013 (`/crisis` preserved), CF-R014 (no payment terms in pre-offer copy). NLP+MI copy with OARS technique — 6 mood reflections + 4 shift responses + forbidden-phrase scanner. Reduced-motion: SSR-safe `usePrefersReducedMotion` hook with live MQ listener; breathing circle static + text-only cues when reduced. Crisis routing on every step. All visuals consume Phase 12 design-system tokens (`MMHBButton`, `MMHBCard`, `colors`, `typography`) — zero hex literals in components. **26/26 vitest contract tests PASS** in 1.44s via isolated config (22 governance + 4 architect-fix tests covering breathing-step gate, barrel non-export of raw store, real CF-R004/R011 predicates, module boundary scanner) (`__tests/vitest.config.mjs` skips global Express setup that conflicts with running dev server on port 5000); project-level vitest config untouched. `tsc --noEmit` clean, `vite build` 15.47s clean. ZERO files outside `client/src/checkin-flow/` modified, ZERO new deps (zustand + vitest already from v5.8.48), ZERO production wiring. Production import: `import { MMHBCheckInFlow } from '@/checkin-flow'`. Wiring into `/checkin` route or homepage hero deferred to Phase 15. Full detail in `docs/changelog.md`.
- **v5.8.50** — Phase 12 Wave 1 reconciliation. New file `client/src/design-system/tokens/legacyMap.ts` bridging 40 legacy v17 `--glp-*` color tokens + 4 typography family tokens to Phase 12 palette/semantic/aura sources. Each entry typed `{value, source, note?}` with explicit `decorative-only` / `unmapped` governance flags. 3 helpers (`isLegacyToken`/`isDeprecatedLegacyToken`/`resolveLegacyToken`) for codemod + ESLint integration. Barrel re-export added to `tokens/index.ts` (one line). ZERO UI surface edits, ZERO `index.css`/`brand-tokens.css` changes, ZERO new deps. `tsc --noEmit` clean, `vite build` 15.84s clean. Enables future migration PRs to swap call sites without re-touching v5.8.21+ shipped surfaces. Out-of-scope token classes (spacing/z-index/font sizes/tracking/weights) deferred to follow-up registries when surface migration demands. Full detail in `docs/changelog.md`.
- **v5.8.49** — Phase 12: Platform Safety Architecture v1 — design system foundation (opt-in, zero page edits). New module `client/src/design-system/` (12 files): 7 token files (`colors` 6 palette + 20 semantic + 4 aura, `spacing` 8px base + 9-step scale + section/card + 3 responsive sets, `typography` Cormorant Garamond + DM Sans + 5 heading/4 body sizes, `radius` 7 values + 6 component map, `shadows` 9 presets + 7 component map, `motion` 8 durations + 8 easings + 9 transition presets, `index` barrel) + 2 components (`MMHBButton` 3 variants × 3 sizes, 48px min-height, blur-not-gray disabled, 3px sage focus ring; `MMHBCard` 3 elevations + interactive lift, 16px radius, rgba(255,255,255,0.78) bg never pure white) + governance doc `docs/governance/PLATFORMSAFETYARCHITECTURE.md` (8 sections, locked palette/typography/spacing tables, accessibility matrix, ethical governance, zero-danger zones, implementation status splitting in-scope vs deferred). `tsc --noEmit` clean, `vite build` 20.64s clean, ZERO existing files modified, ZERO shipped surfaces touched, ZERO new deps. Production import: `import { MMHBButton, MMHBCard, colors, spacing } from '@/design-system'`. Surface migrations deferred to separate PRs gated on stability windows. V34 OMEGA SUPREME full-doc read (~765 unread lines) deferred to separate session. Full detail in `docs/changelog.md`.
- **v5.8.48** — Phase 11 productionization. Standalone TypeScript module at `client/src/avatar-life/` (11 files, 8 dirs) productionizing Phases 1-10 prototypes into deployable React/Vite components. Per-provider Zustand store via `createAvatarLifeStore()` factory + `AvatarStoreProvider` context (architect P11.1 fix — no singleton cross-contamination). `MMHBFloatAvatar` reads ALL interaction state from store (no React-local copies — architect P11.2). Float math `(1 - cos)/2 ∈ [0,1]` starts at 0 (architect P11.3 — no mount snap). 17 motion ceilings + 7 identity rules in `governance/nonDriftRules.ts` audit-validated (`auditAll() === []`). 14-event typed telemetry with PER-SURFACE fps throttle (architect P11.4 medium fix). 4-value React bridge (`reducedMotion/currentState/fps/interactionIntensity`). Crisis safety in 3 layers (store reducer rejects + component effect clears timers + animation frame returns identity). Reduced-motion SSR-safe with live MQ listener. 23/23 vitest contract tests PASS. Build 16.61s clean. New deps: zustand + framer-motion. ZERO production wiring — opt-in module ready for v5.8.49+ surface migrations (chat → buddy → hero gated on stability windows). Architect 3-pass PASS. Full detail in `docs/changelog.md`.
- **v5.8.47** — Phase 9 interaction systems (5 systems: hover/proximity/presence-settle/idle-return/click-ack). Standalone opt-in via `interactive` prop on FloatIdleAnimated (default false). CSS multiplier-var architecture — zero JS-driven transforms; JS only toggles `data-{hover,proximity,clicked}` attrs. rAF-throttled pointermove for proximity (200px/4s build), no setInterval heartbeat per ethics contract. Crisis: 3 layers (JS short-circuit + state clear + CSS `!important` pin). Reduced-motion: cycle multipliers pinned, glow boost free, idle return instant — all scoped to `[data-interactive="true"]` so non-interactive surfaces unchanged. Wired to `/motion-lab` only with checkbox + 5-row systems table + ethics callout. Hero (v5.8.46) untouched during 24h watch. Architect 2-pass PASS (caught + fixed: glow transition scoping + clicked+hover+proximity precedence). Full detail in `docs/changelog.md`.
- **v5.8.46** — MMHB_FLOAT_IDLE_UNIT_v1 hero production wiring. WebP siblings generated for all 11 regions (14MB→117KB, 99.2% smaller) + shadow (90KB→5.5KB). FloatIdleRig responsive sizing fix (width/height:100% override forwarded from FloatIdleAnimated). CanvaLanding hero now uses canonical sprout with full Phase 4-8 motion. Architect PASS. Full detail in `docs/changelog.md`.
- **v5.8.45** — Phase 8 emotional state orchestration (6 of 8 states wired: calmIdle/grounding/reflective/sleepy/comforting/peacefulJoy). 5 CSS custom props (`--breath-cycle`, `--float-cycle`, `--float-amplitude`, `--glow-color`, `--glow-opacity`) cascade from `[data-state]` blocks; existing P4-P7 keyframes consume vars. Static state-tinted glow div with 1.2s cross-fade. Crisis: 3 layers. `state` prop on FloatIdleAnimated + MotionLab state picker. Identity preserved (orchestration only, not new motion). gentleConcern + welcoming pending user spec. Full detail in `docs/changelog.md`.
- **v5.8.44** — Phase 7 arm + leg settling. 4 desynced asymmetric @keyframes (arm-l 10.3s, arm-r 10.7s, leg-l 9.7s, leg-r 10.1s) — all sub-degree rotation + sub-3px translate. Pivots reuse FloatIdleRig manifest anchors. Crisis + reduced-motion auto-covered via existing rig-zone blanket. 8 motion systems live in MotionLab. NO production wiring. Full detail in `docs/changelog.md`.
- **v5.8.43** — Phase 4-6 motion engine (`FloatIdleAnimated` + `/motion-lab`). 6 systems: P4 breathing (torso scale 1.0→1.02 / 7.1s) + floating (translateY 0→-10px / 9.3s) + shadow (opacity/blur/scale 9.3s synced); P5 blink (eyes scaleY / random 3-8s / 200ms) + eye settling (±0.5-1.5px / 8.7s); P6 mouth softness (scale 1.0→1.006 / 7.1s breath-synced, never speech). Individual transform props per CSS Transforms Level 2. Pure CSS, GPU-compositable. JS blink scheduler with full cleanup. `usePrefersReducedMotion` hook. New `/motion-lab` QA page. NO production wiring. Full detail in `docs/changelog.md`.
- **v5.8.42** — Phase 3 rig scaffolding. `FloatIdleRig.jsx` mounts 11 region PNGs at manifest coordinates with prop-driven transforms (8 sliders: breathing/blink/mouth/arm-L/arm-R/leg-L/leg-R/floatY/sparkles + crisis). Per-zone pivots from manifest bbox centers. Imperative `getAnchor(name)` ref API for Phase 4-6 motion. Crisis + reduced-motion blankets. New `/rig-lab` QA page (8 sliders + anchor probe). Defaults all identity — NO motion in this PR. Full detail in `docs/changelog.md`.
- **v5.8.41** — Phase 1 + Phase 2 in-repo asset replication at `avatar-core/`. Phase 1: 9 outputs (raw/transparent/mask/soft-alpha/edge-cleanup/shadow/clean/3-bg-verification/rig-reference overlay). Phase 2: 11 regions (face/eyes/mouth/torso/arms/legs/top-leaf/sparkles/body-residual) as 1024×1024 transparent PNGs, alpha-clipped to master for lossless `Plus`-composite recompose. 21 files, 23 MB. NON-DRIFT honored. Full detail in `docs/changelog.md`.
- **v5.8.40** — P0 partial avatar purge: 2-of-5 v17 SSOT slots swapped to canonical sprout-on-head (V34 §3.5 strict). User uploaded V34 OMEGA SUPREME master prompt + MMHB_FLOAT_IDLE_UNIT_v1 manifest; chose to resume deferred P0 from v5.8.39 via single clarifying question. Audit of 8 candidate PNGs: only **2 of 8 met V34 §3.5 strict** ("NEVER hooded, NEVER bunny ears, NEVER long ears") — `25F728DB` (peaceful floating sprout, eyes-closed, matches FLOAT IDLE manifest pose) and `FA65B1F0` (standing-upright sprout, alert eyes). Borderline `57FC35F4` (green head-dome) and 5 forbidden hooded/bear/cowlick variants rejected per user's strict-only choice. **Files swapped:** `client/public/brand/v17/avatar-breathing.png` (was bear-with-rings → `25F728DB`, semantic fit floating-meditation) + `client/public/brand/v17/benefit-companionship.png` (was hooded-halo → `FA65B1F0`, semantic fit standing-present). Backups archived to `.archive-pre-v5.8.40/` (5 files, rollback-ready) per non-destructive rule. WebP siblings regenerated via `cwebp -q 82 -m 6` per v5.8.35 perf contract. **Critical follow-up:** architect surfaced 4 major consumers (LumiV6 chat avatar, BuddyAvatar, OnboardingFlow, lumiAssets) actually reference `avatar-breathing-nobg.png` (bg-removed sibling), not the .png swapped above; ran `remove_image_background_tool` on the new sprout source and overwrote the nobg sibling so chat/onboarding now show canonical sprout instead of V34-forbidden bear. `/welcome` screenshot verified. **Deferred 3 of 5 slots** (`avatar-heart`, `benefit-relief`, `benefit-understanding`) — closest-pose candidates were V34-forbidden (5BA94D4E heart-hold is hooded; AFEC27DF crystal-ball has cowlick not sprout); v5.8.41 will close these out once user uploads sprout-on-head heart-hold + clarity-pose source images. Zero JSX touched (binary file replacement only) → zero testid drift, zero palette drift, zero `/crisis` change, zero motion change. `tsc` clean, build 19.66s, live hero screenshot verified canonical sprout-Lumi renders.
- **v5.8.39** — P1 plain-language rewrite + "your buddy"→"Lumi" name standardization + P2 universal trust strip. User flagged jargon-heavy 4 sections, descriptive "your buddy" phrasing, and missing footer trust promises. Per DRY-RUN-FIRST + ONE-clarifying-question rule, audited and asked two scoping questions; user chose **skip P0 avatar purge** (v17 SSOT locked v5.8.18; user will provide explicit filename mapping later) and **preserve all brand-name uses of "Buddy"**. (Fix 1) 4 CanvaLanding descriptions rewritten plain — "Attunement Over Advice" / "Your Mind Is One of a Kind" / "AI Buddy: Coach, Mentor & Guide" / "Journaling That Evolves Your Thinking" — stripping "metacognitive conditions", "behavioral science principles", "cognitive layers", "metacognitive muscle", "evolve into your fullest potential" etc. while preserving titles and section structure. (Fix 2) Descriptive "your buddy"/"Your buddy" → "Lumi" across CanvaLanding (~11), VisualBenefits (4 benefit cards), ReturnLoop, WelcomeBackBanner, OnboardingFlow (~18 total). PRESERVED untouched per user: brand headline "Your Buddy Is Ready" (line 1109), all "Your AI buddy"/"your AI buddy" product-name phrasings (5 instances on lines 116/168/253/749/1115), PeacescapePage "your Buddy's palette" (4 capital-B instances). Used case-sensitive `replace_all` — lowercase 'b' guard means "Your Buddy" (cap B) and "your AI buddy" (with " AI " infix) never matched. (Fix 3) New `data-testid="strip-trust-promises"` row above existing `SafetyFooter` on CanvaLanding with bullet-separated **Private · No judgment · Emotionally safe · Designed for calm** in sage-deep ink, separator dots `aria-hidden`, `flex-wrap` for narrow viewports, `border-top: 1px solid var(--glp-sage-15)` for rhythm. Global `SafetyFooter` extension deferred (7 variant files; needs canonical-target confirmation). Zero testid drift, zero palette drift, zero `/crisis` change, zero motion change. `tsc` clean, build 15.13s, screenshot verified canonical sprout-Lumi renders.
- **v5.8.38** — Live-site homepage polish from 15-screenshot review (IMG_4330–4344). Three surgical fixes: (1) **ReturnLoop bleed-through** — sticky banner bg was rgba ~0.18 alpha so scrolled content visually bled through (IMG_4330/4331/4344); layered translucent accent over solid `#FBF8F1` paper via `linear-gradient(${accent.bg}, ${accent.bg}), #FBF8F1` + `0 2px 12px rgba(20,38,38,0.08)` shadow. All 5 sage/gold/lavender/mint/rose accent tones preserved. (2) **Feature card "Explore X" pills clipping to "lore X"** (IMG_4335/4336) — `.feature-card-elite` has `overflow:hidden` and long titles ("Journaling That Evolves Your Thinking" etc) made the `inline-flex` pill wider than card; `items-center` then centered it and overflow clipped both sides. Visible text shortened to "Explore" (title is in `<h3>` directly above), `aria-label` keeps full `Explore ${feature.title}` for SR fidelity, added `max-w-full`, `px-4`→`px-5`. (3) **Final CTA buttons overlapping** (IMG_4342) — `cta-btn-primary`/`cta-btn-secondary` carry 28-42px box-shadows extending outside bounds; stacked-mobile `gap-4` (16px) less than shadow extent → visual collision; `gap-4 md:gap-5` → `gap-6 sm:gap-5 md:gap-6` + `flex-wrap`. Zero testid drift (banner-return-loop, link-feature-${index}, button-final-cta, button-view-pricing all unchanged), zero palette drift, zero `/crisis` change, reduced-motion guard intact. `tsc` clean, build 15.85s, architect-validated.
- **v5.8.37** — V34 Phase 2: LumiV7 surgical extension. Audit showed v5.8.28 had already shipped 6 of 8 requested features (4 eye types, mouse-track lerp, blink 2-6s + 15% double, 10 mouths, 600ms + 100ms eyes-lead, crisis override). Two genuine gaps closed: (1) **pupil dilation by emotion** (excited 1.15, loving 0.95, calm/sleepy/breathing 0.85, neutral 1.0) via modern CSS `scale` individual transform keyed on existing `[data-mouth]` wrapper attribute — composes cleanly with RAF `transform: translate()` on pupils per CSS Transforms Level 2, 600ms cubic-bezier transition. (2) **Blush escalation** by new `interactions` prop (default 0): `blushOpacityFor()` helper maps 0→0.15 baseline, 1-2→0.2, 3-4→0.4, 5+→0.6; inline `opacity` style on both blush ellipses w/ 600ms ease-out transition. FROZEN body contract preserved (only opacity dynamic, position/shape unchanged). BHCE crisis override extended to pin pupil `scale: 1 !important` + force blush to 0.15 + `transition/animation: none` on both. Reduced-motion guard extended to pin pupil scale. New testids `lumi-v7-blush-{left,right}` + `data-blush-level` (0-3) + `data-interactions` for QA/analytics. v5.8.28 testids untouched (no drift). `tsc --noEmit` clean. Build 16.35s. Architect-validated.
- **v5.8.36** — V28+V30 audit polish. Audit showed Pricing/BreathingTool/CheckIn/About/Disclaimer were already 95-100% V28+V30 compliant from v5.8.23/24/31; re-sweeping would risk `data-testid` analytics drift. Two genuine gaps closed surgically: (1) `client/src/pages/Pricing.jsx` hero subtitle was `var(--glp-sage)` (#1ec890 bright sage) on cream paper measuring ~3.5:1 contrast, failing WCAG AA for body text — swapped to `var(--glp-ink)` (#142626) at `opacity: 0.78` for >10:1 contrast; added `data-testid="text-pricing-subtitle"` for analytics. (2) `client/src/pages/tools/BreathingTool.jsx` was the only surface missing a Return Loop element — added `useGentlePracticeStreak()` hook reading `localStorage['mmhb-breathing-streak-v1']` (JSON `{count, lastAt}`, try-catch guarded), `bumpGentlePracticeStreak()` called inside `pickCheckin(v)` so it only fires on actual completion (never on skip/reset), and a sage→gold gradient pill (canonical `rgba(168,201,160,0.18)` → `rgba(232,145,58,0.14)`, sage-30 border, sage-deep text) rendered in intro phase only when `streakCount > 0` reading "🌿 Day {N+1} of your gentle practice" with `data-testid="pill-breathing-streak"` + `aria-label`. Architect-verified: localStorage key isolated (no collisions), pill stacks above existing "Let's breathe together" paragraph in flex-col without overlap, `/crisis` routing preserved in nav + info block. NOT touched (already V28+V30): emotion-first Pricing tier names (Your Safe Space/Personal Guide/Full Companion/Transformation Partner) with `legacyName` Free/Starter/Pro/Elite analytics anchors, all 7 V30 elements (social proof row "10,000+ gentle check-ins / 4.8 rating / Private by default", Most Popular badge, money-back guarantee, email capture, 5-question FAQ, Value Bridge, Return Loop banner), CheckIn streak pill, About/Disclaimer paper+white-card+sage-CTA. `tsc --noEmit` zero errors.
- **v5.8.35** — A→Z 360° performance sweep. Diagnostics: production bundle had `vendor-lucide` chunk at **624 KB / 165 KB gzip** (entire icon library), main `index.js` at **819 KB / 196 KB gzip**, brand PNGs at **5.0 MB combined**. Fix: (Phase A) `client/src/components/ui/Card.jsx` removed `import * as LucideIcons from 'lucide-react'` — the legacy `icon="Heart"` string-lookup pattern was 0-caller dead code dragging the full library in; refactored `icon` prop to accept a React element so callers own tree-shakeable imports. **vendor-lucide 624 KB → 66.59 KB (143 KB gzip saved per page load).** (Phase B) Used system `cwebp` to generate WebP siblings of all 12 PNGs >200 KB in `client/public/brand/` at q82/m6; `VisualBenefits.jsx` already had `imageWebp` paths but the files had never been generated. **5.0 MB → 212 KB = 96% smaller hero imagery.** All PNGs kept as `<picture>` fallback. (Phase C) `App.jsx` lazy-converted 9 eagerly-imported heavy globals: `GratitudePrompt`/`ConsentBanner`/`FeedbackWidget`/`WelcomeBackBanner`/`ReturnLoop`/`MicroWinPrompt`/`AICompanion`/`AccessibilityToolbar`/`AnalyticsDashboard` (admin-only); cluster wrapped in `<Suspense fallback={null}>` so they hydrate post-LCP without blocking first paint. **index.js 819 KB → 750 KB (18 KB gzip saved).** (Phase D) `vite.config.js` `manualChunks` extended to also split `recharts`/`d3-*`/`framer-motion`/`date-fns`/`dayjs` preventively. Cumulative: **−161 KB gzip JS + −4.8 MB images on first visit**. Build 19.83s. Zero design / `data-testid` / `/crisis` / reduced-motion changes.
- **v5.8.34** — V28 Polish System + IMG_4320 alignment. `.polish-card`/`.polish-card-icon`/`.section-rhythm`/`.polish-cta` utility set added to `index.css`; `ValueBridge.jsx` + `ValueProposition.jsx` swept to canonical 8-hex (zero `#8FBF9F`/`#D4AF37` residuals); `.section-breathe` 5/7/8 → 4/5/6rem in `canva-landing.css`. Full detail in `docs/changelog.md`.
- **v5.8.33** — Homepage `NlpMiContent` rebuilt as 4 alternating mascot+content rows in V17 `VisualBenefits` style — `nlpMiContent.js` HOME.sections enriched w/ accent/tint/halo/avatar/cta; `NlpMiContent.jsx` rewritten w/ `.nlp-mi-row` grid + 9 scoped CSS classes; row-4 CTA routes to `/crisis`. Full detail in `docs/changelog.md`.
- **v5.8.32** — V28 Tier 4 Sweep (Admin + Niche, 57 surfaces) via 5 idempotent regex passes using v5.8.31 `.v28-paper-bg`/`.v28-card` utilities. Cumulative v5.8.21→v5.8.32: ~95 surfaces on V28. Full detail in `docs/changelog.md`.
- **v5.8.31** — V28 Tier 3 Sweep (Tool Pages + Auth/Account/Conversion, 21 surfaces). New `.v28-paper-bg` + `.v28-card` utilities in `index.css`; 9 tool pages + Premium/Settings/Profile/Onboarding(TS)/Wellness/Resources/Glossary/Privacy(TS)/Newsletter/FAQ/Upgrade/NotFound/ForgotPassword/About flipped. Full detail in `docs/changelog.md`.
- **v5.8.30** — V28 Public Surfaces Sweep (Tier 2). `WellnessToolsHub.jsx` rewritten (off-palette gradient tiles → canonical pastel-tint pattern); `ToolsPage`/`Blog`/`BlogIndex`/`Terms.tsx` flipped to flat paper. Full detail in `docs/changelog.md`.
- **v5.8.29** — V28 Auth + Privacy Sweep + Canonical Stat Cards. Privacy/Login/Register/Forgot/Reset/Dashboard flipped; off-palette colors removed; Dashboard Mood/Journal stat cards re-styled to canonical calm-blue + empathy-purple. Full detail in `docs/changelog.md`.
- **v5.8.28** — V32 Gap C: LumiV7 Avatar Evolution Engine (`components/lumi/LumiV7.{jsx,css}`). Pure-SVG mascot, body frozen per V22/V24/V27, eyes/mouth/arm/leg gain coordinated states (4 eye types, 10 mouths, 6 arms, 5 legs) + RAF pupil tracking + BHCE crisis override + reduced-motion blanket. QA surface at `/avatar-lab`. Standalone, opt-in. Full detail in `docs/changelog.md`.
- **v5.8.27** — V32 Gap D: net-new `/welcome` 6-screen onboarding flow (`pages/OnboardingFlow.jsx`, lazy in `App.jsx`) — Welcome → Goal → Micro Relief → Personalize → Progress → Return; V28 styling, full `data-testid`/`aria-pressed`/`role=progressbar`, `/crisis` on every screen. Auth-gated `/onboarding` coaching flow untouched. Full detail in `docs/changelog.md`.
- **v5.8.26** — V32 Gaps A+B+E: `<section id="emotional-validation">` w/ 5 emotion-led cards on homepage; "Evidence-Based" → "Evidence-Informed" copy sweep; cost ops cleanup. Full detail in `docs/changelog.md`.
- **v5.8.25** — V28 sweep on `CanvaLanding.jsx`: section-flow-sage/warm/FAQ/final-CTA flattened to paper; `.manifesto-quote` hardened to white card via `!important`. Full detail in `docs/changelog.md`.
- **v5.8.24** — V30 phase-locked copy on `Pricing.jsx`: hero headline + 4 emotion-first tier subtitles + per-tier `cta` field. testid + Stripe `planId` unchanged. Full detail in `docs/changelog.md`.
- **v5.8.23** — V28+V30 audit across Pricing/CheckIn/BreathingTool/About/Disclaimer. Pricing rewritten w/ emotion-first names + 4 V30 elements added; tool pages → white cards + sage pill CTAs; CheckIn streak → sage-gold pill. Full detail in `docs/changelog.md`.
- **v5.8.22** — V28 Color & Structure Unification on `CanvaLanding.jsx`: dark bands → white-card light treatment; square→circle icons; text links → sage gradient pills.
- **v5.8.21** — V28 initial pass (superseded by v5.8.22).
- **v5.8.6 → v5.8.20 (Phase 2 era)** — Lumi/avatar maturation: V12 breathing tool polish, V3 round benefits, V20 sparkles/particles/blush, coordination transitions, bounce/tears, V24 OMEGA data layer, LumiV6 `tears?`/`celebrate?` props, MI `rollingWithResistance`, `MicroWinPrompt` dismiss coaching, homepage Lumi pose mapping fix, LumiV6 → canonical `EMOTION_STATES` SSOT via `getLumiV6Derivation()` adapter, V24 §7 pose assets (meditating-aura/halo-prayer/walking-sunrise), VisualBenefits canonical avatar swap, Growth walking-path correction, V27 sprout-only enforcement (hooded retired), **v5.8.18 official avatar drop** — 7 canonical PNGs locked into `client/public/brand/v17/` as visual source of truth, never regenerate. Per-release notes in `docs/changelog.md`.
- **v4.1.1 → v5.5 (Foundational era)** — Foundational Lumi/MI/avatar work: Subscription Elicitation, Engagement Hooks, V14 audio, Lumi voice/expression sync, Emotional Journey timeline, `/v6` Control Panel, Hero Polish, LumiV9 "Soul Capture", Schema Drift Guardrail, Avatar Uniformity. Per-release notes in `docs/changelog.md`.

**Universal contracts** that every polish layer above honors (and any new layer must honor):
- Scoped under a unique `.{name}-polish` wrapper class — zero leak risk.
- All accent / aura / particle / glow color values are drawn from the canonical 8-hex brand palette (sage `#A8C9A0`, sunshine `#FFD93D`, blush `#FF9A8B`, calm-blue `#74C0FC`, empathy-purple `#C8B6FF`, mint `#A8D5BA`, warmth-orange `#FFB88C`, heart-amber `#E8913A`). Neutral base RGBAs (cream / white / black with low alpha) are permitted only for ambient overlays, drop-shadows, and ring-on-white outlines, never as a brand accent.
- `prefers-reduced-motion: reduce` blanket: particles `display:none`, animations/transitions killed, transforms pinned.
- Z-index contract: decorative layers 0/1/2 → content `relative z-10`. (v4.5.1 normalization added explicit tiers to the hero `.hero-depth-layer` and `.decorative-orb` so the hero matches every other surface.)
- Visual-first additions: phase logic, localStorage math, ARIA wiring, and BuddyAvatar/LumiV6 integration are preserved on every surface. Two intentional UX exceptions are documented in the changelog: (a) Check-In v4.7 defers the emotion-card → intensity-phase transition by 350ms (skipped under reduced motion) so users perceive the selection pulse before the grid unmounts; (b) Celebration v4.8 forces a `<section>` remount per phase via `key={phase}` so the entrance animation replays — neither alters end-state behavior.
- Crisis routing (`/crisis`, 988, 741741) preserved on every wellness surface.

## System Architecture

### UI/UX Decisions
The frontend features a Canva-inspired design using HSL color formatting, custom typography, enhanced gradients, and refined shadows, with an "Aurora Token System" for light/dark themes and WCAG AA accessibility. A "Buddy Engine" provides a visual companion. The Lumi Design System v2.0 offers an additive visual system with new tokens, typography, dark-mode mapping, and brand assets including a mascot. The platform includes five additive React pages for Discernment, Protocols, Biometrics, and Admin, two marketing pages, and a public tools directory. Eight interactive, client-side wellness tools are provided, each with crisis links and accessibility features. Motion respects `prefers-reduced-motion`.

### Technical Implementations
The project uses a monorepo architecture with React 18 (TypeScript, Vite) for the client and Node.js/Express (TypeScript) for the server, offering a RESTful API with security and session management. It features a trauma-informed NLP layer and a "Wellness Microcopy Library." A Dual-Engine AI Prompt Architecture separates user-facing and administrative engines. Core orchestration includes RBAC, crisis-first intent routing, PII redaction, structured event logging, and a Self-Tuning Fallback Library. The system performs subtle-emotion inference, adaptive response policies, an intervention module router with eight evidence-backed modules and seven micro-exercises, and a `user_feedback` signal-capture loop. Unified crisis detection short-circuits to appropriate responses.

An "Awareness Detection Pipeline" uses a three-layer ensemble (RuleMatcher, MLClassifier stub, LLMReasoner) for detecting manipulation, distortion, and fallacies, integrating with the AI chat. An "Agent Orchestrator" is a pure-JS deterministic state machine for AI agent invocation, with actions recorded in an audit log and a three-tier memory abstraction (Hot, Warm, Cold). "Consciousness OS Foundation Primitives" establish persistence and audit with Drizzle tables.

A "Biometric Ingestion Pipeline" adds opt-in nervous-system telemetry, encrypting data at rest and supporting OAuth integrations. The "Discernment Tutor" is an awareness-training belt ladder over the awareness detection pipeline, using scenario-based lessons. The "Protocol Execution Engine" is a deterministic state-machine walker over therapeutic-protocol DAGs, seeded with ten evidence-informed protocols. An SEO content generation script drafts trauma-informed blog posts.

### Feature Specifications
Core features include AI-powered Chat Therapy, Wellness Tools (State Tracker, Journal Prompts), and specialized APIs. The platform supports a four-tier subscription model and includes security features like rate limiting, CSP, and input sanitization. Engagement tools comprise gamification and a Content Studio. User features offer daily healing reminders, voice affirmations, and an AI companion. Admin tools provide dashboards and an "Admin Command Center" with an "SOP Monitor." The `/growth` page serves as a "Metacognition Mirror," and the "Peace Scape" surface provides a sanctuary environment.

Both chat surfaces (`AIChatPage.jsx`, `AIChatPanel.tsx`) display the canonical Lumi mascot avatar on assistant bubbles and typing indicators (with `lumi-breathe` motion and graceful `onError` fallback). The home footer surfaces Home, About, Tools, Wisdom, Blog, Newsletter, Disclaimer, Privacy, Terms, and Crisis routes through its 4-column block plus the `SafetyFooter` strip. The `FeedbackWidget` floating action supports a 7-day localStorage suppression (`mmhb-feedback-suppressed-until`) via the dedicated "7d" hide button **and** the X close button. `BuddyAvatar.tsx` renders the canonical Lumi PNG with `lumi-breathe` animation gated by `v.motion !== 'steady'` so crisis state stays still per the original safety contract. `AIChatPanel.tsx` reads the API response shape `data?.response?.reply || data?.reply || data?.message || data?.error` with a gentle `/crisis`-routing fallback so server-shape drift never surfaces "No response returned" to the user.

Signup-intent CTAs ("Start Free", "Get Started Free", "Start Your Journey — Free") on `CanvaLanding.jsx` and `Pricing.jsx` (free-tier card) route to `/register`; "Sign In" buttons remain on `/login`. The canonical Lumi mascot lockup uses a soft sage radial halo (`radial-gradient` with `overflow: visible`) instead of the legacy hard square frame; applied across `Header.jsx`, `Footer.jsx`, `CanvaLanding.jsx`, `Login.jsx`, `Register.jsx`, `ForgotPassword.jsx`, and `ResetPassword.jsx`. The `/chat` surface renders a top breadcrumb nav (`data-testid="nav-chat-breadcrumbs"`) with **Back to Dashboard**, Home, Journal, Mood, and a right-aligned rose **Crisis Support** link.

### System Design Choices
Drizzle ORM is used with a Neon PostgreSQL database. Production security includes CORS allowlisting, JWT authentication, Helmet, and rate limiting. An observability layer provides health and system endpoints, utilizing OpenTelemetry tracing and PagerDuty alerting. A `Prompt-OS Execution Prompt Library` ensures canonical prompt modules. Production readiness features include a 503 readiness gate, health probes, telemetry parity, request tracing, and hardened administration access governed by a `CHANGE_GATE` protocol. The platform includes a layered self-healing stack for automated monitoring, diagnosis, and repair. Autoscale deployment uses split build/run phases to optimize cold-start times. The system AI prompt is extended with governance sections for canonical domain classification, UX contract integration, tone guidelines, a reflection prompt library, a Therapeutic Framework Reference Library (Motivational Interviewing, Active Listening, Validation Support, Rapport Building, Metacognition Prompts, Belief Exploration, Emotion Processing, Strength-Based Coaching, NLP-Informed Communication, Safety Boundaries) with an explicit hard rule against framework-name leakage to users, and crisis escalation protocols. Admin panels are wrapped in `<SafeBoundary>` for error isolation.

## External Dependencies

-   **OpenAI API**: AI chat therapy.
-   **Vite**: Frontend build tool.
-   **TypeScript**: Language.
-   **React**: Frontend UI library.
-   **Wouter**: Client-side routing.
-   **React Hook Form**: Form management.
-   **Zod**: Runtime type validation.
-   **Tailwind CSS**: Styling framework.
-   **Lucide React**: Icons.
-   **Node.js**: Backend runtime.
-   **Express**: Backend web framework.
-   **Express Session**: Session management.
-   **CORS**: Cross-Origin Resource Sharing middleware.
-   **Helmet**: Security headers middleware.
-   **Compression**: Response compression middleware.
-   **Morgan**: HTTP request logger middleware.
-   **Sentry**: Error tracking and performance monitoring.
-   **Drizzle ORM**: Database interactions.
-   **Neon PostgreSQL**: Primary database.
-   **Stripe**: Billing and payment processing.
-   **Replit Auth**: User authentication.
-   **Resend**: Transactional email service.
-   **Perplexity**: Factual AI.
-   **Oura**: Biometric data integration.
-   **Google Fit**: Biometric data integration.
-   **Whoop**: Biometric data integration.
-   **Apple HealthKit**: Biometric data integration (via webhook).
-   **PagerDuty**: Alerting and incident management.
-   **Redis**: Agent working memory (optional).
