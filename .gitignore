
.archive/
archive/
archive/legacy-server-backups/
archive/runtime-violations/
attached_assets/
audit-report.json
# Audit / scratch artifacts (generated by scripts/platformAudit.mjs etc.)
backups/
.backups_deleting/
# Backups (kept on disk locally; never committed/shipped)
*.bak
*.bak.*
*.BeforePhase*
blog_templates.txt
build/
# Build output
# ===== BUILD OUTPUTS =====
*.cache
.cache/
# checkpoint snapshots the git tree into the build container; without these
# client/dist/
client/dist/
coverage/
data/aiMemory/*.json
/data/memory/
data/memory/*.json
/data/memory-profile/
/data/memory-summary/
/data/usage/
/data/usage-*.json
data/usage/*.json
*.db
# Dependencies
diagnostics/**/client-stripe-references*.txt
diagnostics/**/cors-env-before.txt
diagnostics/**/env*.txt
diagnostics/phase10/
diagnostics/phase11/
diagnostics/phase6/
diagnostics/phase9/
diagnostics/**/secrets*.txt
diagnostics/**/stripe-env-before.txt
diagnostics/**/stripe-references-raw.txt
diagnostics/**/stripe-references*.txt
diagnostics/**/stripe-references.txt
# dist/
dist/
docs/inventory/AI_SURFACE_SCAN.txt
docs/inventory/API_USAGE_SCAN.txt
docs/inventory/DATABASE_SCAN.txt
docs/inventory/**/*.txt
docs/inventory/*.txt
.DS_Store
.env
.env.*
# Environment
.env.*.local
.env.local
.env.production
# Existing generated runtime outputs
# files from being tracked; does not retroactively untrack existing files).
# ===== GENERATED INVENTORY / SCANS =====
gpg-batch
# header for the full rationale.
/*.html
.hx-backups/
# ===== HX-OS GENERATED BACKUPS =====
hxos-lite-scan-*/
# IDE
.idea/
*.jsonl
.local/
# Local archive of cleaned-up files (do not commit)
# ===== LOCAL DATABASES =====
# Local-only backups
# Local production backups - never commit
# Local state
*.log
logs/
# Logs
logs/*.jsonl
logs/*.log
node_modules/
node_modules/.vite-temp/
# NOTE: client/dist/ and dist/ are intentionally tracked. Replit's deploy
npm-debug.log*
# OS files
# Phase 189 generated cleanup exclusions
# Phase 60 — generated inventory dumps
# Preserve structure
# probe 404s before the boot-time rebuild can finish. See .replitignore
production-backups/
publickey.asc
.pythonlibs/
.quarantine/
_quarantine/
# Quarantine
!/README.md
reports/auth-guest-history-final.txt
reports/runtime/*.log
reports/safe-backups/
reports/safe-doc-backups/
# ===== RUNTIME / CACHE =====
# Runtime data artifacts (additive guard: prevents NEW runtime memory/usage
# Runtime-generated state and telemetry
# Runtime isolation
runtime-state/events/*
!runtime-state/events/.gitkeep
runtime-state/logs/*
!runtime-state/logs/.gitkeep
runtime-state/memory/*
!runtime-state/memory/.gitkeep
runtime-state/usage/*
!runtime-state/usage/.gitkeep
screenshots/*.jpeg
screenshots/*.jpg
screenshots/*.png
# Secrets
server/routes/*.bak
*.sqlite
*.swo
*.swp
*.tar.gz
*.temp
# Temp files
# Test coverage
Thumbs.db
*.tmp
.tmp/
# tracked, the runtime VM boots with no SPA bundle and the GET / health
/*.txt
# User-uploaded reference images (path alias only; not bundled)
# V20 cost optimization — orphan artifacts
.vite-temp/
.vscode/
yarn-debug.log*
yarn-error.log*
diagnostics/visual-submit-check/button-contrast-risk-search.txt
diagnostics/visual-submit-check/lumi-motion-search.txt
*.tar.gz
*.tgz
*.sqlite
*.sqlite3
*.db
.agents/

# Runtime data storage policy: keep generated memory/usage records out of git.
data/memory/*
data/memory-summary/*
data/memory-profile/*
data/usage/*

# Generated diagnostics and audit outputs
diagnostics/

# Generated audit/report artifacts
reports/

# Generated/duplicate Lumi backup artifacts
client/public/lumi/official_backup_orig/

# Generated client diagnostic snapshots
client/docs/diagnostics/

# Generated client analytics reports and snapshots
client/analytics/
