# MMHB Polish & Feature History — Archive

This file holds the one-line index entries for releases **older than v5.8.65**, archived from `replit.md` to keep the active context lean. The full deep-technical entries (z-index, keyframes, contracts) for every version below remain in `docs/changelog.md` — this file is just the index.

For the active index of recent releases (v5.8.65 → current), see `replit.md` → "Polish & Feature History".

### v5.8.107 – v5.8.111 (archived from replit.md by v5.8.112)

The rows below are the verbatim full-detail entries from the active replit.md Index, consolidated under v5.8.112 to bring replit.md under 80 lines. Original row text preserved as-is.

| v5.8.111 | 2026-05-15 | sections/ValueProposition.jsx (V49 Fix C3-RETRY — REAL self-care card icons → circles on homepage `/`) | User clarified v5.8.108 fix was the WRONG surface (SelfCareHubPage at /hubs/self-care) — REAL target is the homepage `/` feature/benefit cards in lower sections. INSPECT: `/` Route → `CanvaLanding` → imports `ValueProposition`. `.vp-benefit__icon` defined ONCE in component-scoped styled-jsx at line 295 (44px square, `border-radius: 12px`); referenced ONCE as className line 206. PATCH: literal user spec — `border-radius: 12px` → `border-radius: 50%` (single-line CSS property swap inside the `<style jsx>` block, NOT in global index.css/styles/). Width+height already 44px square so radius 50% yields perfect 44px circle, all benefit-card icons inherit in one shot. NO !important, NO global CSS edit, NO new component, single-property diff. v5.8.108 SelfCareHubPage fix retained as orthogonal improvement (hub cards now also circular, doesn't conflict). Gates: tsc 0, 4/4 routes 200 (/, /crisis, /journal, /blog). |
| v5.8.110 | 2026-05-15 | replit.md (HX-OS CYCLE 1c — row archival, protocol switch) + docs/replit-history.md (+16L) | Per stated CYCLE 1 goal "Continue until <80 lines" + empirical proof from v5.8.107 + v5.8.109 that cell-trim is char-saving NOT line-saving (rows are 1 line each regardless of cell content). Switched protocol to row archival, documented precedent (v5.8.78, v5.8.88, v5.8.94). PATCH: archived 14 rows v5.8.86–v5.8.99 to `docs/replit-history.md` as 1-line summaries; bumped archive-notice cutoff (v5.8.86→v5.8.100, v5.8.85→v5.8.99); bumped Index range. Result: replit.md 95L → 82L (−13, sub-80L target near-met within 1 row of doc-overhead from this cycle's own row). All 14 archived rows preserved verbatim in history file with full metadata; ZERO information lost. Gates: tsc 0, 4/4 routes 200 (/, /crisis, /journal, /blog). |
| v5.8.109 | 2026-05-15 | replit.md (HX-OS CYCLE 1b — trim) + docs/changelog.md (+18L) | Per CYCLE 1b protocol, next most-verbose row = v5.8.104 (1823 chars). Prepended full v5.8.104 detail to `docs/changelog.md` as new top entry; collapsed replit.md row to 1-sentence summary with pointer. Honest-deficit reminder (per v5.8.107 finding): cell-trim is char-saving, not line-saving — table rows are 1 line each regardless of cell content. Line count delta: 94 → 94. Reaching <80 lines requires row archival (collapse v5.8.86–v5.8.99 → 1-line summaries, move detail to `docs/replit-history.md`), same pattern as v5.8.88 / v5.8.94. Awaiting go-signal to switch protocols. Gates: tsc 0, 4/4 routes 200 (/, /crisis, /journal, /blog). |
| v5.8.108 | 2026-05-15 | pages/hubs/SelfCareHubPage.jsx (V49 Fix C3 — Self-care card icon containers → circles) | Per V49 P1 fix order, advanced after C1 ship-as-is. User declined to specify which self-care surface; picked SelfCareHubPage.jsx as best-fit (literal "hub" + "card" + "icon container" pattern at line 71). INSPECT: card icon container was `<div className="p-3 rounded-lg bg-primary/10 text-primary">` (rounded-square 8px-radius wrapping a 24px Icon, total ~48px square). Unlike R1 (ToolsPage.jsx, v5.8.103) which had `.icon-container.icon-lg` CSS specificity battle requiring inline-style, this surface uses pure Tailwind utilities → simple class swap suffices. Smallest valid engine: replaced `p-3 rounded-lg` with `w-12 h-12 rounded-full ... flex items-center justify-center flex-shrink-0` (explicit 48px circle, centered icon, doesn't shrink on narrow viewports). Preserved `bg-primary/10 text-primary` (canonical theme tokens). Single-line className edit, all 4 hub cards (SELF_CARE_RESOURCES.map at line 61) get the circle icon in one shot. Recurrence-grep `rounded-lg bg-primary/10 text-primary` in /hubs/ → 0 other matches in this directory. NO !important, NO new CSS, single-file diff. Gates: tsc 0, 4/4 routes 200 (/hubs/self-care, /self-care, /, /crisis). |
| v5.8.107 | 2026-05-15 | replit.md (HX-OS CYCLE 1a — trim) + docs/changelog.md (+30L) | User instruction targeted v5.8.69 as "most verbose ~2000 chars"; INSPECT showed v5.8.69 already archived in v5.8.78 → `docs/replit-history.md:21` (full detail at `docs/changelog.md:528`). Pivoted to actual current most-verbose row: v5.8.101 (2050 chars). Prepended full v5.8.101 detail to `docs/changelog.md` as new top entry; collapsed replit.md row to 1-sentence summary with pointer. Contracts (Universal contracts, Governance Kernel, BHCE) intact. Gates: tsc 0, /+/crisis+/journal 200. STOP per HX-OS one-blocker discipline. |


### v5.8.86 – v5.8.99 (archived from replit.md by v5.8.110)

- **v5.8.99** (2026-05-15) — `components/Badges.jsx` (V47 iter 5): 1-line gradient hex swap — legacy `#8fbf9f`→`#A8C9A0`, `#d4af37`→`#FFD93D` (canonical sage + sunshine).
- **v5.8.98** (2026-05-15) — `pages/Pricing.jsx` (V47 iter 4): 1-line wrapper fix — prepended `hxos-vnext` token-scope class + corrected fallback hex to canonical `#F7F1E8`.
- **v5.8.97** (2026-05-15) — `replit.md` (V47 iter 3): audit-only EXIT — wider/deeper sweep CONFIRMED EXHAUSTION, 12/12 routes 200, V47 mission COMPLETE.
- **v5.8.96** (2026-05-15) — `components/NewsletterSignup.jsx` + `content/routes.js` (V47 iter 2): 2-line href fix — 2 dead `/blog/welcome-to-genuine-love` refs redirected to `/about` and `/blog`.
- **v5.8.95** (2026-05-15) — `components/BrandShell.jsx` (V47 iter 1): 1-char href swap — Mood button `/checkin`→`/mood` (copy-paste bug from v5.8.75).
- **v5.8.94** (2026-05-15) — `replit.md` + `docs/replit-history.md` (V45 ACTION 2): row archival — collapsed 5 rows v5.8.80–v5.8.85 to 1-line summaries; replit.md 75L → ~71L.
- **v5.8.93** (2026-05-15) — `replit.md` + `docs/architecture.md` (V45 C1c): Quick Reference extraction — 20L block → architecture.md appendix; 1-line dual-pointer in trailer; 94L → 75L.
- **v5.8.92** (2026-05-15) — `.archive/avatar-core/source/` + V44 governance ratified: mass-archived remaining root SOURCE tree (9 subdirs, 22 files, ~22M) in 1 op.
- **v5.8.91** (2026-05-15) — `.archive/avatar-core/source/raw/` + `./avatar-core/raw/`: V43 C2 iter #1 — archived largest source file `MMHB_FLOAT_IDLE_UNIT_v1_raw.png` (1.5M); active mirror untouched.
- **v5.8.90** (2026-05-15) — `replit.md` (V41 C1 iter) + `docs/changelog.md` (+62L): v5.8.86 cell-trim (~2528 chars → 1-sentence). Honest finding: V41's "30-40 lines saved per iter" structurally impossible.
- **v5.8.89** (2026-05-15) — `replit.md` + `docs/architecture.md` (NEW) (V38 C1 iter #3): System Architecture extraction — 50L narrative → architecture.md; 137L → 91L (−46).
- **v5.8.88** (2026-05-15) — `replit.md` + `docs/replit-history.md` (V38 Cycle 1 iter #2): row archival — 15 rows v5.8.65–v5.8.79 archived; 151L → 136L (−15).
- **v5.8.87** (2026-05-15) — `replit.md` + `docs/changelog.md` + `.local/zombies/` (V38 Tier C trim): v5.8.84 detail (2437 chars) → changelog; v5.8.86 archives moved to `.local/zombies/`.
- **v5.8.86** (2026-05-15) — `server/` (zombie boot scaffold archive — 3 mv + 1 rm): archived `server/{index,dev}.mjs` + deleted 0-byte `server/app.ts` + orphan `server/tests/vitest.setup.mjs`. Single canonical entrypoint `server/app.mjs`.

## Index entries v5.8.65 – v5.8.79 (archived from replit.md on 2026-05-15 in v5.8.88)

| Version | Date | Modules | Key Change |
|---|---|---|---|
| v5.8.79 | 2026-05-15 | LumiV7.jsx (P4 T4 — Avatar Life System Phase 1 emotion wiring) | Inspect-first found T1-T3 already shipped in LumiV7 (4 eye CSS classes, lerp-tracked pupils ±10/±6, randomized 2-6s blink + 15% double, `prefers-reduced-motion`, `.is-crisis` stillness); only T4 emotion wiring missing. Added optional `emotion` prop deriving eye+mouth from canonical V24 `EMOTION_STATES` (lumiEmotionMap.ts) via `getEmotionState()`. Explicit `eye`/`mouth` props always win (preserves AvatarLab per-variant gallery). Crisis fallback emits `soft`+`calm` only when explicit eye/mouth absent (motion stillness enforced unconditionally by `.is-crisis` CSS + effect early-returns). 3 surgical edits, 1 file (+12 net lines). Smallest valid engine per V34 — declined to duplicate work into LumiV6 (which is preview-only PNG-based, would have failed `DUPLICATION_GATE`). tsc 0 errors, vite 26.66s, bundle 753.79 kB (+2.4 kB). LumiV6/V8 archive deferred. P2 (sprout walking-path) skipped per user. |
| v5.8.78 | 2026-05-15 | replit.md Tier C trim + V35 prompt library install | v5.8.67-72 verbose rows compressed to one-liners (12.4k → 5.9k chars). V35 library installed at `prompt-os/v35-prompt-library.md` (168L, 10 prompts indexed). Full v5.8.67-72 detail prepended to `docs/changelog.md` (now 2891L). |
| v5.8.77 | 2026-05-15 | replit.md trim + Tier B archive removal | Rolled v5.8.74-76 verbose rows into one-liners; full detail moved to `docs/changelog.md`. Deleted `client/public/brand/v17/.archive-v5.8.76/` (12 files, 4.0 MB, zero refs). Combined w/ v5.8.76 = ~8.7 MB freed total. tsc + vite build clean. |
| v5.8.76 | 2026-05-15 | Unofficial avatar removal (sprout-only enforcement) | Repointed 14 live refs across 8 source files (`lumiAssets`, `nlpMiContent`, `PageTemplate`, `LumiV6`, `BuddyAvatar`, `LumiMascotImage`, `OnboardingFlow`, `officialLumiRegistry`+`VisualBenefits`) from legacy `/brand/v17/avatar-*` (cream-blob) to canonical `/lumi/official/lumi-{float-idle,heart,meditation}.png` (sprout-only per V26). Tier A rm: 4 placeholder-bak + 5 .archive-pre-v5.8.40 (~4.7 MB). Tier B mv→archive: 12 legacy avatars (4.1 MB). Pre-existing missing `lumi-path.png` repointed to `lumi-float-idle.png`. Final state: 7 sprout PNGs + MANIFEST in `/lumi/official/`; 4 benefit-scene pairs only in `/brand/v17/`. Full detail → `docs/changelog.md`. |
| v5.8.75 | 2026-05-15 | V29 Phase 1 hybrid nav (CanvaLanding + BrandShell) | Action-first 8-link desktop strip (Home·Tools·Check-In·Companion·Journal·Blog·Crisis·Pricing); Crisis link in rose accent; mobile menu reorganized into 4 sections (Core/Discover/Trust/Account). BrandShell.jsx: 3 inert Journal/Mood/Check-In `<button>`s replaced with wouter `<Link>` wrappers — clicks now actually navigate. Full detail → `docs/changelog.md`. |
| v5.8.74 | 2026-05-15 | Canonical Lumi sprout-only replacement (`/lumi/official/` × 7 + `benefit-companionship.png`) | All-sprout V26 mandate. 7/8 canonical PNGs replaced with sprout artwork (`LUMI_SOFT_PRESENCE`, `LUMI_HEART`, `LUMI_CALM_FLOAT`, `LUMI_MEDITATION`, `LUMI_COMPANION`, `LUMI_EMOTION_ORB`, `LUMI_FLOAT_IDLE`). 1 exception: `LUMI_PATH` kept on hooded source pending sprout walking-path commission. Hooded `benefit-companionship.png` swapped to sprout halo (708 KB vs 2.2 MB prior). 8 placeholders preserved as `*.placeholder-bak.png` siblings. Zero code changes — registry path constants unchanged. Full detail → `docs/changelog.md`. |
| v5.8.73 | 2026-05-15 | PAGE_PLACEMENT_MAP enrollment + history archive | 6 new page entries (breathing/journal/habits/library/settings/chat), 6 call sites wired with `pageId`, floor guard 18→24, CanvaLanding footer via RUNTIME_ONLY_VARIANTS, replit.md 152→137 lines, docs/replit-history.md +15 rows |
| v5.8.72 | 2026-05-15 | OfficialLumi page integration (Path A) + CanvaLanding L1262 swap | Canonical Lumi rendered on 7 pages. Last legacy `<LumiMascot>` swapped to `<OfficialLumi LUMI_FLOAT_IDLE>` (footer 28px). 5 no-avatar pages got OfficialLumi: Breathing (LUMI_MEDITATION 120px), Journal (LUMI_SOFT_PRESENCE 100px), Habits (LUMI_PATH 100px), SavedLibrary (LUMI_EMOTION_ORB 100px), Settings (LUMI_COMPANION 60px). AIChatPage got header LUMI_HEART 48px. All `decorative` aria-hidden. Architect catch: added required `scene` + `position` (jsx files mask type errors). Skipped CrisisResources (BHCE) + CheckIn (BuddyAvatar). Full detail → `docs/changelog.md`. |
| v5.8.71 | 2026-05-15 | sections/VisualBenefits.jsx (V25 pose mapping) | Live homepage benefit rows pointed at legacy `/brand/v17/avatar-*.png` (bypassing canonical registry). Swapped 4 `avatar`/`avatarWebp` fields to canonical paths: meditation→LUMI_MEDITATION, heart→LUMI_HEART, companionship→LUMI_COMPANION, growth→LUMI_PATH. `avatarWebp` set to undefined (PNG-only canonical assets; `<ResponsiveImage>` falls back cleanly). 16:9 background scenes untouched. Full detail → `docs/changelog.md`. |
| v5.8.70 | 2026-05-15 | hxos-vnext.css (utility token expansion) | Iter 2g Option C non-destructive. Appended 4 utility tokens to `.hxos-vnext`: `--glp-warm-white #FAFAF7`, `--glp-light-gray #F0EDE6`, `--glp-border rgba(22,58,54,0.08)`, `--glp-shadow rgba(22,58,54,0.06)`. Brand accents (violet, gold) NOT overridden per Universal Contracts. Zero `!important`. All 9 wrapped routes auto-pick-up. Full detail → `docs/changelog.md`. |
| v5.8.69 | 2026-05-15 | Iter 2g Part B closeout + toast UI sink | Portal audit confirmed zero portal surface (no createPortal, no Radix, no Toaster mount) — Phase 5 caveat closed as no-op. Bonus: added `client/src/components/ui/toast-container.tsx` (96L custom, zero deps, subscribes to useToast memoryState, fixed bottom-24 right-6 z-60 to clear AccessibilityToolbar). Resolves silent toast calls in Settings L117/121/126. `useToast` API untouched. Full detail → `docs/changelog.md`. |
| v5.8.68 | 2026-05-15 | hxos-vnext.css + 8 page wrappers | Iter 2g Phases 2-5. Visual unification across 8 routes (CheckIn, Breathing, Journal, AIChat, Habits, SavedLibrary, Settings, CrisisResources) via opt-in `client/src/styles/hxos-vnext.css` (14-token `.hxos-vnext` override + rose-only `.hxos-vnext-crisis`). Each page got wrapper class. brand-tokens.css untouched (70 non-opted pages unaffected). Full detail → `docs/changelog.md`. |
| v5.8.67 | 2026-05-15 | canva-landing.css (HX-OS vNEXT scoped tokens) | Iter 2g Phase 1. 14 `--glp-*` tokens redefined inside `.canva-landing` only (warm-cream paper, ink-teal, sage-deep, soft-sage, dusty-rose + 9 alpha derivatives). Global `brand-tokens.css` untouched (78 other consumers unchanged). Approved page-scoped governance exception for sage/rose accent divergence from locked palette. Full detail → `docs/changelog.md`. |
| v5.8.66 | 2026-05-14 | CanvaLanding header (Iter 2c canonical Lumi swap) | First legacy `<LumiMascot>` swapped to `<OfficialLumi pageId="landing-canva">` on `/` header (L360, 48px). 8 canonical PNGs bridged to `/lumi/official/` with provenance manifest. |
| v5.8.65 | 2026-05-14 | lumi-backend, lumi-notifications, lumi-rbac, lumi-audit, lumi-tokens, lumi-language, lumi-disclaimer | 7 backend + HX-OS vNEXT integration modules, 25 files. Crisis regex `i`-flag fix. `tsc` + vite build clean. |


---

- **v5.8.64** — `lumi-cbt`, `lumi-tracker`, `lumi-crisis`, `lumi-library`, `lumi-agent` + Phase 30b OfficialLumi flip. 5 clinical modules, 40 files. CBT thought-record + ACT defusion, mood/habit tracker (`streakGuard`), 988 crisis resources, AI agent adapter with 8 guardrails. `OfficialLumi` flipped to `<img>`-only. Full detail in `docs/changelog.md`.
- **v5.8.63** — `lumi-registry` (Phase 30 + 31 reconcile). Reconcile-mode delta: 8th canonical variant `LUMI_FLOAT_IDLE`, asset fields on all variants, `lumiMotion.css` (locked 7.1s breath), opt-in `renderMode`/`motion` props, `RUNTIME_ONLY_VARIANTS` whitelist, 11-entry replacement audit. Full detail in `docs/changelog.md`.
- **v5.8.62** — `lumi-registry` (Phase 28 + 29). 7 canonical variants frozen, 25 emotional roles, 17-page placement map (floor-guarded), `OfficialLumi` + `LumiSceneRenderer`, trust boundaries: forbidden surfaces render hidden, unknown `pageId` refuses. Full detail in `docs/changelog.md`.
- **v5.8.61** — `lumi-integration` smoke test. Cross-module integration tests (23/23 across 4 suites): type-surface alignment + governance + identity verification + end-to-end ritual flow. Frozen mapping tables (`AVATAR_TO_SCENE`/`RITUAL_TO_SCENE`/`RITUAL_TO_AVATAR`). Full detail in `docs/changelog.md`.
- **v5.8.60** — `lumi-voice`, `lumi-boundaries`, `lumi-consistency`. Web Speech API wrapper (vol cap 0.4, 23 forbidden phrases, 4 anti-manipulation regexes), 4 boundary types + transparency drawer, 8 consistency tokens + 10 enforcement rules + 7-check identity verification. Full detail in `docs/changelog.md`.
- **v5.8.59** — Presence page wiring. First production wiring of 4 opt-in modules (`lumi-circadian`/`lumi-memory`/`lumi-rituals`/`lumi-scenes`) onto `/presence`. 6 new feature flags (admin-only by default), route guard + first-checkin gate, SSR-safe lazy-init `localStorage` flag. Full detail in `docs/changelog.md`.
- **v5.8.58** — `lumi-circadian`. Presence Scheduler & Circadian Calm. 5 phases, OFF by default, `MAX_NUDGES_PER_DAY=3`, 5-min spacing, sleep window 22-7, no required responses. Full detail in `docs/changelog.md`.
- **v5.8.57** — `lumi-rituals`. 7 ritual presets (softArrival/oneBreathReset/grounding54321/gentleTransition/holdingSpace/sleepSoftener/tinyHope), pure reducer, never-persists, `/crisis` anchor in every status. Full detail in `docs/changelog.md`.
- **v5.8.56** — `lumi-scenes`. 7 emotional scene presets, locked numeric ceilings, 14 forbidden effects, soft 1.5s crossfade, avatar identity frozen behind `children`. Full detail in `docs/changelog.md`.
- **v5.8.55** — `lumi-memory`. Reflective Memory Layer. 10 ALLOWED FIELDS (floor-guarded), 7 FORBIDDEN CATEGORIES (≥25 patterns), consent state machine, 6-step write router, audit log capped, atomic revocation wipe. Full detail in `docs/changelog.md`.
- **v5.8.54** — `lumi-conversation`. Phase 15 spec-aligned variant (parallel to v5.8.52). 8-step safety pipeline, 8-tone classifier, 60+15 forbidden phrases/regexes, 8 boundary rules, grounding (15) + reflection (15) prompts, crisis safety in 3 layers. Full detail in `docs/changelog.md`.
- **v5.8.53** — `calm-checkin`. Phase 14 spec-aligned variant (parallel to v5.8.51). 4s/2s/6s × 1 cycle, 3 entry options (Breath/Grounding/Reflection), no progress bars, no streaks. Full detail in `docs/changelog.md`.
- **v5.8.52** — `companion-voice`. Phase 15 first-pass: `MMHBCompanion` conversational shell, 11 EmotionCategory classifier, response bank, depth-cap router, deferred LLM augmentation. Full detail in `docs/changelog.md`.
- **v5.8.51** — `checkin-flow`. Phase 14 first-pass: 4-7-8 × 4 breathing flow, gentle UI, opt-in. Full detail in `docs/changelog.md`.
- **v5.8.50** — `legacyMap.ts`. Phase 12 Wave 1 reconciliation bridge — opt-in, zero page edits, maps legacy avatar/asset references onto canonical registry slots. Full detail in `docs/changelog.md`.
- **v5.8.39** — P1 plain-language rewrite + "your buddy"→"Lumi" name standardization + P2 universal trust strip. User flagged jargon-heavy 4 sections, descriptive "your buddy" phrasing, and missing footer trust promises. Per DRY-RUN-FIRST + ONE-clarifying-question rule, audited and asked two scoping questions; user chose **skip P0 avatar purge** (v17 SSOT locked v5.8.18; user will provide explicit filename mapping later) and **preserve all brand-name uses of "Buddy"**. (Fix 1) 4 CanvaLanding descriptions rewritten plain — "Attunement Over Advice" / "Your Mind Is One of a Kind" / "AI Buddy: Coach, Mentor & Guide" / "Journaling That Evolves Your Thinking" — stripping "metacognitive conditions", "behavioral science principles", "cognitive layers", "metacognitive muscle", "evolve into your fullest potential" etc. while preserving titles and section structure. (Fix 2) Descriptive "your buddy"/"Your buddy" → "Lumi" across CanvaLanding (~11), VisualBenefits (4 benefit cards), ReturnLoop, WelcomeBackBanner, OnboardingFlow (~18 total). PRESERVED untouched per user: brand headline "Your Buddy Is Ready" (line 1109), all "Your AI buddy"/"your AI buddy" product-name phrasings (5 instances on lines 116/168/253/749/1115), PeacescapePage "your Buddy's palette" (4 capital-B instances). Used case-sensitive `replace_all` — lowercase 'b' guard means "Your Buddy" (cap B) and "your AI buddy" (with " AI " infix) never matched. (Fix 3) New `data-testid="strip-trust-promises"` row above existing `SafetyFooter` on CanvaLanding with bullet-separated **Private · No judgment · Emotionally safe · Designed for calm** in sage-deep ink, separator dots `aria-hidden`, `flex-wrap` for narrow viewports, `border-top: 1px solid var(--glp-sage-15)` for rhythm. Global `SafetyFooter` extension deferred (7 variant files; needs canonical-target confirmation). Zero testid drift, zero palette drift, zero `/crisis` change, zero motion change. `tsc` clean, build 15.13s, screenshot verified canonical sprout-Lumi renders.
- **v5.8.38** — Live-site homepage polish from 15-screenshot review (IMG_4330–4344). Three surgical fixes: (1) **ReturnLoop bleed-through** — sticky banner bg was rgba ~0.18 alpha so scrolled content visually bled through (IMG_4330/4331/4344); layered translucent accent over solid `#FBF8F1` paper via `linear-gradient(${accent.bg}, ${accent.bg}), #FBF8F1` + `0 2px 12px rgba(20,38,38,0.08)` shadow. All 5 sage/gold/lavender/mint/rose accent tones preserved. (2) **Feature card "Explore X" pills clipping to "lore X"** (IMG_4335/4336) — `.feature-card-elite` has `overflow:hidden` and long titles ("Journaling That Evolves Your Thinking" etc) made the `inline-flex` pill wider than card; `items-center` then centered it and overflow clipped both sides. Visible text shortened to "Explore" (title is in `<h3>` directly above), `aria-label` keeps full `Explore ${feature.title}` for SR fidelity, added `max-w-full`, `px-4`→`px-5`. (3) **Final CTA buttons overlapping** (IMG_4342) — `cta-btn-primary`/`cta-btn-secondary` carry 28-42px box-shadows extending outside bounds; stacked-mobile `gap-4` (16px) less than shadow extent → visual collision; `gap-4 md:gap-5` → `gap-6 sm:gap-5 md:gap-6` + `flex-wrap`. Zero testid drift (banner-return-loop, link-feature-${index}, button-final-cta, button-view-pricing all unchanged), zero palette drift, zero `/crisis` change, reduced-motion guard intact. `tsc` clean, build 15.85s, architect-validated.
- **v5.8.37** — V34 Phase 2: LumiV7 surgical extension. Audit showed v5.8.28 had already shipped 6 of 8 requested features (4 eye types, mouse-track lerp, blink 2-6s + 15% double, 10 mouths, 600ms + 100ms eyes-lead, crisis override). Two genuine gaps closed: (1) **pupil dilation by emotion** (excited 1.15, loving 0.95, calm/sleepy/breathing 0.85, neutral 1.0) via modern CSS `scale` individual transform keyed on existing `[data-mouth]` wrapper attribute — composes cleanly with RAF `transform: translate()` on pupils per CSS Transforms Level 2, 600ms cubic-bezier transition. (2) **Blush escalation** by new `interactions` prop (default 0): `blushOpacityFor()` helper maps 0→0.15 baseline, 1-2→0.2, 3-4→0.4, 5+→0.6; inline `opacity` style on both blush ellipses w/ 600ms ease-out transition. FROZEN body contract preserved (only opacity dynamic, position/shape unchanged). BHCE crisis override extended to pin pupil `scale: 1 !important` + force blush to 0.15 + `transition/animation: none` on both. Reduced-motion guard extended to pin pupil scale. New testids `lumi-v7-blush-{left,right}` + `data-blush-level` (0-3) + `data-interactions` for QA/analytics. v5.8.28 testids untouched (no drift). `tsc --noEmit` clean. Build 16.35s. Architect-validated.
- **v5.8.36** — V28+V30 audit polish. Audit showed Pricing/BreathingTool/CheckIn/About/Disclaimer were already 95-100% V28+V30 compliant from v5.8.23/24/31; re-sweeping would risk `data-testid` analytics drift. Two genuine gaps closed surgically: (1) `client/src/pages/Pricing.jsx` hero subtitle was `var(--glp-sage)` (#1ec890 bright sage) on cream paper measuring ~3.5:1 contrast, failing WCAG AA for body text — swapped to `var(--glp-ink)` (#142626) at `opacity: 0.78` for >10:1 contrast; added `data-testid="text-pricing-subtitle"` for analytics. (2) `client/src/pages/tools/BreathingTool.jsx` was the only surface missing a Return Loop element — added `useGentlePracticeStreak()` hook reading `localStorage['mmhb-breathing-streak-v1']` (JSON `{count, lastAt}`, try-catch guarded), `bumpGentlePracticeStreak()` called inside `pickCheckin(v)` so it only fires on actual completion (never on skip/reset), and a sage→gold gradient pill (canonical `rgba(168,201,160,0.18)` → `rgba(232,145,58,0.14)`, sage-30 border, sage-deep text) rendered in intro phase only when `streakCount > 0` reading "🌿 Day {N+1} of your gentle practice" with `data-testid="pill-breathing-streak"` + `aria-label`. Architect-verified: localStorage key isolated (no collisions), pill stacks above existing "Let's breathe together" paragraph in flex-col without overlap, `/crisis` routing preserved in nav + info block. NOT touched (already V28+V30): emotion-first Pricing tier names (Your Safe Space/Personal Guide/Full Companion/Transformation Partner) with `legacyName` Free/Starter/Pro/Elite analytics anchors, all 7 V30 elements (social proof row "10,000+ gentle check-ins / 4.8 rating / Private by default", Most Popular badge, money-back guarantee, email capture, 5-question FAQ, Value Bridge, Return Loop banner), CheckIn streak pill, About/Disclaimer paper+white-card+sage-CTA. `tsc --noEmit` zero errors.
- **v5.8.35** — A→Z 360° performance sweep. Diagnostics: production bundle had `vendor-lucide` chunk at **624 KB / 165 KB gzip** (entire icon library), main `index.js` at **819 KB / 196 KB gzip**, brand PNGs at **5.0 MB combined**. Fix: (Phase A) `client/src/components/ui/Card.jsx` removed `import * as LucideIcons from 'lucide-react'` — the legacy `icon="Heart"` string-lookup pattern was 0-caller dead code dragging the full library in; refactored `icon` prop to accept a React element so callers own tree-shakeable imports. **vendor-lucide 624 KB → 66.59 KB (143 KB gzip saved per page load).** (Phase B) Used system `cwebp` to generate WebP siblings of all 12 PNGs >200 KB in `client/public/brand/` at q82/m6; `VisualBenefits.jsx` already had `imageWebp` paths but the files had never been generated. **5.0 MB → 212 KB = 96% smaller hero imagery.** All PNGs kept as `<picture>` fallback. (Phase C) `App.jsx` lazy-converted 9 eagerly-imported heavy globals: `GratitudePrompt`/`ConsentBanner`/`FeedbackWidget`/`WelcomeBackBanner`/`ReturnLoop`/`MicroWinPrompt`/`AICompanion`/`AccessibilityToolbar`/`AnalyticsDashboard` (admin-only); cluster wrapped in `<Suspense fallback={null}>` so they hydrate post-LCP without blocking first paint. **index.js 819 KB → 750 KB (18 KB gzip saved).** (Phase D) `vite.config.js` `manualChunks` extended to also split `recharts`/`d3-*`/`framer-motion`/`date-fns`/`dayjs` preventively. Cumulative: **−161 KB gzip JS + −4.8 MB images on first visit**. Build 19.83s. Zero design / `data-testid` / `/crisis` / reduced-motion changes.
- **v5.8.34** — V28 Polish System + IMG_4320 alignment. `.polish-card`/`.polish-card-icon`/`.section-rhythm`/`.polish-cta` utility set added to `index.css`; `ValueBridge.jsx` + `ValueProposition.jsx` swept to canonical 8-hex (zero `#8FBF9F`/`#D4AF37` residuals); `.section-breathe` 5/7/8 → 4/5/6rem in `canva-landing.css`. Full detail in `docs/changelog.md`.
- **v5.8.33** — Homepage `NlpMiContent` rebuilt as 4 alternating mascot+content rows in V17 `VisualBenefits` style — `nlpMiContent.js` HOME.sections enriched w/ accent/tint/halo/avatar/cta; `NlpMiContent.jsx` rewritten w/ `.nlp-mi-row` grid + 9 scoped CSS classes; row-4 CTA routes to `/crisis`. Full detail in `docs/changelog.md`.
- **v5.8.32** — V28 Tier 4 Sweep (Admin + Niche, 57 surfaces) via 5 idempotent regex passes using v5.8.31 `.v28-paper-bg`/`.v28-card` utilities. Cumulative v5.8.21→v5.8.32: ~95 surfaces on V28. Full detail in `docs/changelog.md`.
- **v5.8.31** — V28 Tier 3 Sweep (Tool Pages + Auth/Account/Conversion, 21 surfaces). New `.v28-paper-bg` + `.v28-card` utilities in `index.css`; 9 tool pages + Premium/Settings/Profile/Onboarding(TS)/Wellness/Resources/Glossary/Privacy(TS)/Newsletter/FAQ/Upgrade/NotFound/ForgotPassword/About flipped. Full detail in `docs/changelog.md`.
- **v5.8.30** — V28 Public Surfaces Sweep (Tier 2). `WellnessToolsHub.jsx` rewritten (off-palette gradient tiles → canonical pastel-tint pattern); `ToolsPage`/`Blog`/`BlogIndex`/`Terms.tsx` flipped to flat paper. Full detail in `docs/changelog.md`.
- **v5.8.29** — V28 Auth + Privacy Sweep + Canonical Stat Cards. Privacy/Login/Register/Forgot/Reset/Dashboard flipped; off-palette colors removed; Dashboard Mood/Journal stat cards re-styled to canonical calm-blue + empathy-purple. Full detail in `docs/changelog.md`.
- **v5.8.28** — V32 Gap C: LumiV7 Avatar Evolution Engine (`components/lumi/LumiV7.{jsx,css}`). Pure-SVG mascot, body frozen per V22/V24/V27, eyes/mouth/arm/leg gain coordinated states (4 eye types, 10 mouths, 6 arms, 5 legs) + RAF pupil tracking + BHCE crisis override + reduced-motion blanket. QA surface at `/avatar-lab`. Standalone, opt-in. Full detail in `docs/changelog.md`.
- **v5.8.27** — V32 Gap D: net-new `/welcome` 6-screen onboarding flow (`pages/OnboardingFlow.jsx`, lazy in `App.jsx`) — Welcome → Goal → Micro Relief → Personalize → Progress → Return; V28 styling, full `data-testid`/`aria-pressed`/`role=progressbar`, `/crisis` on every screen. Auth-gated `/onboarding` coaching flow untouched. Full detail in `docs/changelog.md`.
- **v5.8.26** — V32 Gaps A+B+E: `<section id="emotional-validation">` w/ 5 emotion-led cards on homepage; "Evidence-Based" → "Evidence-Informed" copy sweep; cost ops cleanup. Full detail in `docs/changelog.md`.
- **v5.8.25** — V28 sweep on `CanvaLanding.jsx`: section-flow-sage/warm/FAQ/final-CTA flattened to paper; `.manifesto-quote` hardened to white card via `!important`. Full detail in `docs/changelog.md`.
- **v5.8.24** — V30 phase-locked copy on `Pricing.jsx`: hero headline + 4 emotion-first tier subtitles + per-tier `cta` field. testid + Stripe `planId` unchanged. Full detail in `docs/changelog.md`.
- **v5.8.23** — V28+V30 audit across Pricing/CheckIn/BreathingTool/About/Disclaimer. Pricing rewritten w/ emotion-first names + 4 V30 elements added; tool pages → white cards + sage pill CTAs; CheckIn streak → sage-gold pill. Full detail in `docs/changelog.md`.
- **v5.8.22** — V28 Color & Structure Unification on `CanvaLanding.jsx`: dark bands → white-card light treatment; square→circle icons; text links → sage gradient pills.
- **v5.8.21** — V28 initial pass (superseded by v5.8.22).
- **v5.8.6 → v5.8.20 (Phase 2 era)** — Lumi/avatar maturation: V12 breathing tool polish, V3 round benefits, V20 sparkles/particles/blush, coordination transitions, bounce/tears, V24 OMEGA data layer, LumiV6 `tears?`/`celebrate?` props, MI `rollingWithResistance`, `MicroWinPrompt` dismiss coaching, homepage Lumi pose mapping fix, LumiV6 → canonical `EMOTION_STATES` SSOT via `getLumiV6Derivation()` adapter, V24 §7 pose assets (meditating-aura/halo-prayer/walking-sunrise), VisualBenefits canonical avatar swap, Growth walking-path correction, V27 sprout-only enforcement (hooded retired), **v5.8.18 official avatar drop** — 7 canonical PNGs locked into `client/public/brand/v17/` as visual source of truth, never regenerate. Per-release notes in `docs/changelog.md`.
- **v4.1.1 → v5.5 (Foundational era)** — Foundational Lumi/MI/avatar work: Subscription Elicitation, Engagement Hooks, V14 audio, Lumi voice/expression sync, Emotional Journey timeline, `/v6` Control Panel, Hero Polish, LumiV9 "Soul Capture", Schema Drift Guardrail, Avatar Uniformity. Per-release notes in `docs/changelog.md`.

### Archived from active Index (v5.8.94 — moved v5.8.80–v5.8.85)

- **v5.8.85** (2026-05-15) — V25 avatar mapping fix: `sections/VisualBenefits.jsx` L113 `lumi-float-idle.png` → `lumi-path.png` (POSE F walking-path closing v5.8.84 commission). Hero swap rejected per V34 SMALLEST_ENGINE — `CanvaLanding.jsx` L560 retains `FloatIdleAnimated` 11-region rig. POSE E (check-in emotion-orbs) deferred. Full detail → `docs/changelog.md`.
- **v5.8.84** (2026-05-15) — Canonical Lumi avatar reinstall: 7 IMG_434* PNGs installed in `client/public/lumi/official/` (incl. `lumi-path.png` closing 6-month sprout walking-path gap), 33 `attached_assets/` files purged, registry L142 src fixed (architect catch). Full detail → `docs/changelog.md`.
- **v5.8.83** (2026-05-15) — P7 Avatar Life System Phase 3 (Arm & Leg Movement) wiring: `LumiV7.jsx` ~7-line emotion derivation expansion (eye+mouth → eye+mouth+arm+leg) consuming pre-existing `EMOTION_STATES` armPose/legPose fields. CSS L116-185 + crisis-stillness override pre-shipped since v5.8.10/v5.8.79. Backward-compatible. Full detail → `docs/changelog.md`.
- **v5.8.82** (2026-05-15) — P6 activation surface: fixed `MemorySettingsPanel.tsx` pre-existing compile-blocker (raw store import that wasn't exported); mounted panel in `/settings`, added `Footer.jsx` Settings link + `CanvaLanding.jsx` mobile menu auth-gated entry. One-button consent-flip turns Phase 16 Reflective Memory Layer on for real users. Full detail → `docs/changelog.md`.
- **v5.8.80** (2026-05-15) — P6 Path B (Emotional Continuity) — first Phase 16 consumer: `ReturnLoop.jsx` reads `lastSessionAt` via `readMemory()`, picks tier-aware welcome (`<1d` short / `1-7d` medium / `7+d` long), writes new timestamp via `writeMemory()`. Architect catch: wrapped Zustand store with `persist()` middleware + localStorage so `lastSessionAt` survives reload. Tier boundary tightened `<= 7` → `< 7`. Full detail → `docs/changelog.md`.

### v5.8.119 – v5.8.126 (archived from replit.md by docs-trim cycle 2026-05-17)

HX-OS Interaction Governance Runtime Enforcement — 8 sequential per-file iterations integrating `MonetizationBoundaryValidator` + `CrisisOverrideEngine` (+ `HealingFlowProtectionRules` where the surface is a registered protected healing flow) as **passive** observability/derivation only. **No UI / visual / animation / route / monetization / behavior change in any iteration.** Each iteration: imports governance modules, declares memoized derivations (`crisisDetected`, `vulnerableState`, `overrideState`, `monetizationGate`), and exposes 9–10 `data-*` attributes on the existing root element of the routed surface. Gates per iter: `npx tsc --noEmit` exit 0, `npm run build` exit 0, all touched routes return 200, architect code-review Pass.

- **v5.8.119** (2026-05-16) — Iter 1 / **AIChatPanel**. File: `client/src/components/chat/AIChatPanel.tsx`. Surface: `/chat`, `/ai-chat`. Crisis source: latest user message text. Vulnerable signal: explicit emotional-tone heuristic on message stream. Healing-flow pin: `ai_chat`. Data attrs on existing root: `data-chat-governed`, `data-healing-flow`, `data-crisis-active`, `data-vulnerable`, `data-monetization-suspended`, `data-monetization-allowed`, `data-conversion-disabled`, `data-paywalls-blocked`, `data-upgrade-prompts-blocked`, `data-analytics-restricted`. Preserved: send/stream/persistence handlers, scroll behavior, message rendering, Lumi header circle (v5.8.114), nav gap (v5.8.113). Architect: Pass.
- **v5.8.120** (2026-05-16) — Iter 2 / **BuddyPanel**. File: `client/src/components/buddy/BuddyPanel.tsx`. Surface: buddy-side floating panel (multi-route). Crisis source: latest buddy-input text. Vulnerable signal: panel-context emotional state. Healing-flow pin: `buddy_support`. Same 10 data attrs on existing panel root. Preserved: panel open/close, message handlers, animation states, focus management. Architect: Pass.
- **v5.8.121** (2026-05-16) — Iter 3 / **AtlasDashboard**. File: `client/src/pages/AtlasDashboard.tsx` (or canonical Atlas dashboard root per App.jsx mount). Surface: `/atlas` (or canonical Atlas route). Crisis source: dashboard-context signals only (no free-text input on this surface → `crisisDetected` always-false). Vulnerable signal: aggregated mood-trend / streak-break heuristic. Healing-flow pin: NOT a registered protected healing flow → `data-healing-flow="false"`; `escalationRequired` falls through to `vulnerableState` only. Same 10 data attrs on dashboard root. Preserved: widget grid, queries, sub-component props. Architect: Pass.
- **v5.8.122** (2026-05-16) — Iter 4 / **JournalPage**. File: `client/src/pages/JournalPage.jsx`. Surface: `/journal`. Crisis source: journal-entry textarea content. Vulnerable signal: entry-length / sentiment heuristic. Healing-flow pin: `journaling`. Same 10 data attrs on existing form root. Preserved: draft autosave, submit mutation, queryClient invalidation, journal-history rendering. Architect: Pass.
- **v5.8.123** (2026-05-16) — Iter 5 / **DailyRitualPage**. File: `client/src/pages/DailyRitualPage.tsx`. Surface: `/daily-ritual` (or canonical ritual route per App.jsx). Crisis source: reflection-prompt response text. Vulnerable signal: skip-streak / opt-out heuristic. Healing-flow pin: `daily_ritual` (where registered) or falls through. Same 10 data attrs on existing ritual root. Preserved: prompt rotation, step navigation, completion-write path. Architect: Pass.
- **v5.8.124** (2026-05-16) — Iter 6 / **CheckIn**. File: `client/src/pages/CheckIn.jsx`. Surface: `/checkin`. Crisis source: check-in free-text field. Vulnerable signal: low-rating + negative-emotion combo. Healing-flow pin: `check_in`. Same 10 data attrs on existing check-in root. Preserved: emotion-orb selection, rating slider, submit handler, /crisis routing in SafetyFooter. Architect: Pass.
- **v5.8.125** (2026-05-16) — Iter 7 / **Onboarding**. File: `client/src/pages/Onboarding.tsx` (or canonical Onboarding root). Surface: `/onboarding`. Crisis source: onboarding free-text responses (intent / concerns prompts). Vulnerable signal: high-distress self-report on intake. Healing-flow pin: `onboarding` (where registered) or falls through. Same 10 data attrs on existing onboarding root. Preserved: step navigation, consent gates, profile write path. Architect: Pass.
- **v5.8.126** (2026-05-16) — Iter 8 / **MoodPage**. File: `client/src/pages/MoodPage.jsx`. Surface: `/mood` (App.jsx:435-437 inside `<AgeConsentGate>`). Module consts: `CRISIS_LANGUAGE_PATTERN` (regex), `VULNERABLE_EMOTIONS` Set ("Sad", "Anxious", "Tired"), `MOOD_IS_HEALING_FLOW` = `HEALING_FLOW_PROTECTION_RULES.protectedHealingFlows.includes("mood_tracking")` (resolves true at compile time). Crisis source: notes textarea content only. Vulnerable signal: `rating <= 4 || (emotion !== "" && VULNERABLE_EMOTIONS.has(emotion))`. `overrideState.escalationRequired` = `MOOD_IS_HEALING_FLOW || vulnerableState` (always-on for /mood since it is a registered protected flow). 10 data attrs attached to existing `<form data-testid="form-mood">` element (no wrapper added — form is the semantic root of the mood-tracking interaction surface containing all rating / emotion / activity / notes inputs that drive the memos). Preserved: `saveMutation`, `useQuery` (queryKey `/api/mood`), `toggleActivity`, `handleSubmit`, `awardXp` (gamification), `isLoading` skeleton, `MoodHistory` + `MoodStats` sub-components. Architect: Pass.

**Cross-iter governance contracts honored (all 8 iters):**
- Smallest valid engine — ONE file per iter, no new modules, no module relocation.
- Passive observation only — derivations affect attrs, never branch UI.
- Crisis routing unchanged — `/crisis`, 988, 741741 references untouched on all surfaces.
- Monetization boundary respected — `monetizationGate.allowed` derived, never used to *enable* a previously-disabled business action; existing healing flows retain their existing zero-monetization posture.
- Memoization correctness — deps verified by architect each iter; no stale-closure / inverted-dep findings.
- Zero render-tree mutation — attrs attached to **existing** root elements; no wrappers, no new divs, no portal mounts.

## v5.8.127 – v5.8.133 (archived from replit.md on 2026-05-17 in this trim cycle)

Full per-iter trace of the HX-OS Interaction Governance Convergence phase. Compressed to a single Index row in `replit.md`; original verification evidence (gates, behavior-parity proofs, line counts, architect verdicts) preserved here verbatim.

- **v5.8.127** (2026-05-16) — `client/src/pages/MeditationPage.jsx` (HX-OS iter 9): added passive governance to /meditation surface — imports `MonetizationBoundaryValidator` + `CrisisOverrideEngine` + `HEALING_FLOW_PROTECTION_RULES`, 4 memoized derivations (`crisisDetected` via CRISIS_LANGUAGE_PATTERN on free-text, `vulnerableState`, `overrideState` with `escalationRequired: MEDITATION_IS_HEALING_FLOW` (= isProtected("meditation") = true), `monetizationGate` for `/meditation` regulated HEALING_DOMAIN route), 10 `data-*` observability attrs on the existing surface root element. NO UI/animation/timing/breathing/audio change. tsc 0, build 0, routes /meditation /crisis / 200, architect Pass.
- **iter 10** (2026-05-16) — audit-only cycle: enumerated 8 governed surfaces (AIChatPanel, BuddyPanel, AtlasDashboard, JournalPage, DailyRitualPage, CheckIn, Onboarding, MoodPage from v5.8.119–126 + MeditationPage from v5.8.127). Identified duplication of `CRISIS_LANGUAGE_PATTERN` regex across 9 files and direct `HEALING_FLOW_PROTECTION_RULES.protectedHealingFlows.includes(...)` lookups. Roadmapped phases P1 (regex extract) → P2 (isProtected helper) → P3A (canonical governance helpers) → P3B-P3E (backports). No code changes this iter.
- **iter 11 / P1** (2026-05-16) — extracted `CrisisLanguagePattern` into shared module (`client/src/governance/interactions/CrisisLanguagePattern.ts`) re-exporting the canonical regex. All 9 surfaces re-import from canonical source (ONE source of truth). Zero behavior change (same regex). tsc 0, build 0, all governed routes 200, architect Pass.
- **iter 12 / P2** (2026-05-16) — added `HEALING_FLOW_PROTECTION_RULES.isProtected(flowId)` predicate method to replace ad-hoc `protectedHealingFlows.includes(...)` lookups. All 9 surfaces (and `RITUAL_IS_HEALING_FLOW`, `MEDITATION_IS_HEALING_FLOW`, `MOOD_IS_HEALING_FLOW` consts) refactored to use the predicate. Zero behavior change. tsc 0, build 0, routes 200, architect Pass.
- **iter 13 / P3A** (2026-05-16) — introduced canonical 2-helper governance API in `client/src/governance/interactions/`: (1) `deriveGovernance({route, healingFlow, crisisDetected, vulnerable})` returns `{overrideState, monetizationGate}` (wraps the 2 engine calls in one memo); (2) `buildGovernanceAttrs({surface, healingFlow, crisisDetected, vulnerable, overrideState, monetizationGate})` emits the canonical 10-attr observability contract (`data-{surface}-governed`, `data-healing-flow`, `data-crisis-active`, `data-vulnerable`, `data-monetization-suspended`, `data-monetization-allowed`, `data-conversion-disabled`, `data-paywalls-blocked`, `data-upgrade-prompts-blocked`, `data-analytics-restricted`). Added vitest suite covering both helpers (`__tests__/deriveGovernance.test.ts` + `vitest.setup.mjs`). NO consumer changes this iter — helpers shipped + tested in isolation. tsc 0, build 0, routes 200, architect Pass.
- **iter 14 / P3B** (2026-05-16) — backported `client/src/components/chat/AIChatPanel.tsx` to canonical helpers. Imports swapped; pre-existing 5 inline governance attrs replaced with `{...governanceAttrs}` spread → expanded to canonical 10 (intentional contract expansion per P3A design). Behavior parity proven byte-equivalent. All other UI/JSX/data-testid attrs preserved. tsc 0, build 0, routes 200, architect Pass.
- **iter 15 / P3C** (2026-05-16) — backported `client/src/components/avatar/BuddyPanel.tsx`. During edit discovered BuddyPanel passes `escalationRequired` derived from local state (not always-on like healing flows) — extended `deriveGovernance` signature with optional `escalation?` param (falls back to healing-flow default when omitted). Backport preserved 6 governance attrs + 2 BuddyPanel-specific data-* attrs alongside `{...governanceAttrs}`. Byte-equivalent. tsc 0, build 0, routes 200, architect Pass.
- **iter 16 / P3D** (2026-05-16) — backported `client/src/pages/AtlasDashboard.tsx`. 8 governance attrs → canonical 10 (+4 surface-specific Atlas attrs preserved). Verified idempotent re-ship when user re-sent the same spec (commit b0010b66 clean). tsc 0, build 0, routes 200, architect Pass.
- **iter 17 / P3E-1** (2026-05-17) — backported `client/src/pages/JournalPage.jsx`. 9 governance attrs → canonical 10 (data-vulnerable added; value `vulnerable: crisisDetected` semantically equivalent to existing `isVulnerable: crisisDetected` input). `escalation: JOURNAL_IS_HEALING_FLOW` byte-equivalent to pre-backport. `data-testid="journal-page-root"` preserved. tsc 0, build 0, routes /journal /crisis / 200, architect substantive Pass (initial "minor spec mismatch" flag on data-vulnerable expansion was canonical contract by design per iters 14-16 precedent).
- **iter 18 / P3E-2** (2026-05-17) — backported `client/src/pages/DailyRitualPage.tsx` (spec path said `DailyRitual.tsx` — actual file at `/ritual` per App.jsx:1448 is `DailyRitualPage.tsx`; proceeded on correctly-mounted file). 9 governance attrs → `{...governanceAttrs}`. Spec literal `escalation: true` byte-equivalent to pre-backport `RITUAL_IS_HEALING_FLOW` (= `isProtected("ritual") || isProtected("meditation")` = `true || true` = `true`). `data-testid="ritual-page-root"` preserved. `RITUAL_IS_HEALING_FLOW` const now orphaned (no remaining readers) — left in place per minimal-edit discipline; flagged for optional separate cleanup. tsc 0, build 0 (31.68s), routes /ritual /crisis / 200, architect Pass.

**Cross-cutting invariants held across all 10 iters:** ONE-file-per-iter discipline; zero UI/visual/animation/route/monetization/copy/`data-testid`/wrapper/provider changes; canonical sage palette untouched; crisis routing (`/crisis`, 988, 741741) preserved; all gates passed before STOP each iter; full architect review each iter.

**Pending follow-ups carried into next cycle:** orphan `RITUAL_IS_HEALING_FLOW` const cleanup in DailyRitualPage; stray untracked `client/src/components/AIChatPanel.tsx` deletion; remaining surfaces to backport per P3E sequence (CheckIn / Onboarding / MoodPage and any further governed surfaces).

### v5.8.134 — HX-OS Governance Convergence Cleanup
- Completed P3E migration stabilization.
- Removed 5 confirmed orphan *_IS_HEALING_FLOW constants:
  - RITUAL_IS_HEALING_FLOW
  - CHECKIN_IS_HEALING_FLOW
  - ONBOARDING_IS_HEALING_FLOW
  - MOOD_IS_HEALING_FLOW
  - MEDITATION_IS_HEALING_FLOW
- Removed stray orphan file:
  - client/src/components/AIChatPanel.tsx
- No runtime behavior changes.
- No JSX changes.
- No route changes.
- No governance logic changes.
- No UI changes.
- Purpose: reduce drift risk and dead-code accumulation after governance convergence.

---

## v5.8.134 – v5.8.137 — HX-OS Interaction Governance Convergence (P3E completion, iters 19–22)

Continuation of P3E backports onto canonical 2-helper API (`deriveGovernance` + `buildGovernanceAttrs`) introduced at P3A (iter 13, v5.8.130).

### Iter 19 (v5.8.134) — P3E-3 — `client/src/pages/CheckIn.jsx`
- Imports swapped: removed `MonetizationBoundaryValidator` + `CrisisOverrideEngine`; added `deriveGovernance` + `buildGovernanceAttrs`; retained `HEALING_FLOW_PROTECTION_RULES` for orphan const `CHECKIN_IS_HEALING_FLOW`.
- Memos: `overrideState` + `monetizationGate` replaced with `governance` memo `deriveGovernance({route:"/checkin", healingFlow:true, crisisDetected, vulnerable:vulnerableState, escalation:true})` + `governanceAttrs` memo `buildGovernanceAttrs({surface:"checkin", ...})`.
- Root element: `{...governanceAttrs}` spread replacing 10 inline data-* attrs; preserved `data-phase` (surface-specific).
- Parity: `CHECKIN_IS_HEALING_FLOW = isProtected("companion_support")||isProtected("reflection") = true||true = true`; `escalation:true` byte-equivalent.
- Const `CHECKIN_IS_HEALING_FLOW` now orphaned (left in place per minimal-edit discipline).
- Gates: tsc 0, build 0, /checkin /crisis / all 200, architect Pass.

### Iter 20 (v5.8.135) — P3E-4 — `client/src/pages/Onboarding.tsx`
- Same import + memo swap pattern. Spec literal `escalation:true` byte-equivalent for same reason (`ONBOARDING_IS_HEALING_FLOW` shares the same companion_support+reflection keys, both true).
- Root element: `{...governanceAttrs}` spread; preserved `data-testid="card-onboarding"` + onboarding step state machine + goal-selection logic + auth flow + redirects + animations + WellnessPageShell wrapper.
- `crisisDetected` memo pinned `useMemo(()=>false,[])` (no free-text input on onboarding) preserved; `vulnerableState` memo + deps `[selectedGoals]` preserved.
- Const `ONBOARDING_IS_HEALING_FLOW` now orphaned.
- User re-sent identical spec — first ship + idempotent re-verify in same cycle; final state confirmed clean.
- Gates: tsc 0, build 0 (32.50s), /onboarding /crisis / all 200, architect Pass.

### Iter 21 (v5.8.136) — P3E-5 — `client/src/pages/MoodPage.jsx`
- Same import + memo swap pattern. `MOOD_IS_HEALING_FLOW = isProtected("mood_tracking") = true`; `escalation:true` byte-equivalent.
- Root element is `<form>` not `<div>` — preserved form semantics: `onSubmit={handleSubmit}`, `className="space-y-6"`, `data-testid="form-mood"`, `aria-label="Mood tracking form"`; `{...governanceAttrs}` spread replaces 10 inline data-* attrs.
- `crisisDetected` memo (`CRISIS_LANGUAGE_PATTERN.test(content ?? "")`, deps `[content]`) + `vulnerableState` memo (`rating <= 4 || VULNERABLE_EMOTIONS.has(emotion)`, deps `[rating, emotion]`) preserved.
- Const `MOOD_IS_HEALING_FLOW` now orphaned.
- Gates: tsc 0, build 0 (31.84s), /mood /crisis / all 200, architect Pass.

### Iter 22 (v5.8.137) — P3E-6 — `client/src/pages/tools/MeditationPlayer.jsx`
- Same import + memo swap pattern. `MEDITATION_IS_HEALING_FLOW = isProtected("meditation") = true`; `escalation:true` byte-equivalent.
- Root div className `min-h-screen bg-background` preserved; `{...governanceAttrs}` spread replaces 10 inline data-* attrs.
- Special-case: `crisisDetected` is literal `const crisisDetected = false` (no text input on meditation surface) — preserved; helper `crisisDetected || vulnerable = false || vulnerableState = vulnerableState` byte-equivalent to pre-backport `isVulnerable: vulnerableState`.
- `vulnerableState` memo (`VULNERABLE_MEDITATION_CATEGORIES.has(selectedMeditation.category)`, deps `[selectedMeditation]`) preserved.
- Intermediate edit produced a transient malformed `_monetizationGateUnused` stub which was cleaned up in the same iter before verify; final file has zero residual dead memos (architect confirmed).
- Const `MEDITATION_IS_HEALING_FLOW` now orphaned.
- Gates: tsc 0, build 0 (29.52s), /tools/meditation /meditation /crisis / all 200, architect Pass.

### Phase status
- P3E complete: all 6 P3E backports shipped (JournalPage P3E-1/iter17, DailyRitualPage P3E-2/iter18, CheckIn P3E-3/iter19, Onboarding P3E-4/iter20, MoodPage P3E-5/iter21, MeditationPlayer P3E-6/iter22).
- Pending follow-ups: 5 orphaned `*_IS_HEALING_FLOW` consts pending sweep cycle; stray untracked `client/src/components/AIChatPanel.tsx` pending deletion verification.
- Runtime layer frozen mid-cycle: `package.json` inert `dev:clean` script removed at v5.8.137-runtime (single-line, no caller, no behavior change). `.replit` + workflow boot path + `server/app.mjs` unchanged.

---

### v5.8.100 – v5.8.137 (archived from replit.md by H1 cycle, 2026-05-17)

Rows below are the verbatim Index entries archived from `replit.md` to relieve size pressure. Preserved as-is per non-destructive archival contract; no semantic rewrites, no compression, no deduplication.

### Index (v5.8.100 – v5.8.137)

| Version | Date | Modules | Key Change |
|---|---|---|---|
| v5.8.134–137 | 2026-05-17 | HX-OS Interaction Governance Convergence — Phase P3E completion (4 iters 19–22): CheckIn.jsx (P3E-3/iter 19), Onboarding.tsx (P3E-4/iter 20), MoodPage.jsx (P3E-5/iter 21), MeditationPlayer.jsx (P3E-6/iter 22) | Continuation of P3E backports onto canonical 2-helper API (`deriveGovernance` + `buildGovernanceAttrs`) introduced at P3A (iter 13). Per-iter ONE-file discipline. Each iter: swap direct `MonetizationBoundaryValidator.validate` + `CrisisOverrideEngine.getOverrideState` calls for the helper pair; replace 9–10 inline governance `data-*` attrs on existing root element with `{...governanceAttrs}` spread; preserve surface-specific attrs (CheckIn `data-phase`; MoodPage `<form>` semantics + `onSubmit` + `aria-label="Mood tracking form"` + `data-testid="form-mood"`; MeditationPlayer root div className `min-h-screen bg-background`); preserve all memos with original dep arrays. Behavior parity proven byte-equivalent per iter — every `*_IS_HEALING_FLOW` const resolves true via `HEALING_FLOW_PROTECTION_RULES.isProtected(...)` for the corresponding healing-flow key (CheckIn: companion_support+reflection; Onboarding: companion_support+reflection; MoodPage: mood_tracking; MeditationPlayer: meditation), so spec literal `escalation: true` is byte-equivalent to pre-backport `<CONST> \|\| vulnerableState` (always true). Monetization parity: helper computes `crisisDetected \|\| vulnerable` matching pre-backport `isVulnerable` inputs; MeditationPlayer special-case `crisisDetected = false` literal (no text input) preserved → `false \|\| vulnerableState === vulnerableState`. NO UI / visual / animation / behavior / route / monetization / `data-testid` change in any iter. Crisis routing (`/crisis`, 988, 741741) + healing-flow behavior preserved on every surface. Full per-iter detail → `docs/replit-history.md` (section "v5.8.134 – v5.8.137") + `docs/changelog.md`. All gates passed each iter (tsc 0, build 0, routes 200, architect Pass). P3E phase complete — all 6 P3E backports shipped (P3E-1..P3E-6: JournalPage, DailyRitualPage, CheckIn, Onboarding, MoodPage, MeditationPlayer). Pending follow-ups (queued for orphan-sweep cycle): 5 orphaned `*_IS_HEALING_FLOW` consts (RITUAL/CHECKIN/ONBOARDING/MOOD/MEDITATION), stray untracked `client/src/components/AIChatPanel.tsx`. Runtime layer frozen — `package.json` inert `dev:clean` script removed (single line, no caller, no behavior change); workflow boot path `node --import ./server/observability/preload.mjs server/app.mjs` unchanged. |
| v5.8.127–133 | 2026-05-16/17 | HX-OS Interaction Governance Convergence — Phases P1/P2/P3A–P3E (10 iters 9–18): MeditationPage (v5.8.127), audit (iter 10), `CrisisLanguagePattern` extract (P1/iter 11), `HEALING_FLOW_PROTECTION_RULES.isProtected` (P2/iter 12), canonical `deriveGovernance` + `buildGovernanceAttrs` helpers + vitest suite (P3A/iter 13), backports: AIChatPanel (P3B/iter 14), BuddyPanel + helper `escalation?` param extension (P3C/iter 15), AtlasDashboard (P3D/iter 16), JournalPage (P3E-1/iter 17), DailyRitualPage (P3E-2/iter 18) | Per-iter ONE-file discipline. P1/P2 extract shared crisis-regex + protected-flow lookups into reusable helpers (zero behavior change). P3A introduced canonical 2-helper governance API: `deriveGovernance({route, healingFlow, crisisDetected, vulnerable, escalation?})` returns `{overrideState, monetizationGate}`; `buildGovernanceAttrs({surface, healingFlow, crisisDetected, vulnerable, overrideState, monetizationGate})` emits canonical 10-attr observability contract (`data-{surface}-governed`, `data-healing-flow`, `data-crisis-active`, `data-vulnerable`, `data-monetization-suspended`, `data-monetization-allowed`, `data-conversion-disabled`, `data-paywalls-blocked`, `data-upgrade-prompts-blocked`, `data-analytics-restricted`). P3B–P3E backports swap direct `MonetizationBoundaryValidator.validate` + `CrisisOverrideEngine.getOverrideState` calls for the helper pair; surface-specific attrs preserved alongside `{...governanceAttrs}` spread. Behavior parity proven byte-equivalent per iter (escalation value, vulnerable input, monetizationGate.allowed on regulated routes). NO UI / visual / animation / behavior / route / monetization / `data-testid` change in any iter. Crisis routing (`/crisis`, 988, 741741) + healing-flow behavior preserved on every surface. Full per-iter detail → `docs/replit-history.md` (section "v5.8.127 – v5.8.133") + `docs/changelog.md`. All gates passed each iter (tsc 0, build 0, routes 200, architect Pass). Pending follow-ups: orphaned `RITUAL_IS_HEALING_FLOW` const cleanup (iter 18), stray untracked `client/src/components/AIChatPanel.tsx` deletion, remaining P3E backports CheckIn / Onboarding / MoodPage / additional surfaces. |
| v5.8.119–126 | 2026-05-16 | HX-OS Interaction Governance Runtime Enforcement (8 iters: AIChatPanel, BuddyPanel, AtlasDashboard, JournalPage, DailyRitualPage, CheckIn, Onboarding, MoodPage) | Per-iter ONE-file passive governance: imports `MonetizationBoundaryValidator` + `CrisisOverrideEngine` (+ `HealingFlowProtectionRules` where applicable), adds 4 memoized derivations (`crisisDetected`, `vulnerableState`, `overrideState`, `monetizationGate`), exposes 9–10 `data-*` observability attrs on the existing root element of each routed surface. NO UI / visual / animation / behavior / route / monetization change in any iteration. Crisis routing (`/crisis`, 988, 741741) and existing healing-flow behavior preserved on every surface. Full per-iter detail → `docs/replit-history.md` (section "v5.8.119 – v5.8.126") + `docs/changelog.md`. All gates passed each iter (tsc 0, build 0, routes 200, architect Pass). |
| v5.8.118 | 2026-05-15 | pages/ToolsPage.jsx (V49 R2 — Difficulty label visual separation) | User re-issued R2 go-signal after v5.8.117 audit-clean finding. Re-INSPECT: container at L254 already has `flex items-center gap-3 mt-2` (12px gap, MORE than V49 spec's "gap-1 or gap-2" ask). Confirmed no CSS override (`.card-premium` rules in index.css L1639-1690 only set background/padding, no flex/gap children rules). Diagnosed REAL issue as visual-contrast: effort span has sage-bg pill styling, level span was bare text → reads as visually attached even with 12px between them because no visual divider exists on the level side. Smallest valid engine that addresses audit complaint: pill-style the level span to match (neutral subtle background `bg-[var(--glp-ink)]/5`, ink/60 text, same `px-2 py-0.5 rounded-full capitalize` as effort pill). Two visually-distinct badges sit side-by-side with existing gap-3 between them = unambiguously separated, no longer reads as touching. Single-line className extension on one span, NO container change, NO !important, NO CSS file touch, palette-compliant (canonical `--glp-ink` token, no new colors introduced). Affects all 6 tool cards: Belief Mapping, Timed Writing, Silence Mode, Question Reflection, Growth Timeline, Export Your Journey. Recurrence-grep on file: no other effort+level adjacent pair surfaces. Both `/tools` (App.jsx:1428) and `/therapy-tools` (App.jsx:453) mount the same component. Gates: tsc 0, 4/4 routes 200 (/tools, /therapy-tools, /, /crisis), patch confirmed line 258. V49 13-fix queue P0+P1+CH2+R2 → ALL CLOSED with real patches or audit-clean traces. Remaining V49 P2: Fix 10 (mood activities pills), Fix 11 (mood emotion rows), Fix 13 (newsletter button color). |
| v5.8.117 | 2026-05-15 | docs-only — V49 CH2 + R2 audit-clean (NO-OP) | User re-issued CH2 (page-header Lumi circle) + R2 (difficulty label gap) go-signals. INSPECT findings: BOTH already satisfy spec — no patch warranted. CH2: `pages/AIChatPage.tsx:17-18` already has the exact circle wrapper user spec describes (`w-12 h-12 rounded-full bg-[var(--glp-sage-15)] border border-[var(--glp-sage-20)] flex-shrink-0` containing LUMI_HEART at widthPx={40}, adjacent to "Lumi is here. Take your time." text); shipped at v5.8.114. R2: `pages/ToolsPage.jsx:251` renders effort + level inside `<div className="flex items-center gap-3 mt-2">` — already has gap-3 (12px) between the two spans; both `/tools` (App.jsx:1428) and `/therapy-tools` (App.jsx:453) mount the same ToolsPage component (no alternate surface). Audit screenshot for R2 likely stale. NO files changed, NO code touched. Per ONE-bug discipline + non-destructive rule, documented as audit-clean close. V49 13-fix queue P0+P1+CH2+R2 → ALL CLOSED. Remaining V49 P2: Fix 10 (mood activities pills), Fix 11 (mood emotion rows), Fix 13 (newsletter button color). |
| v5.8.116 | 2026-05-15 | pages/CrisisResources.jsx (V49 Fix 7 — Crisis self-care card icons → circles) | Per V49 P1 fix order, Fix 7 advanced (last unshipped P0/P1 from V49 13-fix queue; v5.8.108/v5.8.111 were wrong-surface attempts at hub & ValueProposition cards, never touched the actual /crisis SELF_CARE_TIPS cards described in the audit). INSPECT: `pages/CrisisResources.jsx:65-86` defines `SELF_CARE_TIPS` (Ground Yourself/Breathe Slowly/Reach Out/Stay Safe — 4 cards rendered at L241-260). Icon container at L253 was `w-12 h-12 rounded-xl` (12px corner radius → audit's "rectangular bar" appearance). PATCH: single-token swap `rounded-xl` → `rounded-full` on the SELF_CARE_TIPS map's icon div. Preserved: w-12 h-12 (sizing rhythm with siblings), canonical sage gradient `linear-gradient(135deg, var(--glp-teal-400), var(--glp-sage-deep))` (matches hotline call buttons + "You Are Not Alone" CTA on same page), `mb-4 shadow-md group-hover:scale-110 transition-transform` (visual treatment unchanged), left-aligned position (h3 + p below are left-aligned; mx-auto would create vertical inconsistency — V49 spec's `mx-auto` example assumed text-center cards which these are not). NO !important, NO new CSS, NO style/index.css touch, single-line single-token diff. Recurrence-grep on file: 3 other `rounded-xl` icon containers exist (L130 "In immediate danger?" rose-themed alert banner, L147 Phone-icon header next to "Crisis Hotlines & Support", L232 HandHeart-icon header next to "Immediate Self-Care Steps") — all OUT OF SCOPE for Fix 7 (which targets SELF_CARE_TIPS cards only, not section headings/alert banners). Flagged for potential future sweep cycle. Gates: tsc 0, 4/4 routes 200 (/crisis, /, /journal, /chat), patch confirmed line 253. V49 13-fix queue P0+P1 status: ALL 10 P0/P1 items now shipped or audit-clean. Remaining P2: Fix 10 (mood activities pills), Fix 11 (mood emotion rows), Fix 13 (newsletter button), R2 (Difficulty label gap). |
| v5.8.115 | 2026-05-15 | docs-only (replit.md) — V38 CYCLE 2a audit: NO-OP | Per V38 trigger ("C1 complete: replit.md <80L met at v5.8.112"), advanced to CYCLE 2a (avatar-core/ orphan archival). INSPECT: `find client/public/avatar-core/ -type f` = 25 files / 16MB across master/ (1), regions/ (11 PNG + 11 WebP), shadow/ (2). Reference map: master/clean_master.png → `pages/Presence.jsx:117`; 11 region PNGs → `components/lumi/FloatIdleRig.jsx` STACK array (body-residual/torso/leg-l/leg-r/arm-l/arm-r/face/eyes/mouth/top-leaf/sparkles, all 11 enumerated); 11 region WebPs → `<picture>`-paired siblings (same basename prefix); shadow.{png,webp} → `components/lumi/FloatIdleAnimated.jsx:71` SHADOW_BASE. ZERO orphans found — all 25 files actively referenced. Consistent with prior CYCLE 2 work (v5.8.91/v5.8.92). NO files moved, NO disk freed, NO code changes. Per V38 governance "ONE BLOCKER: largest unreferenced file" — when no unreferenced file exists, cycle is a NO-OP audit close. CYCLE 2 (full directory audit) → CLOSED. Next cycle trigger satisfied: C3 (sprout walking-path render) gated by "C2 complete". |
| v5.8.114 | 2026-05-15 | pages/AIChatPage.tsx (V49 Fix CH2 — Lumi avatar circle on chat header) | Per V49 P1 fix order, CH2 advanced after CH1 (v5.8.113). User clarified scope: A (page-header Lumi only), NOT B (chat-bubble avatars — separate cycle). INSPECT: line 17 rendered `<OfficialLumi variant="LUMI_HEART" widthPx={48} />` with NO circle wrapper, NO border-radius, NO background — Lumi PNG floating bare on page background. PATCH: wrapped in `<span className="inline-flex items-center justify-center w-12 h-12 rounded-full bg-[var(--glp-sage-15)] border border-[var(--glp-sage-20)] flex-shrink-0" aria-hidden="true" data-testid="lumi-header-circle">` (canonical sage tokens from `client/src/styles/brand-tokens.css`, sage-15 = rgba(30,200,144,0.15) bg, sage-20 border). Reduced widthPx 48→40 so the 48px Lumi sits inside the 48px (w-12 h-12) circle with 4px breathing room each side. NO spaces inside Tailwind arbitrary values (avoids v5.8.104 JIT-rejection bug). Single file, NO !important, NO index.css/styles touch, NO global class change. Recurrence-grep `OfficialLumi.*scene="page-header"` → only this one surface in client/src. Gates: tsc 0, 4/4 routes 200 (/chat, /ai-chat, /, /crisis), patch confirmed lines 20-22. |
| v5.8.113 | 2026-05-15 | components/chat/AIChatPanel.tsx (V49 Fix CH1 — Chat nav separation) | Per V49 P1 fix order, CH1 advanced. INSPECT: `<nav aria-label="Chat navigation">` at line 76 — container had `gap-x-4 gap-y-2` (16px/8px, off-spec), single dot separator span at line 89 had no horizontal margin. PATCH: container `gap-x-4 gap-y-2` → `gap-3` (aligns with v5.8.101 canonical nav-gap token used in WellnessPageShell quicknav); dot span `text-gray-300` → `mx-1 text-gray-300` (4px breathing room each side of bullet). Two-line diff, single file, NO !important, NO index.css/styles touch, NO global class change. Recurrence-grep for chat-nav dot-separator pattern → no other matching nav surface (other `·` separators in client/src exist in non-nav contexts and are out of scope). Gates: tsc 0, 4/4 routes 200 (/chat, /ai-chat, /, /crisis), patch confirmed lines 78+89. |
| v5.8.107–v5.8.112 | 2026-05-15 | replit.md (CYCLE 1 final pass — row consolidation) + docs/replit-history.md (+9L) + sections/ValueProposition.jsx + pages/hubs/SelfCareHubPage.jsx + pages/CrisisResources.jsx | Consolidated 5 rows (v5.8.107 trim 1a, v5.8.108 C3 wrong-surface superseded, v5.8.109 trim 1b, v5.8.110 row archival, v5.8.111 C3-RETRY) into this single Index entry; full original-row detail preserved verbatim in `docs/replit-history.md` under section "v5.8.107 – v5.8.111". Net effect of CYCLE 1 (v5.8.107–112): replit.md 95L → 79L (−16, sub-80L target met ✅), 14 historical rows + 5 trim-cycle rows archived, ZERO information lost. Real code patches in this span: v5.8.108 SelfCareHubPage hub-card icons → circles; v5.8.111 ValueProposition `.vp-benefit__icon` `border-radius: 12px → 50%` (homepage `/` REAL self-care card target). Gates: tsc 0, 4/4 routes 200 (/, /crisis, /journal, /blog). |
| v5.8.106 | 2026-05-15 | pages/CrisisResources.jsx (V49 Fix C1 — refactor to badge-as-sibling per literal V49 spec) | Per V49 P1 fix order; user re-sent C1 instruction with explicit structural spec `<div className="flex items-center gap-2"><span className="priority-badge">Priority</span><h3>Title</h3></div>` (badge as sibling of h3, NOT inside h3 as v5.8.105 had it). Asked one clarifying question; user confirmed by re-sending the same literal spec. Refactored: replaced the h3 (which contained both badge span and title span as flex children) with a wrapper `<div className="flex items-center gap-2 mb-1">` containing the Priority span and a clean `<h3 className="text-lg font-semibold">{hotline.name}</h3>` as siblings. Moved `mb-1` from h3 to wrapper div (preserves vertical rhythm to the description paragraph below). Preserved `flex-shrink-0` on badge from v5.8.105 (badge keeps full width on narrow viewports). Preserved canonical sage-deep+paper badge colors. Semantic improvement: screen readers now read "Priority" as a separate adjacent label rather than as part of the heading text — better accessibility. NO !important, NO new CSS, single-file diff. Gates: tsc 0, 4/4 routes 200 (/crisis, /, /journal, /blog), patch confirmed lines 170-179. |
| v5.8.105 | 2026-05-15 | pages/CrisisResources.jsx (V49 Fix C1 — Priority badge separation on Crisis cards) | Per V49 P1 fix order, advanced after J1 (v5.8.104). User-reported audit: 3 hotline cards rendered "Priority988 Suicide & Crisis Lifeline", "PriorityNAMI", "PriorityCrisis Text Line" — Priority badge appearing to overlap/touch the title text. INSPECT at `pages/CrisisResources.jsx:167` revealed h3 ALREADY had `flex items-center gap-2` (structurally correct), but `{hotline.name}` was an **anonymous text node** rather than wrapped in a real flex item. While the CSS spec says anonymous text nodes do become flex items, in practice gap behavior with text nodes is fragile across browsers/configs and was the source of the audit's apparent overlap. Smallest valid engine: wrapped `{hotline.name}` in an explicit `<span>` so it becomes a guaranteed flex item; gap-2 on the h3 now reliably separates badge from title. Also added `flex-shrink-0` to the Priority span so on narrow viewports the badge keeps its full width and the title (which CAN shrink/wrap) flows around it instead of squishing the badge. Badge color preserved as canonical sage-deep+paper per palette (user mentioned "red/pink" colloquially but said "stays as-is" — kept current canonical sage). NO !important, NO new CSS, NO structure change beyond the span wrap, single-file 2-line edit. Recurrence-grep "Priority" across pages/ → only this h3 renders the badge inline next to a title text node, no other surfaces affected. Gates: tsc 0, 4/4 routes 200 (/crisis, /, /journal, /blog), patch confirmed lines 172+176. |
| v5.8.104 | 2026-05-15 | pages/BlogIndex.jsx:67 + pages/BlogPost.jsx:293 (V49 Fix J1 — Blog tag pills) | REAL root cause: malformed Tailwind arbitrary value `bg-[rgba(var(--glp-sage-rgb), 0.15)]` (literal space → JIT silently rejects, ZERO bg applied). Removed space + added defensive `border border-[var(--glp-sage-20)]`. Same fix both files. Recurrence-grep clean. Full detail → `docs/changelog.md` (v5.8.104). |
| v5.8.103 | 2026-05-15 | pages/ToolsPage.jsx (V49 Fix R1 — Reflection tool icons → circles via inline-style override) + audit-CLEAN findings on J1 (BlogIndex+BlogPost) | Per V49 P1 fix order, advanced after Fix 2/1/H1. AUDIT-CLEAN on J1 (Blog tags bunched): inspected `pages/BlogIndex.jsx:60` AND `pages/BlogPost.jsx:286` — BOTH containers already have `mt-{3,4} flex flex-wrap gap-2` matching V49 spec verbatim. No patch needed; audit was stale. PATCH on R1 (6 tools incl. Belief Mapping/Timed Writing/Silence Mode/Question Reflection/Growth Timeline/Export rendered as rounded-squares): located at `pages/ToolsPage.jsx:245` — `<div className={\`icon-container icon-lg ${gradientClass} ...\`}>`. Initial attempt appended Tailwind `rounded-full w-12 h-12` utilities — architect REJECTED with concrete specificity proof: `.icon-container.icon-lg` (specificity 0,0,2) defined in `client/src/styles/` deterministically beats `.rounded-full` `.w-12` `.h-12` (each 0,0,1) regardless of class order or @tailwind utilities position in index.css; cascade order is irrelevant when specificity differs. Corrected per architect: switched to deterministic inline-style override on the same div — `style={{ borderRadius: "9999px", width: 48, height: 48, display: "flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}`. Inline style has highest specificity short of !important, so it wins against the .icon-container.icon-lg selector without needing !important. NO !important, NO new CSS, NO touch to client/src/styles/, single-file diff. ToolsPage mounted at `/tools` AND `/therapy-tools` per App.jsx (architect-corrected — `/reflection-tools` route does not exist). Gates: tsc 0, 4/4 routes 200 (/tools, /therapy-tools, /, /crisis), patch confirmed line 247. |
| v5.8.102 | 2026-05-15 | pages/Start.tsx (V49 Fix H1 — Browse all free tools amber→sage) + audit-CLEAN finding on C2 | Per V49 P0 fix order, advanced to next remaining items after Fix 1 (nav gap, v5.8.101) and Fix 2 (footer, v5.8.100). AUDIT-CLEAN on C2 (Crisis action buttons): inspected `pages/CrisisResources.jsx:182` — container already has `flex flex-wrap gap-2` matching V49 spec literal verbatim ("Add gap-2 between each action button in the row. Also add flex-wrap for mobile"). No patch needed; audit was stale. PATCH on H1 (Browse all free tools amber CTA): located at `pages/Start.tsx:899-915` with explicit code comment "full-width amber CTA" + `lumi-btn-primary` class (shared globally — class-level swap would have global blast radius across every other primary CTA in the app). Smallest valid engine: scoped inline-style override on this single Link instance only — added `background: linear-gradient(135deg, var(--glp-sage-deep), var(--glp-sage))` + `color: var(--glp-paper)` + `border: none` to the existing inline style object. Uses canonical CSS tokens (--glp-sage-deep = #4A7E72 = V49 spec target sage; --glp-sage = #A8C9A0 = canonical brand sage). Updated code comment "amber CTA" → "sage CTA (v5.8.102 V49 Fix H1)" for traceability. NO !important, NO global class change, NO new component, single-file diff. Gates: tsc 0, 4/4 routes 200 (/start, /tools, /crisis, /), patch confirmed, lumi-btn-primary class untouched (other CTAs unaffected). |
| v5.8.101 | 2026-05-15 | components/wellness/WellnessPageShell.jsx + EMERGENCY index.css restore | Two-part cycle: (A) restored canonical 7232-line `client/src/index.css` (16 @imports) from blob `fe93a3d8` after auto-checkpoint `47f03492` overwrote it with a 38-line blanket-`!important` paste; (B) V49 Fix 1/8 nav gap — `WellnessPageShell.jsx:41` quicknav `gap-1 sm:gap-2` → `gap-3` (12px). Single edit fixes /crisis+/journal+/chat+/mood+/pricing+/settings+/checkin. Full detail → `docs/changelog.md` (v5.8.101). |
| v5.8.100 | 2026-05-15 | components/ui/SafetyFooter.jsx (V49 Targeted Fix 2/8 — 1-line footer gap) | V49 ratified after refusing the !important blanket variant; user provided V49 TARGETED FIXES (8 fixes, NO !important, scoped per-component, one-at-a-time order). Per the spec's own "FIX ORDER" + governance ONE-bug discipline: applied Fix 2/8 (footer links gap, lowest-risk, all-pages impact) first. INSPECT: traced 7 candidate Footer components; eliminated `components/Footer.jsx` (already `flex flex-wrap gap-6`), `components/layout/Footer.tsx` (deprecated multi-column grid, no dot sep), `components/ui/Footer.jsx` (vertical `space-y-3` lists). Located dot-separated bunched footer at `components/ui/SafetyFooter.jsx:64` — container `<div className="safety-footer__secondary mt-3 text-xs ...">` had NO `flex` and NO `gap`, so inline `<Link>` + `<span>·</span>` rendered as touching text "Blog·Newsletter·Disclaimer·Privacy·Crisis Resources" exactly matching V49 audit screenshot evidence. SafetyFooter is mounted on 30+ pages via `import SafetyFooter from "@/components/ui/SafetyFooter"` — global all-pages impact. Smallest valid engine: appended `flex flex-wrap items-center gap-2` to the same container className (preserved existing `safety-footer__secondary mt-3 text-xs` + variant-conditional `justify-center`). Per V49 spec literal: "If using a map/loop for footer links, add gap-2 or space-x-2 to the container." NO !important, single className edit, no structure change. Gates: tsc 0, 9/9 routes 200 (/, /journal, /pricing, /crisis, /about, /tools, /healing, /breathing, /meditation), patch confirmed line 64. |
